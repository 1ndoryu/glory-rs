# Tareas completadas — 2026-04-16

## 164A-1 — Fix tabs del panel no persisten al recargar + plan SSH/SFTP
- **Qué:** (1) Corregido bug donde los tabs del panel siempre regresaban a la primera al recargar la página. (2) Creado plan completo de SSH/SFTP seguro en `Agente/planes/plan-ssh-sftp-seguro-2026-04-16.md`.
- **Archivos:** `frontend/src/islands/PanelIsland.tsx`, `roadmap.md`, `Agente/planes/plan-ssh-sftp-seguro-2026-04-16.md`
- **Gotchas:** El `useEffect([effectiveRole])` se ejecutaba en cada mount y reseteaba incondicionalmente el tab guardado en localStorage, destruyendo la persistencia. La corrección lee localStorage DENTRO del effect y solo resetea si el tab guardado no es válido para el rol actual.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica — lógica específica del panel de Nakomi

## 164A-2 — Fix chatBell badge no se actualiza al abrir sesión
- **Qué:** Corregido bug donde el número del badge de chat no se actualizaba ni desaparecía al abrir una sesión de chat desde el ChatBell. Ahora `handleSelectSession` llama directamente a `apiMarkSessionViewed` + invalida cache de React Query, sin depender del roundtrip indirecto vía SeccionChat.
- **Archivos:** `frontend/src/components/panel/ChatBell.tsx`
- **Gotchas:** El flujo anterior dependía de sessionStorage + custom event → PanelIsland → SeccionChat → useEffect → apiMarkSessionViewed. Si el timing fallaba (tab ya estaba en mensajes, o useEffect no se re-disparaba), el mark-viewed nunca se ejecutaba. La solución directa elimina la dependencia del roundtrip.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica — lógica específica del panel

## 164A-3+164A-4+164A-5 — Correcciones visuales del panel
- **Qué:** (3) Renombrado "Historial del proyecto" → "Fases del proyecto". (4) Unificado color de badges numéricos a `var(--brand-black)`. (5) Actividad: reemplazados iconos por dots tipo faseTimelineDot. Corregida variable CSS `--text-secondary` → `--text-muted`.
- **Archivos:** `OrdenDetalle.tsx`, `NotificationBell.css`, `SeccionProyectos.css`, `OrdenHistorialActividad.tsx/.css`
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 164A-6 — SSH/SFTP Plan Fase 2: openssh-server + resource limits
- **Qué:** Reemplazado `atmoz/sftp` por `linuxserver/openssh-server` en compose template. Resource limits en 3 contenedores. Extraído compose a `build_hosting_compose()`. Creado `ServerInfo` struct. Frontend TabAcceso con SSH + SFTP.
- **Archivos:** `coolify.rs`, `hosting.rs` (repo + handlers + stripe), `mod.rs`, `HostingDetalleTabs.tsx`
- **Gotchas:** Puerto interno 22→2222. Hostings existentes necesitan redeploy.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 164A-7+164A-8 — CSS: wallet titulo sin negrita, metodosPago sin borde, ordenes sin bold
- **Qué:** (7) `walletHistorialTitulo` font-weight 600→400. (8) `metodosPagoSubtitulo` eliminado padding-top y border-top. `pagosOrdenesNumero` font-weight 700→400. Corregidos errores pre-existentes: `--text-secondary`→`--text-muted`, rgba hardcodeados→variables CSS, 999px→`var(--radius-full)`.
- **Archivos:** `SeccionWallet.css`, `SeccionMetodosPago.css`, `SeccionPagos.css`
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 164A-9 — Delegaciones Fase 2: UI de solicitudes de cancelación
- **Qué:** Implementado flujo completo de cancel-request: empleados crean solicitudes (en vez de cancel directo), clientes ven banner con Accept/Reject, wallet credit automático al aceptar. GET endpoint para listar cancel requests. Extraído OrdenInfoGrid para cumplir límite 300 líneas.
- **Archivos:** `wallet.rs` (handler), `wallet.ts` (api), `useCancellationRequest.ts` (hook), `CancellationBanner.tsx/.css`, `OrdenDetalle.tsx`, `OrdenInfoGrid.tsx`
- **Gotchas:** OrdenDetalle ya excedía 300 líneas — se extrajo el grid de info a componente separado.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica — lógica específica del marketplace Nakomi

## 164A-10 — Delegaciones Fase 3: notificaciones 48h a empleados
- **Qué:** Cuando una orden pasa 48h sin ser tomada por admin y se marca `open_to_employees = true`, se crea una notificación para cada empleado disponible. Usa `NotificationRepository::create()` directamente (sin WS push).
- **Archivos:** `src/services/assignment.rs`
- **Gotchas:** El loop auto_assign solo tiene acceso a `PgPool`, no a `NotificationHub`, así que la notificación es solo DB (visible en próximo page load, no push en tiempo real).
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica — lógica específica de delegación Nakomi

## 164A-11 — Plan profundo de seguridad del servicio de hosting
- **Qué:** Auditoría de seguridad completa del servicio de hosting (10 áreas). Plan creado con 5 fases: secrets+backups (crítico), aislamiento+credenciales (alto), recursos+puertos+WP (medio), DNS+rate-limiting (medio), monitoreo (bajo). 20+ items accionables con priorización.
- **Archivos:** `Agente/planes/plan-seguridad-hosting-2026-04-16.md`, `roadmap.md`
- **Gotchas:** Dos hallazgos críticos: (1) secrets en .env plaintext incluyendo Stripe live keys y Coolify token, (2) no existen backups — delete_service borra volúmenes sin respaldo.
- **Sentinel:** No aplica — documento de planificación
- **GLORY-RS:** No aplica

## 164A-12 — Auditoría completa del sistema de chatbot
- **Qué:** Auditoría profunda de 20+ archivos del sistema de chat. Documentado comportamiento por tipo de usuario (visitante, cliente, empleado, admin), todos los endpoints con auth, contexto de reportes, naming "Chat general", y arquitectura completa. Identificados 3 bugs críticos (rich messages no renderizan en panel, REST sin rate limit, info panel solo admin), 5 altos y 3 bajos.
- **Archivos:** `Agente/documentacion/chat/auditoria-chatbot-2026-04-16.md`, `roadmap.md`
- **Gotchas:** El widget de visitante SÍ renderiza rich messages correctamente, pero SeccionChat (panel staff) NO — el mismo código de rendering existe solo en ChatWidget.
- **Sentinel:** No aplica — documento de auditoría
- **GLORY-RS:** No aplica