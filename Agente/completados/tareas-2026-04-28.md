## 274A-23+274A-24+274A-25+274A-26+274A-48 - Endpoints reales de contribuciones
- **Que:** Se implemento el flujo real de contribuciones comunitarias: crear propuesta nueva, proponer edicion, proponer eliminacion, listar mis contribuciones, actualizar/eliminar pendientes propias y moderar desde admin aplicando aprobaciones contra canciones y relaciones reales.
- **Archivos:** `src/handlers/admin_contribuciones.rs`, `src/repositories/contribuciones.rs`, `src/services/contribuciones/mod.rs`, `src/handlers/mod.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `openapi.json`, `roadmap.md`, `Agente/documentacion/backend/contribuciones-2026-04-28.md`, `Agente/prevencion/prevencion-directory-size-legacy-2026-04-28.md`.
- **Gotchas:** El auditor no detecta rutas dinamicas template como `PUT/DELETE /api/contribuciones/{id}`, pero se implementaron porque el frontend las usa. SQLx offline obliga a usar queries runtime parametrizadas o regenerar `.sqlx`; se documento el uso con suppressions justificadas. `cargo run -- --emit-openapi` fallo primero por falta de espacio en `C:\tmp\glory-target`; se limpio incremental con `scripts/clean-cargo-target.ps1 -Hard` y luego paso.
- **Validacion:** `cargo check` OK; `cargo clippy --all-targets -- -D warnings` sin diagnosticos; `cargo test --all-targets` OK (181 passed); `cargo run -- --emit-openapi openapi.json` OK; `npm run audit:api` bajo de 17 a 12 faltantes esperados.
- **Sentinel:** Se registro `Agente/prevencion/prevencion-directory-size-legacy-2026-04-28.md` por falso positivo/mejora de `directory-size` al editar modulos legacy existentes.
- **GLORY-RS:** No aplica. La logica es especifica de Kamples (`contribuciones_pendientes`, relaciones sample y moderacion de canciones).

## 274A-29+274A-30+274A-32+274A-33+274A-34 - Acciones reales de moderacion admin
- **Que:** Se implementaron las acciones admin faltantes: aprobar/rechazar publicaciones, comentarios y articulos; resolver/descartar reportes; rechazar pendientes en masa; aplicar ban manual legacy; rechazar publicaciones de un usuario con notificacion.
- **Archivos:** `src/handlers/admin_moderacion.rs`, `src/repositories/admin_moderation.rs`, `src/services/admin_moderation/mod.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `src/handlers/mod.rs`, `openapi.json`, `roadmap.md`, `Agente/documentacion/backend/moderacion-admin-2026-04-28.md`.
- **Gotchas:** Al tocar `admin_moderacion.rs`, Sentinel expuso SQL directo legacy en handler; se movieron tambien las lecturas existentes al repositorio nuevo. `cargo run -- --emit-openapi` volvio a requerir limpieza de `C:\tmp\glory-target` por falta de espacio.
- **Validacion:** `get_errors` sin errores; `cargo check` OK; `cargo clippy --all-targets -- -D warnings` sin diagnosticos; `cargo test --all-targets` OK (181 passed); `cargo run -- --emit-openapi openapi.json` OK; `npm run audit:api` bajo de 12 a 7 faltantes esperados.
- **Sentinel:** No requiere regla nueva; se corrigio la violacion visible moviendo SQL de handler a repositorio.
- **GLORY-RS:** No aplica. La logica depende de tablas y reglas especificas de Kamples (`publicaciones`, `articulos`, `reportes`, bans legacy).

## 274A-49+274A-50+274A-51+274A-52 - Embeddings, experimentos y benchmark admin
- **Que:** Se implementaron endpoints admin reales para generar embeddings faltantes, regenerar embeddings, crear experimento social (`alice_test`, notificacion y mensaje) y ejecutar benchmark Rust sobre consultas reales de samples/feed/pgvector.
- **Archivos:** `src/handlers/admin_experiments.rs`, `src/repositories/admin_experiments.rs`, `src/services/admin_experiments/mod.rs`, `src/handlers/mod.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `src/handlers/admin_ia_queue.rs`, `src/handlers/admin_contribuciones.rs`, `src/handlers/admin_moderacion.rs`, `src/handlers/admin_duplicados.rs`, `src/handlers/admin_automatizacion.rs`, `openapi.json`, `frontend/src/api/generated/**`, `roadmap.md`, `Agente/documentacion/backend/admin-experimentos-embeddings-2026-04-28.md`.
- **Gotchas:** Orval expuso referencias OpenAPI huerfanas previas (`crate.repositories.AdminIaQueueStats`, `ItemPublicacion`, schemas `ListarResponse`/`HistorialResponse` duplicados). Se registraron schemas faltantes y se renombraron respuestas admin para referencias estables. `cargo fmt` formateo archivos ya tocados por tareas previas.
- **Validacion:** `get_errors` sin errores en archivos editados; `cargo fmt --check` OK; `cargo check --message-format=short` OK; `cargo clippy --all-targets --message-format=short -- -D warnings` OK; `cargo test --all-targets` OK (181 passed); `cargo run -- --emit-openapi openapi.json` OK; `npm run codegen` OK; `npm --prefix frontend run type-check` OK; `npm run audit:api` bajo de 7 a 3 faltantes esperados.
- **Sentinel:** No requiere regla nueva; se mantuvo `directory-size` como falso positivo documentado para el registro central legacy `handlers/mod.rs` y se dejaron suppressions justificadas por SQLx offline en el repositorio batch.
- **GLORY-RS:** No aplica. El batch usa `AudioEmbedding` reutilizable existente, pero los endpoints y queries son especificos de Kamples (`samples`, `usuarios_ext`, admin experimental y pgvector local).

## 274A-43 - Backfill real de duplicados por audio_hash
- **Que:** Se implemento `POST /api/admin/duplicados/backfill` con port real de `BackfillHashService`: lee `ruta_original`, calcula SHA-256 en streaming, guarda hashes unicos y registra duplicados pendientes (`mismo_usuario`/`cross_usuario`). Tambien se movio el SQL de listar/contar duplicados fuera del handler hacia repositorio.
- **Archivos:** `src/handlers/admin_duplicados.rs`, `src/repositories/admin_duplicates.rs`, `src/services/admin_duplicates/mod.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `src/handlers/mod.rs`, `openapi.json`, `roadmap.md`, `Agente/documentacion/backend/admin-duplicados-2026-04-28.md`.
- **Gotchas:** El legado guardaba el hash aun si habia duplicado, pero el schema Rust tiene indice unico parcial sobre `audio_hash`; el port registra el duplicado y marca el sample como `en_supervision` sin duplicar el hash para no romper la constraint.
- **Validacion:** `get_errors` sin errores; `cargo fmt --check` OK; `cargo check --message-format=short` OK; `cargo clippy --all-targets --message-format=short -- -D warnings` OK; `cargo test --all-targets` OK (181 passed); `cargo run -- --emit-openapi openapi.json` OK; `npm run codegen` OK; `npm --prefix frontend run type-check` OK; `npm run audit:api` bajo de 3 a 2 faltantes esperados.
- **Sentinel:** No requiere regla nueva; se eliminaron suppressions temporales de `admin_duplicados.rs` al extraer SQL a repositorio. El repositorio nuevo mantiene suppression justificada por SQLx offline y queries runtime parametrizadas.
- **GLORY-RS:** No aplica. El flujo depende de tablas y reglas especificas de Kamples (`samples`, `duplicados_pendientes`, estados admin y constraint local de `audio_hash`).

## 274A-46+274A-53 - Reactivacion admin y borrado masivo de samples
- **Que:** Se implementaron los dos endpoints finales que faltaban en el audit legacy: `POST /api/admin/automatizacion/reactivar` reactiva `extraccion` o `scraping` via `app_config`, y `DELETE /api/admin/samples/todos` borra assets de storage antes de eliminar registros y recalcular totales de creadores.
- **Archivos:** `src/handlers/admin_automatizacion.rs`, `src/repositories/admin_automation.rs`, `src/services/admin_automation/mod.rs`, `src/handlers/admin.rs`, `src/repositories/admin_samples.rs`, `src/services/admin_samples/mod.rs`, `src/handlers/mod.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `openapi.json`, `frontend/src/api/generated/**`, `roadmap.md`, `.github/instructions/test.instructions.md`, `Agente/documentacion/backend/admin-automatizacion-samples-2026-04-28.md`, `Agente/planes/completados/plan-23-endpoints-faltantes-2026-04-27.md`.
- **Gotchas:** El legacy guardaba banderas en options WP; Rust usa `app_config` porque el scraper lee esa tabla como contrato runtime. El borrado masivo no elimina la fila si falla borrar algun asset, y suma esos casos en `errores`. Se mantiene SQL runtime parametrizado con suppressions justificadas por `SQLX_OFFLINE=true`.
- **Validacion:** `cargo fmt --check` OK; `cargo check --message-format=short` OK; `cargo clippy --all-targets --message-format=short -- -D warnings` OK; `cargo test --all-targets` OK (181 passed); `cargo run -- --emit-openapi openapi.json` OK; `npm run codegen` OK; `npm --prefix frontend run type-check` OK; `npm run audit:api` OK con 110/110 matched y 0 missing.
- **Sentinel:** No requiere regla nueva para estos endpoints. Siguen pendientes las mejoras historicas documentadas en `Agente/prevencion/`.
- **GLORY-RS:** No aplica. La logica depende de tablas y contratos especificos de Kamples (`lotes_procesamiento`, `app_config`, `samples`, `cola_extraccion_samples`, `usuarios_ext`).

## 284A-1 - Configuracion editable de automatizacion admin
- **Que:** La pestaña de historial de lotes ahora permite habilitar/deshabilitar extracción y scraping, modificar items por lote e intervalo en segundos, guardar contra `PUT /api/admin/config/extraccion|scraping`, refrescar estado y mostrar el último lote como señal operacional.
- **Archivos:** `frontend/src/legacy/services/apiAutomatizacion.ts`, `frontend/src/legacy/hooks/useHistorialLotes.ts`, `frontend/src/legacy/components/admin/TabHistorialLotesAdmin.tsx`, `frontend/src/legacy/components/admin/automation/AutomationProcessCard.tsx`, `frontend/src/legacy/components/admin/automation/AutomationBatchDetails.tsx`, `frontend/src/legacy/styles/componentes/adminTablas.css`, `frontend/src/legacy/styles/componentes/adminPanel.css`, `roadmap.md`, `Agente/documentacion/backend/admin-automatizacion-samples-2026-04-28.md`.
- **Gotchas:** `Activo` se renombro a `Habilitado` porque el flag indica que el ciclo automatico puede ejecutarse; no prueba por si solo que haya corrido un lote. `adminPanel.css` solo se toco para corregir una sintaxis legacy detectada por Vite (`/* Paginación */ */`). VarSense mantiene diagnosticos stale/preexistentes en CSS legacy, pero `type-check` y build validan la salida real.
- **Validacion:** `npm --prefix frontend run type-check` OK; `npm run audit:api` OK con 110/110 matched y 0 missing; `npm --prefix frontend run build` OK, con warnings existentes de fonts/chunk size.
- **Sentinel:** No requiere regla nueva. Se dejaron suppressions documentadas en CSS legacy compartido porque la deuda reportada es preexistente o stale tras reemplazar los valores señalados.
- **GLORY-RS:** No aplica. Es UI y contrato admin especifico de Kamples sobre `app_config`.
