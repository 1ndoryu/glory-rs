## 274A-23+274A-24+274A-25+274A-26+274A-48 - Endpoints reales de contribuciones
- **Que:** Se implemento el flujo real de contribuciones comunitarias: crear propuesta nueva, proponer edicion, proponer eliminacion, listar mis contribuciones, actualizar/eliminar pendientes propias y moderar desde admin aplicando aprobaciones contra canciones y relaciones reales.
- **Archivos:** `src/handlers/admin_contribuciones.rs`, `src/repositories/contribuciones.rs`, `src/services/contribuciones/mod.rs`, `src/handlers/mod.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `openapi.json`, `roadmap.md`, `Agente/documentacion/backend/contribuciones-2026-04-28.md`, `Agente/prevencion/prevencion-directory-size-legacy-2026-04-28.md`.
- **Gotchas:** El auditor no detecta rutas dinamicas template como `PUT/DELETE /api/contribuciones/{id}`, pero se implementaron porque el frontend las usa. SQLx offline obliga a usar queries runtime parametrizadas o regenerar `.sqlx`; se documento el uso con suppressions justificadas. `cargo run -- --emit-openapi` fallo primero por falta de espacio en `C:\tmp\glory-target`; se limpio incremental con `scripts/clean-cargo-target.ps1 -Hard` y luego paso.
- **Validacion:** `cargo check` OK; `cargo clippy --all-targets -- -D warnings` sin diagnosticos; `cargo test --all-targets` OK (181 passed); `cargo run -- --emit-openapi openapi.json` OK; `npm run audit:api` bajo de 17 a 12 faltantes esperados.
- **Sentinel:** Se registro `Agente/prevencion/prevencion-directory-size-legacy-2026-04-28.md` por falso positivo/mejora de `directory-size` al editar modulos legacy existentes.
- **GLORY-RS:** No aplica. La logica es especifica de Kamples (`contribuciones_pendientes`, relaciones sample y moderacion de canciones).
