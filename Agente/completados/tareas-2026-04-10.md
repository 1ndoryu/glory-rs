# Tareas completadas — 2026-04-10

## P-1 — Refactorizar handlers/chat.rs en módulo

- **Qué:** Dividir `src/handlers/chat.rs` (~670 líneas) en módulo con 4 archivos: `mod.rs` (~95 líneas), `ws_visitor.rs` (~178 líneas), `ws_staff.rs` (~153 líneas), `rest.rs` (~280 líneas). Interfaz pública (`ws_routes()` + `rest_routes()`) sin cambios.
- **Archivos:**
  - `src/handlers/chat.rs` → eliminado
  - `src/handlers/chat/mod.rs` → nuevo (tipos compartidos, enrich_messages, re-exports)
  - `src/handlers/chat/ws_visitor.rs` → nuevo (WS visitante + helper maybe_ai_reply)
  - `src/handlers/chat/ws_staff.rs` → nuevo (WS staff JWT)
  - `src/handlers/chat/rest.rs` → nuevo (todos los endpoints REST)
  - `roadmap.md` → actualizado con plan chatbot v2
  - `Agente/planes/plan-chatbot-v2-2026-04-10.md` → creado
- **Gotchas:**
  - utoipa genera tipos `__path_*` internos que deben ser re-exportados con `pub use rest::*;` en mod.rs, no solo `rest_routes`.
  - clippy too_many_lines (100) obligó a extraer `maybe_ai_reply()` de `handle_visitor_ws`.
- **Sentinel:** No aplica (refactoring puro, no nueva funcionalidad).
- **GLORY-RS:** No aplica (lógica específica de Nakomi).

## P-2 — Migración BD chat v2

- **Qué:** Creadas tablas `visitor_profiles` (memoria visitantes) y `chat_attachments` (adjuntos). Columnas `message_type` + `metadata` (JSONB) en `chat_messages` para mensajes ricos. Columnas `ai_intermediary_enabled` + `ai_summary` en `orders`. Actualizados todos los modelos Rust, queries (ChatMessage + todas las queries Order), cache sqlx regenerada.
- **Archivos:**
  - `migrations/20260413000000_chat_v2.up.sql` / `.down.sql` → nuevos
  - `src/models/chat.rs` → `VisitorProfile`, `ChatAttachment`, campos nuevos en `ChatMessage` y `ChatMessageResponse`
  - `src/models/order.rs` → campos `ai_intermediary_enabled`, `ai_summary`
  - `src/models/mod.rs` → re-exports
  - `src/repositories/chat.rs` → queries con nuevos campos
  - `src/repositories/order.rs` → 13 queries actualizadas con nuevos campos
  - `src/services/chat.rs` → WsServerMessage con campos ricos
  - `src/handlers/chat/mod.rs` → enrich_messages con campos ricos
  - `src/handlers/chat/ws_visitor.rs` → WS replay con campos ricos
  - `src/handlers/mod.rs` → OpenApi schemas nuevos
  - `.sqlx/` → cache regenerada
- **Gotchas:**
  - `query_as!` requiere que TODOS los campos del struct estén en el SELECT/RETURNING — `#[sqlx(default)]` solo funciona con `query_as` (runtime), no con la macro. Hay que agregar columnas a cada query.
  - La BD local estaba accesible (`localhost:5432`), lo que permitió `sqlx migrate run` + `cargo sqlx prepare`.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (esquema específico de Nakomi).

## T-7 — Sin disclosure IA

- **Qué:** El agente ya no revela que es IA. Se presenta como "Nakomi", miembro del equipo. System prompt actualizado con instrucciones explícitas de no revelar su naturaleza AI. Labels frontend cambiados de "IA" a "Nakomi". Status "ai_handling" cambiado a "Equipo". Mensajes de fallback limpiados de "asistente virtual".
- **Archivos:**
  - `src/services/ai_chat.rs` → system prompt, fallback message
  - `frontend/src/api/chat.ts` → SENDER_LABELS, SESSION_STATUS_LABELS
- **Gotchas:** El icono `<Bot>` en el panel admin para indicar `ai_enabled` se mantiene (es info interna para admin, no visible al visitante).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

## T-6 — Escalación humana

- **Qué:** La IA ahora detecta cuándo necesita intervención humana y notifica a todos los admins en tiempo real. Se agregó tag `[ESCALATE]` al system prompt (con criterios: cliente frustrado, tema legal, problema técnico urgente, solicitud de hablar con humano, info insuficiente). `generate_response` retorna `AiResponse { text, needs_escalation }` en vez de `String`. El tag se parsea y elimina antes de mostrar al usuario. Cuando `needs_escalation = true`, se envía notificación `escalation_needed` a todos los admins activos vía `NotificationHub.notify_many()`.
- **Archivos:**
  - `src/services/ai_chat.rs` → `AiResponse` struct, `parse_escalation()`, instrucción `[ESCALATE]` en system prompt, cambio de retorno de `String` a `AiResponse`
  - `src/services/mod.rs` → re-export `AiResponse`
  - `src/handlers/chat/ws_visitor.rs` → `maybe_ai_reply()` con notificación de escalación
  - `src/handlers/chat/rest.rs` → endpoint REST con escalación
  - `src/models/notification.rs` → constante `NOTIF_ESCALATION_NEEDED`
  - `src/models/mod.rs` → re-export de la constante
  - `src/repositories/user.rs` → `admin_ids()` para obtener IDs de admins activos
  - `.sqlx/` → cache actualizada con nueva query
- **Gotchas:**
  - `query_scalar!` necesita cache sqlx regenerada (SQLX_OFFLINE mode).
  - Módulo `notification` en models es privado — re-exportar la constante en `mod.rs`.
  - Clippy `doc_markdown` requiere backticks en nombres de campo en docstrings.
- **Sentinel:** Posible regla: detectar `generate_response` llamadas que no manejan `needs_escalation`.
- **GLORY-RS:** La lógica de escalación es agnostica (detectar tag → notificar). Candidato para extraer a glory-rs cuando haya más proyectos con chat IA.

## T-1 — Anti-spam + timing inteligente

- **Qué:** Servicio `ChatTimingService` con rate limiting progresivo, máquina de estados de timing y clasificador de relevancia. Rate limit: 10 msgs/min por visitor_id con cooldown progresivo (warning → 30s mute → cierre sesión). Timing: IDLE → WAITING(4s) → LISTENING(8s) → RESPONDING con detección de typing y acumulación inteligente. Relevancia: llama-3.1-8b-instant clasifica off-topic, 3 strikes → escalación. El handler ws_visitor fue refactorizado: `maybe_ai_reply()` eliminado y reemplazado por eventos al timing service. Se corrigió race condition en tests de hosting_stripe (env vars no thread-safe → Mutex estático).
- **Archivos:**
  - `src/services/chat_timing.rs` → nuevo (~350 líneas): `ChatTimingService`, `TimingEvent`, `RateCheckResult`, state machine loop, `check_relevance()`, `send_escalation()`
  - `src/services/mod.rs` → exports: `ChatTimingService`, `RateCheckResult`, `TimingEvent`
  - `src/services/ai_chat.rs` → `pub(crate) fn next_key()` (era privado)
  - `src/handlers/chat/ws_visitor.rs` → reescrito: split en 4 funciones (`handle_visitor_ws`, `send_history`, `process_visitor_messages`), integración con timing service
  - `src/handlers/chat/rest.rs` → fix clippy useless_format
  - `src/handlers/mod.rs` → `ChatTimingService::new()` en AppState
  - `src/lib.rs` → `pub chat_timing: ChatTimingService` en AppState
  - `src/services/hosting_stripe.rs` → fix race condition en tests (Mutex para env vars)
- **Gotchas:**
  - clippy `too_many_lines` requiere <100 líneas por función — extractar helpers agresivamente.
  - `set_var` en tests Rust NO es thread-safe — los tests que modifican env vars deben serializarse con Mutex.
  - `DashMap` + `tokio::spawn` por sesión es el patrón correcto para state machines per-connection.
  - El timing service necesita acceso a `pool`, `ai_config`, `chat_hub` y `notification_hub` — se pasan en `register_session()`.
- **Sentinel:** Posible regla: detectar tests que usan `set_var`/`remove_var` sin mutex de serialización.
- **GLORY-RS:** El rate limiter y timing state machine son agnosticos. Candidato para extraer a glory-rs como utilidad reutilizable de chat.

## T-2 — Generación pedidos + facturas (tool use Groq, rich messages, Stripe invoices)

- **Qué:** Implementado sistema de tool use completo para el chatbot. La IA ahora puede ejecutar herramientas (show_service, list_services, create_invoice, request_human_assistance) usando el protocolo OpenAI-compatible de Groq (tool_calls → execute → tool results → loop). Refactorizado `ai_chat.rs` con loop de tool calls (max 3 iteraciones), helper `call_groq_api()` extraído, `process_tool_calls()` para ejecución. Creado `ai_tools.rs` con definiciones de herramientas, ejecución por dispatch, y flujo completo de Stripe invoices (find_or_create_customer → create invoice → add line item → finalize → hosted_invoice_url). Rich messages (service_card, invoice) se guardan en BD con message_type + metadata JSONB y se transmiten por WebSocket. `ChatHub.send_rich_message()` nuevo método. `WsClientMessage::Action` variant para botones de acción desde el frontend.
- **Archivos:**
  - `src/services/ai_tools.rs` → **nuevo** (~340 líneas): tool_definitions, execute_tool, 4 exec handlers, Stripe helpers
  - `src/services/ai_chat.rs` → reescrito: AiResponse con rich_messages, tool call loop, call_groq_api, process_tool_calls
  - `src/services/chat_timing.rs` → TimingSessionDeps struct (fix clippy too_many_arguments), params http_client/stripe_key
  - `src/services/chat.rs` → send_rich_message()
  - `src/services/mod.rs` → exports ai_tools, TimingSessionDeps
  - `src/repositories/chat.rs` → save_rich_message()
  - `src/models/chat.rs` → WsClientMessage::Action variant

## T-3 — Memoria usuario + contexto persistente

- **Qué:** El chatbot ahora tiene memoria persistente entre sesiones usando visitor_profiles. Incluye: (3a) upsert de perfil al conectar WS (IP, device fingerprint, total_sessions), (3b) herramientas `capture_email` y `save_client_info` invocables por la IA, (3c) resumen automático de sesión al cerrar (Groq llama-3.1-8b-instant, concatena resúmenes previos con cap de 2000 chars), (3d) contexto del visitante inyectado en el system prompt (nombre, email, visitas, resumen, preferencias).
- **Archivos:**
  - `src/repositories/chat.rs` → 6 métodos visitor_profile: find, upsert, update_email, update_context_summary, update_preferences, link_to_user
  - `src/services/ai_tools.rs` → 2 tools nuevas (capture_email, save_client_info) + handlers, split tool_definitions en 3 funciones
  - `src/services/ai_chat.rs` → visitor_id param en generate_response/process_tool_calls/build_system_prompt, sección CONTEXTO DEL VISITANTE en prompt
  - `src/services/chat_timing.rs` → visitor_id en TimingSessionDeps y cadena de llamadas, generate_context_summary + call_summary_api (extraído para clippy)
  - `src/handlers/chat/ws_visitor.rs` → upsert_visitor_profile en conexión, visitor_id en deps
  - `src/handlers/chat/rest.rs` → visitor_id en generate_response call
  - `.sqlx/` → cache regenerada
- **Gotchas:**
  - clippy too_many_lines en tool_definitions (126 líneas) → split en service_tool_defs + visitor_tool_defs + combiner
  - clippy too_many_lines en generate_context_summary (103 líneas) → extraído call_summary_api como función independiente
  - config.next_key() retorna Option<&str> → usar let Some(key) = ... else pattern
  - Las 6 queries de visitor_profile usan query_as runtime (no macro) → no generan entries en .sqlx/ pero sqlx prepare regenera todo
- **Sentinel:** No aplica.
- **GLORY-RS:** La lógica de resumen de sesión con modelo ligero (call_summary_api) es agnostica — candidato para glory-rs como utilidad de chat.
  - `src/handlers/chat/ws_visitor.rs` → register_session con TimingSessionDeps, Action handling
  - `src/handlers/chat/ws_staff.rs` → Action match arm
  - `src/handlers/chat/rest.rs` → generate_response con http_client/stripe_key, rich_messages loop
  - `.sqlx/` → cache regenerada con nueva query
- **Gotchas:**
  - Groq tool use sigue formato OpenAI: `tools` array en body, respuesta `tool_calls[].function.{name,arguments}`, resultados como `role: "tool"` con `tool_call_id`. Max 3 iteraciones para evitar loops infinitos.
  - `cargo clippy` reportó 5 errores: manual_let_else (×2, match → let...else), cast_precision_loss (i64→f64 para cents→dollars, allowed), needless_borrows (&format! en .post()), too_many_arguments (9 args → TimingSessionDeps struct).
  - La query save_rich_message necesitó `cargo sqlx prepare` para regenerar cache offline.
- **Sentinel:** Posible regla: detectar funciones con >7 args sin struct agrupador.
- **GLORY-RS:** Tool use engine (definitions + dispatch + loop) es agnostico. Stripe invoice helpers también reutilizables. Candidatos para glory-rs cuando se estabilice el patrón.

## T-4 — Sync cross-device/tab

- **Qué:** Implementado soporte multi-conexión WS por sesión. Múltiples pestañas/dispositivos pueden conectarse a la misma sesión de chat simultáneamente. Al cerrar una pestaña, la sesión permanece activa para las demás. Solo se cierra cuando la última conexión se desconecta o el usuario cierra explícitamente.
- **Archivos:**
  - `src/services/chat.rs` → `connection_counts` (AtomicUsize refcount por sesión), `subscribe()` incrementa, `unsubscribe()` decrementa y retorna remaining, `close_session()` limpia refcount
  - `src/services/chat_timing.rs` → `register_session()` reutiliza timing loop existente si sesión ya registrada (no duplica tarea tokio)
  - `src/handlers/chat/ws_visitor.rs` → `process_visitor_messages()` retorna bool (cierre explícito vs stream ended), cleanup usa `unsubscribe()` para decidir si cerrar sesión
  - `frontend/src/hooks/useChatWidget.ts` → BroadcastChannel `nakomi-chat` para relay de mensajes entre pestañas, `handleServerMessage()` extraído como helper reutilizable
- **Gotchas:**
  - El `broadcast::Sender` de tokio ya soporta multi-consumer (cada `subscribe()` crea un nuevo receiver). El problema era el lifecycle: cerrar una conexión mataba la sesión para todas.
  - `AtomicUsize` refcount con `Ordering::Relaxed` es suficiente (no hay dependencias de orden entre increment/decrement).
  - clippy `doc_markdown`: nombres de campos en docstrings necesitan backticks.
  - clippy `must_use_candidate`: `unsubscribe()` retorna usize que el caller debe usar.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón refcount multi-conexión WS es agnostico — candidato para glory-rs como utilidad de ChatHub.

## T-5 — Archivos en chat (upload multipart, Groq Vision, Whisper STT, PDF extraction)

- **Qué:** Implementado upload multipart de archivos en chat con procesamiento IA en background. Soporte para imágenes (Groq Vision base64), audio (Whisper STT multipart), y PDF (pdf-extract crate). Frontend: file picker con Paperclip button, rich message rendering (imágenes inline clickables, audio player, enlaces PDF).
- **Archivos:**
  - `Cargo.toml` → Agregadas dependencias `pdf-extract = "0.7"`, `base64 = "0.22"`, feature `multipart` en reqwest
  - `src/repositories/chat.rs` → `save_attachment()`, `update_attachment_description()`, `list_attachments_for_message()`
  - `src/handlers/chat/rest.rs` → `upload_chat_file` endpoint (multipart, 10MB limit, MIME whitelist), `spawn_file_ai_processing`, `process_file_with_ai`, `process_image_vision`, `process_audio_whisper`, `process_pdf_extract`. Ruta: POST `/chat/sessions/:session_id/upload`
  - `frontend/src/api/chat.ts` → `ChatUploadResponse`, `apiUploadChatFile()`, `message_type`/`metadata` en interfaces
  - `frontend/src/hooks/useChatWidget.ts` → `uploadFile()` callback, `uploading` state, passthrough de `message_type`/`metadata` desde WS
  - `frontend/src/components/chat/ChatWidget.tsx` → `renderMessageContent()` (switch por message_type → img/audio/link), file picker UI, Paperclip button
  - `frontend/src/components/chat/ChatWidget.css` → Estilos para `.chatWidgetMsgRich`, `.chatWidgetMsgImage`, `.chatWidgetMsgAudio`, `.chatWidgetMsgFileLink`, `.chatWidgetMsgCaption`, `.chatWidgetAttachBtn`
- **Gotchas:**
  - Block comment con `image/*` creaba nested comment en Rust → solucionado con `//` line comments
  - reqwest necesita feature `multipart` explícita para FormData (Whisper API)
  - pdf-extract puede fallar silenciosamente en PDFs corruptos → se usa `ok()` con fallback "No se pudo leer"
  - Procesamiento IA es background (tokio::spawn) para no bloquear la respuesta del upload
  - El MIME whitelist previene uploads maliciosos; el límite de 10MB previene DoS
- **Sentinel:** No aplica
- **GLORY-RS:** El patrón upload multipart + procesamiento IA background es agnostico — candidato para glory-rs como utilidad de chat con archivos.

## T-9 — Clientes registrados (JWT en WS, contexto de usuario/pedidos/hosting)

- **Qué:** El chatbot ahora detecta clientes autenticados y adapta la experiencia. Al conectarse por WS, se intenta extraer el JWT (`token` query param) para identificar al usuario. Si es válido, el system prompt incluye datos del cliente (nombre, email, rol), sus últimos 5 pedidos (servicio, plan, estado, fase) y suscripciones de hosting (plan, dominio, estado). Se agregó herramienta `create_support_ticket` para que la IA cree tickets de soporte categorizados. El frontend pasa el token del authStore en la URL del WS.
- **Archivos:**
  - `src/handlers/chat/mod.rs` → `token: Option<String>` en `VisitorWsParams`
  - `src/handlers/chat/ws_visitor.rs` → `try_extract_user_id()` helper (JWT verification), user_id en `TimingSessionDeps`
  - `src/handlers/chat/rest.rs` → 2 call sites actualizados a `AiSessionContext`
  - `src/repositories/hosting.rs` → `list_by_user_id(pool, uid)` (query_as runtime, no macro por SQLX_OFFLINE)
  - `src/services/ai_chat.rs` → `AiSessionContext<'a>` struct, refactor generate_response con ctx, `build_system_prompt` con user_id, helpers `append_visitor_context()` y `append_registered_client_context()` extraídos (fix clippy too_many_lines)
  - `src/services/ai_tools.rs` → `registered_client_tool_defs()`, `exec_create_support_ticket()`, dispatch en `execute_tool()`
  - `src/services/chat_timing.rs` → `user_id` en `TimingSessionDeps`, propagado a `generate_ai_response` y `AiSessionContext`
  - `src/services/mod.rs` → re-export `AiSessionContext`
  - `frontend/src/api/chat.ts` → `buildVisitorWsUrl` acepta `token` opcional
  - `frontend/src/hooks/useChatWidget.ts` → importa `useAuthStore`, pasa token en WS URL
- **Gotchas:**
  - `query_as!` macro requiere SQLX_OFFLINE cache regenerada — para queries nuevas usar `query_as::<_, Type>()` runtime con `.bind()` como workaround
  - clippy `too_many_arguments` (8 params en generate_response) → creado `AiSessionContext` struct
  - clippy `too_many_lines` (147 líneas en build_system_prompt) → extraídos `append_visitor_context()` y `append_registered_client_context()`
  - clippy `manual_let_else` → usar `let Ok(Some(x)) = ... else { return }` en lugar de match
- **Sentinel:** No aplica
- **GLORY-RS:** `AiSessionContext` y el patrón de inyección de contexto por rol de usuario es agnostico. Candidato para glory-rs cuando se estabilice.

## T-10 — IA intermediaria en pedidos (toggle, contexto completo, resúmenes, sender_type ai_intermediary)

- **Qué:** Los pedidos ahora soportan una IA intermediaria que responde en el chat del pedido cuando está activada. Incluye: toggle por orden (PUT endpoint), prompt intermediario con contexto completo del pedido (servicio, plan, estado, fases, notas, empleado, cliente), resúmenes automáticos (>5 msgs → llama-3.1-8b-instant genera resumen de 200 palabras → orders.ai_summary), sender_type `ai_intermediary` con estilo visual diferenciado (azul), y UI de toggle + resumen en el detalle del pedido.
- **Archivos:**
  - `src/repositories/order.rs` → `toggle_ai_intermediary()`, `update_ai_summary()` (runtime query_as)
  - `src/handlers/orders.rs` → `PUT /api/orders/{order_id}/ai-intermediary` endpoint con utoipa
  - `src/handlers/mod.rs` → registro OpenAPI path + schema
  - `src/models/order.rs` → `ToggleAiIntermediaryRequest`, `ai_intermediary_enabled`+`ai_summary` en `OrderResponse`
  - `src/models/mod.rs` → re-export `ToggleAiIntermediaryRequest`
  - `src/handlers/chat/rest.rs` → `spawn_intermediary_if_enabled()` (tokio::spawn, check order flag, genera respuesta, auto-summary)
  - `src/services/ai_chat.rs` → `generate_intermediary_response()`, `maybe_update_order_summary()`, `build_intermediary_prompt()` (contexto completo del pedido)
  - `src/services/order.rs` → 3 OrderResponse con campos nuevos
  - `src/services/assignment.rs` → 2 OrderResponse con campos nuevos
  - `frontend/src/api/orders.ts` → `apiToggleAiIntermediary()`, `ai_intermediary_enabled`+`ai_summary` en interface
  - `frontend/src/components/panel/OrdenDetalle.tsx` → toggle switch (admin/employee), resumen IA
  - `frontend/src/components/panel/OrdenDetalle.css` → estilos toggle + resumen IA
  - `frontend/src/components/panel/OrderChat.tsx` → renderizado `ai_intermediary` con estilo diferenciado
  - `frontend/src/components/panel/OrderChat.css` → `.orderChatBurbuja--intermediaria`, `.orderChatLabelIntermed`
- **Gotchas:**
  - Order chat usa REST polling (5s), no WS+timing. La respuesta intermediaria se genera en background (tokio::spawn) y el siguiente poll la recoge.
  - `query_as!` macro no usable por SQLX_OFFLINE — todas las queries nuevas usan runtime `query_as::<_, Type>()` con `.bind()`
  - El auto-summary usa llama-3.1-8b-instant (modelo pequeño/rápido) para no bloquear ni consumir cuota del modelo principal
  - Code Sentinel reporta falso positivo con `<input type="checkbox">` oculto del toggle CSS (no es campo de formulario)
- **Sentinel:** Falso positivo: Code Sentinel detecta `<input>` nativo en toggle CSS switch, pero es patrón estándar de accessibility (checkbox oculto). No requiere regla nueva.
- **GLORY-RS:** El patrón de IA intermediaria (toggle + prompt contextual + auto-summary) es agnostico. Candidato para glory-rs como módulo de chat con IA intermediaria cuando se estabilice.

## 084A-25 — Fix CarruselShowcase hooks order

- **Qué:** Movido `useCarruselInfinito()` y `itemsTotales` ANTES del early return `if (proyectosConImagen.length === 0) return null` para cumplir Rules of Hooks de React (hooks no pueden estar condicionados por retornos tempranos).
- **Archivos:** `frontend/src/components/home/CarruselShowcase.tsx`
- **Gotchas:** El error era intermitente (solo cuando no había proyectos con imagen).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

## 084A-26 — Renderizado de mensajes ricos en ChatWidget (invoice, service_card, order_card)

- **Qué:** Agregados 3 nuevos tipos de mensaje en `renderMessageContent`: `invoice` (tarjeta con monto, descripción, badge de estado, botón "Pagar ahora" que enlaza a Stripe `payment_url`), `service_card` (título, descripción, precio), `order_card` (número de orden, servicio, estado). Cada tipo con metadata destructuring del JSONB del backend.
- **Archivos:** `frontend/src/components/chat/ChatWidget.tsx`, `frontend/src/components/chat/ChatWidget.css`
- **Gotchas:** Los metadatos vienen del backend en `ai_tools.rs` con estructura específica por tipo (payment_url, amount_cents, currency, etc).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (UI específica de Nakomi).

## 084A-27 — Model fallback chain y rotación inteligente de API keys

- **Qué:** Reordenado `allowed_models` por inteligencia (70b → 90b → mixtral → 8b → gemma). Nuevo método `model_fallback_chain()` en `AiChatConfig` que construye cadena de fallback con modelo primario primero. Reescrito `call_groq_api` con doble loop: externo por modelos de la cadena, interno por API keys. Solo cae al siguiente modelo si TODAS las keys retornan 429 (rate limit). Errores no-rate-limit cortan inmediatamente. Log de fallback exitoso.
- **Archivos:** `src/services/ai_chat.rs`
- **Gotchas:** La detección de 429 es necesaria para diferenciar rate limit de errores reales.
- **Sentinel:** No aplica.
- **GLORY-RS:** La lógica de fallback chain es agnostica. Candidato para glory-rs como utilidad de API con rate limit.

## 084A-29 — Context token management (truncamiento inteligente de historial)

- **Qué:** Implementadas funciones `estimate_tokens()` (heurística chars/4+1) y `build_context_messages()` (truncamiento inteligente). El presupuesto de tokens (~16k) se calcula descontando system_prompt + user_message + 1000 buffer. Los mensajes más antiguos que no caben se comprimen en un resumen inline. Los recientes se incluyen verbatim. Extraído como función independiente para cumplir clippy max 100 líneas.
- **Archivos:** `src/services/ai_chat.rs`
- **Gotchas:** Clippy `too_many_lines` obligó a extraer de `generate_response` a función helper separada.
- **Sentinel:** No aplica.
- **GLORY-RS:** `estimate_tokens` y `build_context_messages` son agnosticos. Candidatos para glory-rs como utilidades de contexto de chat IA.

## 084A-28 — Botones contextuales de soporte (chat abre con contexto de página)

- **Qué:** Implementado sistema de contexto de origen para el chat. Cuando el usuario abre el chat desde un botón de soporte de hosting, servicio o cualquier página, se pasa un contexto que la IA usa para personalizar su saludo. Flujo: chatStore.abrir(context) → buildVisitorWsUrl con param context → VisitorWsParams.context → TimingSessionDeps → AiSessionContext.context → build_system_prompt con append_page_context(). Soporta: `hosting:{uuid}`, `service:{slug}`, `page:{nombre}`.
- **Archivos:** chatStore.ts, chat.ts, useChatWidget.ts, ChatWidget.tsx, HostingDetalle.tsx, SolucionHostingIsland.tsx, ServicioIndividualIsland.tsx, SeccionContacto.tsx, Header.tsx, mod.rs, ws_visitor.rs, rest.rs, ai_chat.rs, chat_timing.rs
- **Gotchas:** onClick={abrirChat} directo ya no compila (MouseEvent vs string) — wrapear con arrow fn. Clippy similar_names context/content — renombrar a page_ctx.
- **Sentinel:** No aplica.
- **GLORY-RS:** Patrón de contexto de origen es agnostico. Candidato para glory-rs.

## 084A-30 — Auditoría de seguridad del sistema de chat

- **Qué:** Auditoría completa de seguridad del chat (15 hallazgos). Correcciones implementadas:
  1. **File upload extension mapping:** Reemplazar derivación de extensión desde nombre original (`rsplit('.')`) con `mime_to_safe_extension()` que mapea MIME→extensión segura. Previene subida de .php/.exe disfrazados.
  2. **Prompt injection sanitization:** Nuevo helper `sanitize_for_prompt()` que elimina caracteres de control, trunca longitud, y strip de keywords peligrosas. Aplicado a: nombre/email/resumen de visitante, nombre/email de usuario registrado, nombre de página en contexto.
  3. **Blog XSS:** Instalado DOMPurify y sanitizado `dangerouslySetInnerHTML` en `BlogSingleIsland.tsx`.
- **Archivos:**
  - `src/handlers/chat/rest.rs` — función `mime_to_safe_extension()`, uso en `upload_chat_file`
  - `src/services/ai_chat.rs` — función `sanitize_for_prompt()`, aplicada en 3 helpers de contexto
  - `frontend/src/islands/BlogSingleIsland.tsx` — DOMPurify sanitize
  - `frontend/package.json` — dependencia dompurify + @types/dompurify
- **Gotchas:** Hallazgos de .env exposure ya cubiertos por .gitignore. Rate limit bypass y CORS son mejoras futuras de menor riesgo.
- **Sentinel:** No aplica — reglas de seguridad son runtime, no estáticas.
- **GLORY-RS:** `sanitize_for_prompt` y `mime_to_safe_extension` son agnosticos. Candidatos para glory-rs.

## 084A-31 — System prompt: prohibir simulación de herramientas

- **Qué:** Reescrito system prompt con sección "REGLA CRÍTICA — PROHIBIDO SIMULAR ACCIONES". El modelo ahora tiene instrucciones explícitas de NUNCA escribir texto que simule facturas, links ficticios u outputs de herramientas. Flujo de factura documentado paso a paso: sin email→capture_email primero, con email→create_invoice directo. Descripciones de herramientas cambiadas de sugerentes ("Úsala cuando...") a obligatorias ("Úsala SIEMPRE que...").
- **Archivos:** `src/services/ai_chat.rs`
- **Gotchas:** El problema original era que el modelo generaba texto que parecía factura en lugar de llamar `create_invoice`. Toda la pipeline (tool→RichMessage→WS→frontend card con botón) funciona correctamente; el fallo era en el prompt.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón anti-simulación en system prompts es agnostico. Candidato para un kit de system prompt en glory-rs.

## 084A-32 — Context budget 64k + resumen mejorado

- **Qué:** Budget de tokens subido de 16k a 64k (Groq soporta 131k). Overhead de 1000 a 2000 tokens. Truncamiento de resumen mejorado: de 3000 chars con corte brusco a 8000 chars con tail-selection (prioriza mensajes más recientes del lote truncado) e indicador "[...N mensajes omitidos...]".
- **Archivos:** `src/services/ai_chat.rs` — `build_context_messages()`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** `build_context_messages` ya es candidato a glory-rs (agnostico).

## 084A-34 — Verificar rotación de API keys + fallback de modelos

- **Qué:** Verificación empírica del código de rotación. Hallazgos: (1) Round-robin funciona correctamente (AtomicUsize fetch_add con mod), (2) fallback modelo solo si TODAS las keys dan 429 — correcto. Mejoras: añadido `llama-4-maverick-17b-128e-instruct` (400B MoE, 128 expertos) como modelo primario default y #1 en fallback chain (era el más capaz y no estaba). Log de éxito ahora incluye modelo y key usados en CADA request (antes solo logeaba fallback). Default cambiado de gpt-oss-120b a maverick.
- **Archivos:** `src/services/ai_chat.rs` — `from_env()`, `model_fallback_chain()`, `call_groq_api()`
- **Gotchas:** El usuario ya había commiteado maverick en un commit previo pero no se había actualizado el código Rust. Los logs existentes ahora permiten verificar empíricamente la rotación en producción revisando los logs del servidor.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de rotación de keys + fallback de modelos es completamente agnostico. Candidato para glory-rs como utilidad de multi-model AI client.

## 084A-33 — Plan de pruebas E2E + unit tests del chatbot

- **Qué:** Creado plan completo de testing E2E en `Agente/planes/plan-testing-chatbot-e2e-2026-04-10.md` (12 categorías: API rotation, system prompt, tools, timing, WS, frontend, errores, seguridad, profiles, escalación, rich messages, file upload). Implementados 11 unit tests en `ai_chat.rs`: sanitización (3), estimación tokens (1), parseo escalación (2), contexto (1), fallback chain (2), round-robin keys (2).
- **Archivos:** `src/services/ai_chat.rs` (módulo `tests`), `Agente/planes/plan-testing-chatbot-e2e-2026-04-10.md`
- **Gotchas:** Tests de round-robin usan `AtomicUsize` global — si se ejecutan en paralelo con otros tests que llamen `next_key`, el orden puede variar. Pero el test verifica wrap-around (k4==k1) que es independiente del offset.
- **Sentinel:** No aplica.
- **GLORY-RS:** Tests de sanitización, tokens, y fallback son agnosticos. Candidatos para glory-rs test suite.

## 084A-36 — Eliminar maverick deprecated, usar gpt-oss-120b

- **Qué:** Eliminado `meta-llama/llama-4-maverick-17b-128e-instruct` de toda la base de código (deprecated en Groq abril 2026). El modelo primario ahora es `openai/gpt-oss-120b`. Actualizada whitelist de modelos (`allowed_models`), cadena de fallback (`model_fallback_chain`), default de `AI_MODEL`, y 2 unit tests que referenciaban maverick. La cadena Groq ahora tiene 6 modelos: gpt-oss-120b → scout → qwen3-32b → llama-3.3-70b → gpt-oss-20b → llama-3.1-8b.
- **Archivos:** `src/services/ai_chat.rs`
- **Gotchas:** Maverick no aparece en la lista de modelos de Groq (ni production, ni preview, ni deprecated). Fue eliminado silenciosamente.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — lógica específica de proveedores de IA de Nakomi.

## 084A-37 — Agregar Google Gemini como segundo proveedor AI

- **Qué:** Implementado soporte multi-proveedor: Groq (primario, múltiples keys × modelos) → Gemini (fallback, OpenAI-compatible). La API de Gemini usa el mismo formato de request que Groq (endpoint `/chat/completions`). Se refactorizó `call_groq_api` en 3 funciones: `call_groq_api` (dispatcher), `try_groq_provider` (itera modelos×keys Groq), `try_gemini_provider` (itera modelos Gemini: gemini-2.5-flash → gemini-2.5-pro). Se extrajo `build_chat_body` como helper compartido. Nuevos campos en `AiChatConfig`: `gemini_key`, `gemini_url`. Env var: `GOOGLE_GEMINI_API`. 3 tests nuevos: `is_configured_with_only_gemini`, `is_configured_with_groq_and_gemini`, `gemini_url_default`. Helper `test_config` para reducir boilerplate en tests.
- **Archivos:** `src/services/ai_chat.rs`, `.env.example`
- **Gotchas:** La autenticación Gemini usa `Bearer {key}` igual que Groq (OpenAI-compatible). Los modelos Gemini no necesitan prefijo de proveedor (solo `gemini-2.5-flash`, no `google/gemini-2.5-flash`). El usuario pidió "Gemma 4" pero ese modelo no existe — Gemini es la línea correcta.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón multi-proveedor OpenAI-compatible es agnostico y podría moverse a glory-rs. Pendiente evaluación cuando glory-rs tenga módulo AI.

## 084A-38 — Fix factura duplicada/texto plano + monto $0.00

- **Qué:** Corregidos 3 bugs relacionados en el flujo de facturación del chatbot:
  1. **Bug raíz ($0.00 en Stripe):** La respuesta de `invoiceitems` (line item) se ignoraba con `_item_resp`. Si la creación del line item fallaba silenciosamente, el invoice se finalizaba sin monto → $0.00 → Stripe lo marcaba como "paid" instantáneamente. Fix: validar `item_resp.status().is_success()` y retornar error si falla.
  2. **Bug consecuencia ("Pagada" inmediato):** Invoice $0.00 es trivialmente "paid" en Stripe → frontend mostraba badge "Pagada" y ocultaba botón de pago. Fix: agregar estado "Pendiente" para status "open"/"draft"/"". Botón "Pagar ahora" visible siempre que no sea "paid".
  3. **Bug texto plano (link repetido):** `tool_result_json` incluía `payment_url`, el modelo AI lo repetía en texto. Fix: eliminar `payment_url` del JSON del tool result y agregar mensaje explícito "NO repitas el link de pago".
- **Archivos:** `src/services/ai_tools.rs`, `frontend/src/components/chat/ChatWidget.tsx`, `frontend/src/components/chat/ChatWidget.css`
- **Gotchas:** La validación del line item era el bug raíz que causaba los otros dos. El `_item_resp` con prefijo `_` hacía que clippy no advirtiera del resultado ignorado.
- **Sentinel:** Regla candidata: detectar `let _resp = client.post(...)` sin validar status en código de producción.
- **GLORY-RS:** No aplica — lógica específica de Stripe invoicing de Nakomi.
## 084A-40  Comando /reset en chat
- **Qu�:** Implementar comando `/reset` que el visitante puede escribir en el chat para borrar todo su contexto (mensajes, perfil, sesi�n) y empezar de cero. �til para testeo.
- **Archivos:**
  - `src/repositories/chat.rs`  nuevos m�todos `delete_session_messages` y `delete_visitor_profile`
  - `src/models/chat.rs`  variante `Reset` en `WsServerMessage`
  - `src/handlers/chat/ws_visitor.rs`  funci�n `handle_reset` + detecci�n de `/reset` en mensajes
  - `frontend/src/api/chat.ts`  tipo `'reset'` en `WsServerMessage`
  - `frontend/src/hooks/useChatWidget.ts`  handler `reset`: limpia localStorage, mensajes, desconecta
- **Gotchas:** No se hard-delete la sesi�n (`close_session` la marca `closed`), solo se borran mensajes y perfil. El visitante al reconectar obtiene nuevo `visitor_id` (UUID generado en frontend) y nueva sesi�n.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica (l�gica espec�fica de Nakomi)
## 084A-41  Ampliar modelos Gemini
- **Qu�:** Expandir cadena fallback Gemini de 2 a 6 modelos: `gemini-2.5-flash`, `gemini-2.5-flash-lite`, `gemini-2.5-pro` (estables) + `gemini-3-flash-preview`, `gemini-3.1-pro-preview`, `gemini-3.1-flash-lite-preview` (previews). Gemini 3 Pro excluido (deprecated 9 mar 2026).
- **Archivos:** `src/services/ai_chat.rs`  array `gemini_models` en `try_gemini_provider`
- **Gotchas:** Los modelos preview pueden deprecarse con 2 semanas de aviso. Los estables son m�s fiables para producci�n.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica (configuraci�n espec�fica de Nakomi)