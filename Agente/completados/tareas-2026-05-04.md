## 045A-1 — Wallet seed y render: datos visibles en panel
- **Que:** se corrigió el render del frontend para aceptar los campos reales del backend (`transactions` y `requests` en vez de asumir `items`), se añadieron etiquetas legibles para tipos `credit`, `debit`, `refund` y `commission`, y el seed ahora limpia/recrea wallet + retiros para la cuenta que ejecuta la acción desde Configuración, no solo para emails hardcodeados. También se verificó el flujo real: `POST /api/admin/seed` sobre la BD de la rama (`glory_backend_glory_rust_nakomi`) y render correcto en la wallet de `cliente@test.com`.
- **Archivos:** `src/handlers/admin_seed.rs`, `src/services/seed.rs`, `frontend/src/api/wallet.ts`, `frontend/src/hooks/useWallet.ts`, `frontend/src/components/panel/SeccionWallet.tsx`
- **Gotchas:** `npm run dev` no usa necesariamente la BD de `.env`; usa una BD aislada por rama (`glory_backend_glory_rust_nakomi`). La primera comprobación contra `glory_kamples` daba falso negativo. Además, el saldo admin estaba bien en la BD pero el frontend seguía vaciando tablas por el desajuste de nombres de campos.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 045A-2 — CMS servicios: update con validacion y errores 4xx utiles
- **Que:** se endureció el update admin de servicios para validar el payload antes de persistir, pasar por params tipados en repositorio y traducir errores frecuentes de BD a respuestas de dominio (`409` para slug duplicado, `422` para longitudes/constraints) en vez de dejar un `500` genérico en el CMS.
- **Archivos:** `src/handlers/admin_services.rs`, `src/models/service.rs`, `src/repositories/service.rs`, `src/repositories/mod.rs`, `Agente/documentacion/panel/servicios-admin-validacion-2026-05-04.md`
- **Gotchas:** no apareció una traza local única del `PUT 500`, así que la corrección fue sobre la fragilidad estructural del slice de services: faltaba `validate()` en update y los errores SQL comunes subían como `Internal`.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 045A-3 — Catalogo publico: shell compartida para servicios y proyectos
- **Que:** se extrajo `CatalogPageShell` para que servicios y proyectos compartan hero, contenedor y espaciado responsive; servicios deja de sumar padding lateral interno y ambos catálogos quedan alineados bajo una misma receta visual.
- **Archivos:** `frontend/src/components/layout/CatalogPageShell.tsx`, `frontend/src/components/layout/CatalogPageShell.css`, `frontend/src/islands/ServiciosIsland.tsx`, `frontend/src/islands/ProyectosIsland.tsx`, `frontend/src/islands/ProyectosIsland.css`, `Agente/documentacion/publico/catalogo-shell-compartida-2026-05-04.md`
- **Gotchas:** `serviciosContenedor` y `proyectosContenedor` ya estaban reutilizados en otras superficies (`home` y `panel`), así que el refactor necesitó clases nuevas `catalogPage*` en vez de retocar nombres legacy.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 045A-4 — CMS servicios: conflicto de slug visible y prevalidado
- **Que:** se ajustó el panel para que los errores de Axios de servicios usen el `message` real devuelto por la API y, además, se añadió un preflight local de slugs duplicados antes de hacer `PUT/POST`, evitando roundtrips innecesarios cuando el conflicto ya está en la lista cargada.
- **Archivos:** `frontend/src/hooks/useContenidoServicios.ts`, `frontend/src/components/panel/SubTabServicios.tsx`, `Agente/documentacion/panel/servicios-admin-validacion-2026-05-04.md`
- **Gotchas:** el backend ya respondía correctamente con `409`, pero el hook del CMS lo degradaba a `err.message`; el problema restante era de propagación UX, no de contrato HTTP.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 045A-5 — CMS servicios: planes validados antes de persistir
- **Que:** se cerró el hueco del segundo paso de guardado (`PUT /api/admin/services/:id/plans`) validando localmente slug/nombre/fases/títulos de planes antes del submit y capturando el `422` del endpoint con el mensaje real del backend cuando aún así falla.
- **Archivos:** `frontend/src/components/panel/SubTabServicios.tsx`, `Agente/documentacion/panel/servicios-admin-validacion-2026-05-04.md`
- **Gotchas:** el servicio principal podía guardarse y luego fallar solo la persistencia de planes, dejando la sensación de “guardado parcial”. El fix se aplicó en el boundary del editor, no en el repositorio de servicios.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 045A-6 — CMS servicios: no guardar planes si no cambiaron
- **Que:** se ajustó el editor para comparar el snapshot original de planes con el payload actual y saltar `PUT /api/admin/services/:id/plans` cuando solo cambian campos generales del servicio, como imagen, SEO o metadatos.
- **Archivos:** `frontend/src/components/panel/SubTabServicios.tsx`, `Agente/documentacion/panel/servicios-admin-validacion-2026-05-04.md`
- **Gotchas:** el 422 persistía aunque el cambio principal se guardara porque el editor seguía disparando el guardado de planes en todos los casos; el error no estaba en `image_url`, sino en un segundo request redundante.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 045A-7 — Editor inline: unificar el control path de servicios
- **Que:** se extrajo la lógica de validación/detección de cambios de planes a un helper compartido y se conectó también al `AdminEditorProvider`, que era el camino real usado desde `/servicios` y seguía lanzando `PUT /plans` aunque no hubiera cambios en planes.
- **Archivos:** `frontend/src/utils/servicePlanEditorUtils.ts`, `frontend/src/components/panel/SubTabServicios.tsx`, `frontend/src/components/AdminEditorProvider.tsx`, `Agente/documentacion/panel/servicios-admin-validacion-2026-05-04.md`
- **Gotchas:** el fix previo estaba correcto para el CMS panel, pero no tocaba el overlay admin de páginas públicas. El stack `AdminEditorProvider.tsx:142` fue la pista que faltaba.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.
