## 045A-1 — Wallet seed y render: datos visibles en panel
- **Que:** se corrigió el render del frontend para aceptar los campos reales del backend (`transactions` y `requests` en vez de asumir `items`), se añadieron etiquetas legibles para tipos `credit`, `debit`, `refund` y `commission`, y el seed ahora limpia/recrea wallet + retiros para la cuenta que ejecuta la acción desde Configuración, no solo para emails hardcodeados. También se verificó el flujo real: `POST /api/admin/seed` sobre la BD de la rama (`glory_backend_glory_rust_nakomi`) y render correcto en la wallet de `cliente@test.com`.
- **Archivos:** `src/handlers/admin_seed.rs`, `src/services/seed.rs`, `frontend/src/api/wallet.ts`, `frontend/src/hooks/useWallet.ts`, `frontend/src/components/panel/SeccionWallet.tsx`
- **Gotchas:** `npm run dev` no usa necesariamente la BD de `.env`; usa una BD aislada por rama (`glory_backend_glory_rust_nakomi`). La primera comprobación contra `glory_kamples` daba falso negativo. Además, el saldo admin estaba bien en la BD pero el frontend seguía vaciando tablas por el desajuste de nombres de campos.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.
