# Tareas completadas — 2026-04-15

## 154A-1 — Snap horizontal/vertical al dibujar paredes + largo mínimo
- **Qué:** Las paredes en el plano de sala ahora solo se dibujan en ejes horizontal y vertical (snap automático), y se requiere un largo mínimo de 30px para que la pared se cree.
- **Archivos:** `frontend/src/componentes/plano-sala/usePlanoSala.ts`
- **Cambios técnicos:**
  - `handleWallDrawMove`: Reemplazado cálculo libre con `atan2` por snap basado en eje dominante (`|dx| >= |dy|` → horizontal, else vertical). Longitud proyectada solo al eje dominante. Constante `MIN_WALL_LENGTH = 30`.
  - `handleWallDrawEnd`: Guard que descarta paredes menores a `MIN_WALL_LENGTH`.
- **Gotchas:** La rotación post-creación en `ParedDraggable.tsx` ya tenía snap a 15° — no se modificó.
- **Sentinel:** No aplica — cambio puramente frontend de lógica de interacción.
