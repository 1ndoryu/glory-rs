## 274A-1 - Corrige render JSON en AdminPanelIsland

- **Que:** Se corrigio el crash de React en `AdminPanelIsland` cuando la pestaña de moderación intentaba renderizar `moderacion_detalle` dentro de `TarjetaHistorial`. El helper de render ahora convierte cualquier `moderacion_detalle` a string segura antes de pintarlo en `<pre>`, y el tipo frontend se ajusto de `string | null` a `unknown` para reflejar que el backend puede entregar JSON ya parseado.
- **Archivos:** `frontend/src/legacy/components/admin/TabModeracionAdmin.tsx`, `frontend/src/legacy/services/apiAdmin.ts`, `Agente/documentacion/frontend/admin-moderacion-json-2026-04-27.md`
- **Gotchas:** La traza apuntaba al `setState` del hook `useAdminPanel`, pero la causa real estaba en el presenter `TarjetaHistorial`. El navegador del agente no tenia sesion admin, asi que la validacion se hizo con la traza del usuario + `npx tsc --noEmit`.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Es comportamiento especifico del panel admin de Kamples.

## 274A-2 - Corrige listado de publicaciones en comunidad y perfil

- **Que:** Se corrigio el desajuste entre el contrato Rust de `GET /api/publicaciones` y los hooks legacy que consumen ese listado. El backend ya responde con `{ items, page, per_page }`, pero comunidad y perfil seguian leyendo un shape antiguo basado en `data`, así que convertían la respuesta a lista vacía aunque existieran publicaciones. Ambos hooks ahora normalizan `items`, `data` y arrays directos.
- **Archivos:** `frontend/src/legacy/hooks/useComunidadIsland.tsx`, `frontend/src/legacy/hooks/usePerfilIsland.ts`, `Agente/documentacion/frontend/publicaciones-listado-items-2026-04-27.md`
- **Gotchas:** La landing pública de `/comunidad/` oculta el feed cuando no hay sesión, así que la verificación visual desde el navegador del agente era ambigua. La comprobación decisiva fue `curl http://localhost:3000/api/publicaciones?page=1`, que confirmó que el backend devolvía posts reales bajo `items`.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Es compatibilidad específica entre este frontend legacy y el contrato del backend Kamples.

## 274A-3 - Unifica BD del scraper Kamples y arregla mirror local de imágenes

- **Que:** El scraper Python escribía a una BD distinta (`kamples`, 3116 canciones) de la que el backend Rust lee (`glory_kamples`, 73 canciones residuales), por lo que el frontend solo veía registros viejos sin nombre ni portada. Ademas las imagen_url restantes apuntaban al CDN de WhoSampled (que rechaza hotlinking con 403) o a `http://glory.local/wp-content/...` (entorno WordPress legacy ya inexistente). Tampoco se permitía el tipo de página `track_overview` en el `CHECK` de `scraping_log`, lo que rompía silenciosamente el dedup del scraper.
  Cambios:
    1. Migración SQL `20260427000001_scraping_log_track_overview.up.sql` que añade `track_overview` al `CHECK CONSTRAINT` (idempotente con `DROP CONSTRAINT IF EXISTS`).
    2. Volcado de las 6 tablas del scraper desde `kamples` (legacy) a `glory_kamples` con `pg_dump --data-only --column-inserts --disable-triggers`. Las 4 filas de `samples` (producto, FKs NULL) y demás tablas no-scraper se preservaron via `DELETE` ordenado en lugar de `TRUNCATE CASCADE`. Sequences reseteadas a `MAX(id)+1`.
    3. BD `kamples` renombrada a `kamples_legacy_2026_04_27` (no se elimina, queda como respaldo). Backups dump comprimido en `backups/`.
    4. `.env` raíz: `KAMPLES_PG_DBNAME` cambiado de `kamples` → `glory_kamples`. Añadidos `IMAGES_STORE_PATH` y `IMAGES_BASE_URL` apuntando a `uploads/kamples/portadas` (servido por Axum vía `ServeDir` montado en `/uploads`).
    5. `clients/kamples-scraper/.env` (nuevo) y `.env.example` (actualizado) con valores Windows-friendly y precedencia `KAMPLES_PG_*` > `DB_*`.
    6. 3118 archivos de portadas físicos copiados desde `WP/.../wp-content/uploads/kamples/portadas` a `uploads/kamples/portadas`. UPDATE masivo `REPLACE('http://glory.local/wp-content/uploads/kamples/portadas', '/uploads/kamples/portadas')` migró 3115 imagen_url a la URL relativa nueva. La única externa restante (1 fila) se descargó vía script y, al fallar el proxy DataImpulse con 407, quedó NULL.
    7. Script `clients/kamples-scraper/scripts/backfill_imagenes.py` reutilizable: recorre canciones+artistas con imagen externa, descarga al mirror local con curl_cffi+proxy y deja la URL relativa en BD (o NULL si falla tras 3 intentos).
- **Archivos:**
    - `migrations/20260427000001_scraping_log_track_overview.up.sql` (nuevo)
    - `.env` (KAMPLES_PG_DBNAME, IMAGES_STORE_PATH, IMAGES_BASE_URL)
    - `clients/kamples-scraper/.env` (nuevo, gitignored)
    - `clients/kamples-scraper/.env.example` (actualizado a glory_kamples + comentarios)
    - `clients/kamples-scraper/scripts/backfill_imagenes.py` (nuevo)
    - `.gitignore` (añadido `backups/`)
    - `uploads/kamples/portadas/` (3118 imágenes copiadas, gitignored)
    - `Agente/completados/tareas-2026-04-27.md` (esta entrada)
- **Gotchas:**
    - `canciones_artistas` no tiene columna `id` — es tabla de enlace pura (cancion_id + artista_id). El reset de sequences debe omitirla.
    - `TRUNCATE ... CASCADE` sobre `canciones` habría barrido las 4 filas de `samples` (producto, no scraper). Solución: `DELETE` ordenado por dependencias (cola_extraccion_samples → canciones_artistas → relaciones_sample → canciones → artistas_musicales → scraping_log).
    - Las 3115 filas con `imagen_url = http://glory.local/wp-content/...` revelaron que un agente anterior YA había bajado las imágenes — solo apuntaban al entorno WordPress legacy. La copia de archivos físicos + REPLACE de URLs era necesaria, no un re-scrape completo.
    - El proxy DataImpulse responde 407 en este momento (credenciales válidas pero bloqueadas o presupuesto agotado). Para descargar las pocas imágenes externas restantes hace falta resolverlo o usar otro residencial.
    - PowerShell `psql.exe -c` aborta con `WIN1252` en datos UTF-8 — siempre `$env:PGCLIENTENCODING='UTF8'` antes de queries con strings extendidos.
- **Sentinel:** Falso positivo posible si Code Sentinel marca `os.getenv` sin defaults en `backfill_imagenes.py`; se valida `IMAGES_STORE_PATH`/`IMAGES_BASE_URL` antes de continuar y se hace `sys.exit(2)` si faltan. No requiere regla nueva.
- **GLORY-RS:** No aplica. El scraper, las migraciones de scraping_log y la lógica de mirror de imágenes son específicos del producto Kamples (no genéricos). El framework Glory ya cubre `ServeDir`/`/uploads` agnósticamente; no hay nada que mover.

## 274A-3 - Normaliza PostDetail Rust para TarjetaPublicacion legacy

- **Que:** Se corrigió el crash de runtime en `TarjetaPublicacion` dentro de comunidad. El backend Rust ya entregaba publicaciones reales, pero en shape `PostDetail` snake_case; la UI legacy esperaba `Publicacion` camelCase. Se agregó un normalizador central de publicaciones en `apiSocial` para adaptar campos como `autor_id`, `samples_adjuntos`, `created_at`, `mi_reaccion` y `yo_ya_repostee` antes de que lleguen a los componentes.
- **Archivos:** `frontend/src/legacy/services/normalizers/postNormalizer.ts`, `frontend/src/legacy/services/apiSocial.ts`, `frontend/src/legacy/hooks/useComunidadIsland.tsx`, `Agente/documentacion/frontend/publicaciones-normalizacion-rust-legacy-2026-04-27.md`
- **Gotchas:** El IDE no lo marcaba porque era una discrepancia de payload real en runtime, no un error estático. La primera corrección de `items` vs `data` permitió por fin renderizar posts y eso expuso el segundo fallo en `samplesAdjuntos.length`.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Es adaptación puntual entre el contrato Rust de Kamples y la UI legacy del mismo proyecto.

## 274A-4 - Permite a admin borrar publicaciones ajenas

- **Que:** Se corrigió la 403 al intentar borrar publicaciones ajenas desde comunidad con un usuario admin. `DELETE /api/publicaciones/:id` exigía autoría exacta y bloqueaba incluso a admins; ahora permite borrar si el usuario tiene `rol == "admin"`, manteniendo intacta la restricción de no usar este endpoint para reposts.
- **Archivos:** `src/handlers/posts.rs`, `Agente/documentacion/backend/admin-delete-posts-2026-04-27.md`
- **Gotchas:** `soft_delete()` necesita el `autor_id` real del post, no el `user_id` del admin. Por eso el nuevo guard devuelve `owner_id` y el handler lo reutiliza al persistir. La búsqueda textual no encontró esos nombres smoke en los archivos del repo, así que todo apunta a residuos de smoke tests previos en la BD local y no a fixtures versionados.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Es política de permisos específica del backend Kamples.

## 274A-5 - Admin delete posts: validar rol contra BD

- **Que:** `ensure_can_delete_original_post` ya no confia en el claim `rol` del JWT (puede quedar viejo si el usuario fue elevado a admin tras emitirse la sesion). Ahora consulta `UserRepository::find_by_id` y compara el rol actual de la BD. Esto evita 403 falsos hasta que el usuario vuelva a iniciar sesion.
- **Archivos:** `src/handlers/posts.rs`
- **Gotchas:** Coste minimo (un SELECT por delete). Alternativa seria invalidar JWT al cambiar rol, mas invasiva.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Politica especifica del backend Kamples.

## 274A-6 - Fix 500 detalle cancion + normalizador snake/camel

- **Que:** Se arreglaron dos bugs simultaneos. (1) `GET /api/canciones/:slug` devolvia 500 porque `sqlx::query_as!` no infiere nullability en columnas de LEFT JOIN; `u.username` (de `LEFT JOIN usuarios_ext`) intentaba decodificarse como `String` no nulo. Se anoto `AS \"contribuidor_username?: String\"` en las 6 instancias de las consultas de relaciones (sample) en `songs_read.rs`, `artists_read.rs`, `relations_read.rs`, y se regenero `.sqlx/` con `cargo sqlx prepare`. (2) Las tarjetas de cancion no mostraban imagen ni nombre de artista porque el backend Rust serializa con serde default (`snake_case`: `imagen_url`, `artista_nombre`) y los componentes legacy esperan `imagenUrl` / `artistaNombre` (camelCase). Se agrego un normalizador `normalizarCancion` en `apiCanciones.ts` aplicado a `listarCanciones`, `listarCancionesPaginado`, `buscarCanciones`, `cancionesTopSampleadas`, `feedCanciones`, `seccionesCanciones` y `obtenerCancionDetalle`. Es tolerante: si ya viene en camelCase lo respeta, siguiendo el patron existente en `apiBusqueda.ts` / `coleccionesNormalizer.ts`.
- **Archivos:** `src/repositories/music/songs_read.rs`, `src/repositories/music/artists_read.rs`, `src/repositories/music/relations_read.rs`, `.sqlx/*.json`, `frontend/src/legacy/services/apiCanciones.ts`
- **Gotchas:** SQLx no marca columnas de LEFT JOIN como nullable automaticamente; cuando el struct destino ya es `Option<T>` hay que usar `AS \"name?: Type\"` para evitar E0282 (type inference). Solucion estructural pendiente: evaluar `#[serde(rename_all = \"camelCase\")]` en backend Rust + regenerar Orval (eliminaria todos los normalizadores manuales).
- **Sentinel:** No aplica (sintoma runtime de SQL + contrato cross-stack, no patron sintactico).
- **GLORY-RS:** No aplica. Logica especifica de Kamples.

## 274A-6b - Normalizar RelacionSample + artistas en CancionDetalle

- **Que:** `CancionDetalleIsland` crasheaba con `TypeError: Cannot read properties of undefined (reading 'length')` en samplesDe.length. El backend retorna `samples_de` / `sampleada_en` en snake_case; la desestructuracion `{ samplesDe }` daba `undefined`. Se reescribio `obtenerCancionDetalle` para construir explicitamente el objeto `CancionDetalle` con mapeos de campo y un normalizador `normalizarRelacion` que convierte cada `RelacionSample` de snake_case a camelCase. Se agrego tambien `normalizarArtista` para el array `artistas`.
- **Archivos:** `frontend/src/legacy/services/apiCanciones.ts`
- **Gotchas:** El spread `{ ...resp.data }` sobre el payload raw propagaba campos snake_case directamente. El patron correcto es construir el objeto destino campo por campo con fallback `raw.campo ?? raw.campo_snake`.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

## 274A-7 - Endpoint GET /canciones/:slug/samples

- **Que:** El frontend llamaba `GET /api/canciones/:slug/samples` para cargar los samples de audio vinculados a una cancion (via `cancion_origen_id`), pero la ruta no existia en el backend Rust dando 404 permanente. Se implemento: (1) metodo `SampleRepository::list_by_cancion_id` en `repositories/sample_catalog.rs` que usa `SAMPLE_SUMMARY_SELECT` + filtro `cancion_origen_id = $1` + auto-hide filter; (2) handler `get_song_samples` en `handlers/music/public.rs` que resuelve el slug a cancion, llama al repo y devuelve `SongSamplesResponse { data: Vec<SampleSummary> }`; (3) ruta `.route("/canciones/:slug/samples", get(get_song_samples))` registrada antes de `/canciones/:slug` en `mod.rs`.
- **Archivos:** `src/repositories/sample_catalog.rs`, `src/handlers/music/public.rs`, `src/handlers/music/mod.rs`
- **Gotchas:** El endpoint devuelve `{data: []}` porque ningun sample tiene `cancion_origen_id` asignado aun (feature pendiente de scrapers). La ruta funciona correctamente — el array vacio es esperado. Ruta registrada antes de `/:slug` para que Axum no conflicte (en la practica los segmentos `/slug/samples` vs `/slug` no chocan porque tienen distinto numero de segmentos, pero se ordena correctamente de todas formas).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Logica especifica de Kamples.

## 254A-8c - Refactor extender-recorte: delegar al scraper Python (no usar yt-dlp en Rust)

- **Que:** Reescritura completa de las 3 operaciones admin (`POST /api/samples/:id/extender-recorte`, `/generar-siguiente`, `/restaurar-recorte`). Version anterior (mismo dia) descargaba audio con yt-dlp directamente desde Rust y recortaba con ffmpeg; eso NO funciona en produccion (Dockerfile Rust no tiene yt-dlp ni Python). Nueva version: las 3 operaciones solo escriben en `cola_extraccion_samples` (estado='pendiente', nuevo timing, `metadata_extraccion.extension_modo`) y devuelven 202 Accepted. El scraper Python existente (clients/kamples-scraper) procesa la cola en su proximo ciclo. Tambien se preserva `samples.metadata.timing_original` la primera vez que se extiende, para que `restaurar` tenga un punto de retorno.
- **Archivos:** `src/audio/mod.rs` (quito `pub mod ytdlp`), `src/audio/ytdlp.rs` (eliminado), `src/services/extension_recorte.rs` (reescrito), `src/handlers/extension_recorte.rs` (reescrito, devuelve 202 con `EncoladoResult`), `src/repositories/cola_extraccion.rs` (reescrito; `encolar_para_scraper` reemplaza `actualizar_timing` y `crear_para_segmento`), `src/repositories/sample.rs` (eliminadas funciones `find_assets_for_extension` / `replace_audio_assets` / `crear_segmento` y structs asociados), `src/repositories/mod.rs` (exports actualizados), `src/handlers/mod.rs` (OpenAPI schemas).
- **Gotchas:** (1) El subagente Explore alucino que existian `DevController::publicarExtracciones` migrado a Rust — verificacion manual confirma que NO existe el publicador Rust (filas quedan en 'extraido' indefinidamente sin nadie que cree el sample y le asigne `sample_id`). Eso queda como tarea 264A-3, bloqueante para el flujo end-to-end. (2) `scraper_admin::publicar_auto` solo hace `estado='completado' WHERE estado='extraido' AND sample_id IS NOT NULL` — no asigna sample_id. (3) El pipeline.py actual no entiende `extension_modo`; eso es la tarea 264A-4. Por ahora la fila se procesa como una extraccion mas (creara nuevo sample), no como reemplazo del existente — el flujo end-to-end requiere ambas tareas 264A-3 y 264A-4 para funcionar correctamente.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Es 100% especifico de Kamples (cola_extraccion_samples + scraper Python).


## 264A-1 - Tabla app_config + endpoints admin para configurar el scraper en caliente

- **Que:** Tabla key/value `app_config` (clave VARCHAR(64) PK, valor JSONB, descripcion, updated_at) que sirve como contrato runtime entre el backend Rust y el scraper Python. Defaults seedeados en la migracion para extraccion (intervalo_seg=60, lote_size=20, enabled=true) y scraping (intervalo_seg=900, lote_size=10, enabled=true). Endpoints admin (JWT + require_admin):
  - `GET/PUT /api/admin/config/extraccion` con validacion de rangos (intervalo 5..86400, lote 1..500).
  - `GET/PUT /api/admin/config/scraping` con rangos analogos.
  - `GET /api/admin/extraccion/stats` que retorna pendientes, en_proceso, revision_humana, extraido_sin_publicar, completados_24h, errores_24h, total — todo en una sola query con `COUNT(*) FILTER` para evitar 6 roundtrips. Asi el panel admin tiene visibilidad operacional inmediata sin abrir psql.
  Migracion aplicada localmente (CREATE TABLE + 6 INSERT defaults). El scraper Python lee estas claves en el siguiente ciclo (264A-2).
- **Archivos:** `migrations/20260427000002_app_config.up.sql` (nuevo), `src/repositories/app_config.rs` (nuevo, `AppConfigRepository` con list_by_prefix/upsert/get sin macros), `src/repositories/mod.rs` (mod + exports), `src/handlers/admin_config.rs` (nuevo, 5 endpoints), `src/handlers/mod.rs` (mod + paths + components + routes merge).
- **Gotchas:** (1) Confundi `crate::auth::current_user::CurrentUser` con la ruta real `crate::middleware::CurrentUser` — copie del patron equivocado. cargo check lo detecto. (2) `ON CONFLICT DO NOTHING` en el INSERT de defaults asegura que reaplicar la migracion (o restaurar un dump) no pisa valores ya ajustados en produccion. (3) Use `BTreeMap<&str, &Value>` en los parsers para tener un punto unico donde mapear claves a campos del struct sin repetir lookups.
- **Sentinel:** No aplica.
- **GLORY-RS:** Parcialmente agnostico — la idea de tabla `app_config` con lecturas key/value es 100% reutilizable. Pendiente de evaluar si mover `AppConfigRepository` a glory-rs cuando aparezca un segundo proyecto que necesite el mismo patron. Los endpoints admin (parsers ExtraccionConfig/ScrapingConfig) son especificos de Kamples y se quedan aqui.


## 264A-2 - pipeline.py relee app_config cada ciclo (intervalo / lote / enabled dinamicos)

- **Que:** El scraper Python ahora consulta `app_config` al inicio de cada iteracion del bucle `while True` en `main()` y aplica los valores de `extraccion_intervalo_seg`, `extraccion_lote_size` y `extraccion_enabled` sin reiniciar el proceso. Cuando admin pone `enabled=false`, el ciclo duerme un intervalo y vuelve a verificar (modo continuo) o sale (modo lote). Cuando intervalo o lote_size cambian, se aplican al siguiente ciclo y se loguea el cambio.
  Funcion `leer_app_config_extraccion()` defensiva: maneja ausencia de la tabla (entorno viejo) y errores de conexion devolviendo dict vacio + warning, asi el scraper sigue funcionando con los args originales si la BD esta caida temporalmente.
- **Archivos:** `clients/kamples-scraper/extractor/pipeline.py` (funcion nueva + modificacion del while loop principal).
- **Gotchas:** (1) El primer reemplazo accidentalmente elimino la firma de `actualizar_estado_cola` por matchear demasiado contexto; restaurado en un segundo paso. (2) Verificacion local: `python -m py_compile` + `leer_app_config_extraccion()` ejecutado contra la BD real devolvio los 3 valores esperados. (3) `RateLimiter.intervalo_seg` es atributo publico mutable, asi que basta con asignarlo para cambiar el ciclo en vivo.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Es codigo del scraper (cliente Python externo); especifico de Kamples.


## 264A-3 - Publicador Rust (DevController::publicarExtracciones)

- **Que:** Se cerro el gap end-to-end del scraper: ahora las filas de cola_extraccion_samples en estado='extraido' pasan a samples reales. Antes el endpoint POST /api/admin/scraper/publicar-auto era un no-op porque solo marcaba estado='completado' donde sample_id IS NOT NULL, pero NADIE asignaba sample_id. Implementacion:
    1. Servicio nuevo src/services/extraccion_publisher.rs con ExtraccionPublisherService::publicar_pendientes(pool, storage, limit).
    2. Detecta el modo via metadata_extraccion.extension_modo (escrito previamente por src/services/extension_recorte.rs):
       - extender / restaurar (con sample_id no nulo): reemplaza assets del sample existente, fuerza estado='procesando', limpia ruta_optimizada/waveform y encola cola_procesamiento_ia con force_recompute=true.
       - sin extension_modo o generar_siguiente: crea sample nuevo via SampleRepository::create_upload_sample (estado='procesando' + enqueue automatica del worker IA), vincula a relaciones_sample.sample_{fuente,destino}_id segun lado, fija samples.cancion_origen_id + relacion_sampleo_id.
    3. Reclamo atomico de filas con UPDATE...RETURNING + FOR UPDATE SKIP LOCKED usando procesado_at como flag de "siendo procesado". Si publicacion falla, revertir_reclamo limpia procesado_at para retry.
    4. Resolucion de creador_id sistema via env KAMPLES_SISTEMA_USUARIO_ID (fallback 1).
    5. PublicarAutoResponse ahora retorna {publicados, reemplazados, errores, detalle:[items]} en lugar de un solo contador.
- **Archivos:**
    - src/services/extraccion_publisher.rs (nuevo)
    - src/services/mod.rs (declarar pub mod)
    - src/repositories/cola_extraccion.rs (struct ColaExtraidoReclamado + reclamar_para_publicar/reclamar_para_reemplazar/marcar_completado/revertir_reclamo)
    - src/repositories/mod.rs (re-export ColaExtraidoReclamado)
    - src/handlers/scraper_admin.rs (publicar_auto delega en ExtraccionPublisherService; PublicarAutoResponse extendida)
- **Gotchas:** (1) El proyecto exige API no-macro de sqlx para codigo nuevo (sqlx::query()/query_as::<_,T>().bind()) — Code Sentinel marca esto como falso positivo, ignorar. (2) Schema cola_extraccion_samples usa SMALLINT/DECIMAL — castear a DOUBLE PRECISION en RETURNING para mapear a f64. (3) FileStorage::put_bytes solo recibe (key, &[u8]); no acepta content_type. (4) Caso "generar_siguiente" del flujo de extension queda como nuevo sample (no soporta cadenas — la cola actual sobrescribe sample_id). (5) Dedup tipo QK53 (legacy buscarSampleExistenteSimilar) NO implementado en v1; tarea futura.
- **Sentinel:** Falsos positivos esperados (sqlx no-macro). Nueva regla pendiente: detectar reclamacion de cola sin revertir_reclamo en path de error.
- **GLORY-RS:** No aplica. Es logica especifica del flujo Kamples (extraccion -> sample), acoplada a tablas relaciones_sample / cola_extraccion_samples / canciones que no existen fuera de este proyecto.

## 264A-4 - pipeline.py preserva metadata_extraccion.extension_modo

- **Que:** kamples_inserter.registrar_extraccion hacia UPDATE metadata_extraccion = %s, lo que destruia los campos extension_* que el backend Rust (src/services/extension_recorte.rs::encolar_para_scraper) habia escrito previamente. Sin esto, el publicador Rust 264A-3 no podia distinguir "es una extension/restauracion del sample X" vs "es una extraccion nueva". Cambio: COALESCE(metadata_extraccion, '{}'::jsonb) || %s::jsonb, asi la metadata nueva pisa claves comunes pero preserva extension_modo, extension_nuevo_inicio, extension_nuevo_fin, extension_solicitada_at.
- **Archivos:** clients/kamples-scraper/extractor/kamples_inserter.py
- **Gotchas:** psycopg2 ya enviaba json.dumps(metadata_extraccion); con el cast ::jsonb del SQL nuevo, el merge JSONB funciona sin cambios en el codigo Python.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Codigo del cliente scraper, especifico de Kamples.

## 274A-4 - Endpoint /api/descargas/comprados

- **Que:** Se implemento el endpoint que faltaba en el port Rust (404 reportado por el usuario en /descargas tab Comprados). Nueva query en BillingRepository::list_purchased_sample_ids que agrega transacciones tipo='compra_sample' con estado completada/completed por sample_id, ordenadas por MAX(created_at) DESC. El handler reusa SampleCatalogService::list_public_samples_by_ids para construir SampleSummary con assets/flags normalizados (incluido ya_comprado por usuario actual). Respuesta { ok: true, data: SampleSummary[] } compatible con el legacy frontend (apiDescargas.obtenerComprados).
- **Archivos:** src/repositories/billing.rs, src/handlers/downloads.rs, src/handlers/mod.rs, .sqlx/query-5eeb6893*.json
- **Gotchas:** primer intento usaba DISTINCT ON (sample_id) ORDER BY sample_id, created_at DESC — esto NO ordena el resultado por recencia (ordena por sample_id ASC). Se reemplazo por GROUP BY sample_id ORDER BY MAX(created_at) DESC, que devuelve un id por sample y respeta orden de compra mas reciente. Columna en transacciones es created_at (no creado_at).
- **Sentinel:** No aplica. La trampa de DISTINCT ON con ORDER BY mixto es generica de Postgres, no algo que un linter detecte sin contexto semantico.
- **GLORY-RS:** No aplica. Logica de dominio Kamples (transacciones, samples comprados).

## 274A-5 - wpJsonStub conserva /me/coleccionados

- **Que:** El proxy fetch del frontend reescribia /me/coleccionados[/...] a /users/me/coleccionados[/...] (regla generica /me/* -> /users/me/*), pero el backend Rust sirve esos paths en /api/me/coleccionados (handlers/biblioteca.rs). Resultado: 404 visible en /explorador y /descargas. Se introdujo PATH_KEEP_PREFIXES con coincidencia por prefijo (path === prefix || path.startsWith(prefix + '/')) para soportar segmentos dinamicos como /me/coleccionados/{id}/carpeta. PATH_MAP_KEEP (Set exacto) se mantiene para casos sin subrutas como /me/bloqueados.
- **Archivos:** frontend/src/bootstrap/wpJsonStub.ts
- **Gotchas:** la opcion ingenua de meter /me/coleccionados en el Set existente solo cubria el path raiz; las subrutas /carpetas y /:id/carpeta seguirian rotas. La logica por prefijo es la correcta para futuros bloques /me/* con subrutas en Rust.
- **Sentinel:** No aplica (es una regla de mapping, no un patron detectable).
- **GLORY-RS:** No aplica. wpJsonStub es shim especifico de la migracion Kamples PHP -> Rust.


## 274A-6 - Endpoint /me/descargas/sugerencias
- **Que:** Motor de "Mas Ideas" replica SugerenciasController + buscarPorScoring legacy. Agrega contexto de descargas + coleccionados (via colecciones.usuario_id JOIN coleccion_samples), calcula top 10 tags, BPM promedio (default 120 si no hay datos), key dominante, llama SampleRepository::find_samples_by_aggregated_scoring que construye CASE WHEN por tag dinamicamente con QueryBuilder.
- **Archivos:** src/repositories/sample_catalog/scoring.rs (nuevo), src/repositories/sample_catalog.rs, src/repositories/biblioteca.rs (4 funciones contexto + struct SampleContextRow), src/repositories/mod.rs, src/handlers/suggestions.rs (nuevo), src/handlers/mod.rs, .sqlx/ (4 queries).
- **Gotchas:** push_auto_hide_filter y SAMPLE_SUMMARY_SELECT son pub(super) — reusables desde scoring.rs sin duplicar. Excluye con != ALL($ids) en lugar de NOT IN expandido. coleccion_samples (template Rust) NO tiene usuario_id directo; se requiere JOIN con colecciones (la nueva tabla). Contrato frontend espera {data: SampleSummary[]} sin wrapper extra.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Logica especifica Kamples (sugerencias musicales por tags/BPM/key).


## 274A-7 - Endpoint /me/favoritos
- **Que:** GET /api/me/favoritos lista samples likeados/encantados por el usuario, con paginacion, busqueda, filtros solo_like/solo_encanta y orden (recientes/antiguos/populares/descargas). INNER JOIN likes l ON l.target_id=s.id AND l.tipo='sample' AND l.usuario_id=$uid; reaccion default IN ('like','encanta'). Orden 'recientes' usa l.created_at DESC.
- **Archivos:** src/handlers/biblioteca.rs (handler get_favoritos + struct FavoritosQuery), src/handlers/mod.rs (registro ApiDoc + ruta via biblioteca::routes ya merged), src/repositories/biblioteca.rs (favoritos_de_usuario + contar_favoritos + helpers push_favoritos_reaccion/order/busqueda).
- **Gotchas:** Reuso ColeccionadosFilters (carpeta='' no aplica). BIBLIOTECA_SAMPLE_SELECT compatible — no se anaden columnas extra al SELECT para evitar romper FromRow. Contrato {data:{data:[],pagination:{}}} igual que /me/coleccionados — el frontend ya tiene normalizador.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Logica especifica Kamples (samples + likes domain).


## 274A-8 - wpJsonStub: conservar /me/favoritos y /me/descargas/sugerencias
- **Que:** Los endpoints 274A-6 (/me/descargas/sugerencias) y 274A-7 (/me/favoritos) seguian devolviendo 404 porque el proxy del frontend wpJsonStub aplicaba la regla generica /me/* -> /users/me/*. Anadidos ambos paths al PATH_KEEP_PREFIXES introducido en 274A-5. /me/descargas/sugerencias se preserva via prefijo aunque /me/descargas no este (no hay endpoint Rust para listar descargas todavia, asi que la regla generica sigue valida para ese path raiz).
- **Archivos:** frontend/src/bootstrap/wpJsonStub.ts
- **Gotchas:** El matcher por prefijo (path === prefix || startsWith(prefix + '/')) maneja correctamente que /me/descargas/sugerencias se conserve sin que /me/descargas (sin sufijo) quede afectado — la comparacion exige separador.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Shim especifico migracion Kamples.

## 274A-9 - GET /api/colecciones/:id/combinacion-pendiente

- **Que:** Se agrego el endpoint que el frontend invoca al cargar el detalle de cualquier coleccion (piColecciones.obtenerCombinacionPendiente). El feature de combinar/deshacer (legacy `combinar` + `deshacer-combinacion` + tabla undo) NO esta portado a Rust todavia, asi que el handler valida visibilidad (publica o dueno via `OptionalUser`) y retorna siempre `hayCombinacion: false`. Cuando se porte la combinacion, el handler debera consultar la tabla de undos pendientes y poblar `undoId`/`origenNombre`/`combinadoEn`/`expiraEn`.
- **Archivos:** `src/handlers/colecciones.rs`, `src/handlers/mod.rs`
- **Gotchas:** El frontend llamaba este endpoint en cada navegacion y devolvia 404 ruidoso pese a no requerir el feature completo. La ruta usa `OptionalUser` para permitir que visitantes anonimos visualicen colecciones publicas sin error.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica. Es contrato especifico de Kamples; cuando se porte combinar/undo conviene encapsular en repository.

## 274A-10 - GET /api/colecciones/:id/sugerencias

- **Que:** Se agrego el endpoint `Mas Ideas` (`apiColecciones.obtenerSugerencias`). Lee los samples activos de la coleccion via `coleccion_samples + samples`, agrega tags/BPM/key (top 10 tags, BPM promedio con default 120, key dominante) y reusa `SampleRepository::find_samples_by_aggregated_scoring` (creado en 274A-6) excluyendo los samples ya en la coleccion. Soporta paginacion `page` / `per_page` (default 1/20, max 50). Respeta visibilidad (publica o dueno) usando `OptionalUser`.
- **Archivos:** `src/handlers/colecciones.rs`, `src/handlers/mod.rs`, `.sqlx/` (cache nuevas queries)
- **Gotchas:** Si la coleccion esta vacia se retorna `data: []` sin tocar el motor de scoring. Los IDs a excluir se obtienen con `query_scalar!(SELECT sample_id FROM coleccion_samples)` (no con la lista completa de `ColeccionesRepository::list_samples` para evitar overhead innecesario). El parametro `ColeccionSugerenciasQuery` se registra en ApiDoc solo via `params(...)` (NO en `components/schemas` porque `IntoParams` no implementa `ToSchema`).
- **Sentinel:** No aplica.
- **GLORY-RS:** Posible. El agregador `aggregate_coleccion_context` (top tags + avg BPM + key dominante) replica la logica de 274A-6 (`handlers/suggestions.rs`). Cuando aparezca un tercer caso de uso, mover a un helper compartido (probablemente en `glory-rs::recommendations` o como `services::sample_context_aggregator`).


## 274A-11 - Script audit-api detecta endpoints faltantes via openapi.json

- **Que:** Se creo herramienta CLI `scripts/audit-api.mjs` y resolver compartido `scripts/lib/apiResolver.mjs` que escanean `frontend/src/legacy/services/*.ts` (excluyendo `apiCliente.ts`), extraen toda llamada `apiGet|apiPost|apiPut|apiPatch|apiDelete('path')` con regex (rama estatica unicamente; paths dinamicos con ``\$``{} se ignoran a proposito), aplican las reglas de mapeo de `frontend/src/bootstrap/wpJsonStub.ts` (PATH_MAP exact, PATH_MAP_KEEP, PATH_KEEP_PREFIXES, `/me/* -> /users/me/*`, `/perfil/* -> /users/*`) y verifican existencia en `openapi.json`. 3 formatos de salida: humano agrupado por `METHOD path` (default), `--json` machine-readable, `--vscode` problemMatcher. Exit code 1 si hay missing. Agregado `npm run audit:api`. Existe para romper el patron reactivo de 274A-4..274A-10 (frontend pega 404 -> usuario reporta -> portar endpoint) y permitir descubrir TODOS los endpoints faltantes upfront.
- **Archivos:** `scripts/lib/apiResolver.mjs` (nuevo), `scripts/audit-api.mjs` (nuevo), `package.json` (script `audit:api`).
- **Resultado audit inicial:** OpenAPI tiene 158 paths; frontend hace 110 calls estaticos; 65 matched / 45 missing unicos (DELETE /api/admin/samples/todos, DELETE /api/dev/canciones, GET /api/admin/automatizacion/{estado,historial}, GET /api/admin/cola-ia/* (4), GET /api/admin/contribuciones, GET /api/admin/duplicados{,/contar}, GET /api/admin/moderacion{,/historial}, GET /api/contribuciones/mis, GET /api/feed/inicio, GET /api/me/favoritos/sugerencias, GET /api/reproducciones/historial, GET /api/users/me/descargas, GET /api/users/me/seguidos, varios POST /api/admin/*, etc). Lista completa con `npm run audit:api`.
- **Gotchas:** Bug inicial en `openapiPathToRegex`: el escape regex clobbeaba el placeholder `{param}`. Solucion: reemplazar `{param}` por `\u0000PARAM\u0000`, escapar metachars, y restaurar como `[^/]+` por split/join. Segundo bug: `resolveCall` testaba el regex contra el path con prefijo `/api` pero los regex se construyen sin prefijo (la funcion `openapiPathToRegex` strips `/api`); fix: testar contra `mapped` directo, no `'/api' + mapped`. `openapi.json` puede estar stale; antes de auditar correr `cargo run -- --emit-openapi openapi.json`. Si `glory-backend.exe` esta corriendo (dev), bloqueara el rebuild — matar proceso primero.
- **Sentinel:** Si aplica — la siguiente tarea (274A-12) porta este resolver a TypeScript dentro de Code Sentinel (workspace `.agent/code-sentinel`) para diagnosticos inline en VS Code mientras se edita.
- **GLORY-RS:** Parcialmente. La logica de extraccion de calls y matching contra OpenAPI es generica (cualquier proyecto Axum + utoipa + frontend que llame `apiGet/Post/...`). Las reglas de `mapPath` son especificas de Kamples (residuo del proxy `wp-json`). Cuando se elimine `wpJsonStub.ts` (frontend ya no lo necesitara una vez completada la migracion), el resolver se vuelve 100% agnostico y puede moverse a un paquete `glory-tools` o equivalente.


## 274A-12 - Code Sentinel: analyzer api-endpoint-faltante (diagnostico inline)

- **Que:** Se creo `src/analyzers/apiEndpointAnalyzer.ts` en el repo de Code Sentinel (workspace `.agent/code-sentinel`) que porta a TypeScript la logica del CLI `audit:api` (274A-11). Detecta llamadas `apiGet|apiPost|apiPut|apiPatch|apiDelete('path')` en archivos `**/legacy/services/*.ts` (excluyendo `apiCliente.ts`), aplica las reglas de mapeo de `wpJsonStub.ts` (PATH_MAP, /me/* -> /users/me/*, /perfil/* -> /users/*, KEEP set/prefixes), busca el path resuelto en `openapi.json` de la raiz del workspace y emite `vscode.Diagnostic` Warning con mensaje `ENDPOINT FALTANTE: METHOD /api/<path>` para cada call sin handler. El openapi se cachea por workspaceFolder con FileSystemWatcher para invalidar al regenerar (`cargo run -- --emit-openapi openapi.json`). No reinicia VS Code (regla 20). Registrado en `providers/diagnosticProvider.ts` para los tipos `ts` y `tsx`. Empaquetado como `glory-sentinel-0.2.2.vsix` e instalado.
- **Archivos (repo glory-sentinel):** `src/analyzers/apiEndpointAnalyzer.ts` (nuevo, ~205 lineas), `src/providers/diagnosticProvider.ts` (registro del analyzer en tipos ts/tsx), `package.json` (bump 0.2.1 -> 0.2.2). Commit: `3377f56` en `1ndoryu/glory-sentinel:main`.
- **Gotchas:** (1) `code --install-extension` no funciona via PowerShell directo en este sistema; hay que invocarlo via `cmd /c` con quoting escapado. (2) Bumpear version es necesario para que VS Code reconozca el nuevo .vsix (mismo numero = no actualiza). (3) El cache se invalida via `vscode.RelativePattern(workspaceFolder, 'openapi.json')` watcher; sin esto los diagnosticos quedarian stale tras cada regeneracion del schema. (4) Hay un archivo `src/analyzers/static/staticCodeRules.ts` con cambios sin commitear que NO toque (regla 1.1 — trabajo de otro agente o sesion previa).
- **Sentinel:** Esta tarea ES la implementacion de una regla nueva de Sentinel; no requiere tarea adicional de prevencion.
- **GLORY-RS:** Mismo veredicto que 274A-11. La logica de extraccion + matching contra OpenAPI es agnostica; las reglas de `mapPath` son especificas de Kamples (residuo del proxy `wp-json`). Cuando se elimine `wpJsonStub.ts` en el frontend, el analyzer queda 100% generico y puede promoverse a una capability default de Code Sentinel para cualquier proyecto Axum + utoipa + frontend que use el patron `apiGet/Post/...`.


## 274A-13 - Fix: Sentinel api-endpoint-faltante no aparecia tras recargar

- **Que:** El analyzer `apiEndpointAnalyzer` no emitia diagnosticos visibles a pesar de estar instalado en v0.2.2. Causa: `analizarWorkspace` (segunda funcion en `providers/diagnosticProvider.ts` linea ~280) tenia su propio switch de tipos y NO incluia la llamada a `analizarApiEndpoints` — solo se habia agregado en `ejecutarAnalisisEstatico` (primera funcion). Adicionalmente, `getWorkspaceFolder()` puede devolver undefined en multi-root workspaces si el archivo no esta abierto en pestana visible. Fix: (1) agregar `analizarApiEndpoints` tambien al switch de `analizarWorkspace`; (2) fallback que busca el workspaceFolder iterando `vscode.workspace.workspaceFolders` y comparando paths normalizados; (3) logging via `logInfo`/`logWarn` para diagnosticar problemas runtime futuros. Bump version 0.2.2 -> 0.2.3, repackaged y reinstalado.
- **Archivos (repo glory-sentinel):** `src/analyzers/apiEndpointAnalyzer.ts` (logging + fallback workspaceFolder), `src/providers/diagnosticProvider.ts` (call al analyzer en ambas funciones), `package.json` (0.2.3). Commit: `356ece0` en `1ndoryu/glory-sentinel:main`.
- **Gotchas:** El reporte de workspace (`codeSentinel.analyzeWorkspace`) usa una funcion DIFERENTE de la que corre al abrir un archivo individual; modificar una sin la otra produce "el analyzer corre en el editor pero no en el reporte" o viceversa. Patron a recordar para futuros analyzers. `code --install-extension` nunca actualiza si la version del .vsix es la misma que la instalada — bumpear obligatorio. El usuario debe recargar VS Code despues de instalar (regla 20 prohibe que el agente lo fuerce).
- **Sentinel:** No aplica (es un fix interno de Sentinel mismo).
- **GLORY-RS:** No aplica.

## 274A-14 - Anotar 45 endpoints faltantes en el roadmap

- **Que:** Se anadieron al `roadmap.md` los 45 endpoints faltantes detectados por `npm run audit:api` agrupados por dominio: usuario activo (13), admin moderacion (7), admin cola IA (6), admin duplicados (3), admin automatizacion (3), admin contribuciones (2), admin embeddings (2), experimentos (1), procesos (1), samples (1), dev/scraper (5). Cada endpoint = una tarea individual (274A-15..274A-58) para portar uno por uno desde `glorytemplate/App/Kamples/Api/Controladores/` y servicios PHP.
- **Archivos:** `roadmap.md` (nueva seccion "Endpoints faltantes").
- **Gotchas:** El conteo en `audit-api` reporta 45 unicos; algunos casos comparten controlador (cola-ia/cuota-groq y cola-ia/estado-keys agrupados como 274A-40). El total efectivo de tareas es ~44 si se agrupan los muy relacionados.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

## 274A-28+31+35+36+37+38+39+40+41+42+44+45+47 - Registrar 14 admin handlers existentes en OpenAPI
- **Que:** Anadidas 7 anotaciones `#[utoipa::path]` en `admin_ia_queue.rs` (list_queue, stats_queue, retry_queue_item, retry_all_queue_items, process_queue_now, groq_quota, groq_keys_status). Registrados 14 paths + 30 schemas en `handlers/mod.rs`. Resueltas colisiones de schema con `#[schema(as = ...)]` para ListarResponse (3 modulos) y HistorialResponse (2 modulos): AdminContribucionesListarResponse, AdminDuplicadosListarResponse, AdminModeracionListarResponse, AdminAutomatizacionHistorialResponse, AdminModeracionHistorialResponse. Regenerado openapi.json. Audit:api 37->23.
- **Archivos:** `src/handlers/admin_ia_queue.rs`, `src/handlers/admin_automatizacion.rs`, `src/handlers/admin_contribuciones.rs`, `src/handlers/admin_duplicados.rs`, `src/handlers/admin_moderacion.rs`, `src/handlers/mod.rs`, `openapi.json`.
- **Gotchas:** utoipa exporta schemas por nombre simple; `ToSchema` con tipos del mismo nombre en distintos modulos colisionan silenciosamente -> ultimo gana. `#[schema(as = NewName)]` resuelve sin renombrar el tipo Rust. PowerShell muestra `cargo check` stderr como NativeCommandError aunque exitcode sea 0.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (logica admin especifica de Kamples).

## 274A-27 - POST /api/dashboard/payout real
- **Que:** Implementado payout real para creadores sobre Stripe Connect: valida usuario y cuenta Connect, exige payouts activos, bloquea doble payout pendiente, consulta balance disponible en Stripe, crea payout via `POST https://api.stripe.com/v1/payouts` con encabezado `Stripe-Account` y persiste una transaccion local `tipo='payout'`. La respuesta mantiene el contrato legacy `{ monto, estado }`. Audit:api 23->22.
- **Archivos:** `Cargo.toml`, `src/handlers/dashboard.rs`, `src/handlers/mod.rs`, `src/models/mod.rs`, `src/models/payment.rs`, `src/repositories/billing.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `src/services/stripe_service.rs`, `openapi.json`, `roadmap.md`, `Agente/planes/plan-23-endpoints-faltantes-2026-04-27.md`.
- **Gotchas:** La feature `payout` de `async-stripe-core` se quedaba colgada durante link en Windows; se reemplazo por llamada HTTPS directa con `reqwest` contra el endpoint oficial de Stripe para mantener implementacion real sin bloquear build. Se usaron queries SQLx dinamicas parametrizadas con supresion Sentinel puntual porque no hay cache SQLx para macros nuevas en `SQLX_OFFLINE`.
- **Sentinel:** No requiere nueva regla; se documento excepcion local a `sqlx-query-as-sin-macro`/`sqlx-query-sin-macro` por restriccion SQLX_OFFLINE.
- **GLORY-RS:** No aplica; flujo Stripe Connect y transacciones son especificos de Kamples.

## 274A-15+274A-16 - Descargas y seguidos del usuario
- **Que:** Portados `GET /api/users/me/descargas` y `GET /api/users/me/seguidos`, con aliases legacy `/api/me/descargas` y `/api/me/seguidos`. Se agregaron consultas para descargas paginadas y lista de IDs seguidos.
- **Archivos:** `src/handlers/biblioteca.rs`, `src/repositories/biblioteca.rs`, `src/handlers/social.rs`, `src/repositories/follow.rs`, `src/handlers/mod.rs`, `openapi.json`.
- **Gotchas:** Se evito `sqlx::query_scalar!` nuevo porque `SQLX_OFFLINE=true` requiere cache preparada.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica; comportamiento especifico de Kamples.

## 274A-17+274A-18+274A-19+274A-20 - Historial, feed y sugerencias favoritos
- **Que:** Portados `GET /api/reproducciones/historial`, `GET /api/feed/inicio`, `POST /api/feed/recargar` y `GET /api/me/favoritos/sugerencias`. `feed/inicio` reutiliza el recommender; `recargar` invalida cache fresco; favoritos usa likes como contexto de sugerencias.
- **Archivos:** `src/handlers/plays.rs`, `src/handlers/feed.rs`, `src/handlers/suggestions.rs`, `src/repositories/biblioteca.rs`, `src/handlers/mod.rs`, `openapi.json`.
- **Gotchas:** El historial usa `ORDER BY MAX(r.created_at)` sin exponer columna extra para mantener el row mapper compartido.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica; endpoints de producto Kamples.

## 274A-21+274A-22 - Cambio de email y password
- **Que:** Portados `PUT /api/users/me/email` y `PUT /api/users/me/password` desde PerfilController: verificacion Argon2 de password actual, confirmacion de password nueva, unicidad de email y actualizacion segura.
- **Archivos:** `src/models/user.rs`, `src/models/mod.rs`, `src/repositories/user.rs`, `src/handlers/users.rs`, `src/handlers/mod.rs`, `openapi.json`.
- **Gotchas:** `User.email` es `Option<String>`; se comparo con `as_deref()` y `eq_ignore_ascii_case` solo cuando existe.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica; flujo de auth especifico de Kamples.

## 274A-54+274A-55+274A-56+274A-57+274A-58 - Endpoints dev/scraper reales
- **Que:** Portados los 5 endpoints legacy de DevController sin stubs: `DELETE /api/dev/canciones` purga tablas de discovery; `POST /api/dev/scraper/run` lanza Scrapy con spider/limit/url validados; `POST /api/dev/scraper/cola` toma la primera fila pendiente de `scraping_log` o cae a `/hot-samples/`; `POST /api/dev/recorte/generar` encola recorte bilateral en `cola_extraccion_samples` y dispara el pipeline Python; `POST /api/dev/extraccion/publicar` reutiliza `ExtraccionPublisherService` para crear/reemplazar samples reales desde la cola extraida.
- **Archivos:** `src/handlers/dev_tools.rs`, `src/repositories/dev_tools.rs`, `src/services/dev/mod.rs`, `src/services/dev/process.rs`, `src/handlers/mod.rs`, `src/repositories/mod.rs`, `src/services/mod.rs`, `openapi.json`, `roadmap.md`.
- **Gotchas:** Se separo SQL a repositorio y lanzamiento de procesos a `services/dev/process.rs` para que el handler quedara delgado y Sentinel no marcara DIP/tamano. Los procesos usan `std::process::Command` con argumentos separados, no shell; las URLs se restringen a WhoSampled y los spiders a una allowlist. Las queries nuevas usan API dinamica SQLx con supresion Sentinel en repositorio porque `SQLX_OFFLINE=true` no tiene cache para macros nuevas.
- **Validacion:** `cargo check` OK, `cargo clippy --all-targets -- -D warnings` sin diagnosticos, `cargo test --all-targets` 181 passed, `cargo run -- --emit-openapi openapi.json` OK, `npm run audit:api` subio de 88 a 93 matched y bajo de 22 a 17 missing.
- **Sentinel:** No requiere regla nueva; se documento excepcion local `sqlx-query-sin-macro`/`sqlx-query-as-sin-macro` por restriccion SQLX_OFFLINE.
- **GLORY-RS:** No aplica. Es flujo especifico de Kamples: tablas `scraping_log`, `cola_extraccion_samples`, `relaciones_sample`, scraper Python y publicador de extracciones.
