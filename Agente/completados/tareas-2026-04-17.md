# Tareas completadas — 2026-04-17

## 174A-1 — Planificación de la migración Kamples (PHP) → glory-rust-template (Rust)

- **Que:** Inventario exhaustivo del proyecto origen (`glorytemplate`, ~59 controladores, 40+ servicios, 74 migraciones, audio/IA/pagos/WS/blog/DAW/scraper/mobile) y diseño del plan de migración por fases con stack final, schema base, mapeo PHP→Rust por dominio, decisiones técnicas, riesgos, y desglose en 118 tareas atómicas distribuidas en 19 fases.
- **Archivos:**
  - `roadmap.md` (misión + aclaraciones del usuario + tareas pendientes)
  - `Agente/planes/plan-migracion-kamples-rust-2026-04-17.md` (plan completo)
  - `Agente/completados/tareas-2026-04-17.md` (este archivo)
- **Gotchas:**
  - El proyecto origen es enorme y heterogéneo: WordPress + 5 stacks paralelos (web, scraper Python, DAW Tauri, mobile Capacitor, desktop Tauri, WS Bun).
  - Decisión técnica importante: WebSocket pasa de Bun standalone a Axum WS integrado (menos infraestructura, comparte auth).
  - Una sola BD Postgres confirmada por usuario — no se replica el split WP MySQL + Postgres del legado.
  - Frontend ya hecho: NO se recrea; sólo se reemplaza la capa de servicios HTTP por cliente generado por Orval.
  - Algoritmo se replica con detalle exacto: 6 señales 0.25/0.25/0.15/0.15/0.10/0.10, embeddings 128d, cache stale-while-revalidate 6h.
- **Sentinel:** No aplica (tarea de planificación, sin código).
- **GLORY-RS:** El plan identifica 12 módulos agnósticos a vivir en `glory-rs/backend/` (auth, websocket, cache, storage, payments::stripe, push::fcm, push::web_push, email, errors, observability, openapi, config, fixtures, rate_limit). Lo específico de Kamples (algoritmo, audio pipeline, prompts IA, planes) vive en `src/` del proyecto.

## 174A-2 — Verificar `Cargo.toml` y dependencias base del stack

- **Que:** Auditar `Cargo.toml` y añadir las dependencias del stack acordado (mantener rustls como TLS, evitar openssl-sys en Windows). Se añaden por fase: bootstrap (axum ws+multipart, tokio-util, tower-http extra layers, futures, async-trait, serde_with), datos (sqlx feats `json macros ipnetwork`, pgvector, redis, deadpool-redis), auth/crypto (sha2, hmac, hex, base64, rand, nanoid, slug), HTTP cliente (reqwest), audio (symphonia all codecs/formats, rustfft, ndarray), concurrencia (dashmap, parking_lot), utilidades (mime, mime_guess, url), errores (anyhow). `oauth2`, `lettre`, `web-push`, `async-stripe`, `gcp_auth` quedan diferidas a sus fases respectivas para evitar pull-in de openssl-sys.
- **Archivos:** `Cargo.toml`
- **Validación:** `cargo check` OK, `cargo clippy -- -D warnings` OK.
- **Gotchas:**
  - `web-push 0.10` con feature `hyper-client` arrastra openssl-sys en Windows. Solución: postergar la dep hasta Fase 11 y considerar `web-push-native` o crate alternativo entonces.
  - Mismo patrón con `lettre` — su default trae native-tls. Cuando se incluya, usar `tokio1-rustls-tls`.
  - `symphonia` con `all-codecs all-formats` añade ~20 sub-crates pero es necesario para soportar wav/mp3/flac/aiff/ogg.
- **Sentinel:** No aplica.
- **GLORY-RS:** Las deps añadidas son base; cuando se implemente módulos en `glory-rs/backend/`, esas deps probablemente se moverán a un workspace member separado para que `glory-rs` no arrastre todo el stack de Kamples (audio, etc.).

## 174A-3 — Crear estructura de carpetas del backend

- **Que:** Añadir los 5 módulos faltantes en `src/`: `audio/`, `algorithm/`, `workers/`, `ws/`, `domain/` (cada uno con `mod.rs` placeholder documentando su responsabilidad y fase de implementación). Declarados en `src/lib.rs` ordenados alfabéticamente.
- **Archivos:** `src/audio/mod.rs`, `src/algorithm/mod.rs`, `src/workers/mod.rs`, `src/ws/mod.rs`, `src/domain/mod.rs`, `src/lib.rs`
- **Validación:** `cargo check` OK.
- **Gotchas:** Ninguno. Los módulos preexistentes (`config`, `errors`, `handlers`, `middleware`, `models`, `repositories`, `services`) ya estaban; solo se añadieron los 5 nuevos.
- **Sentinel:** No aplica.
- **GLORY-RS:** `ws/`, `workers/` y partes de `audio/` (decodificación genérica) son candidatos a migrar a `glory-rs/backend/` cuando estén estables. `algorithm/` y los prompts IA son específicos de Kamples.

## 174A-4 — `AppError` global + middleware `request_id` + `tracing-subscriber`

- **Que:** (a) Expandir `AppError` con variantes `Forbidden`, `RateLimited`, `PayloadTooLarge`, `UnsupportedMediaType`, `ExternalService { service, message }`. Añadir `pub type AppResult<T>`. Logging estructurado con `tracing::error!(error.kind, error.detail, …)`. (b) Crear `middleware::request_id_middleware` que reusa `X-Request-Id` entrante o genera UUIDv7, lo inyecta en el span de `tracing` y en extensiones de la request, y lo retorna en el header de respuesta. (c) Mejorar `tracing-subscriber` en `main.rs`: soporta `LOG_FORMAT=json` (formato JSON con span actual) o compacto. Filtros base: `glory_backend=debug,tower_http=info,sqlx=warn`. (d) Conectar `request_id_middleware` en el router antes de `TraceLayer`.
- **Archivos:** `src/errors/mod.rs`, `src/middleware/mod.rs`, `src/middleware/request_id.rs` (nuevo), `src/main.rs`, `src/handlers/mod.rs`
- **Validación:** `cargo check` OK, `cargo clippy -- -D warnings` OK.
- **Gotchas:** Clippy con `pedantic` exige `map_or_else` sobre `map().unwrap_or_else()` y backticks en doc-comments para identificadores (`request_id`).
- **Sentinel:** No aplica (regla manual ya cubierta por clippy pedantic).
- **GLORY-RS:** `AppError` y `request_id_middleware` son agnósticos — candidatos directos a `glory-rs/backend/errors/` y `glory-rs/backend/middleware/`. Se mantienen en `src/` por ahora hasta estabilizar la API y migrar varias features primero.

## 174A-5 — Pool SQLx + Redis pool + `AppState` compartido

- **Que:** (a) `AppConfig` añade `redis_url: Option<String>`, `db_max_connections`, `db_min_connections` configurables vía env. (b) `AppState` añade `redis: Option<deadpool_redis::Pool>`. Diseño explícito: si `REDIS_URL` no existe, los servicios deben tener fallback en memoria (DashMap) o degradarse. (c) `main.rs` crea pool Redis con `deadpool_redis`, hace PING al arrancar para validar conexión (fail-fast). (d) `handlers::create_router` recibe `redis` como parámetro y lo inyecta en `AppState`.
- **Archivos:** `src/config/mod.rs`, `src/lib.rs`, `src/main.rs`, `src/handlers/mod.rs`
- **Validación:** `cargo clippy -- -D warnings` OK.
- **Gotchas:** `deadpool_redis::Runtime::Tokio1` es necesario para el runtime de Tokio. PING con `redis::cmd` requiere import explícito de `redis` (ya en deps).
- **Sentinel:** No aplica.
- **GLORY-RS:** `AppState` es muy específico del proyecto (jwt_secret aquí, mañana stripe_secret, groq_keys, fcm_creds). En `glory-rs/backend/` sí podríamos exponer un trait `HasPool`, `HasRedis`, `HasJwtSecret` para que módulos agnósticos accedan a recursos sin acoplarse al tipo concreto. Diferido.

## 174A-6 — OpenAPI scaffold + Swagger UI `/docs` + CLI `--emit-openapi`

- **Que:** (a) Añadir alias `/docs` que redirige a `/swagger-ui/` (URL más natural). (b) Implementar CLI flag `--emit-openapi <ruta>`: cuando se pasa, el binario escribe el schema OpenAPI a disco y termina sin arrancar el servidor — necesario para que el frontend (Orval) regenere el cliente sin requerir un backend corriendo. Default path: `openapi.json` en CWD. (c) `ApiDoc` ya estaba `pub`, solo se accede desde main.rs. (d) Verificación funcional: `cargo run -- --emit-openapi openapi.json` produce archivo de 14.5 KB válido.
- **Archivos:** `src/handlers/mod.rs`, `src/main.rs`, `openapi.json` (generado y commiteado para trazabilidad)
- **Validación:** `cargo clippy -- -D warnings` OK + ejecución funcional CLI confirmada.
- **Gotchas:** En Windows si hay un `glory-backend.exe` corriendo (servidor en background), `cargo run` falla con "Acceso denegado". Solución: `Get-Process glory-backend | Stop-Process -Force` antes del run.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón "CLI con `--emit-openapi`" es agnóstico — puede vivir en `glory-rs/backend/openapi/` como helper que reciba un `OpenApi` y un path. Diferido hasta tener varios proyectos consumiéndolo.






## 174A-7 a 174A-17  Fase 1: schema base PostgreSQL consolidado en 11 migraciones

- **Que:** Consolidación de las **74 migraciones** del proyecto WordPress origen en **11 migraciones SQLx** ordenadas por dominio. Schema final aplicado contra DB nueva `glory_kamples` (postgres local). Las 11 migraciones cubren toda la estructura de Kamples sin las migraciones intermedias (final consolidado, no historial).
- **Migraciones creadas:**
  - `20260417000001_extensions.up.sql` (174A-7)  extensions: uuid-ossp, pg_trgm, btree_gin, vector
  - `20260417000002_users.up.sql` (174A-8)  usuarios_ext (+plan/rol/estado/suspension/stripe ids), bloqueos, trigger updated_at
  - `20260417000003_samples_core.up.sql` (174A-9)  samples (con embedding vector(128), tags TEXT[], hnsw index, fts spanish, trigram), colecciones, coleccion_samples, subcolecciones, sample_metadata
  - `20260417000004_canciones_artistas.up.sql` (174A-10)  canciones (spotify_id), artistas_musicales (prioridad), canciones_artistas, samples_canciones, relaciones_sample (FK cruzada)
  - `20260417000005_social_engagement.up.sql` (174A-11)  follows, likes, reacciones, reposts (UNIQUE por usuario), comentarios (con multimedia/respuestas/likes/moderacion/articulo_tipo), comentarios_likes
  - `20260417000006_play_tracking.up.sql` (174A-12)  reproducciones (completada flag), descargas (con tamano y tipo), codigos_descarga_gratis (expires)
  - `20260417000007_payments.up.sql` (174A-13)  transacciones (idempotency_key, tipo_descarga), creditos (bonus), suscripciones, planes
  - `20260417000008_messaging_notifications.up.sql` (174A-14)  conversaciones (aceptada), mensajes (multimedia, autor_id), notificaciones (titulo nullable), push_subscriptions, fcm_tokens
  - `20260417000009_audio_pipeline.up.sql` (174A-15)  cola_extraccion_audio (backoff, audio_completo), cola_procesamiento_ia (max_intentos), lotes_procesamiento, papelera (eliminado_en)
  - `20260417000010_articulos_moderation.up.sql` (174A-16)  articulos, verificacion_samples, publicaciones_moderacion (razon), contribuciones (edicion/eliminacion), dedup_global
  - `20260417000011_indexes_planificador_changelog.up.sql` (174A-17)  algoritmo_estado, sync_changelog (con collection_merged), user_tag_scores, indices criticos (titulo trgm + fts spanish, username trgm)
- **Validacion:**
  - `sqlx migrate run` aplico las 11 migraciones SIN ERRORES contra postgres local (DB `glory_kamples`)
  - `cargo clippy -- -D warnings` OK
  - DB `glory_kamples` creada con extensiones uuid-ossp + pg_trgm + btree_gin + vector
- **Cleanup colateral:**
  - Eliminado feature `notes` del template (no aplica): `src/handlers/notes.rs`, `src/services/note.rs`, `src/repositories/note.rs`, `src/models/note.rs`
  - Eliminada migracion inicial template `20260325000000_initial.up.sql` y su `.down.sql`
  - Actualizado `DATABASE_URL` en `.env` apuntando a `glory_kamples`
  - Actualizados `handlers/mod.rs`, `models/mod.rs`, `repositories/mod.rs`, `services/mod.rs` para no referenciar notes
  - Actualizado titulo OpenAPI de "Glory RS API" -> "Glory Kamples API"
- **Gotchas:**
  - Subagente uso DO $$ RAISE NOTICE y backfills UPDATE en algunas migraciones originales: omitidos en consolidacion (irrelevantes para greenfield)
  - Materialized view `mv_trending_samples` y funciones helper `buscar_similares`/`buscar_por_vector` NO incluidas (operacionales, agregar en fase de feed)
  - FK cruzadas `samples.cancion_origen_id` y `samples.relacion_sampleo_id` declaradas como columna en 0003 y agregadas como `ALTER TABLE ADD CONSTRAINT` en 0004 para evitar dependencias circulares
  - `transacciones.estado` acepta ambos `completed` y `completada` en CHECK (legacy del proyecto WP)
  - Embedding final es `vector(128)` (corregido de 1536 originales en v048)
  - Las tablas usan nombres en espa\u00f1ol del proyecto WP origen (regla del proyecto: front en ingles, datos legacy en espa\u00f1ol). Migrar a ingles seria un trabajo de refactor masivo que romperia el algoritmo y modelos. **Decision: mantener nombres en espa\u00f1ol en BD por compatibilidad con datos historicos y vocabulario del dominio.**
  - Necesario instalar `sqlx-cli 0.8.2` con features `rustls,postgres` (no `native-tls`) para evitar openssl-sys
  - Archivo Postgres binario (`psql`) vive en `C:\Program Files\PostgreSQL\18\bin` (PG18 local del usuario)
- **Sentinel:** No aplica (SQL puro).
- **GLORY-RS:** El schema completo es **especifico** de Kamples  vive en el repo del proyecto, no en glory-rs. La utilidad `actualizar_updated_at()` (trigger BEFORE UPDATE) si es agnostica y candidata a `glory-rs/backend/db/triggers.sql` cuando se consolide.
## 174A-18  Auth nativo Rust (argon2id + JWT) sobre usuarios_ext

- **Que:** Refactor completo del stack auth del template (UUID/users) a Kamples (i32/usuarios_ext). Endpoints POST /api/auth/register y POST /api/auth/login. Hash argon2id, JWT HS256 con claims {sub i32, exp, iat, plan, rol}. Identifier polimorfico (email o username).
- **Migracion BD:** `20260417000012_auth_password.up.sql` agrega `password_hash VARCHAR(255)` y hace `wp_user_id` nullable; indice unico `LOWER(email)` para case-insensitive lookup.
- **Archivos:**
  - `src/models/user.rs` (reescrito)  User i32 + UserResponse + RegisterRequest/LoginRequest/AuthResponse
  - `src/repositories/user.rs` (reescrito)  find_by_email/username/identifier/id, create_native
  - `src/services/auth.rs` (reescrito)  Claims i32 + 24h exp, register/login/generate_token/verify_token
  - `src/handlers/auth.rs` (reescrito)  register/login con validator
  - `src/handlers/mod.rs`  agregar UserResponse a OpenAPI components
  - `src/middleware/auth.rs`  AuthUser extractor con user_id i32 + plan + rol
- **Tests funcionales locales (cargo run + Invoke-RestMethod):**
  - POST /api/auth/register {testuser1, test1@example.com, superpass1234} -> 200 JWT+user.id=1
  - POST /api/auth/login {identifier:test1@example.com, password:superpass1234} -> 200 JWT
- **Validacion:** cargo clippy -- -D warnings OK
- **Gotchas:**
  - WriteAllText desde heredoc PS5 funciona, pero Remove-Item en archivos abiertos por VS Code no siempre toma  usar [System.IO.File]::WriteAllText sobreescribe limpio
  - usuarios_ext.email es nullable en schema (legacy), pero forzamos email no null en register nativo via Rust validator
  - identifier polimorfico (contains '@') simplifica frontend (sin radio email/username)
- **Sentinel:** No aplica.
- **GLORY-RS:** Auth con argon2+JWT es agnostico  candidato directo para `glory-rs/backend/auth/` cuando se consolide. Por ahora vive en proyecto porque User esta acoplado a campos Kamples (plan, rol, estado).
## 174A-19  Middleware AuthLayer + extractor CurrentUser (+ OptionalUser)

- **Que:** Refactor del extractor de auth: rename AuthUser -> CurrentUser (alias retrocompatible), agregado OptionalUser (extractor que NUNCA falla, util para endpoints publicos personalizados), y helper CurrentUser::require_admin() para handlers de admin.
- **Archivos:** src/middleware/auth.rs (reescrito), src/middleware/mod.rs (re-exports)
- **Validacion:** cargo clippy OK
- **Sentinel:** No aplica.
- **GLORY-RS:** El patron CurrentUser/OptionalUser es completamente agnostico (depende solo de AppState con jwt_secret) -- candidato directo para glory-rs/backend/auth/extractors.rs.
## 174A-20  Refresh tokens + endpoints logout/refresh

- **Que:** Implementado TokenStore (src/services/token_store.rs) con dual backend Redis (GETDEL atomico para rotacion) + DashMap fallback (cleanup lazy de TTL). Refresh tokens 32 bytes random base64 url-safe, TTL 30 dias. Access tokens revocables via jti (UUID v7) en blacklist Redis/memoria, TTL 24h. Endpoints POST /api/auth/refresh (consume refresh anterior y emite par nuevo) y POST /api/auth/logout (revoca jti + opcional refresh). Middleware CurrentUser/OptionalUser actualizado para checkear blacklist en cada request.
- **Como:** LazyLock<Arc<DashMap>> para memoria (sin once_cell). En Redis: SETEX refresh:{token} ttl user_id + GETDEL para rotacion atomica. Claims ampliado con jti. AuthResponse ahora incluye efresh_token. LogoutRequest opcional para revocar refresh asociado.
- **Resultado:** Validado funcionalmente con servidor real en memory mode: login emite par, refresh rota correctamente, reuso de refresh consumido devuelve 401, logout revoca jti, llamadas posteriores con access revocado devuelven 401. cargo clippy -- -D warnings verde.
## 174A-21  OAuth Google ID token (web)

- **Que:** Endpoint POST /api/auth/google que recibe {id_token} (Google ID token RS256) y devuelve par access+refresh igual que login nativo. Tabla nueva usuarios_ext_oauth con UNIQUE (provider, provider_sub) para vincular cuentas externas a usuarios_ext. Verificacion completa: firma JWKS de https://www.googleapis.com/oauth2/v3/certs (cache 1h), iss whitelist (accounts.google.com), ud  GOOGLE_CLIENT_IDS, exp, email_verified requerido.
- **Como:** Servicio GoogleVerifier (src/services/google_oauth.rs) con Arc<RwLock<Option<CacheEntry>>> para cache JWKS thread-safe. Resolucion: si existe link en usuarios_ext_oauth -> login. Si no, busca por email; si existe, vincula. Si no, crea usuario via UserRepository::create_oauth (sin password_hash) con username sanitizado de email/given_name + sufijo numerico ante colisiones. AppState ahora incluye google: Arc<GoogleVerifier>. Config nueva GOOGLE_CLIENT_IDS (CSV). Migration 13.
- **Resultado:** cargo clippy -- -D warnings verde. Endpoint expuesto en Swagger UI. Probado con id_token falso -> 401 unauthorized correcto. La verificacion real requiere id_token de Google legitimo (validacion en cliente web).
## 174A-22  OAuth Google PKCE (desktop)

- **Que:** Endpoint POST /api/auth/google/pkce que recibe {code, code_verifier, redirect_uri, client_id?} (flujo PKCE para clientes desktop sin client_secret), intercambia con https://oauth2.googleapis.com/token, valida el id_token recibido via GoogleVerifier::verify, y reusa la logica de google_login (link/create + emite par tokens). El client_id enviado por el cliente debe estar en la whitelist GOOGLE_CLIENT_IDS; si se omite, se usa el primero configurado.
- **Como:** Metodo nuevo GoogleVerifier::exchange_pkce (POST form-encoded con grant_type=authorization_code) y AuthService::google_pkce_login que delega a google_login. Reutiliza GooglePkceRequest validado (length min 32 en code_verifier por spec PKCE). Sin nuevos crates: solo reqwest existente.
- **Resultado:** Build verde. Probado con datos falsos contra Google real -> 403 con Google PKCE: invalid_client (round-trip correcto). Endpoint listo para clientes desktop con client_id valido tipo "Desktop App" en GCP Console.
## 174A-23  OAuth Google mobile (Android/iOS)

- **Que:** Validacion de soporte mobile sobre la infra OAuth ya construida en 174A-21/22. Apps moviles usan Google Sign-In SDK nativo que devuelve un id_token firmado por Google con ud = mobile_client_id. El endpoint existente POST /api/auth/google lo acepta sin cambios mientras el client_id mobile este en GOOGLE_CLIENT_IDS (CSV).
- **Como:** GoogleVerifier::verify usa Validation::set_audience(&self.client_ids) que acepta CUALQUIERA del slice como aud valido, asi un mismo backend acepta tokens de web, Android e iOS. Configuracion documentada: GOOGLE_CLIENT_IDS=web.apps.googleusercontent.com,android.apps.googleusercontent.com,ios.apps.googleusercontent.com. Para iOS/Android nativos (sin browser) el SDK gestiona el flujo y solo entrega id_token al cliente -> POST a backend.
- **Resultado:** Sin cambios de codigo necesarios. El flujo es identico a 174A-21 (web). Documentado en este registro y en config (GOOGLE_CLIENT_IDS CSV multi-cliente).
## 174A-24  Perfil endpoints (/me, /users/{username})

- **Que:** Tres endpoints: GET /api/users/me (auth, perfil propio con email/estado), PATCH /api/users/me (auth, actualiza nombre_visible/bio/avatar_url/portada_url/sitio_web/generos_favoritos), GET /api/users/{username} (publico, oculta email/paypal/estado y rechaza usuarios no-activos con 404).
- **Como:** Modelo nuevo UserProfile (FromRow) con todas las columnas relevantes de usuarios_ext. DTOs separados: PrivateProfileResponse (flatten de PublicProfileResponse + email + estado) y PublicProfileResponse. UpdateProfileRequest con validacion alidator (url, length). ProfileRepository::update construye SQL dinamico con clausulas SET solo para campos Some(_) (binders heterogeneos via enum Binder { Str, Json }). Si patch vacio devuelve perfil actual.
- **Resultado:** Build verde. Probado: login -> GET /me retorna username/email; PATCH /me con bio+sitio_web aplicado correctamente; GET /users/testuser1 retorna perfil publico con bio actualizada y SIN campo email (filtrado a nivel DTO).