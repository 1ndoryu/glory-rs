# Tareas completadas — 2026-05-01

## 154A-2 — Fix prod: Coolify/Contabo no configurados + Traefik 503 en nakomi.studio

- **Que:**
  - Subidos 17 env vars de producción al servicio Coolify `do8k4w8swccwwogoc0os0ck0` (studio): `COOLIFY_BASE_URL`, `COOLIFY_API_TOKEN`, `COOLIFY_SERVER_UUID`, `CONTABO_*`, `SMTP_*`, Google OAuth y Stripe.
  - Diagnosticada raíz del 503 público: el último redeploy había quedado a medias — solo postgres en estado "Created" y NO existía container `app-do8k4w8swccwwogoc0os0ck0`.
  - Fix de fallback en `src/services/email.rs`: `EmailConfig::from_env()` ahora acepta `SMTP_*` y `GLORY_SMTP_*` para que el binario funcione tanto con `.env` local (GLORY_SMTP_*) como con vars de Coolify (SMTP_*).
  - Levantado el container app desde el compose ya en disco (`/data/coolify/services/do8k4w8swccwwogoc0os0ck0/docker-compose.yml`) preservando bind mount `/data/uploads/studio:/app/uploads` (77MB intactos: avatars, content, deliverables, .cache).
  - Conectado el container app a la red Docker `coolify` para que Traefik pueda enrutarlo.
  - Verificado: `https://nakomi.studio/` → 200 OK, `https://nakomi.studio/api/health` → `{"status":"ok"}`.
- **Archivos:**
  - `src/services/email.rs` (fallback dual SMTP_/GLORY_SMTP_)
  - Cambios en producción aplicados sólo en disco del servidor (compose y red Docker), no en el repo.
- **Gotchas:**
  - `PATCH /api/v1/services/{uuid}` con `docker_compose_raw` requiere base64 obligatoriamente (devuelve 422 con YAML plano).
  - `POST /api/v1/services/{uuid}/envs/bulk` no existe (404); hay que postear vars individualmente.
  - `coolify-manager exec --target app` con UUID inexistente cae al fallback "buscar por nombre" y conecta a OTRO container que sí contenga "app" en su nombre — produce diagnóstico engañoso.
  - El FQDN sslip.io que Coolify auto-genera nunca se actualiza por API; los labels Traefik personalizados ya están en el compose en disco y conviven con los autogenerados.
  - El compose procesado por Coolify omite redes externas declaradas en `networks:` del raw — hubo que hacer `docker network connect coolify` manualmente.
  - **Datos NUNCA se perdieron**: bind mount `/data/uploads/studio` (77MB) y volumen `do8k4w8swccwwogoc0os0ck0_uploads-data` (6MB) intactos durante todo el incidente.
- **Sentinel:** No aplica (problema de infraestructura, no de código).
- **GLORY-RS:** El fallback dual `SMTP_*` / `GLORY_SMTP_*` en `EmailConfig::from_env()` es agnóstico — debe moverse a `glory-rs` cuando ese módulo se promueva al framework. Por ahora vive en el proyecto.

### Mejoras pendientes para `coolify-manager-rs` (detectadas en esta tarea)
1. **`find_container` debería ser estricto al fallback**: si se busca por UUID y no existe, NO debe caer al fallback "por nombre" (que conecta a containers de otros sitios). O al menos requerir flag explícito.
2. **Comando `host-exec`**: hoy no hay forma oficial de ejecutar comandos en el host del servidor; toda diagnostic a nivel host ("¿existe el container?", "¿qué hay en `/data/uploads/...`?", `docker network connect`, etc.) tuvo que hacerse via SSH directo con clave de la API. Añadir `coolify-manager host-exec --server <uuid> -- <cmd>` resolvería esto.
3. **`push_service_envs` debe usar POST individual**: hoy intenta POST bulk a `/envs/bulk` y devuelve 404. Cambiar a un loop de POST a `/api/v1/services/{uuid}/envs` por cada var.
4. **`patch_service_compose` debe codificar base64**: cualquier helper que envíe `docker_compose_raw` debe encodear base64 antes del PATCH (la API rechaza YAML plano con 422).
5. **Recuperación post-redeploy fallido**: añadir comando `recover --name <sitio>` que detecte containers faltantes y haga `docker compose up -d <service>` desde el compose en disco (sin tocar `docker_compose_raw`, sin perder volumes ni network adjuntas).
