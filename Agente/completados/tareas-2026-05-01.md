# Tareas completadas â€” 2026-05-01

## 154A-2 â€” Fix prod: Coolify/Contabo no configurados + Traefik 503 en nakomi.studio

- **Que:**
  - Subidos 17 env vars de producciÃ³n al servicio Coolify `do8k4w8swccwwogoc0os0ck0` (studio): `COOLIFY_BASE_URL`, `COOLIFY_API_TOKEN`, `COOLIFY_SERVER_UUID`, `CONTABO_*`, `SMTP_*`, Google OAuth y Stripe.
  - Diagnosticada raÃ­z del 503 pÃºblico: el Ãºltimo redeploy habÃ­a quedado a medias â€” solo postgres en estado "Created" y NO existÃ­a container `app-do8k4w8swccwwogoc0os0ck0`.
  - Fix de fallback en `src/services/email.rs`: `EmailConfig::from_env()` ahora acepta `SMTP_*` y `GLORY_SMTP_*` para que el binario funcione tanto con `.env` local (GLORY_SMTP_*) como con vars de Coolify (SMTP_*).
  - Levantado el container app desde el compose ya en disco (`/data/coolify/services/do8k4w8swccwwogoc0os0ck0/docker-compose.yml`) preservando bind mount `/data/uploads/studio:/app/uploads` (77MB intactos: avatars, content, deliverables, .cache).
  - Conectado el container app a la red Docker `coolify` para que Traefik pueda enrutarlo.
  - Verificado: `https://nakomi.studio/` â†’ 200 OK, `https://nakomi.studio/api/health` â†’ `{"status":"ok"}`.
- **Archivos:**
  - `src/services/email.rs` (fallback dual SMTP_/GLORY_SMTP_)
  - Cambios en producciÃ³n aplicados sÃ³lo en disco del servidor (compose y red Docker), no en el repo.
- **Gotchas:**
  - `PATCH /api/v1/services/{uuid}` con `docker_compose_raw` requiere base64 obligatoriamente (devuelve 422 con YAML plano).
  - `POST /api/v1/services/{uuid}/envs/bulk` no existe (404); hay que postear vars individualmente.
  - `coolify-manager exec --target app` con UUID inexistente cae al fallback "buscar por nombre" y conecta a OTRO container que sÃ­ contenga "app" en su nombre â€” produce diagnÃ³stico engaÃ±oso.
  - El FQDN sslip.io que Coolify auto-genera nunca se actualiza por API; los labels Traefik personalizados ya estÃ¡n en el compose en disco y conviven con los autogenerados.
  - El compose procesado por Coolify omite redes externas declaradas en `networks:` del raw â€” hubo que hacer `docker network connect coolify` manualmente.
  - **Datos NUNCA se perdieron**: bind mount `/data/uploads/studio` (77MB) y volumen `do8k4w8swccwwogoc0os0ck0_uploads-data` (6MB) intactos durante todo el incidente.
- **Sentinel:** No aplica (problema de infraestructura, no de cÃ³digo).
- **GLORY-RS:** El fallback dual `SMTP_*` / `GLORY_SMTP_*` en `EmailConfig::from_env()` es agnÃ³stico â€” debe moverse a `glory-rs` cuando ese mÃ³dulo se promueva al framework. Por ahora vive en el proyecto.

### Mejoras pendientes para `coolify-manager-rs` (detectadas en esta tarea)
1. **`find_container` deberÃ­a ser estricto al fallback**: si se busca por UUID y no existe, NO debe caer al fallback "por nombre" (que conecta a containers de otros sitios). O al menos requerir flag explÃ­cito.
2. **Comando `host-exec`**: hoy no hay forma oficial de ejecutar comandos en el host del servidor; toda diagnostic a nivel host ("Â¿existe el container?", "Â¿quÃ© hay en `/data/uploads/...`?", `docker network connect`, etc.) tuvo que hacerse via SSH directo con clave de la API. AÃ±adir `coolify-manager host-exec --server <uuid> -- <cmd>` resolverÃ­a esto.
3. **`push_service_envs` debe usar POST individual**: hoy intenta POST bulk a `/envs/bulk` y devuelve 404. Cambiar a un loop de POST a `/api/v1/services/{uuid}/envs` por cada var.
4. **`patch_service_compose` debe codificar base64**: cualquier helper que envÃ­e `docker_compose_raw` debe encodear base64 antes del PATCH (la API rechaza YAML plano con 422).
5. **RecuperaciÃ³n post-redeploy fallido**: aÃ±adir comando `recover --name <sitio>` que detecte containers faltantes y haga `docker compose up -d <service>` desde el compose en disco (sin tocar `docker_compose_raw`, sin perder volumes ni network adjuntas).

## varsense-report — Normalizar variables CSS y eliminar box-shadow

- **Que:** Corrección completa del .varsense-report.md: 41 archivos CSS normalizados.
  - Variables inexistentes mapeadas: --text-secondary→--text-tertiary, --bg-secondary→--bg-accent, --accent→--accent-primary, --success→--success-color, etc.
  - Todos los box-shadow eliminados (propiedad prohibida).
  - Font-sizes hardcoded normalizados: 10px→--text-2xs, 12px→--text-xs, 0.75rem→--text-xs, 1.125rem→--text-lg.
  - Variables runtime agregadas a variables.css: --hosting-bar-width, --panel-card-icono-*.
  - 7 botones únicos marcados con sentinel-disable EstiloBotonEspecifico + justificación.
- **Archivos:** 41 CSS + variables.css
- **Gotchas:** Colores hex en VpsSubscriptionsPanel badges no normalizados — semánticos sin token equivalente.
- **Sentinel:** No aplica
- **GLORY:** No aplica

