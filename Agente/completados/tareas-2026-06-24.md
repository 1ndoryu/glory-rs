## 246A-1 — Sync changelog write-side (implementación completa)

- **Que:** Implementada la escritura de `sync_changelog` desde los 10 handlers mutantes de Rust. Agregado `SyncChangelogRepository::insert_entry()` con `RETURNING id`, e integrado en todos los puntos de mutación: samples (upload, update, delete), colecciones (create, update, delete, merge, add_sample, remove_sample) y downloads (register_download). Creada documentación completa del sistema de sincronización desktop.
- **Archivos:**
  - `src/repositories/sync_changelog.rs` — nuevo método `insert_entry()`
  - `src/handlers/samples.rs` — upload escribe SampleAdded
  - `src/services/sample_catalog/mod.rs` — update/delete escriben SampleUpdated/SampleRemoved
  - `src/handlers/colecciones.rs` — 6 handlers escriben eventos de colecciones
  - `src/handlers/downloads.rs` — register_download escribe SampleAdded (origen: download)
  - `src/handlers/admin.rs` — solo formateo (cargo fmt)
  - `Agente/documentacion/sync/sync-desktop-estado-2026-06-24.md` — documentación nueva
- **Gotchas:** Clippy `cast_possible_truncation` en i64→i32 resuelto con `#[allow]` locales. `too_many_lines` preexistente en `register_download`. `cargo fmt` arregló imports en `samples.rs` y formato en `admin.rs`.
- **Sentinel:** No aplica
- **GLORY:** No aplica (específico del proyecto sync desktop)
- **Validación:** `cargo check` OK, `cargo clippy --all-targets -- -D warnings` OK, `cargo fmt --check` OK, `cargo test` 184 passed, `npm run type-check` OK
- **Commit:** `eb64a6a2` — push exitoso a `kamples`
