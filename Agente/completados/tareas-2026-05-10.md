# Tareas completadas — 10 mayo 2026

## 095A-21 — Corregir pantalla blanca al entrar/salir del panel
- **Que:** Se corrigió el orden de hooks de `ChatWidget` para que el widget se oculte en `/panel` sin romper las reglas de React al navegar entre panel y sitio público.
- **Archivos:** `frontend/src/components/chat/ChatWidget.tsx`
- **Gotchas:** El síntoma aparecía como pantalla blanca/minified React #310; el fix correcto era ejecutar hooks siempre y retornar `null` después.
- **Sentinel:** No aplica.
- **GLORY:** No aplica; comportamiento específico de Nakomi Studio.

## 095A-22 — Resolver de raíz el “no available server” recurrente
- **Que:** Se corrigió el drift de Postgres/Coolify en `deploy-service`: hostname único `postgres-{uuid}`, alineación de password del rol `rust_app`, bind mount persistente de uploads y verificación post-deploy con `coolify-manager-rs`.
- **Archivos:** `.agent/coolify-manager-rs/config/templates/rust-stack.yaml`, `.agent/coolify-manager-rs/src/commands/deploy_service.rs`, `Agente/planes/completados/plan-no-available-server-panel-2026-05-09.md`
- **Gotchas:** Coolify puede dejar un contenedor “healthy” aunque Traefik no tenga backend útil; no basta con mirar Docker health ni hacer restart desde UI.
- **Sentinel:** No aplica; prevención implementada directamente en `coolify-manager-rs`.
- **GLORY:** Cambio agnóstico en herramienta del agente.

## 095A-23 — Sincronización segura de envs de producción
- **Que:** `sync-env` ahora audita, compara, filtra por `--only`, enmascara valores y empuja envs seguras desde `.env`/`frontend/.env`; `VITE_STRIPE_PUBLISHABLE_KEY` quedó sincronizada y Stripe pasó a resolverse en runtime mediante `/api/public-config`.
- **Archivos:** `.agent/coolify-manager-rs/src/commands/sync_env.rs`, `.agent/coolify-manager-rs/config/templates/Dockerfile.rust`, `.agent/coolify-manager-rs/config/templates/rust-stack.yaml`, `src/config/mod.rs`, `src/handlers/public_config.rs`, `src/handlers/mod.rs`, `src/lib.rs`, `frontend/src/api/publicConfig.ts`, `frontend/src/hooks/useStripeClient.ts`, `frontend/src/components/panel/CheckoutModal.tsx`, `frontend/src/components/panel/AddPaymentMethodModal.tsx`
- **Gotchas:** Vite sustituye `import.meta.env` en build-time; las envs runtime de Coolify no cambian un bundle ya compilado. Para publishable keys, config pública runtime es más robusta.
- **Sentinel:** No aplica; cubierto con tests de `sync_env` y verificación real de bundle.
- **GLORY:** El endpoint runtime es específico de este template; el patrón es reutilizable para otros proyectos Rust.

## 105A-1 — Health Docker por IP de red para Rust
- **Que:** Se eliminó el falso healthy donde `localhost` respondía dentro del contenedor pero Traefik no alcanzaba la IP Docker: el template Rust ahora healthcheckea la IP del contenedor, `coolify-manager-rs health` hace probe host→IP del contenedor y `deploy-service` recrea una sola vez `app` sin build si detecta ese fallo, verificando antes el bind mount de uploads.
- **Archivos:** `.env.example`, `src/config/mod.rs`, `glory-rs/backend/src/config.rs`, `.agent/coolify-manager-rs/config/templates/rust-stack.yaml`, `.agent/coolify-manager-rs/templates/rust-stack.yaml`, `.agent/coolify-manager-rs/src/services/health_manager.rs`, `.agent/coolify-manager-rs/src/commands/deploy_service.rs`, `Agente/documentacion/hosting/coolify-rust-healthcheck-red-docker-2026-05-10.md`, `Agente/lecciones/lecciones-aprendidas.md`
- **Gotchas:** Un probe a `localhost` no prueba el camino de Traefik. La verificación final confirmó `HOST=0.0.0.0`, IP Docker `10.0.11.3` y `NETWORK_IP_HEALTH_OK`.
- **Sentinel:** No aplica; prevención implementada en health/deploy de `coolify-manager-rs`.
- **GLORY:** Cambio agnóstico en `coolify-manager-rs` y default Docker-safe en `glory-rs/backend`.

## 105A-2 — Guardar recovery no-build contra imágenes ausentes
- **Que:** `deploy-service` ahora verifica que la imagen Docker local del servicio exista antes de ejecutar `--no-build --force-recreate --no-deps`; si falta, aborta con mensaje explícito en vez de borrar el contenedor sano y dejar el sitio en 503. También se agregó regla anti-atasco al protocolo para comandos remotos largos y builds con binario release fijo.
- **Archivos:** `.agent/coolify-manager-rs/src/commands/deploy_service.rs`, `.github/instructions/conducta.instructions.md`, `Agente/documentacion/hosting/coolify-rust-healthcheck-red-docker-2026-05-10.md`, `Agente/lecciones/lecciones-aprendidas.md`
- **Gotchas:** `CARGO_TARGET_DIR` puede compilar en otro directorio y dejar viejo el binario `target/release/coolify-manager.exe`; para este flujo se validó con `cargo build --release --target-dir target` y salida `Imagen disponible`.
- **Sentinel:** No aplica; prevención implementada en `coolify-manager-rs` y protocolo operativo.
- **GLORY:** Cambio agnóstico en herramienta del agente.

## 105A-3 — Respuestas del chatbot sin Markdown visible
- **Que:** Se agregó sanitización de salida para eliminar énfasis, encabezados, listas y backticks de Markdown antes de persistir/enviar respuestas de IA; el prompt queda reforzado pero la garantía vive en backend.
- **Archivos:** `src/services/ai_chat.rs`, `src/services/ai_prompts.rs`, `Agente/documentacion/chatbot/chatbot-deepseek-abuso-2026-05-09.md`, `Agente/lecciones/lecciones-aprendidas.md`
- **Gotchas:** El prompt solo reduce probabilidad; modelos distintos siguen devolviendo `**Texto**` ocasionalmente, así que el boundary de salida debe normalizar texto.
- **Sentinel:** No aplica; cubierto con unit test `parse_escalation_strips_markdown_formatting`.
- **GLORY:** Patrón agnóstico, implementación específica del servicio de chat de Nakomi.

## 105A-4 — Visión Groq para imágenes del chatbot
- **Que:** El procesamiento de imágenes del chat usa modelos Groq Vision actuales (`llama-4-scout` con fallback `llama-4-maverick`), guarda la descripción en metadata del mensaje para el historial IA y evita uploads silenciosos antes de tener `sessionId`.
- **Archivos:** `src/handlers/chat/file_ai.rs`, `src/repositories/chat.rs`, `src/services/ai_chat.rs`, `src/services/ai_prompts.rs`, `frontend/src/components/chat/ChatWidget.tsx`, `frontend/src/hooks/useChatWidget.ts`, `frontend/src/hooks/chatWidgetServerMessages.ts`, `Agente/documentacion/chatbot/chatbot-deepseek-abuso-2026-05-09.md`, `Agente/lecciones/lecciones-aprendidas.md`
- **Gotchas:** La descripción debe vivir en `chat_messages.metadata`; si solo queda en `chat_attachments`, preguntas posteriores sobre la imagen pierden contexto.
- **Sentinel:** No aplica; cubierto con unit test `build_context_includes_attachment_description_metadata`.
- **GLORY:** Patrón agnóstico, implementación específica del chat de Nakomi.

## 105A-5 — Selected Work a dos columnas con imágenes más nítidas
- **Que:** `.showcaseGridProyectos` pasó de tres a dos columnas, las imágenes de proyectos del home piden variantes optimizadas de 800x600 con calidad 86 y el proxy de imágenes codifica rutas para que `srcset` funcione con assets que tienen espacios.
- **Archivos:** `frontend/src/components/home/SeccionShowcase.css`, `frontend/src/components/home/SeccionShowcase.tsx`, `frontend/src/utils/imageUtils.ts`
- **Gotchas:** La home local requiere API de proyectos; la verificación visual se hizo con `/api/projects` interceptado en Vite/Puppeteer. El mock reveló que rutas con espacios rompían `srcset`, así que se corrigió antes de cerrar.
- **Sentinel:** No aplica.
- **GLORY:** No aplica; ajuste visual específico de Nakomi Studio.