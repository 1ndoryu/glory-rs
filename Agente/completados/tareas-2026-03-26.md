# Tareas completadas — 2026-03-26

## 263A-1+263A-2 — Revisión de indicaciones del cliente + Transcripción y análisis Data II

- **Que:** Se transcribieron los 18 archivos multimedia de `cliente/Data II/` (15 videos + 3 audios) usando Groq Whisper large-v3 con rotación de 3 API keys y extracción de audio con ffmpeg para archivos >20MB. Se organizaron los nuevos requerimientos en un documento de análisis en `Agente/documentacion/reservas/`. Se realizó revisión completa del frontend vs lo que pidió el cliente. Gaps encontrados: (1) falta "olvidé mi contraseña", (2) falta opción "otros" en método de pago ventas. El roadmap fue actualizado con todas las tareas nuevas derivadas del análisis, organizadas en 4 fases para el módulo de reservas.
- **Archivos:** `scripts/transcribir-data-ii.py` (nuevo), `cliente/Data II/transcripciones-data-ii.md` (nuevo), `Agente/documentacion/reservas/analisis-data-ii-2026-03-26.md` (nuevo), `roadmap.md` (actualizado)
- **Gotchas:** Video 6 (37.5MB) falló después de 4 intentos por desconexión del servidor Groq — archivos muy grandes (>30MB) pueden tener problemas incluso extrayendo audio. Se guardó como error en la transcripción. No es bloqueante (contenido inferible por contexto). Los audios 1 y 2 son idénticos (duplicado de WhatsApp).
- **Sentinel:** No aplica — es documentación y scripting, no código de producción.

## 263A-3 — Verificar datos de prueba y credenciales admin

- **Que:** Se confirmó que el seed (`src/bin/seed.rs`, ejecutable con `cargo run --bin seed` o `npm run seed`) genera: usuario demo, 2-5 ventas/día (60 días), 1-3 gastos/día (60 días), 3-8 reservas/día (15 días futuros), 10 categorías de gastos. Credenciales: **demo@restaurante.com / demo1234** (nombre: "Demo Admin").
- **Archivos:** Ninguno modificado — solo verificación de `src/bin/seed.rs`.
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 263A-5 — CRM de clientes + Gestor de etiquetas + Método de pago "Otros"

- **Que:** Implementación completa del módulo CRM de clientes y gestor de etiquetas, full-stack. Backend: migración SQL (6 tablas nuevas + constraints actualizados), modelos, repositorios con búsqueda ILIKE paginada, servicios, handlers con OpenAPI/utoipa. Frontend: página de clientes con búsqueda, paginación, modal crear/editar, formulario completo con todos los campos del CRM (nombre, apellidos, teléfono con prefijo, email, empresa, notas, alergias, preferencias, consentimientos). También agrega "Otros" como método de pago en ventas y gastos (backend + frontend). Agrega "NoShow" como estado de reserva. Agrega campos `cliente_id`, `canal_id`, `no_show` a reservas. Etiquetas: CRUD de categorías y etiquetas, asignación M:N a clientes y reservas, etiquetas preestablecidas del sistema (VIP, Frecuente, No Paga, etc.).
- **Archivos:** `migrations/20260326000000_crm_etiquetas.up.sql`, `migrations/20260326000000_crm_etiquetas.down.sql`, `src/models/cliente.rs`, `src/models/etiqueta.rs`, `src/models/common.rs`, `src/models/reserva.rs`, `src/models/mod.rs`, `src/repositories/cliente.rs`, `src/repositories/etiqueta.rs`, `src/repositories/mod.rs`, `src/services/cliente.rs`, `src/services/etiqueta.rs`, `src/services/mod.rs`, `src/handlers/clientes.rs`, `src/handlers/etiquetas.rs`, `src/handlers/mod.rs`, `frontend/src/api/generated.ts`, `frontend/src/componentes/ListaClientes.tsx`, `frontend/src/componentes/FormularioCliente.tsx`, `frontend/src/componentes/FormularioVenta.tsx`, `frontend/src/componentes/FormularioGasto.tsx`, `frontend/src/componentes/BarraLateral.tsx`, `frontend/src/App.tsx`, `frontend/src/hooks/useFormularioCliente.ts`, `frontend/src/hooks/useListaClientes.ts`, `frontend/src/estilos/Formularios.css`, `frontend/openapi-debug.json`, `.sqlx/*`
- **Gotchas:** La tabla categorias_etiqueta tiene `aplica_a` (cliente/reserva) para distinguir el tipo de categoría. No tiene `color` propio — cada etiqueta individual tiene su color. El cache `.sqlx/` tuvo que regenerarse después de aplicar la migración.
- **Sentinel:** No aplica — Code Sentinel detectó bien los problemas (checkboxes nativos → Input, useState excesivo → hook), ambos corregidos.

## 263A-6+263A-7 — Vista de reservas por día + Vista de reservas por mes

- **Que:** Implementación completa de vistas de reservas. **Vista día (263A-6):** Filtros por fecha (date picker, default hoy), turno (desayuno 7-12h / comida 12-18h / cena 18-24h / día completo), estado (confirmada, pendiente, lista_espera, no_show, cancelada, completada). Tabla muestra: nº mesa, hora, nombre, apellidos, personas, estado (con badge), teléfono. Nuevo campo `num_mesa` (INT nullable) y `apellidos_cliente` (VARCHAR) en reservas via migración. Nuevo estado `lista_espera`. Formulario de reserva actualizado con campos num_mesa y apellidos. **Vista mes (263A-7):** Cuadrícula calendario con navegación mes anterior/siguiente. Cada celda muestra total reservas + personas del día. Totales mensuales en cabecera. Click en día → navega a vista día con fecha en URL. Endpoint `GET /api/reservas/resumen-mes?anio=&mes=` con query optimizada usando `make_date()` + rango (aprovecha índice).
- **Archivos:** `migrations/20260326100000_reservas_vista_dia.up.sql`, `migrations/20260326100000_reservas_vista_dia.down.sql`, `src/models/reserva.rs`, `src/models/mod.rs`, `src/repositories/reserva.rs`, `src/services/reserva.rs`, `src/handlers/reservas.rs`, `src/handlers/mod.rs`, `frontend/src/componentes/ListaReservas.tsx`, `frontend/src/componentes/CalendarioReservas.tsx`, `frontend/src/componentes/FormularioReserva.tsx`, `frontend/src/componentes/BarraLateral.tsx`, `frontend/src/App.tsx`, `frontend/src/hooks/useVistaReservas.ts`, `frontend/src/hooks/useFormularioReserva.ts`, `frontend/src/estilos/Calendario.css`, `frontend/src/api/generated.ts`, `.sqlx/*`
- **Gotchas:** `EXTRACT(YEAR FROM fecha)` en PostgreSQL retorna `numeric` que SQLx mapea a `Decimal`, no `f64`. Solución: usar `make_date($2, $3, 1)` con rango, que además es más eficiente con índices. Clippy `too_many_arguments` en repo.list → refactorizado a `FiltrosReserva` struct.
- **Sentinel:** No aplica.

## 263A-8+263A-9 — Backend no-shows stats + Canales de reserva CRUD

- **Que:** 263A-8: Endpoint `GET /api/reservas/no-shows` con estadísticas de no-shows (total reservas, total no-shows, ratio %, desglose por canal). Modelos `NoShowStats` y `NoShowPorCanal`. Query SQL con LEFT JOIN a canales_reserva y agrupación por canal. 263A-9: CRUD completo para canales de reserva (`GET /api/canales-reserva`, `POST /api/canales-reserva`, `DELETE /api/canales-reserva/{id}`). Modelo, repositorio, servicio, handler + registro en OpenAPI.
- **Archivos:** `src/handlers/canales_reserva.rs` (nuevo), `src/models/canal_reserva.rs` (nuevo), `src/repositories/canal_reserva.rs` (nuevo), `src/services/canal_reserva.rs` (nuevo), `src/handlers/reservas.rs`, `src/handlers/mod.rs`, `src/models/reserva.rs`, `src/models/mod.rs`, `src/repositories/reserva.rs`, `src/repositories/mod.rs`, `src/services/reserva.rs`, `src/services/mod.rs`, `.sqlx/*`
- **Gotchas:** Ninguno significativo.
- **Sentinel:** No aplica.

## 263A-10 — OpenAPI tags + Orval tags-split (generated.ts dividido)

- **Que:** Se añadió `tag = "X"` a todos los endpoints `#[utoipa::path]` en los 9 handlers (Auth, Ventas, Gastos, Reservas, Clientes, Etiquetas, Canales, Dashboard, Health). Se cambió `orval.config.ts` de `mode: 'single'` a `mode: 'tags-split'`. Se creó binario `src/bin/dump_openapi.rs` para generar el schema OpenAPI sin necesitar base de datos. El archivo `generated.ts` (3369 líneas, 149KB) fue reemplazado por un barrel de re-exports, y los archivos reales viven en `src/api/generated/{tag}/{tag}.ts`. El input de Orval cambió de URL del backend a archivo local (`openapi-debug.json`).
- **Archivos:** Todos los `src/handlers/*.rs`, `frontend/orval.config.ts`, `frontend/src/api/generated.ts` (barrel), `frontend/src/api/generated/` (10 archivos nuevos), `src/bin/dump_openapi.rs` (nuevo), `.gitignore`
- **Gotchas:** PS5 no tiene `-Encoding utf8NoBOM` — usar `Start-Process -RedirectStandardOutput` para escritura limpia UTF-8. El nombre del schema file contiene `ó` (`gestiónRestauranteAPI.schemas.ts`) porque deriva del título de la API.
- **Sentinel:** No aplica — es reorganización de código generado.

## 263A-11 — Frontend canales de reserva + estadísticas no-shows

- **Que:** Componente `ListaCanales.tsx` — CRUD de canales de reserva (listar, crear en modal, eliminar). Componente `EstadisticasNoShows.tsx` — panel de estadísticas de no-shows con 3 tarjetas (total reservas, no-shows, ratio %) y tabla desglose por canal con ratios coloreados (verde <10%, naranja 10-20%, rojo >20%). Filtros de fecha (desde/hasta). CSS `NoShows.css` responsive. Rutas `/canales` y `/reservas/no-shows` en App.tsx. Enlaces en BarraLateral con iconos lucide-react (Radio, UserX).
- **Archivos:** `frontend/src/componentes/ListaCanales.tsx` (nuevo), `frontend/src/componentes/EstadisticasNoShows.tsx` (nuevo), `frontend/src/estilos/NoShows.css` (nuevo), `frontend/src/App.tsx`, `frontend/src/componentes/BarraLateral.tsx`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 263A-12 — Seed actualizado (clientes, canales, etiquetas, no-shows)

- **Que:** Reescritura completa de `src/bin/seed.rs`. Ahora es idempotente (limpia datos anteriores del usuario demo). Inserta: 6 canales de reserva (Teléfono, WhatsApp, Web, Walk-in, Google Maps, Instagram), 18 clientes demo con datos variados (alergias, preferencias bebida/ubicación, empresas), 299 ventas, 171 gastos (ambos con método "otros"), 240 reservas (30 días pasados con ~15% no-shows + 15 días futuros, con canal_id, cliente_id, num_mesa, apellidos_cliente), 36 asignaciones cliente-etiqueta del sistema, y 2 etiquetas personalizadas del usuario. Corregido warning clippy pre-existente `similar_names` en `reservas.rs`.
- **Archivos:** `src/bin/seed.rs`, `src/handlers/reservas.rs`
- **Gotchas:** Clippy exige `is_multiple_of()` en vez de `% n == 0`. Casting `i64→usize` necesita allows explícitos en funciones con loops sobre días.
- **Sentinel:** No aplica.

## 263A-13 — Dashboard reservas Fase 2 (3 paneles + recharts + Glory UI)

- **Que:** Dashboard completo de reservas con 3 paneles tabulados (Resumen, Ocupación, Análisis). Backend: modelo `DashboardReservas` con 9 structs, repositorio con 10 queries SQL (total, por_dia, por_dia_semana, por_canal, clientes_nuevos, ocupación, por_hora, por_turno, antelacion_media, reservas_efectivas, total_ventas_periodo), servicio con orquestación y cálculo de variación %/medias/ticket medio, handler `GET /api/dashboard/reservas`. Frontend: componente `DashboardReservas.tsx` con selector mes/año, 3 paneles: PanelResumen (KPIs + BarChart por día + BarChart día semana + PieChart canal), PanelOcupacion (4 KPIs + BarChart hora + BarChart turno + tabla procedencia), PanelAnalisis (5 KPIs financieros). Usa `recharts` para gráficos. Ruta `/reservas/dashboard` en App.tsx, enlace en BarraLateral con icono LayoutDashboard. CSS responsive. Elementos nativos reemplazados por componentes Glory (`Select`, `Boton`).

## 263A-17 — Configuración del restaurante

- **Que:** Full stack: migración con tabla `configuracion_restaurante` (campos obligatorios reserva: email/teléfono/nombre/apellidos como booleans, IVA por defecto como NUMERIC(5,2), nombre restaurante). Backend: modelo con `rust_decimal::Decimal` para IVA + validación custom (validator no soporta `validate(range)` en Decimal), repositorio con upsert (obtener_o_crear) + update parcial COALESCE, servicio, handler GET/PATCH `/api/configuracion`. Frontend: hook `useConfiguracion` (2 useState), componente `Configuracion.tsx` con secciones (nombre restaurante, toggles campos obligatorios, IVA), CSS responsive. Ruta `/configuracion` en App.tsx, enlace en BarraLateral con icono Settings. Orval regenerado con fix de encoding (schemas renombrado `gestiónRestauranteAPI` → `gestiNRestauranteAPI` por encoding UTF-8 de PowerShell).
- **Archivos:** `migrations/20260326400000_configuracion.{up,down}.sql`, `src/models/configuracion.rs`, `src/repositories/configuracion.rs`, `src/services/configuracion.rs`, `src/handlers/configuracion.rs`, `src/{models,repositories,services,handlers}/mod.rs`, `frontend/src/hooks/useConfiguracion.ts`, `frontend/src/componentes/Configuracion.tsx`, `frontend/src/estilos/Configuracion.css`, `frontend/src/App.tsx`, `frontend/src/componentes/BarraLateral.tsx`, `frontend/src/api/generated.ts`, `frontend/src/api/generated/configuracion/configuracion.ts`, `frontend/src/api/generated/gestiNRestauranteAPI.schemas.ts`
- **Gotchas:** (1) `validator::Validate` derive con `range(min/max)` no funciona para `rust_decimal::Decimal` — requiere función custom. (2) Clippy `struct_excessive_bools` para 4 flags — allow justificado. (3) PowerShell `Out-File` genera BOM UTF-8 que Orval no puede leer — usar `[System.IO.File]::WriteAllText` con UTF8Encoding sin BOM. (4) dump_openapi usa `print!` no args — redirigir stdout, no pasar filename como arg.
- **Sentinel:** No aplica nueva regla.

## 263A-18 — Unificar npm run verify y self-check.ps1

- **Que:** `self-check.ps1` ahora tiene 2 fases: Fase 1 ejecuta validación automática (detecta stack: Rust→cargo check/clippy/test, Frontend→tsc --noEmit, PHP→manual) y Fase 2 muestra el checklist de reglas. Las instrucciones en `.github/instructions/test.instructions.md` actualizadas para ser stack-agnostic: sección V ahora documenta detección automática de stack, regla 11 lista comandos por tipo de archivo en vez de asumir un stack concreto.
- **Archivos:** `scripts/self-check.ps1`, `.github/instructions/test.instructions.md`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 263A-19 — Regla de generated.ts siempre dividido (tags-split)

- **Que:** Regla añadida en instrucciones (punto 9: Estándares de código) prohibiendo `generated.ts` monolítico. Orval debe usar siempre `mode: 'tags-split'`. MD de prevención creado en `Agente/prevencion/` para futura implementación en Code Sentinel.
- **Archivos:** `.github/instructions/test.instructions.md`, `Agente/prevencion/prevencion-generated-ts-monolitico-2026-03-26.md`
- **Gotchas:** Ninguno.
- **Sentinel:** Regla pendiente de implementar (documentada en prevención).

## 263A-20 — Preparar deploy con Coolify

- **Que:** Dockerfile multi-stage (Rust builder + Node builder + runtime Debian slim), backend ahora sirve estáticos con `tower-http::services::ServeDir` + fallback SPA a `index.html`, `.dockerignore` creado, cache `.sqlx/` regenerado con `cargo sqlx prepare`, plan de deploy documentado en `Agente/planes/`. El deploy real en Coolify requiere configuración manual del servicio (PostgreSQL + app genérica) porque coolify-manager-rs aún no soporta apps no-WordPress.
- **Archivos:** `Dockerfile`, `.dockerignore`, `Cargo.toml` (feature `fs`), `Cargo.lock`, `src/handlers/mod.rs` (fallback SPA), `.sqlx/` (regenerado), `Agente/planes/plan-deploy-coolify-2026-03-26.md`
- **Gotchas:** (1) coolify-manager-rs está orientado a WordPress (ver QM3 de mision.md) — deploy manual via Coolify UI por ahora. (2) `SQLX_OFFLINE=true` en Dockerfile requiere `.sqlx/` actualizado en el repo. (3) `STATIC_DIR` env var configurable (default `static`).
- **Sentinel:** No aplica.
- **Archivos:** `src/models/dashboard_reservas.rs` (nuevo), `src/repositories/dashboard_reservas.rs` (nuevo), `src/services/dashboard.rs`, `src/handlers/dashboard.rs`, `src/handlers/mod.rs`, `src/models/mod.rs`, `src/repositories/mod.rs`, `frontend/src/componentes/DashboardReservas.tsx` (nuevo), `frontend/src/estilos/DashboardReservas.css` (nuevo), `frontend/src/App.tsx`, `frontend/src/componentes/BarraLateral.tsx`, `frontend/package.json`, `frontend/src/api/generated/dashboard/dashboard.ts`, `frontend/src/api/generated/gestiónRestauranteAPI.schemas.ts`
- **Gotchas:** Recharts `PieLabel` type no es directamente importable — usar inline type `{ name?: string; value?: number }`. Types del dashboard se importan directamente del generated en vez de extraer con ReturnType.
- **Sentinel:** Code Sentinel detectó correctamente 5 usos de elementos nativos (`<select>`, `<button>`) → reemplazados por `<Select>` y `<Boton>` de Glory.

## 263A-14 — Plano de sala: constructor drag-and-drop (backend + frontend)

- **Que:** Sistema completo para que el dueño construya el plano de sala arrastrando mesas. Backend: migración `20260326200000_plano_sala` (4 tablas: `zonas_sala`, `mesas`, `combinaciones_mesas`, `combinacion_mesa_items` + `mesa_id` en reservas), 18 structs en modelo, repositorio con 17 métodos CRUD, servicio con 12 métodos (incluye export/import con resolución de refs `zona_nombre:numero`), 12 endpoints REST con OpenAPI. Frontend: componente `PlanoSala.tsx` dividido en 4 archivos (orquestador + `MesaDraggable.tsx` + `PanelConfigMesa.tsx` + `usePlanoSala.ts`). Drag-and-drop con `@dnd-kit/core`, tabs de zonas, canvas con grid, panel de config lateral, export/import JSON, combinaciones de mesas. CSS responsive. Ruta `/plano-sala`, enlace en BarraLateral con icono Map.
- **Archivos:** `migrations/20260326200000_plano_sala.up.sql` (nuevo), `migrations/20260326200000_plano_sala.down.sql` (nuevo), `src/models/plano_sala.rs` (nuevo), `src/repositories/plano_sala.rs` (nuevo), `src/services/plano_sala.rs` (nuevo), `src/handlers/plano_sala.rs` (nuevo), `src/handlers/mod.rs`, `src/models/mod.rs`, `src/models/reserva.rs`, `src/repositories/mod.rs`, `src/repositories/reserva.rs`, `src/services/mod.rs`, `src/services/reserva.rs`, `frontend/src/componentes/PlanoSala.tsx` (nuevo), `frontend/src/componentes/plano-sala/MesaDraggable.tsx` (nuevo), `frontend/src/componentes/plano-sala/PanelConfigMesa.tsx` (nuevo), `frontend/src/componentes/plano-sala/usePlanoSala.ts` (nuevo), `frontend/src/estilos/PlanoSala.css` (nuevo), `frontend/src/App.tsx`, `frontend/src/componentes/BarraLateral.tsx`, `frontend/src/api/generated/plano-sala/plano-sala.ts` (generado), `frontend/package.json`
- **Gotchas:** Clippy `too_many_arguments` en `crear_mesa`/`actualizar_mesa` → refactorizado para aceptar structs request en vez de parámetros individuales. PowerShell corrompe template literals en here-strings → usar `replace_string_in_file` para corregir. Code Sentinel marcó 7 useState en PanelConfigMesa → consolidado en un solo state object. `dump_openapi` necesita `Start-Process -RedirectStandardOutput` para escritura limpia.
- **Sentinel:** No requiere regla nueva. Correctamente detectó límite de líneas y useState excesivo → corregidos.

## 263A-15 — Recuperación de contraseña (backend + frontend)

- **Que:** Flujo completo "olvidé mi contraseña". Backend: migración `20260326300000_password_reset` (añade `reset_token`, `reset_token_expires_at` con índice parcial a `users`), `EmailService` con envío SMTP via lettre (fallback a log en console si SMTP no configurado), `AuthService::forgot_password` genera token hex 64 chars con 1h expiración (siempre retorna Ok para no revelar si email existe), `AuthService::reset_password` valida token y hashea nueva contraseña con argon2. Config: `SmtpConfig` struct opcional + `app_url`. Repos: `set_reset_token`, `find_by_reset_token` (verifica expiración), `update_password` (limpia token). Handlers: POST `/api/auth/forgot-password` y POST `/api/auth/reset-password`, registrados en OpenAPI. Frontend: `ForgotPassword.tsx` (formulario email), `ResetPassword.tsx` (form nueva contraseña con token de URL), `useResetPasswordForm.ts` (hook para consolidar useState). Login actualizado con enlace "¿Olvidaste tu contraseña?". Rutas públicas `/forgot-password` y `/reset-password` en App.tsx. Orval regenerado con hooks `useForgotPassword` y `useResetPassword`.
- **Archivos:** `migrations/20260326300000_password_reset.up.sql`, `migrations/20260326300000_password_reset.down.sql`, `Cargo.toml`, `Cargo.lock`, `src/config/mod.rs`, `src/models/user.rs`, `src/models/mod.rs`, `src/repositories/user.rs`, `src/services/email.rs` (nuevo), `src/services/auth.rs`, `src/services/mod.rs`, `src/lib.rs`, `src/handlers/auth.rs`, `src/handlers/mod.rs`, `frontend/src/componentes/ForgotPassword.tsx` (nuevo), `frontend/src/componentes/ResetPassword.tsx` (nuevo), `frontend/src/hooks/useResetPasswordForm.ts` (nuevo), `frontend/src/componentes/Login.tsx`, `frontend/src/App.tsx`, `frontend/src/api/generated/auth/auth.ts`, `frontend/src/api/generated/gestiónRestauranteAPI.schemas.ts`
- **Gotchas:** lettre necesita `default-features = false` para evitar conflicto native-tls vs rustls. Clippy `format_collect` warning → usar `write!` loop para hex encoding. SMTP es opcional — si no está configurado, el enlace de reset se loguea a console (útil para dev local).
- **Sentinel:** Code Sentinel detectó 4 useState en ResetPassword → refactorizado a hook `useResetPasswordForm` con estado consolidado. No requiere regla nueva.

## 263A-16 — Plano mesas ocupadas en vista día (Fase 3b)

- **Que:** Integración del plano de sala en la vista de reservas por día. Backend: nuevo endpoint `GET /api/plano-sala/ocupacion?fecha=&turno=` que devuelve zonas con mesas y sus reservas asociadas (por `mesa_id`). Repo: query `reservas_por_mesa` con JOIN a reservas filtradas por fecha/turno/no canceladas. Servicio: `plano_ocupacion` que indexa reservas por mesa_id con HashMap para O(1) lookup. Modelos: `PlanoOcupacion`, `ZonaOcupacion`, `MesaOcupacion`, `ReservaMesa`, `PlanoOcupacionQuery`. Frontend: componente `PlanoOcupacion.tsx` (solo lectura) que muestra mesas coloreadas por estado (libre/ocupada/no_show/inactiva) con tooltip al hover mostrando reservas. Integrado debajo de la tabla en `ListaReservas.tsx`. CSS `PlanoOcupacion.css` responsive. Orval regenerado con hook `useObtenerOcupacion`.
- **Archivos:** `src/models/plano_sala.rs`, `src/models/mod.rs`, `src/repositories/plano_sala.rs`, `src/services/plano_sala.rs`, `src/handlers/plano_sala.rs`, `src/handlers/mod.rs`, `frontend/src/componentes/PlanoOcupacion.tsx` (nuevo), `frontend/src/estilos/PlanoOcupacion.css` (nuevo), `frontend/src/componentes/ListaReservas.tsx`, `frontend/src/api/generated/plano-sala/plano-sala.ts`, `frontend/src/api/generated/gestiónRestauranteAPI.schemas.ts`
- **Gotchas:** Clippy: `map_unwrap_or` → usar `map_or`, `doc_markdown` → backticks en `mesa_id` del doc comment. `openapi-debug.json` está en `.gitignore` (es generado).
- **Sentinel:** No requiere regla nueva. No aplica.
