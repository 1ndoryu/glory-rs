## 044A-38 Fase 10 — Dashboard admin (revenue, métricas, empleados, alertas)

- **Que:** Implementación completa de Fase 10 del marketplace:
  - **Backend models (dashboard.rs)**: RevenueStats (total_revenue, monthly_revenue, held_amount, refunded_amount f64), OrderCounts (total, active, completed, cancelled, awaiting_assignment i64), EmployeePerformance (employee_id, email, active_orders, completed_orders, average_rating Option<f64>), DashboardAlerts (unassigned_orders, pending_refunds, overdue_orders, unread_admin_notifications i64), DashboardResponse (agrupa todo)
  - **Backend repository (dashboard.rs)**: DashboardRepository con query_as! macros: get_revenue (SUM CASE en order_payments con CAST AS DOUBLE PRECISION/100.0), get_order_counts (COUNT FILTER por status), get_employee_performance (JOIN users+employee_profiles+subqueries), get_alerts (subqueries: unassigned, pending refunds, overdue via auto_assign_deadline)
  - **Backend handlers (dashboard.rs)**: GET /api/admin/dashboard (admin only via require_role), tokio::join! para 4 queries paralelas + conteo notificaciones no leídas, consolida en DashboardResponse
  - **Frontend api/dashboard.ts**: Types + apiGetDashboard
  - **Frontend hooks/useDashboard.ts**: React Query con 30s refetch, 15s staleTime
  - **Frontend SeccionDashboard.tsx**: Grid con sección alertas (colores por tipo), tarjetas de revenue ($-formatted), tarjetas de órdenes, tabla de empleados (email, activas, completadas, rating con estrellas)
  - **Frontend SeccionDashboard.css**: Card grid, tabla, colores por valor, responsive ≤600px
  - **Frontend PanelIsland.tsx**: Añadido case 'dashboard' → SeccionDashboard (tab ya existía)
- **Archivos:** 15 archivos (4 .sqlx cache + 3 backend Rust nuevos + 3 mod files + 4 frontend nuevos + 1 frontend modificado)
- **Gotchas:** Column es `auto_assign_deadline` (NO `assignment_deadline`). NUMERIC(3,2) sin rust_decimal → CAST AS DOUBLE PRECISION. Campos Option<> en Row helpers para coalescer NULLs. Tab 'dashboard' ya pre-configurado en panel.ts y SidebarPanel
- **Sentinel:** Solo falsos positivos persistentes (Boton/GloryLink/MenuContextual)

## 044A-38 Fase 9 — Notificaciones (BD + REST + WebSocket real-time + campana frontend)

- **Que:** Implementación completa de Fase 9 del marketplace:
  - **Backend models (notification.rs)**: Notification (FromRow, Serialize), NotificationResponse (From<Notification> UUIDs→String, DateTimes→rfc3339), WsNotification enum (Notification push + UnreadCount), CreateNotification (struct interna), MarkReadBody (Validate), UnreadCountResponse, 16 constantes de tipos de notificación (new_order, payment_received, etc.)
  - **Backend repository (notification.rs)**: NotificationRepository con query_as!/query_scalar!/query! macros: create (INSERT RETURNING), list_for_user (paginado DESC), count_unread (COUNT WHERE read=false), mark_read (UPDATE WHERE id=ANY), mark_all_read (UPDATE WHERE user_id)
  - **Backend service (notification.rs)**: NotificationHub con DashMap<user_id, broadcast::Sender<WsNotification>>: notify (persist BD + broadcast WS + send unread count), notify_many (batch a múltiples usuarios con CreateNotification base), subscribe, send_unread_count, cleanup_empty_channels
  - **Backend handlers (notifications.rs)**: GET list_notifications (paginado limit/offset), GET get_unread_count, PATCH mark_read (lista de IDs), PATCH mark_all_read. WS: GET /ws/notifications?token=JWT → handle_notification_ws (envía conteo inicial + forward broadcast→WS, auto-reconnect en frontend)
  - **AppState**: Añadido notification_hub: NotificationHub
  - **Frontend api/notifications.ts**: Types + REST functions (list, unreadCount, markRead, markAllRead), WS URL builder, NOTIF_TYPES map con labels e iconos
  - **Frontend hooks/useNotifications.ts**: useNotifications (React Query: lista 60s refetch, conteo 30s refetch, marcarLeidas/marcarTodasLeidas mutations), useNotificationWs (WebSocket auto-connect + auto-reconnect 5s, invalidación de queries on message)
  - **Frontend NotificationBell.tsx**: Campana con badge de no leídas, dropdown con lista (unread highlights con punto azul), marcar individual/todas como leídas, tiempo relativo (ahora/hace Xm/Xh/Xd), click outside to close
  - **Frontend NotificationBell.css**: Badge rojo animado, dropdown con sombra, items con hover, responsive ≤480px
  - **Frontend HeaderPanel.tsx**: Integrada campana de notificaciones antes de "Chat" link
- **Archivos:** 19 archivos (5 .sqlx cache + 4 backend Rust nuevos + 4 mod files modificados + lib.rs + 3 frontend TS nuevos + 2 frontend TSX/CSS nuevos + 1 TSX modificado)
- **Gotchas:** axios-instance (no axiosInstance) como import. Clippy: doc_markdown backticks en `user_id`, too_many_arguments en notify_many → refactorizado a recibir CreateNotification base. useless_conversion: json.into() innecesario en Message::Text. Constantes de tipos con #[allow(dead_code)] hasta que se integren en otros handlers
- **Sentinel:** Falsos positivos `<Boton>`/`<GloryLink>`/`<MenuContextual>` persisten — componentes UI atómicos no existen aún

## 044A-38 Fase 8 — Reviews (rating estrellas + respuesta empleado + frontend)

- **Que:** Implementación completa de Fase 8 del marketplace:
  - **Backend models (review.rs)**: OrderReview (FromRow, Serialize), CreateReviewBody (Validate rating 1-5), RespondReviewBody (Validate length 1-2000), ReviewResponse con From<OrderReview> (UUIDs→String, DateTimes→rfc3339)
  - **Backend repository (review.rs)**: ReviewRepository con query_as! macros: create (INSERT + UNIQUE constraint handling), find_by_order, respond (UPDATE + employee_responded_at=NOW()), list_by_employee, update_employee_average (CAST AVG(rating) AS DOUBLE PRECISION), list_all
  - **Backend handlers (reviews.rs)**: POST create_review (valida dueño, order completed, busca empleado asignado, crea review, actualiza average), POST respond_review (valida empleado asignado, no respuesta existente), GET get_order_review (dueño/asignado/admin), GET list_reviews (admin=all, others=own). OpenAPI utoipa annotations
  - **SQLx offline cache**: Generados 7 archivos .sqlx/ con cargo sqlx prepare
  - **Frontend api/reviews.ts**: Types (ReviewResponse, CreateReviewBody, RespondReviewBody), REST functions (create, respond, getOrderReview, list)
  - **Frontend hooks**: useReviews (React Query list + crearReview/responderReview mutations), useOrderReview, useReviewForm (rating/comment state + enviarReview extraído para Sentinel compliance)
  - **Frontend ReviewPanel.tsx**: Muestra review existente (estrellas + comentario + respuesta empleado) O formulario de creación para clientes (star input interactivo + textarea + submit). Empleado puede responder reviews existentes
  - **Frontend ReviewPanel.css**: Estilos con animación de estrellas (filled/empty, hover scale), layout responsive
  - **Frontend OrdenDetalle.tsx**: Integra ReviewPanel cuando order.status === 'completed'
- **Archivos:** 19 archivos (7 .sqlx cache + 3 backend Rust nuevos + 3 mod.rs modificados + 3 frontend TS nuevos + 2 frontend TSX/CSS nuevos + 1 TSX modificado)
- **Gotchas:** NUMERIC(3,2) sin rust_decimal → CAST(AVG(rating) AS DOUBLE PRECISION) + Option<f64>. query_as! macros requieren cargo sqlx prepare con DATABASE_URL para generar cache offline. UNIQUE constraint on order_reviews.order_id manejado con string match en error
- **Sentinel:** Falsos positivos `<Boton>`/`<Textarea>` persisten — componentes UI atómicos no existen aún. useReviewForm extraído para cumplir regla 3 useState max

## 044A-38 Fase 7 — Reembolsos (Stripe refund + admin review + frontend)

- **Que:** Implementación completa de Fase 7 del marketplace:
  - **Backend models (refund.rs)**: RefundStatus enum (sqlx Type, rename_all snake_case): Requested/UnderReview/Approved/Completed/Rejected. OrderRefund (FromRow), RequestRefundBody, ReviewRefundBody con ReviewAction (Approve/Reject), RefundResponse con From<OrderRefund>
  - **Backend repository (refund.rs)**: RefundRepository con create, find_by_id, find_active_for_order (NOT IN rejected/completed), list_pending (requested/under_review/approved), list_for_user, find_for_order, approve, reject, mark_completed (stripe_refund_id), set_under_review
  - **Backend service (payment.rs)**: refund_payment método: Held→cancela PaymentIntent via Stripe API, Released→crea Refund via Stripe API. StripeRefundMin struct para parseo respuesta. Stripe key via Option unwrap con error descriptivo
  - **Backend handlers (refunds.rs)**: POST request_refund (valida dueño, no activo existente, busca pago held/released), PATCH review_refund (admin only via require_role, approve→Stripe refund+mark completed+cancel order, reject), GET list_refunds (admin=pending, others=own), GET get_order_refund (dueño o admin). OpenAPI utoipa anotaciones en todos
  - **Frontend api/refunds.ts**: Types (RefundStatus, RefundResponse, RequestRefundBody, ReviewRefundBody), REST functions (request, review, list, get_order_refund), labels/classes maps
  - **Frontend hooks/useRefunds.ts**: useRefunds (React Query list + solicitarReembolso + revisarReembolso mutations con invalidaciones cruzadas), useOrderRefund (query por orden)
  - **Frontend hooks/useRefundModal.ts**: Hook extraído para modal de solicitud reembolso (3 useState, abrirModal, cerrarModal, enviarSolicitud con useCallback)
  - **Frontend SeccionReembolsos.tsx**: Panel admin de reembolsos. Lista cards con estado/monto/razón/fecha. Formulario inline de revisión (textarea respuesta + botones aprobar/rechazar/cancelar). Loading/error states
  - **Frontend SeccionPagos.tsx**: Botón "Solicitar reembolso" en pagos held/released (solo client). Modal con textarea razón + enviar/cancelar
  - **Frontend panel.ts**: SeccionPanel type + TABS_ADMIN incluyen 'reembolsos'
  - **Frontend SidebarPanel.tsx**: Icono RotateCcw para tab reembolsos
  - **Frontend PanelIsland.tsx**: Case 'reembolsos' → SeccionReembolsos
- **Archivos:** 18 archivos (8 nuevos: 3 backend Rust, 4 frontend TS/TSX/CSS, 1 hook; 10 modificados: mod.rs x3, payment.rs, SeccionPagos.tsx/css, SidebarPanel.tsx, PanelIsland.tsx, panel.ts, roadmap.md)
- **Gotchas:** Claims no existe → AuthUser de crate::middleware. role es UserRole enum, no string → auth.role != UserRole::Admin. stripe_secret_key es Option<String> → match con as_ref(). Clippy doc-markdown: backticks requeridos para PaymentIntent/Refund en doc comments. SeccionPagos excedía 3 useState → extraer useRefundModal hook
- **Sentinel:** Falsos positivos `<Boton>`/`<Textarea>`/`<Modal>` persisten — componentes UI atómicos no existen aún en el proyecto

## 044A-38 Fase 6 — Entregables y revisiones (upload multipart + descarga + frontend)

- **Que:** Implementación completa de Fase 6 del marketplace:
  - **Backend models**: PhaseDeliverable (FromRow, Serialize, ToSchema), DeliverPhaseRequest/Response, PhaseDeliverablesResponse, ALLOWED_MIME_TYPES (13 types), MAX_FILE_SIZE (10MB), MAX_FILES_PER_DELIVERY (5)
  - **Backend repository**: DeliverableRepository con CreateDeliverableParams struct, create (insert + return), list_by_phase (ORDER BY revision DESC, created_at DESC), list_by_revision, find_by_id, current_revision_number (MAX aggregate)
  - **Backend handlers**: POST deliver_phase_with_files (multipart: valida acceso, status fase, calcula revisión, crea dir uploads, procesa archivos, guarda en BD, marca fase entregada, actualiza order status → UnderReview), GET list_deliverables (por fase con approval_status + revision counts), GET download_deliverable (lee archivo de disco, Content-Disposition attachment)
  - **Helpers**: process_multipart_files (extrae notes + files de multipart, valida MIME/size/extension, guarda a disco), verify_order_access (Admin=all, Client=own, Employee=assigned), sanitize_filename (remueve /, \, .., control chars), has_suspicious_extension (detecta doble extensión .exe.pdf)
  - **File storage**: Local en `./uploads/deliverables/{order_id}/{phase_number}/`, formato `{phase_id}-{revision}-{filename}`
  - **Frontend api/deliverables.ts**: Types, REST (apiDeliverPhase con FormData multipart, apiListDeliverables, apiDownloadDeliverable con blob→createObjectURL), constants (PHASE_STATUS_LABELS/COLORS), formatFileSize helper
  - **Frontend hooks**: useDeliverables (React Query: deliverables query, entregarFase mutation multipart, descargar mutation, invalida order+deliverables), useEntregablesPanel (estado de upload, validación archivos, agrupación por revisión)
  - **Frontend EntregablesPanel.tsx**: Componente extraído (SRP). Upload zone (file input + file list con remove + textarea notas + botón Entregar), historial agrupado por revisión con download buttons per file
  - **Frontend OrdenDetalle.tsx**: FaseCard integra EntregablesPanel para fases con entregables. Removido onEntregar prop (delivery ahora maneja archivos directo). showDeliverables flag para fases con historial
  - **Cleanup**: Removida apiDeliverPhase (PUT) de orders.ts, entregarFase de useOrdenes.ts, handleEntregar de SeccionProyectos.tsx — reemplazado por flow multipart en useDeliverables
- **Archivos:** 18 archivos (8 nuevos: 3 backend Rust, 4 frontend TS/CSS, 1 hook; 10 modificados: Cargo.toml/lock, mod.rs x2, orders.rs, orders.ts, useOrdenes.ts, OrdenDetalle.tsx, SeccionProyectos.tsx/css)
- **Gotchas:** axum multipart requiere feature flag explícito. String literal '..' no es char válido → usar .replace("..", ""). Clippy: too_many_arguments → CreateDeliverableParams struct, too_many_lines → extract process_multipart_files, no_effect_underscore_binding, format_push_string, doc_markdown backticks. OrdenDetalle excedía 300 líneas → extraer EntregablesPanel. CSS excedía 600 líneas → extraer EntregablesPanel.css
- **Sentinel:** No aplica (backend multipart + REST, frontend React). Falsos positivos `<Boton>`/`<Input>`/`<Textarea>` persisten (componentes UI atómicos no existen aún).

## 044A-38 Fase 5 — Chat integrado con órdenes (WebSocket + IA + REST + frontend)

- **Que:** Implementación completa de Fase 5 del marketplace:
  - **Migration**: chat_sessions (visitor_id, user_id FK, order_id FK, status, assigned_staff_id, ai_enabled), chat_messages (session_id FK, sender_type, sender_id, content), chat_typing (UPSERT), 4 indexes, ALTER orders ADD chat_session_id
  - **Backend models**: ChatSession, ChatMessage, WsClientMessage (Message/Typing/Join/Close/ToggleAi), WsServerMessage (Message/Typing/Status/SessionNew/SessionClosed/Error), request/response types con utoipa
  - **Backend repository**: ChatRepository con create_session, find_by_id/order/visitor, list_active, assign_staff, toggle_ai, close_session, save_message (+ update session.updated_at), list_messages (paged ASC), last_messages_for_sessions (DISTINCT ON), update_typing (UPSERT)
  - **Backend ChatHub service**: DashMap<Uuid, broadcast::Sender> para canales por sesión (capacity=64). get_or_create_visitor_session, get_or_create_order_session, send_message (persist+broadcast), send_typing, staff_join, toggle_ai, close_session, enrich_sessions (last_message preview)
  - **Backend AiChatService**: AiChatConfig from_env (AI_API_KEY/GEMINI_API_KEY/OPENAI_API_KEY, model default gemini-2.0-flash). generate_response con system prompt (catálogo servicios + contexto orden). Fallback message si no hay API key
  - **Backend handlers WS**: ws_visitor (GET /ws/chat/visitor?visitor_id&visitor_name, sin auth), ws_staff (GET /ws/chat/staff?token, JWT en query param). Visitor: split socket con futures, spawn send task, triggers AI cuando ai_enabled y sin staff. Staff: mpsc channel pattern para multiplexar suscripciones → single WS writer
  - **Backend handlers REST**: list_sessions (GET, admin/employee=all, client=own), get_messages (GET paged), create_session (POST), send_message (POST + trigger AI)
  - **Frontend api/chat.ts**: Types, REST functions (axios), WS URL builders, UI constants (SENDER_LABELS/COLORS, SESSION_STATUS_LABELS)
  - **Frontend hooks/useChat.ts**: useChat (React Query: sessions 15s refetch, messages 5s, crearSesion/enviarMensaje mutations), useChatWs (WebSocket: connect/disconnect, session/message state, joinSession/toggleAi/sendWsMessage)
  - **Frontend SeccionChat.tsx**: Two-column layout (session list + message area), SessionItem con status badges + preview, MessageBubble con sender colors + timestamp, input con Enter to send, auto-scroll, responsive mobile (toggle list/chat)
  - **Panel integration**: SeccionPanel += 'mensajes', tabs en client/employee/admin, PanelIsland += SeccionChat, SidebarPanel += MessageSquare icon
- **Archivos:** 21 archivos (11 nuevos: migration up/down, 5 backend Rust, 4 frontend TS/CSS; 10 modificados: Cargo.toml/lock, mod.rs x4, lib.rs, panel.ts, PanelIsland.tsx, SidebarPanel.tsx)
- **Gotchas:** SplitSink (futures) no implementa Clone → mpsc channel pattern para staff WS (cada sub-task envía a mpsc tx, single send_task lee rx→WS). axiosInstance es default export no named. crearSesion mutation requiere `undefined` explícito. Clippy: useless_conversion (.into() en String→String), manual_let_else, doc_markdown backticks, must_use en 5 métodos, format_push_string→write!/writeln!
- **Sentinel:** No aplica (backend WS + REST, frontend React). Falsos positivos `<Boton>` persisten.

## 044A-38 Fase 4 — Asignación y delegación (backend + frontend + auto-assign 24h)

- **Que:** Implementación completa de Fase 4 del marketplace:
  - **Backend models**: `DelegationStatus` enum (sqlx::Type), `Delegation` struct (FromRow), `EmployeeProfile` struct con average_rating como Option<f64> (CAST desde NUMERIC), request/response types con validator + utoipa
  - **Backend repository**: `DelegationRepository` con CRUD delegaciones (create, find, list incoming/outgoing/all, accept/reject/complete), perfiles de empleado (find, ensure con UPSERT ON CONFLICT, count_active_orders), `list_employees_with_stats` (JOIN users + LEFT JOIN employee_profiles + subquery current_orders), `find_available_employees` (specialty matching con `@> ARRAY[]::TEXT[]`, availability + capacity check)
  - **Backend service**: `AssignmentService` con take_order (valida status, capacity, availability), list_unassigned, list_employees, create_delegation (valida assigned employee), respond_to_delegation (accept→reasigna si delegate, reject), list_delegations (admin=all, employee=incoming+outgoing dedup), `auto_assign_loop` (tokio::spawn, 60s interval), `check_and_auto_assign` (overdue orders + best employee match)
  - **Backend handlers**: 7 endpoints con OpenAPI annotations: POST /orders/:id/take, GET /orders/unassigned, GET /admin/employees, POST /orders/:id/delegate, POST /orders/:id/help, PATCH /delegations/:id, GET /delegations
  - **Routing**: `assignment::routes()` merged BEFORE `orders::routes()` para que `/orders/unassigned` (literal) tome prioridad sobre `/orders/:order_id` (param) en matchit
  - **Background task**: `tokio::spawn(AssignmentService::auto_assign_loop(pool.clone()))` en main.rs
  - **payment.rs**: 3 llamadas a `update_order_status(AwaitingAssignment)` reemplazadas por `set_awaiting_assignment` (status + NOW() + 24h deadline)
  - **order.rs**: 3 métodos nuevos: `set_awaiting_assignment`, `list_overdue_unassigned`, `find_service_by_id`
  - **Frontend API**: `assignment.ts` con tipos, 7 funciones API, constantes de labels/clases por status
  - **Frontend hooks**: `useAsignaciones.ts` con 3 hooks: `useDisponibles` (unassigned + take mutation), `useDelegaciones` (list + create/help/respond mutations), `useEmpleados` (admin list)
  - **Frontend SeccionDisponibles**: Cards de órdenes sin asignar con botón "Tomar orden" (UserPlus icon), loading/empty states, responsive
  - **Frontend SeccionDelegaciones**: Lista delegaciones con badges status, tipo (delegate/help), botones aceptar/rechazar, responsive
  - **Panel integration**: `SeccionPanel` type += `'delegaciones'`, `TABS_EMPLOYEE` += tab "Delegaciones", `PanelIsland` += cases para disponibles/delegaciones, `SidebarPanel` += icono ArrowRightLeft
- **Archivos:** 20 archivos (10 nuevos backend+frontend, 10 modificados: mod.rs x4, main.rs, order.rs, payment.rs, panel.ts, PanelIsland.tsx, SidebarPanel.tsx)
- **Gotchas:** NUMERIC(3,2) sin rust_decimal feature → CAST AS DOUBLE PRECISION + Option<f64>. Clippy requirió backticks en doc comments (`in_progress`, `under_review`, `employee_profiles`) y if/else en vez de single_match_else. DelegationStatus import no usado en repo.
- **Sentinel:** No aplica (backend REST + frontend React). Falsos positivos Code Sentinel `<Boton>` persisten.

## 044A-38 Fase 3 — Pagos Stripe (escrow backend + Stripe Elements frontend)

- **Que:** Implementación completa de la Fase 3 del marketplace:
  - **Backend Stripe REST API (reqwest)**: PaymentService con `initiate_payment` (crea PaymentIntent con capture_method=manual para escrow), `handle_webhook` (verifica firma HMAC-SHA256, procesa payment_intent.succeeded), `capture_held_payments` (captura fondos retenidos cuando orden se completa), `list_payments`
  - **3 modos de pago**: Full (20% desc, pago único), HalfHalf (10% desc, mitad al inicio + mitad al entregar mitad de fases), Phased (0% desc, pago por fase individual)
  - **State machine de pagos**: handle_payment_success avanza fases automáticamente según modo (Full→todas Paid, HalfHalf→primera/segunda mitad, Phased→fase individual)
  - **Webhook handler**: POST /api/webhooks/stripe sin auth, raw Bytes body, verificación de firma con hmac::Hmac<sha2::Sha256> y mac.verify_slice() (constant-time)
  - **Frontend Stripe Elements**: CheckoutModal con loadStripe, PaymentElement, confirmPayment. Flujo: botón Pagar → apiInitiatePayment → client_secret → Elements → confirmPayment
  - **SeccionPagos**: Historial de pagos por orden con selector de órdenes, status badges, integrado en PanelIsland
  - **OrdenDetalle**: Botón "Pagar" para pending_payment (non-phased), "Pagar fase" en FaseCard para phased mode
  - **Auto-capture en approve_phase**: Cuando orden se completa (todas fases aprobadas), captura automática de pagos held
  - **Config opcional**: STRIPE_SECRET_KEY y STRIPE_WEBHOOK_SECRET opcionales (server arranca sin Stripe, endpoints devuelven error)
- **Archivos:** 25 archivos (10 nuevos: payment model/repo/service/handler + payments API/hook/CheckoutModal/SeccionPagos con CSS, 15 modificados: Cargo deps, config, lib, handlers/mod, orders handler, models/repos/services mod, OrdenDetalle, SeccionProyectos, PanelIsland, package.json)
- **Gotchas:** Clippy requirió backticks en doc comments para `PaymentIntent`, `capture_method`, `phase_id`, `payment_mode`. Manual div_ceil reemplazado por `.div_ceil(2)`. Se eligió reqwest directo sobre async-stripe crate para minimizar dependencias. No testeado con Stripe real (requiere API keys).
- **Sentinel:** No aplica (backend REST + frontend React). Falsos positivos Code Sentinel `<Boton>` persisten.

## 044A-38 Fase 2 — CRUD de órdenes backend + frontend panel

- **Que:** Implementación completa de la Fase 2 del marketplace:
  - **Backend state machine**: 4 nuevos endpoints (cancel_order, deliver_phase, approve_phase, request_revision) con validación de permisos y máquina de estados
  - `services/order.rs`: cancel (PendingPayment|PaymentHeld|AwaitingAssignment → Cancelled), deliver (InProgress|Paid|RevisionRequested → Delivered), approve (Delivered → Approved + auto-unlock siguiente fase + auto-complete orden), revision (Delivered → RevisionRequested con max revisions)
  - `repositories/order.rs`: 7 métodos nuevos (find_phase_by_number, deliver_phase, approve_phase, request_revision, cancel_order, update_current_phase, complete_order)
  - `handlers/orders.rs`: 4 handlers con utoipa OpenAPI annotations y role-based access control
  - **Frontend API**: `api/orders.ts` con tipos TypeScript (OrderStatus, PhaseStatus, PaymentMode), interfaces (OrderResponse, OrderPhaseResponse), 7 funciones API, helpers de labels/colores/precio
  - **Frontend hook**: `useOrdenes.ts` con React Query (queries + 5 mutations con cache invalidation)
  - **Frontend componentes**: `SeccionProyectos.tsx` (lista de órdenes con cards, progress bar, status badges) + `OrdenDetalle.tsx` (detalle con timeline de fases, action buttons role-aware)
  - **CSS**: 607 líneas con status badges por CSS class, timeline, phase cards, responsive mobile
  - `PanelIsland.tsx`: integrado SeccionProyectos para tabs proyectos/asignados/todos-ordenes
  - Fix guardián: `SolucionesIsland.css` variable `--accent-primary` → `--bg-accent`
- **Archivos:** 11 archivos (4 backend modificados, 5 frontend nuevos, 2 frontend modificados)
- **Gotchas:** Code Sentinel advirtió >300 líneas → se dividió en SeccionProyectos + OrdenDetalle. Emojis Unicode reemplazados por lucide-react icons. Inline styles de badges reemplazados por CSS classes (STATUS_CLASS map). Progress bar width inline es necesario (data-driven).
- **Sentinel:** No se necesita regla nueva. Falsos positivos: progress bar inline style (data-driven width), native button en cards (styled cards necesitan button nativo).

## 044A-38 Fase 1 — Frontend roles y panel dinámico

- **Que:** Implementación del frontend role-aware:
  - `authStore.ts`: Añadido `role`/`effectiveRole` en AuthUser, acción `actualizarRol()` para switch-role, backward compat con localStorage antiguo
  - `api/auth.ts`: Actualizado `AuthResponse` con `role`/`effective_role`, nueva función `apiSwitchRole()`
  - `data/panel.ts`: Tabs dinámicos por rol (TABS_CLIENT, TABS_EMPLOYEE, TABS_ADMIN), funciones `obtenerTabsPorRol()` y `seccionInicialPorRol()`
  - `SidebarPanel.tsx`: Tabs dinámicos según effectiveRole, botón switch-role (solo admin) con ciclo admin→employee→client, iconos lucide nuevos por sección
  - `PanelIsland.tsx`: Sección inicial según rol, redirect a / si no logueado, reset de sección al cambiar rol
  - `HeaderPanel.tsx`: Logout real conectado a authStore + redirect, botón button en vez de anchor
  - `App.tsx`: Redirect / → /panel si logueado vía componente HomeOrPanel
  - `ServicioIndividualIsland.tsx`: CTA apunta a /panel si logueado (prep para Fase 2 order creation)
  - `useAutenticacion.ts`: Pasa role/effective_role del backend al store en login y registro
  - `SidebarPanel.css`: Estilos para botón switch-role (dashed border, hover, disabled)
- **Archivos:** 10 archivos frontend modificados
- **Gotchas:** Campo se llama `role` (no `target_role`) en `SwitchRoleRequest`. Advertencias de Code Sentinel sobre GloryLink/Boton son aspiracionales (componentes no existen aún).
- **Sentinel:** Falsos positivos GloryLink/Boton — ignorados porque no existen como componentes. No se necesita regla nueva.

## 044A-38 Fase 0 — Modelo de datos y migración marketplace

- **Que:** Implementación completa del backend del marketplace (Fase 0):
  - Migración SQL `20260404000000_marketplace` con 7 enums PostgreSQL + ALTER TABLE users + 13 tablas nuevas + seed de 7 servicios, 21 planes y fases template
  - Migración rollback completa
  - `UserRole` enum (Admin/Employee/Client) con `effective_role()` para admin switch
  - Modelos de orden: `OrderStatus` (8 estados), `PaymentMode` (3 modos), `PhaseStatus` (8 estados)
  - `AuthResponse` extendida con `role` y `effective_role`
  - JWT Claims con role + effective_role
  - `AuthUser` middleware con `require_role()` guard
  - `AppError::Forbidden` variante añadida
  - Repository de órdenes: CRUD servicios/planes/órdenes/fases con param structs
  - Service de órdenes: lógica de descuentos (full=20%, half=10%, phased=0%), generación automática de fases
  - Handlers: GET /api/services, GET /api/services/:slug (público), POST/GET /api/orders, GET /api/orders/:id, PUT /api/orders/:id/assign/:eid, POST /api/auth/switch-role
  - OpenAPI/Swagger actualizado con todos los schemas nuevos
  - Testeado: registro devuelve role:client, creación de orden con descuento correcto, detalle con 3 fases, check de acceso por client_id
- **Archivos:** 16 archivos (2 migraciones, 5 nuevos en src/, 9 modificados)
- **Gotchas:** Axum 0.7 usa `:param` para rutas (no `{param}` como 0.8). Dos binarios en el proyecto requieren `--bin glory-backend`. PowerShell trata stderr de cargo como error (exit code 1 falso).
- **Sentinel:** No aplica (solo backend, reglas CSS/React no relevantes)

## 044A-30 — Borrar servicio chatbot (guillermo-stack)

- **Que:** Eliminado servicio `guillermo-stack` (guillechatbots.es) de Coolify vía API DELETE. Eliminación encolada correctamente.
- **Archivos:** Ninguno (operación en infraestructura)
- **Sentinel:** No aplica

## 044A-31 — Eliminar servicios sin imagen y usar assets reales

- **Que:** Reducido SERVICIOS_DATA de 9 a 5 servicios (solo los que tienen imagen en /public/assets/Servicios/). Eliminados: Chatbots (#6), Diseño UX/UI (#7), Automatización (#8), Consultoría (#9). Imágenes cambiadas de `obtenerImagen(N)` placeholder a paths reales. Eliminados planes asociados: PLANES_CHATBOTS, PLANES_UXUI, PLANES_AUTOMATIZACION, PLANES_CONSULTORIA. Actualizado índice de planes y aliases.
- **Archivos:** `data/servicios.ts`, `data/planes/planesIA.ts`, `data/planes/planesExtras.ts`, `data/planes/index.ts`
- **Gotchas:** PLANES_SEO y PLANES_MARKETING se mantienen aunque no tienen servicio asociado (son planes de crecimiento genéricos).
- **Sentinel:** No aplica

## 044A-32 — Centralizar precios en un solo archivo

- **Que:** Consolidado todos los planes de precios (7 servicios: Web, Apps, IA, Branding, E-commerce, SEO, Marketing) en un único `planes.ts`. Eliminados los 4 archivos individuales (planesCreacion, planesIA, planesCrecimiento, planesExtras). Actualizado `index.ts` para importar del archivo único.
- **Archivos:** `data/planes/planes.ts` (nuevo), `data/planes/index.ts` (actualizado), eliminados: `planesCreacion.ts`, `planesIA.ts`, `planesCrecimiento.ts`, `planesExtras.ts`
- **Gotchas:** Ninguno. Ningún archivo importaba directamente de los archivos individuales; todo pasaba por `index.ts`.
- **Sentinel:** No aplica

## 044A-33 — Arreglar blog Kamples + traducir a español + imagen

- **Que:** Corregida navegación SPA en tarjetas de blog (tanto BlogIsland como SeccionBlog en home). Las tarjetas usaban `<a href>` que causaba recarga completa en vez de navegación SPA. Ahora usan `navegar()` con `preventDefault`. Artículo de Kamples traducido completamente a español. Imagen de Kamples portada ya estaba correcta. SeccionBlog ahora muestra imagen real del post en vez del placeholder.
- **Archivos:** `islands/BlogIsland.tsx`, `components/home/SeccionBlog.tsx`, `data/blog.ts`
- **Gotchas:** El problema no era el routing (que estaba bien configurado), sino que `<a href>` sin SPA navigation causa full reload en una SPA. SeccionBlog ni siquiera tenía links en las cards.
- **Sentinel:** No aplica

## 044A-34 — Crear usuario admin local

- **Que:** Creado `examples/seed_admin.rs` que genera hash Argon2 para password "admin" e inserta usuario admin@admin.com en la BD local. Usa ON CONFLICT para ser idempotente. Ejecutado con éxito: usuario creado.
- **Archivos:** `examples/seed_admin.rs` (nuevo)
- **Gotchas:** El DATABASE_URL estaba comentado en .env — se pasó como variable de entorno directamente. Credenciales: email=admin@admin.com, password=admin.
- **Sentinel:** No aplica

## 044A-35 — Rediseño minimalista de blog cards

- **Que:** Rediseñadas las tarjetas de blog tanto en BlogIsland como en SeccionBlog (home). Nuevo diseño: imagen a pantalla completa con overlay degradado (gradient) en la parte inferior que muestra categoría y título en blanco. Eliminados: resumen, fecha, "Leer más", meta separado. Layout cambiado de horizontal (lista) a grid responsive. Aspect ratio 3:4 para cards normales, 16:9 para destacadas.
- **Archivos:** `islands/BlogIsland.tsx`, `islands/BlogIsland.css`, `components/home/SeccionBlog.tsx`, `components/home/SeccionBlog.css`
- **Gotchas:** Se eliminó el import de `useTranslation` del componente TarjetaArticulo ya que el "Leer más" ya no se muestra.
- **Sentinel:** No aplica

## 044A-38 — Plan maestro marketplace tipo Fiverr

- **Que:** Creado plan exhaustivo `Agente/planes/plan-marketplace-2026-04-04.md` que cubre: modelo de datos completo (13 tablas SQL), extensión del sistema de auth con roles (admin/employee/client), CRUD de órdenes con máquina de estados, pagos Stripe con escrow (3 modos: completo 20% desc, 50/50 10% desc, fases), auto-asignación 24h, delegación entre empleados, entregables/archivos, reembolsos, reviews, notificaciones y dashboard admin. Incluye 20 puntos que el usuario probablemente olvida (verificación email, GDPR, 2FA, facturas, disputas, etc.). Actualizado el plan de chat para ser coherente (chat vinculado a órdenes, roles extendidos, contexto de IA). Roadmap actualizado con 11 fases ejecutables priorizadas.
- **Archivos:** `Agente/planes/plan-marketplace-2026-04-04.md` (nuevo), `roadmap.md` (actualizado con fases)
- **Gotchas:** El plan de chat necesita extensiones (order_id en chat_sessions, sender_type expandido). Stripe requiere account + API keys. Plan diseñado para ejecutarse fase a fase.
- **Sentinel:** No aplica

## 044A-29 — Plan de hosting con Coolify Manager RS

- **Que:** Creado plan exhaustivo en `Agente/planes/plan-hosting-coolify-2026-04-04.md` con 5 fases: (1) Infraestructura base (VPS2 standby, backups Google Drive, Contabo DNS, failover), (2) Flujo de onboarding de clientes con templates, (3) Integración con sitio web (planes frontend, endpoint provisioning, modelo de BD), (4) Panel de administración de hosting (dashboard + endpoints de gestión), (5) Automatización completa (provisioning auto, health monitoring, renovación/suspensión Stripe). Incluye inventario de lo que ya funciona en Coolify Manager RS (29 comandos, 26 herramientas MCP) y lo que está bloqueado.
- **Archivos:** `Agente/planes/plan-hosting-coolify-2026-04-04.md` (nuevo)
- **Gotchas:** VPS2 sin Coolify operativo, Contabo DNS API bloqueada, Google Drive requiere OAuth manual. Plan diseñado para ser ejecutable fase a fase.
- **Sentinel:** No aplica

## 044A-36 — Plan de chat en vivo con IA

- **Que:** Creado plan completo en `Agente/planes/plan-live-chat-2026-04-04.md` con 5 fases: (1) Backend WebSocket + modelo de datos, (2) AI Service con OpenAI/Gemini, (3) Widget de chat para clientes anónimos, (4) Panel de staff, (5) Typing preview en tiempo real. Incluye esquema de BD, protocolo WS, arquitectura, estimaciones y consideraciones de seguridad.
- **Archivos:** `Agente/planes/plan-live-chat-2026-04-04.md` (nuevo)
- **Gotchas:** Ninguno. Tarea de planificación solamente.
- **Sentinel:** No aplica

## 044A-37 — Quitar selector de idioma del header

- **Que:** Removido `<LanguageSelector />` del header. El componente se mantiene en el footer. Import eliminado también.
- **Archivos:** `components/layout/Header.tsx`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica

- **Que:** Implementación completa de SEO Fase 1:
  - `react-helmet-async` instalado, `HelmetProvider` envuelve la app
  - Componente `SEOHead` reutilizable: title, description, OG, Twitter Card, canonical, hreflang, JSON-LD, noindex
  - Schemas JSON-LD centralizados: Organization, WebSite, Service, BlogPosting, Breadcrumb
  - `SEOHead` integrado en las 12 islands/páginas (home, servicios, proyectos, nosotros, blog, contacto, soluciones + detalles + panel)
  - Backend Rust: endpoints `/robots.txt` y `/sitemap.xml` en Axum
  - `index.html`: meta description por defecto + link a favicon
  - Favicon SVG creado (N. sobre fondo negro)
  - Página 404 real (`NotFoundIsland`) reemplaza el catch-all silencioso que renderizaba home
  - `<html lang>` dinámico basado en idioma activo de i18n
  - hreflang alternates para es/en/ja
- **Archivos:** 24 archivos (SEOHead.tsx, schemas.ts, main.tsx, App.tsx, index.html, favicon.svg, seo.rs, mod.rs, 12 islands, package.json/lock, NotFoundIsland.tsx/css)
- **Gotchas:** SPA sin SSR sigue siendo la limitación principal — Fase 3 (pre-rendering) pendiente
- **Sentinel:** No aplica

---

## 044A-27 — selectorIdiomaBoton en footer + nombres completos

- **Que:** Agregado LanguageSelector al Footer (sección inferior, junto al copyright). Array LANGUAGES ahora incluye `nombre` con nombre completo del idioma (Español, English, 日本語). Dropdown muestra nombres completos. CSS del footer invierte colores para fondo oscuro y abre dropdown hacia arriba.
- **Archivos:** `LanguageSelector.tsx`, `LanguageSelector.css`, `Footer.tsx`, `Footer.css`
- **Sentinel:** No aplica

---

## 044A-26 — Inconsistencia visual proyectoRelacionadosContenedor

- **Que:** Igualado `.proyectoRelacionadosContenedor` a `.showcaseContenedor`: agregado `display: flex`, `flex-direction: column`, `gap: var(--spacing-xl)`. Se mantiene `margin: 0 auto` que showcase no tiene.
- **Archivos:** `ProyectoIndividualIsland.css`
- **Sentinel:** No aplica

---

## 044A-25 — Aspect ratio natural en galería de imágenes

- **Que:** Eliminado aspecto forzado 1:1 en la galería de servicios/proyectos. Removido `--galeria-item-height`, removido `height` de `.galeriaImagenWrapper`, cambiado `.galeriaImagen` de `height: 100%; object-fit: cover` a `height: auto`. Las imágenes se adaptan a su relación de aspecto natural.
- **Archivos:** `SeccionGaleriaServicio.css`
- **Sentinel:** No aplica

---

## 044A-24 — Quitar gap serviciosContenedor + verificar duplicación CSS

- **Que:** Eliminado `gap: var(--spacing-xl)` de `.serviciosContenedor` en ServiciosIsland.css y SeccionServicios.css. Confirmada duplicación de la clase en ambos archivos (diferencia: SeccionServicios añade padding). No es conflictivo porque la especificidad del contexto padre lo resuelve.
- **Archivos:** `ServiciosIsland.css`, `SeccionServicios.css`
- **Sentinel:** No aplica

---

## 044A-23 — Desactivar autoplay en todos los carruseles

- **Que:** Agregado prop `autoplay?: boolean` (default `false`) a `useCarruselInfinito`. Guardado el useEffect de autoplay con condicional `if (!autoplay) return`. Ningún componente pasa `autoplay: true`, así que todos quedan manuales.
- **Archivos:** `useCarruselInfinito.ts`
- **Sentinel:** No aplica

---

## 044A-22 — Ocultar simpleImageWrapper en seccionServicios (temporal)

- **Que:** `display: none` permanente en `.simpleImageWrapper`. Comentada media query que lo mostraba en tablet. TO-DO dejado para cuando se agreguen imágenes reales.
- **Archivos:** `ServiceCard.css`
- **Sentinel:** No aplica

---

## 044A-21 — Tarjetas background a --bg-accent

- **Que:** Reemplazado `background-color: var(--bg-card-subtle)` por `var(--bg-accent)` en 9 archivos de tarjetas: ServiceCard, ProyectosIsland, ProyectoIndividualIsland, NosotrosIsland, BlogIsland (x2), SeccionTestimonios, SeccionPlanesServicio, SolucionPlaceholderIsland.
- **Archivos:** 9 archivos CSS
- **Sentinel:** No aplica

---

## 044A-20 — Integración login frontend con backend REST

- **Que:** Creado `authStore.ts` (Zustand) con login/logout/inicializar + persistencia localStorage. Creado `api/auth.ts` con apiLogin/apiRegister. Reemplazados handlers mock (alert) en useAutenticacion.ts por llamadas reales a POST /api/auth/login y /register. Modal muestra errores con role="alert". Header usa authStore para estado de sesión y botón logout. main.tsx inicializa sesión desde localStorage al arrancar.
- **Archivos:** `authStore.ts` (nuevo), `api/auth.ts` (nuevo), `useAutenticacion.ts`, `ModalAutenticacion.tsx`, `ModalAutenticacion.css`, `Header.tsx`, `main.tsx`
- **Gotchas:** El backend devuelve `{token, user_id}` — no incluye email, así que el email se guarda desde el formulario.
- **Sentinel:** No aplica

---

## 044A-19 — modalTabs margin-bottom reducido

- **Que:** Reducido margin-bottom de .modalTabs de var(--spacing-xl) a calc(var(--spacing-xl) / 2)
- **Archivos:** `ModalAutenticacion.css`
- **Sentinel:** No aplica

---

## 044A-18 — tarjetaSolucion fondo acento

- **Que:** Cambiado background-color de .tarjetaSolucion de var(--bg-primary) a var(--accent-primary)
- **Archivos:** `SolucionesIsland.css`
- **Sentinel:** No aplica

---

## 044A-17 — Selector idioma como subMenuDesplegable

- **Que:** Rediseñado LanguageSelector.css para replicar estilo visual de subMenuDesplegable: fondo blanco con ::before, bordes redondeados, sombra, animación de entrada, mismos paddings
- **Archivos:** `LanguageSelector.css`
- **Sentinel:** No aplica

---

## 044A-16 — subMenuDesplegable fondo blanco

- **Que:** Cambiado background-color del ::before de subMenuDesplegable de var(--bg-primary) a #ffffff
- **Archivos:** `header.css`
- **Sentinel:** No aplica

---

## 044A-15 — Modal inicio sesión

- **Que:** Eliminado título (h2.modalTitulo), botón cerrar (X), padding cambiado a 40px, fondo a #ffffff
- **Archivos:** `ModalAutenticacion.css`, `ModalAutenticacion.tsx`
- **Gotchas:** El modal aún cierra haciendo click en el overlay
- **Sentinel:** No aplica

---

## 044A-12 — Instrucciones roadmap watcher en test.instructions.md

- **Que:** Agregada sección VI "ROADMAP WATCHER" al protocolo de desarrollo con instrucciones completas para crear scripts/check-roadmap.mjs y .vscode/tasks.json si no existen al iniciar sesión
- **Archivos:** `.github/instructions/test.instructions.md`
- **Sentinel:** No aplica

---

## 044A-11 — 7 fixes UI/CSS

- **Que:** 7 correcciones CSS/UI solicitadas por el usuario
- **Archivos:**
  - `ProyectoIndividualIsland.css` — .proyectoHeroMeta: agregado font-weight: 500 !important
  - `SeccionHero.css` — .heroTitulo span: eliminado text-decoration underline
  - `ServiciosIsland.css` — .serviciosHero .heroTitulo span: eliminado text-decoration underline
  - `SeccionPlanesServicio.css` — .tarjetaPlanDescripcion: font-size 17px; .tarjetaPlanItemTexto: eliminado font-serif en incluido y no-incluido
  - `ServicioIndividualIsland.tsx` — CTA: precio mínimo se obtiene dinámicamente de los planes del servicio en vez de hardcodear $997
  - `useCarruselInfinito.ts` — Prevención de click al arrastrar: draggedRef + onClickCapture en fase de captura
  - `SeccionGaleriaServicio.css` — Galería cuadrada: --galeria-item-height igualado a --galeria-item-width en todos los breakpoints
  - `SeccionSkillsServicio.css` — .skillDescripcion p: eliminado font-family y font-size
- **Gotchas:** preventDefault en div padre no previene click en <a> hijo — se usa onClickCapture (fase captura) para interceptar antes de que el <a> procese el click
- **Sentinel:** No aplica

---

## 044A-10 — UI fixes y traducciones

- **Que:** 8 correcciones UI/CSS + traducciones faltantes en español
- **Archivos:**
  - `LanguageSelector.css` — selectorIdiomaBoton: removido border, color secundario, más minimal
  - `Badge.css` — font-size: 12px (antes var(--text-sm))
  - `SeccionShowcase.css` — proyectoInfo gap: 8px (antes 4px)
  - `CarruselShowcase.css` — overlay gradient 10%/90% (antes 15%/85%); carruselTítulo sin font-weight
  - `SeccionSkillsServicio.css` — skillsContenedor gap: 0 (antes spacing-xl)
  - `ProyectosIsland.css` — tarjetaProyectoImagen aspect-ratio: 1/1 (antes 4/3)
  - `ProyectoIndividualIsland.css` — proyectoRelacionadoImagen aspect-ratio: 1/1 (antes 4/3)
  - `locales/es.json` — traducidos: section_title → "Hablemos", heading → "¿Tienes un proyecto...?", newsletter_title → "Mantente al día", brand_desc, newsletter_placeholder, newsletter_submit, newsletter_sending, btn_contact, btn_hire, description
- **Sentinel:** No aplica

---

## 044A-9 — Deploy Rust + Coolify Manager + fixes locales

- **Que:** Adaptar Coolify Manager para deploy de apps Rust, desplegar nakomi.studio, fix fuentes Vite y DATABASE_URL por defecto.
- **Archivos modificados:**
  - `frontend/vite.config.ts` — Agregado `server.fs.allow: ['..']` para servir fuentes desde App/Assets/fonts/
  - `src/config/mod.rs` — DATABASE_URL ahora tiene valor por defecto (nakomi_db), no requiere estar en .env
  - `coolify-manager-rs/config/templates/Dockerfile.rust` — Reescrito: multi-stage build (frontend node + backend Rust + runtime debian), git clone
  - `coolify-manager-rs/config/templates/rust-stack.yaml` — Labels Traefik explícitos para dominio custom (Coolify ignora SERVICE_FQDN_APP para routing)
  - `coolify-manager-rs/src/infra/template_engine.rs` — Agregado DOMAIN_SLUG (puntos→guiones) y APP_BIN
  - `coolify-manager-rs/src/commands/deploy_service.rs` — Dockerfile upload vía SSH, detección primer deploy, eliminado pipe tail-30
  - `coolify-manager-rs/config/settings.json` — stackUuid actualizado para nuevo servicio
- **Gotchas:**
  - Coolify v4 genera FQDN sslip.io al crear service applications y NUNCA lo cambia vía API/env. Workaround: labels Traefik explícitos en el compose.
  - `| tail -30` en comandos SSH causa timeout por inactividad — Docker progress output mantiene el canal vivo.
  - `utoipa-swagger-ui` requiere curl durante build (descarga assets).
  - Docker socket reset al final del build es transient — reintentar.
  - Primer deploy: Coolify no escribe compose a disco hasta start_service; detectar y llamar API.
- **Sentinel:** No aplica (cambios en tooling/infra, no código de producción)
