## 225A-1 — Refactorizar modelos hosting y endurecer limite-lineas
- **Qué:** `src/models/hosting.rs` se dividió en `src/models/hosting/` por entidades, requests, responses, sanitización y validación. Code Sentinel ahora agrega `limite-lineas-nivel-2`, `limite-lineas-nivel-3` y `limite-lineas-nivel-4` con disables independientes.
- **Archivos:** `src/models/hosting/*`, `.agent/code-sentinel/src/analyzers/static/staticCodeRules.ts`, `.agent/code-sentinel/src/analyzers/staticAnalyzer.ts`, `.agent/code-sentinel/src/config/ruleRegistry.ts`, `.agent/code-sentinel/src/utils/lineCounter.ts`, tests y docs de Sentinel.
- **Gotchas:** La suite de Mocha de Sentinel requiere `--ui tdd` al ejecutar tests aislados con `--no-config`.
- **Sentinel:** Nueva prevención implementada: niveles escalonados de `limite-lineas` para evitar que un disable-file base silencie monolitos.
- **GLORY:** No aplica.

## 225A-1 — Refactorizar controlador hosting
- **Qué:** `src/handlers/hosting.rs` dejó de ser un controlador monolítico y ahora vive como módulo de carpeta en `src/handlers/hosting/`, dividido por checkout, suscripciones, dominios, planes, provisioning, control, stats, deploys, VPS y rutas.
- **Archivos:** `src/handlers/hosting/mod.rs`, `src/handlers/hosting/checkout.rs`, `src/handlers/hosting/control.rs`, `src/handlers/hosting/deployments.rs`, `src/handlers/hosting/domain.rs`, `src/handlers/hosting/plans.rs`, `src/handlers/hosting/provisioning.rs`, `src/handlers/hosting/routes.rs`, `src/handlers/hosting/stats.rs`, `src/handlers/hosting/subscriptions.rs`, `src/handlers/hosting/vps.rs`, `src/handlers/hosting.rs` eliminado.
- **Gotchas:** PowerShell no expande `src/handlers/hosting/*.rs` para `rustfmt`; usar `Get-ChildItem` o `cargo fmt`. El cierre también exigió limpiar lints mecánicos preexistentes para que `cargo clippy -- -D warnings` quedara verde.
- **Sentinel:** No hizo falta nueva regla; el handler ya no usa disable-file para tamaño y queda cubierto por los niveles escalonados.
- **GLORY:** No aplica.

## 225A-2 — Corrección recursos Coolify y Caché SWR
- **Qué:** Se corrigió el mapeo de despliegues (usando el Service UUID de Coolify cruzado contra los nombres de los contenedores), se arregló el cálculo de disco leyendo directamente `/var/lib/docker/volumes/*/_data` y se implementó un caché global Stale-While-Revalidate para el endpoint de despliegues.
- **Archivos:** `src/handlers/hosting/deployments.rs`
- **Gotchas:** `docker stats` puede recibir un timeout o bloquearse si hay shells interactivos. Para el almacenamiento se ejecutaba un `du` sobre las configs de coolify en lugar de los contenedores; arreglado midiendo los volúmenes directamente y mapeando su prefijo.
- **Sentinel:** No aplica.
- **GLORY:** No aplica.

## 225A-3 — Infraestructura multi-VPS en panel hosting
- **Qué:** El inventario de VPS ahora parte de los servidores Coolify configurados y se enriquece con Contabo por IP; despliegues, eliminación y stats recorren todos los targets Coolify en vez de asumir una VPS única. El panel usa tabs `Despliegues`/`VPS`, elimina `infraResumen` y separa filas de despliegues en `DeploymentRow`.
- **Archivos:** `src/services/infrastructure.rs`, `src/handlers/hosting/vps.rs`, `src/handlers/hosting/deployments.rs`, `src/handlers/hosting/stats.rs`, `src/services/docker_stats.rs`, `frontend/src/api/hosting.ts`, `frontend/src/components/panel/*`, `Agente/planes/plan-recursos-hosting-2026-05-22.md`.
- **Gotchas:** Produccion no tenia `COOLIFY_VPS1_*`; se sincronizaron con `coolify-manager-rs sync-env --name studio` y falta redeploy para cargar el entorno nuevo. Contabo puede devolver solo VPS1, asi que no debe ser fuente autoritativa del inventario.
- **Sentinel:** No aplica; se resolvieron diagnosticos usando `MenuContextual` y separando `DeploymentRow`.
- **GLORY:** No aplica.
