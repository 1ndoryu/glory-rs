## 124A-IMAGE404 — Fix permanente: imágenes 404 por pérdida de bind mount

- **Que:** Coolify normaliza CUALQUIER bind mount path a named volume en su API interna. Cuando escribe el compose a disco (restart desde UI, redeploy, auto-restart), reemplaza el bind mount `/data/uploads/studio:/app/uploads` con un named volume `UUID_uploads-data:/app/uploads`. Docker crea un named volume vacío → imágenes perdidas.
  
  **Solución:** `volume_manager::ensure_uploads_bind_mount()` — módulo compartido que ejecuta `sed` en el compose en disco después de cada operación de Coolify, forzando el bind mount correcto. Se ejecuta en `deploy_service`, `redeploy` y `restart_site`.

  Patrón sed: `'[^']*:/app/uploads'` → `'/data/uploads/{site}:/app/uploads'`
  - Cubre named volumes Coolify (`UUID_uploads-data`)
  - Cubre bind mounts incorrectos
  - Es idempotente cuando ya está correcto

- **Archivos:**
  - `coolify-manager-rs/src/services/volume_manager.rs` (nuevo)
  - `coolify-manager-rs/src/services/mod.rs`
  - `coolify-manager-rs/src/commands/deploy_service.rs`
  - `coolify-manager-rs/src/commands/redeploy.rs`
  - `coolify-manager-rs/src/commands/restart_site.rs`

- **Gotchas:**
  - La API de Coolify NO soporta `file_storages` para bind mounts via API (endpoints no existen)
  - Coolify `docker_compose_raw` almacena `uploads_data:/app/uploads` (normalizado)
  - El compose en disco puede diferir completamente del raw/processed de la API
  - El test del sed desde PowerShell→SSH causa problemas de escape; el ssh client Rust envía el comando directamente a bash sin intermediario
  - 5 imágenes de proyectos están permanentemente perdidas (UUIDs en BD no existen en disco)
  - Si alguien reinicia desde Coolify UI (no coolify-manager-rs), el compose se reescribe con named volumes. El PRÓXIMO deploy desde coolify-manager-rs lo corregirá.

- **Sentinel:** Posible regla para detectar compose files con named volumes para `/app/uploads` en templates. Baja prioridad.

- **GLORY-RS:** No aplica — la lógica es específica de Coolify deploy, no del framework.

- **Root cause chain (documentar para futuras referencias):**
  1. Template `rust-stack.yaml` tiene bind mount correcto
  2. `sync_compose` PATCH a Coolify API → Coolify normaliza a named volume
  3. Coolify escribe compose procesado a disco con named volume
  4. `docker compose up -d` usa named volume → contenedor nuevo = uploads vacío
  5. Fix: `ensure_uploads_bind_mount()` sed-reemplaza after Coolify writes

---

## 124A-LIMPIEZA — Limpiar roadmap de todo lo completado

- **Que:** Roadmap tenía ~93 líneas de tareas completadas con ~~strikethrough~~ acumuladas. Se eliminaron todas, dejando solo las pendientes organizadas por sección: CMS/Frontend, Delegaciones, Hosting/Infra.
- **Archivos:** `roadmap.md`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-ESTILOS — Agregar estilos .heroImagenFondo

- **Que:** Se agregó `.heroImagenFondo { padding: var(--spacing-xl) var(--spacing-lg); }` al CSS base de SeccionHero. Ya existía en la media query mobile pero faltaba el estilo base.
- **Archivos:** `frontend/src/components/home/SeccionHero.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-PROTOCOLO — Regla obligatoria de deploy via coolify-manager-rs

- **Que:** Se agregó la **regla 19** al protocolo de desarrollo v5.0: todo despliegue, chequeo de estado, reinicio, logs, backup y cualquier operación contra producción DEBE hacerse via coolify-manager-rs. Prohibido usar comandos directos (ssh, docker, scp, curl). También se reforzó en el Paso 9 del flujo con referencia explícita a la regla 19.
- **Archivos:** `.github/instructions/test.instructions.md`
- **Gotchas:** La regla admite excepción para diagnóstico de emergencia documentado.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — es regla de protocolo, no de código.

---

## 124A-CMS1 — Fix blog fallback: sección oculta si 0 posts

- **Que:** `SeccionBlog` usaba `useState(POSTS_BLOG)` como estado inicial, mostrando posts estáticos aunque el usuario hubiera borrado todos los blogs del CMS. Se cambió a `useState(null)` con flag `cargado`. Si API retorna 0 posts → `setPosts([])` → sección retorna null (oculta). Si error de red → fallback a POSTS_BLOG. Elimina el fantasma de blogs borrados.
- **Archivos:** `frontend/src/components/home/SeccionBlog.tsx`
- **Gotchas:** El patrón anterior causaba que `data.posts.length > 0` nunca actualizara si API retornaba vacío, dejando los estáticos visibles indefinidamente.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — lógica específica de Nakomi home page.

---

## 124A-CMS3 — Drag-to-reorder proyectos (full stack)

- **Que:** Sistema completo de reordenamiento por arrastre:
  - **Backend:** `ProjectRepository::reorder()` usa CTE con `UNNEST($1::uuid[], $2::int[])` para batch update en un solo query. Handler PUT `/api/admin/projects/reorder` con auth Admin.
  - **Frontend:** Grid reemplazada por lista vertical sortable con `@dnd-kit`. Componente `FilaProyecto` con `useSortable` hook, grip handle, thumbnail 48px, badges, menú contextual. `handleDragEnd` llama `arrayMove` + callback.
  - **Hook:** `reordenar()` en `useContenidoProyectos` con optimistic local reorder + API call + rollback en error.
  - **CSS:** Grid eliminada, reemplazada por flex column (`.listaProyectosLista`) + flex row por fila (`.listaProyectosFila`).
- **Archivos:** `src/handlers/projects.rs`, `src/models/project.rs`, `src/models/mod.rs`, `src/repositories/project.rs`, `frontend/src/api/admin-projects.ts`, `frontend/src/components/panel/ListaProyectos.tsx`, `frontend/src/components/panel/ListaProyectos.css`, `frontend/src/components/panel/SubTabProyectos.tsx`, `frontend/src/hooks/useContenidoProyectos.ts`, `frontend/package.json`
- **Gotchas:** Axum resuelve rutas estáticas antes que `:id` dinámico, así que `/admin/projects/reorder` no conflicta con `/admin/projects/:id`. El tipo de `setMenuActivo` requiere `React.Dispatch<React.SetStateAction<string | null>>` para soportar forma callback.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de batch reorder con CTE UNNEST es reutilizable. Evaluar mover a glory-rs como trait genérico en futuro.

---

## 124A-CMS4 — Toggle visibilidad servicios en home

- **Que:** Agregado toggle "Mostrar en Home" / "Ocultar del Home" en el menú contextual de cada servicio en `ListaServicios`. Usa el campo existente `is_active` que ya controla la visibilidad pública en la API. Sin cambios de backend necesarios.
- **Archivos:** `frontend/src/components/panel/ListaServicios.tsx`, `frontend/src/components/panel/SubTabServicios.tsx`
- **Gotchas:** `is_active` ya existía en el modelo y la API lo respeta para filtrar servicios públicos. Solo faltaba la UI toggle.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — UI específica de CMS Nakomi.

---

## 124A-CMS2 — Selected Work: is_featured + categorías dinámicas

- **Que:** Sistema completo para elegir qué proyectos aparecen en "Selected Work" del home:
  - **Backend:** Migración `is_featured BOOLEAN NOT NULL DEFAULT false`. Campo añadido a Project, ProjectResponse, Create/Update requests, repository (SELECT, INSERT, UPDATE con COALESCE).
  - **Frontend CMS:** Toggle de estrella (Star icon) en cada fila de ListaProyectos. Click → `actualizar(id, {is_featured: true/false})`. Estrella llena = featured, vacía = no featured.
  - **Home showcase:** `SeccionShowcase` ahora filtra proyectos con `is_featured=true`. Si ninguno tiene featured, muestra todos (backward compatible).
  - **Categorías dinámicas:** `buildCategoriasShowcase` ya no tiene categorías hard-coded. Agrupa dinámicamente según la primera categoría de cada proyecto con mapa de títulos legibles. Max 6 proyectos por grupo.
- **Archivos:** `migrations/20260412100000_projects_is_featured.up.sql`, `.down.sql`, `src/models/project.rs`, `src/repositories/project.rs`, `src/handlers/projects.rs`, `frontend/src/api/admin-projects.ts`, `frontend/src/data/showcase.ts`, `frontend/src/components/home/SeccionShowcase.tsx`, `frontend/src/components/panel/ListaProyectos.tsx`, `frontend/src/components/panel/ListaProyectos.css`, `frontend/src/components/panel/SubTabProyectos.tsx`
- **Gotchas:** Necesita `cargo check` con CARGO_TARGET_DIR fuera de OneDrive si rust-analyzer bloquea DLLs en el target dir.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de campo `is_featured` con toggle en lista CMS es reutilizable. Considerar trait genérico en futuro.

---

## 124A-CMS7 — Convertir listas CMS de grid a filas verticales

- **Que:** Servicios, Blog y Equipo tenían layout de grid con cards grandes (280px min, imagen 120px). Convertido a lista vertical con filas compactas: miniatura 48px + info inline + badge + menú contextual. Mismo patrón que ListaProyectos (ya convertido en 124A-CMS3).
- **Archivos:** `frontend/src/components/panel/ListaServicios.tsx`, `ListaServicios.css`, `ListaBlog.tsx`, `ListaBlog.css`, `ListaEquipo.tsx`, `ListaEquipo.css`
- **Gotchas:** El menú contextual pasó de `position: absolute` (esquina de la card) a `flex-shrink: 0` en la fila, más fiable para overflow. El click se mueve de la card entera a `.{prefix}FilaInfo` para no conflictar con el menú.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — UI específica de CMS Nakomi.

---

## 124A-CMS5 — Carrusel: proporción libre, altura fija

- **Que:** El carrusel del home tenía proporción fija (3:4) con `object-fit: cover` que recortaba imágenes. Cambio:
  - CSS: eliminada `--carrusel-item-width`. `.carruselItem` y `.carruselImagenWrapper` ahora tienen `width: auto`. `.carruselImagen`: `height: 100%; width: auto;` (sin `object-fit: cover`). Altura sigue fija: 400/550/600px según breakpoint.
  - TSX: reemplazado el `translateX(calc(...))` basado en variable CSS fija por medición DOM real. Se usa `useLayoutEffect` + `ResizeObserver` para calcular `offsetLeft` del item actual. Soporta items con anchos distintos.
  - `OptimizedImage`: quitados props `width={450} height={600}`. Se deja que el hook `useOptimizedImage` mida el ancho real renderizado.
- **Archivos:** `frontend/src/components/home/CarruselShowcase.tsx`, `CarruselShowcase.css`
- **Gotchas:** `ResizeObserver` sobre la pista re-mide cuando imágenes terminan de cargar (lazy load). Si una imagen no ha cargado aún, su item tiene 0 width — el observer corrige cuando carga.
- **Sentinel:** No aplica.
- **GLORY-RS:** El hook `useCarruselInfinito` sigue siendo agnóstico y reutilizable. El patrón de medición DOM es específico de esta implementación variable-width.

---

## 124A-CMS6 — Verificar optimización de imágenes de blog

- **Que:** Verificación: las imágenes de blog ya pasan por `OptimizedImage` → `optimizedUrl()` → `/api/img/` con WebP en todos los puntos: SeccionBlog (home), ListaBlog (admin thumbnails), EditorBlog (preview). El flujo es: upload → `/uploads/blog.jpg` → render con `OptimizedImage` detecta prefijo `/uploads/` → genera `<picture>` con `<source type="image/webp" srcSet="/api/img/blog.jpg?w=...&q=80&fmt=webp">`. No requiere cambios de código.
- **Archivos:** Ninguno modificado (verificación).
- **Gotchas:** Solo URLs con prefijo `/uploads/` o `/assets/` son optimizables (definido en `isOptimizableSrc`). URLs externas (http) pasan sin proxy.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-OPT1 — Optimizar compilación Rust (velocidad + disco)

- **Que:** El usuario reportó compilaciones lentas y alto uso de disco. Cambios aplicados:
  1. **Cargo.toml `[profile.dev]`:** `debug = "line-tables-only"` (reduce ~60% datos de debug), `split-debuginfo = "packed"`.
  2. **Cargo.toml `[profile.dev.package."*"]`:** Dependencias con `opt-level = 1` (se compilan más rápido en incrementales) y `debug = false` (no generan debug info).
  3. **`.cargo/config.toml`:** `target-dir = "C:/tmp/glory-target"` — saca el directorio target de OneDrive, eliminando conflictos de file locking y sincronización lenta.
  4. **`.gitignore`:** Agregado `/.cargo/` (contiene path local, no debe ir a git).
  - El directorio `target/` viejo en OneDrive (2.34 GB) ya no se usa y puede borrarse manualmente (`Remove-Item target -Recurse -Force`).
- **Archivos:** `Cargo.toml`, `.cargo/config.toml` (nuevo), `.gitignore`
- **Gotchas:** Primera compilación tras cambiar perfil recompila todo. rust-analyzer debe reiniciarse para usar el nuevo target-dir. `C:\tmp\glory-target` no está en OneDrive → sin conflictos de sync.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — configuración local del proyecto.

---

## 124A-OPT2 — Persistencia de config.toml entre ramas

- **Que:** El usuario reportó que `.cargo/config.toml` (creada en OPT1) se perdería al cambiar de rama. Esto es parcialmente incorrecto — archivos gitignored (untracked) NO se borran con `git checkout`. Sin embargo, para máxima robustez, se estableció `CARGO_TARGET_DIR=C:\tmp\glory-target` como variable de entorno permanente a nivel User de Windows. Ahora hay doble protección: el env var (persiste en cualquier escenario: cambio de rama, re-clone, nueva terminal) + `.cargo/config.toml` como backup local (project-level). Cargo prioriza `.cargo/config.toml` sobre el env var cuando ambos existen.

---

## 124A-SHOW1 — showcase_category per-project (full stack)

- **Que:** El usuario no podía editar la categoría del showcase desde el CMS. Las categorías estaban hardcodeadas en un diccionario `tituloCategoria` en `showcase.ts`. Se implementó un campo `showcase_category TEXT` en la tabla `projects` que permite definir desde el CMS cómo se agrupa cada proyecto en la sección "Selected Work" del home. Proyectos con el mismo `showcase_category` se agrupan juntos. Si el campo queda vacío, se usa el fallback al diccionario antiguo.
- **Archivos:**
  - `migrations/20260412180000_showcase_category.up.sql` — ALTER TABLE ADD COLUMN
  - `migrations/20260412180000_showcase_category.down.sql` — DROP COLUMN
  - `src/models/project.rs` — campo en Project, ProjectResponse, Create/UpdateRequest, into_response()
  - `src/repositories/project.rs` — SELECT (x4), INSERT, UPDATE con COALESCE
  - `src/handlers/projects.rs` — pasar showcase_category en create/update params
  - `frontend/src/api/admin-projects.ts` — AdminProject, CreateProjectBody, UpdateProjectBody
  - `frontend/src/types/contenido.ts` — Proyecto.showcaseCategory
  - `frontend/src/data/showcase.ts` — mapAdminProjectsToProyectos + buildCategoriasShowcase usa showcaseCategory con prioridad
  - `frontend/src/hooks/useEditorProyecto.ts` — estado, sync, buildBody
  - `frontend/src/components/panel/EditorProyecto.tsx` — input "Showcase Category" con hint
  - `frontend/src/components/panel/EditorProyecto.css` — .editorProyectoHint
- **Gotchas:** La migración se auto-ejecuta al startup via `sqlx::migrate!()`. El campo es nullable (TEXT sin NOT NULL) para retrocompatibilidad — proyectos existentes empiezan con NULL y usan fallback del diccionario.
- **Sentinel:** No aplica.
- **GLORY-RS:** Evaluado. El campo showcase_category es específico de nakomi.studio (portfolio showcase). No aplica mover a glory-rs.

---

## 124A-CSS3 — proyectoHeroTitulo alineado derecha + padding responsive móvil

- **Que:** Alineado `proyectoHeroTitulo` a la derecha (`text-align: right`). En el responsive mobile (≤768px) se cambió padding de `proyectoCaseIntroContenido` y `proyectoPortada` de `var(--spacing-lg)` a `var(--spacing-md)`.
- **Archivos:** `frontend/src/components/proyectos/SeccionHeroProyecto.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-RULE1 — Regla 20: Prohibido reiniciar VS Code

- **Que:** Agregada regla #20 al protocolo de desarrollo (`.github/instructions/test.instructions.md`): prohibido ejecutar comandos que reinicien, recarguen o cierren VS Code. Si una extensión se modifica o reinstala, el usuario decide cuándo reiniciar.
- **Archivos:** `.github/instructions/test.instructions.md`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-SEARCH1 — Buscador real-time en las 4 tabs del CMS

- **Que:** Implementado buscador en tiempo real en todas las pestañas del CMS: Proyectos, Blog, Servicios, Equipo. Hook reutilizable `useBusquedaLista` que filtra client-side normalizando acentos y case. Cada lista filtra por campos relevantes (título, slug, cliente, categorías, nombre, rol).
- **Archivos:**
  - `frontend/src/hooks/useBusquedaLista.ts` — hook genérico de búsqueda
  - `frontend/src/components/panel/ListaProyectos.tsx` — búsqueda por title, slug, client, categories
  - `frontend/src/components/panel/ListaBlog.tsx` — búsqueda por title, slug
  - `frontend/src/components/panel/ListaServicios.tsx` — búsqueda por title, slug
  - `frontend/src/components/panel/ListaEquipo.tsx` — búsqueda por name, role
  - `frontend/src/components/panel/SeccionContenido.css` — estilos compartidos `.listaBusqueda`
- **Gotchas:** El filtro es puramente visual (client-side). No afecta el orden de drag-to-reorder porque las operaciones de reorden trabajan sobre el array completo, no el filtrado.
- **Sentinel:** No aplica.
- **GLORY-RS:** El hook `useBusquedaLista` es reutilizable y agnóstico. Candidato para mover a glory-rs en el futuro.
- **Archivos:** Ninguno modificado en el repo (cambio es variable de entorno del sistema).
- **Gotchas:** El env var User-level aplica a TODOS los proyectos Rust del mismo usuario en esta máquina. Esto beneficia también a coolify-manager-rs (también en OneDrive). Todos comparten `C:\tmp\glory-target` — cargo maneja subdirectorios por package hash, no hay conflictos.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — configuración de entorno local.

---

## 124A-CMS8 — Deploy a producción

- **Que:** Deploy completo via `coolify-manager deploy-service --name studio`. Build completado en 923s (15 min). nakomi.studio respondiendo HTTP 200. Todos los sitios healthy excepto `kamples` (503 — no relacionado con nuestro deploy).
- **Archivos:** Ninguno (deploy).
- **Gotchas:** Build de Rust en el servidor tarda ~15 min. El deploy incluye backup pre-deploy automático transferido VPS1→VPS2. El bind mount se forzó correctamente.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-CMS9 — Quitar padding del grip de proyectos

- **Que:** El usuario indicó "listaProyectosGrip no necesita padding". Se eliminó `padding: var(--spacing-xs)` de `.listaProyectosGrip` en ListaProyectos.css. Los nuevos grips (servicios, blog) ya se crearon sin padding.
- **Archivos:** `frontend/src/components/panel/ListaProyectos.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-CMS10 — Drag-to-reorder servicios y blog (full stack)

- **Que:** Sistema completo de reordenamiento por arrastre para servicios y blog, replicando el patrón de proyectos (CMS3):
  - **Backend servicios:** Handler PUT `/api/admin/services/reorder` con `OrderRepository::reorder_services()` usando CTE UNNEST.
  - **Backend blog:** Migración `blog_sort_order` (ADD COLUMN + inicialización por created_at). Handler PUT `/api/admin/blog/reorder` con `BlogRepository::reorder()`. Campo `sort_order` añadido a BlogPost y BlogPostResponse.
  - **Frontend:** `apiReorderServices()` y `apiReorderBlog()` en API layer. `reordenar` callbacks en hooks con optimistic update + rollback. `ListaServicios` y `ListaBlog` refactorizados con `@dnd-kit` (DndContext, SortableContext, useSortable, sensors). Componentes extraídos: `FilaServicio` y `FilaBlogPost`. Grip handles con GripVertical icon.
  - **Modelo genérico:** `ReorderRequest { items: Vec<ReorderItem> }` reutilizable para cualquier entidad (junto al existente `ReorderProjectsRequest`).
- **Archivos:** `migrations/20260412140000_blog_sort_order.up.sql`, `.down.sql`, `src/models/blog.rs`, `src/models/project.rs`, `src/models/mod.rs`, `src/repositories/blog.rs`, `src/repositories/order.rs`, `src/handlers/blog.rs`, `src/handlers/admin_services.rs`, `frontend/src/api/admin-blog.ts`, `frontend/src/api/admin-services.ts`, `frontend/src/hooks/useContenidoBlog.ts`, `frontend/src/hooks/useContenidoServicios.ts`, `frontend/src/components/panel/ListaServicios.tsx`, `frontend/src/components/panel/ListaServicios.css`, `frontend/src/components/panel/ListaBlog.tsx`, `frontend/src/components/panel/ListaBlog.css`, `frontend/src/components/panel/SubTabServicios.tsx`, `frontend/src/components/panel/SubTabBlog.tsx`
- **Gotchas:** Las rutas `/reorder` deben definirse ANTES de `/:id` en Axum para que el router no las confunda con un UUID. El campo `sort_order` del blog se inicializa con ROW_NUMBER() OVER (ORDER BY created_at ASC) en la migración.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón `ReorderRequest` + CTE UNNEST es genérico y reutilizable. Ya vive en `models/project.rs` como struct compartido. Evaluar mover a glory-rs cuando se consolide el patrón en más entidades.

---

## 124A-PROJ1 — Rediseño página individual de proyectos (estilo kontrapunkt)

- **Que:** Rediseño completo full-stack de la página individual de proyectos, inspirado en kontrapunkt.com/work/true-anomaly:
  - **Backend:** Modelo de galería cambiado de `Vec<String>` (solo URLs) a `Vec<GalleryImage>` (URL + layout "full"/"half"). Migración SQL convierte datos existentes con `jsonb_build_object` + `jsonb_agg`. Parsing dual en `into_response()` para backward compatibility.
  - **Frontend tipos:** `GaleriaImagen { url: string; layout: 'full' | 'half' }` reemplaza `string[]` en `Proyecto.galeria`, API types y hooks.
  - **CMS Editor:** `GaleriaEditor` reescrito: acepta `GaleriaImagen[]`, upload crea `{url, layout: 'full'}`, botón toggle layout (Maximize2/Columns2 icons) por imagen mostrando "1/1" o "1/2", controles agrupados con opacity transition.
  - **Hero rewrite:** `SeccionHeroProyecto` ahora renderiza 3 secciones: (1) Hero grid 1fr+2fr (izq: cliente, der: H1 título), (2) Portada full-width OptimizedImage, (3) Case Introduction grid 1fr+2fr (izq: Client/Industry/Deliveries/Links metadata, der: descripción). Eliminados todos los iconos de enlace ICONOS_ENLACE.
  - **Galería nueva:** `SeccionGaleriaProyecto` (nuevo componente): CSS Grid 2 columnas, `--full` = span 2, `--half` = span 1, OptimizedImage con sizes según layout. Responsive a 1 columna en mobile.
  - **Related:** Grid de 3 a 4 columnas, slice de 3 a 4 resultados, breakpoint 1024px para 2 cols.
  - **Island rewrite:** `ProyectoIndividualIsland` simplificado: eliminados useTranslation, useChatStore, SeccionSkillsServicio, SeccionGaleriaServicio, SeccionCta. Nueva interfaz `ProyectoDetalle` y `convertirDesdeApi()`.
- **Archivos:** `migrations/20260412160000_gallery_layout.up.sql`, `.down.sql`, `src/models/project.rs`, `src/models/mod.rs`, `frontend/src/types/contenido.ts`, `frontend/src/api/admin-projects.ts`, `frontend/src/data/showcase.ts`, `frontend/src/hooks/useEditorProyecto.ts`, `frontend/src/components/panel/GaleriaEditor.tsx`, `frontend/src/components/panel/EditorProyecto.css`, `frontend/src/components/proyectos/SeccionHeroProyecto.tsx`, `SeccionHeroProyecto.css`, `SeccionGaleriaProyecto.tsx` (nuevo), `SeccionGaleriaProyecto.css` (nuevo), `SeccionProyectosRelacionados.tsx`, `SeccionProyectosRelacionados.css`, `frontend/src/islands/ProyectoIndividualIsland.tsx`, `frontend/src/App.tsx`
- **Gotchas:** El parsing dual en `into_response()` es necesario porque datos pre-migración pueden existir como `["url"]` string arrays. El fallback asigna layout "full" a todas. La migración solo afecta filas donde el primer elemento de gallery es de tipo string (no ya un objeto).
- **Sentinel:** No aplica.
- **GLORY-RS:** El componente `SeccionGaleriaProyecto` con sistema de layout grid (full/half) es potencialmente reutilizable. Evaluar mover a glory-rs si otro proyecto necesita galería con layouts variables.

---

## 124A-AUTH1 — JWT expiración permanente (10 años)

- **Que:** JWT expiraba a 24h causando 401 en todas las requests. Cambiado a `Duration::days(3650)` (10 años) per request del usuario.
- **Archivos:** `src/services/auth.rs`
- **Gotchas:** Token anterior vencido — re-login necesario para obtener nuevo token.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-CSS1 — Fix proyectoCaseIntroMeta layout horizontal + portada spacing

- **Que:** `proyectoCaseIntroMeta` cambiado a `flex-direction: row` con gap `2xl` (horizontal como kontrapunkt). Portada con `padding: var(--spacing-lg)`. Responsive vuelve a columna.
- **Archivos:** `frontend/src/components/proyectos/SeccionHeroProyecto.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-CSS3 — proyectoHeroTitulo alineado derecha + padding responsive

- **Que:** Título H1 del hero alineado derecha (`text-align: right`). Padding responsive reducido a `md` en mobile.
- **Archivos:** `frontend/src/components/proyectos/SeccionHeroProyecto.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-SEARCH1 — Buscador real-time en cada tab del CMS

- **Que:** Input de búsqueda en ListaProyectos, ListaBlog, ListaServicios, ListaEquipo. Hook `useBusquedaLista` reutilizable. Filtrado client-side por título/nombre.
- **Archivos:** `frontend/src/hooks/useBusquedaLista.ts` (nuevo), ListaProyectos/Blog/Servicios/Equipo `.tsx`, `SeccionContenido.css`
- **Gotchas:** Filtrado no afecta DnD — drag opera sobre lista completa.
- **Sentinel:** No aplica.
- **GLORY-RS:** Hook `useBusquedaLista` es agnóstico y reutilizable.

---

## 124A-SHOW1 — showcaseTituloCategoria per-project

- **Que:** Campo `showcase_category` en modelo de proyectos. CMS editor con input + hint. `buildCategoriasShowcase` agrupa por campo con fallback al diccionario.
- **Archivos:** Migración, `src/models/project.rs`, `src/repositories/project.rs`, `src/handlers/projects.rs`, frontend types/API/showcase/hook/editor
- **Gotchas:** Proyectos con mismo `showcase_category` se agrupan bajo ese título.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

---

## 124A-BLOG1 — Blog is_featured toggle + filtro en home

- **Que:** Migración `is_featured`, toggle estrella en ListaBlog, SeccionBlog filtra featured con fallback a recientes.
- **Archivos:** Migración, `src/models/blog.rs`, `src/repositories/blog.rs`, `src/handlers/blog.rs`, frontend API/ListaBlog/SubTabBlog/SeccionBlog
- **Gotchas:** API pública ya devuelve `is_featured`. Filtrado client-side en SeccionBlog.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

## 124A-DETAIL1  detail_title + use_first_gallery_image (full stack)
- **Que:** Nuevos campos para elegir titulo alternativo en pagina de detalle de proyecto y opcion de usar primera imagen de galeria como portada en vez de featured_image.
- **Archivos:** Migracion 20260412200000, src/models/project.rs, src/repositories/project.rs, src/handlers/projects.rs, rontend/src/api/admin-projects.ts, rontend/src/types/contenido.ts, rontend/src/hooks/useEditorProyecto.ts, rontend/src/components/panel/EditorProyecto.tsx, rontend/src/components/panel/EditorProyecto.css, rontend/src/islands/ProyectoIndividualIsland.tsx
- **Gotchas:** Renumeracion de binds en UPDATE -. EditorProyecto excede 300 lineas, sentinel-disable justificado.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.
