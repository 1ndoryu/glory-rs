# Tareas completadas — 2026-04-30

## 304A-3 — Asignación admin de hostings a clientes + SeccionDominios + Auditoría flujo compra

- **Que:** 
---

## PM-1 — Contabo auth: trim espacios trailing en credenciales
- **Que:** `first_env()` en `src/services/contabo.rs` hacía filter por `!is_empty()` pero no quitaba espacios. `CLIENT_ID_CONTABO=INT-12767452 ` con espacio al final hacía que Contabo rechazara la autenticación. Se agregó `.map(|v| v.trim().to_string())` antes del filter.
- **Archivos:** `src/services/contabo.rs`
- **Gotchas:** El `.env` tiene varios valores con espacio al final — el trim previene cualquier caso similar.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (específico de configuración de proyecto).

## PM-2 — Despliegue huérfano: crear suscripción vinculada desde Vps2DeploymentsPanel
- **Que:** `Vps2DeploymentsPanel` mostraba el aviso de "despliegue sin vínculo" pero no ofrecía acción. Ahora cada card huérfana tiene botón "Crear suscripción vinculada" que despliega inline `CreateHostingForm` con `coolify_site_name` pre-llenado. Backend: `CreateHostingRequest` y `CreateHostingParams` aceptan `coolify_site_name` opcional; el INSERT lo guarda. Cache SQLx regenerado.
- **Archivos:** `src/models/hosting.rs`, `src/repositories/hosting.rs`, `src/handlers/hosting.rs`, `frontend/src/api/hosting.ts`, `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/components/panel/Vps2DeploymentsPanel.tsx`, `frontend/src/components/panel/VpsPanel.css`, `.sqlx/`
- **Gotchas:** `useHostingMutations` requiere 2 argumentos — no reutilizable en `DeploymentCard`. Se creó la mutation inline con `useMutation` directamente.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (específico de panel admin Nakomi).
  1. Backend: `PATCH /api/hosting/subscriptions/:id/assign` — admin busca usuario por email y lo vincula a la suscripción.
  2. Frontend Tarea 7: modal de asignación en `HostingCard`, badge "vinculado"/"sin cuenta", `onAssign`/`assignLoading` props. Admin genera link de pago (copia URL) vs cliente (redirige).
  3. Tarea 6: `SeccionDominios.tsx` + `SeccionDominios.css` — lista dominios Contabo con estado, nameservers y fecha de vencimiento. Agregado a sidebar como entrada independiente con `Globe` icon.
  4. `useHostingMutations`: `assignMutation` + `adminCheckoutMutation` agregados.
  5. `.sqlx/` regenerado con `cargo sqlx prepare` contra `nakomi_db` para incluir nueva query `assign_user`.
  6. Tarea 8: `Agente/documentacion/hosting/auditoria-compra-2026-04-30.md` — trazado completo del flujo de compra, variables críticas, puntos de fallo, checklist manual.

- **Archivos:**
  - `src/repositories/hosting.rs` — `assign_user()`
  - `src/models/hosting.rs` — `AssignHostingRequest`
  - `src/models/mod.rs` — re-export
  - `src/handlers/hosting.rs` — handler `assign_hosting` + ruta `PATCH /hosting/subscriptions/:id/assign`
  - `frontend/src/api/hosting.ts` — `apiAssignHostingToUser`
  - `frontend/src/components/panel/HostingSubComponents.tsx` — modal asignación, badges, props nuevas
  - `frontend/src/components/panel/SeccionHosting.css` — `.hostingAsignadoBadge`, `.hostingNoAsignadoBadge`, `.hostingFormNota`
  - `frontend/src/components/panel/SeccionHosting.tsx` — pasa `onAssign`, `assignLoading`, `adminCheckoutMutation`
  - `frontend/src/hooks/useHostingMutations.ts` — `assignMutation`, `adminCheckoutMutation`
  - `frontend/src/data/panel.ts` — `'dominios'` en `SeccionPanel` type + `TABS_ADMIN`
  - `frontend/src/components/panel/SeccionDominios.tsx` — nuevo componente
  - `frontend/src/components/panel/SeccionDominios.css` — estilos
  - `frontend/src/components/panel/SidebarPanel.tsx` — import `Globe`, `'dominios': Globe`
  - `frontend/src/islands/PanelIsland.tsx` — import + `case 'dominios'`
  - `.sqlx/` — regenerado con cargo sqlx prepare
  - `Agente/documentacion/hosting/auditoria-compra-2026-04-30.md` — nueva documentación

- **Gotchas:**
  - `cargo sqlx prepare` requiere `SQLX_OFFLINE=false` y `DATABASE_URL=postgres://...nakomi_db`. La DB `glory_kamples` del `.env` no tiene el schema completo.
  - `adminCheckoutMutation` copia la URL al portapapeles (no redirige) para que admin pueda compartirla con el cliente.
  - El spread `...mutations` en `useSeccionHosting` propaga automáticamente las nuevas mutations a `SeccionHosting.tsx`.

- **Sentinel:** No aplica regla nueva. No hubo falsos positivos.

- **GLORY-RS:** La lógica de asignación admin es específica de Nakomi Studio. No aplica mover a glory-rs.
