# Tareas completadas — 2026-04-30

## 304A-1 — Fix paridad legacy publicador extraccion + worker enricher de imagenes
- **Que:**
  - Quitado el tag `lado_fuente`/`lado_destino` de `generar_tags_legacy` — el legacy PHP nunca los generaba; contaminaban el feed/buscador. Lado se preserva en `samples.metadata.lado_extraccion`.
  - UPDATE retroactivo: 355 samples extraccion limpiados de tags `lado_X`.
  - `register_play` ahora valida existencia del sample y devuelve 404 en vez de 500 (FK violation `reproducciones_sample_id_fkey` sobre samples borrados).
  - DELETE de 4 reproducciones huerfanas (sample_id sin existencia en samples).
  - Nuevo worker `cancion_image_enricher_worker.rs`: lento (1 cancion / 30s), sin lotes grandes, consulta iTunes Search API gratis, hace upgrade `100x100bb.jpg` -> `600x600bb.jpg`, marca `metadata.imagen_lookup_at` con cooldown de 30 dias para no reintentar inutilmente. Cubre el gap de ~2.7k canciones sin portada (causa raiz por la que muchos samples extraccion no heredan imagen).
- **Archivos:**
  - `src/services/extraccion_publisher.rs` — `generar_tags_legacy` sin `lado_X`
  - `src/handlers/plays.rs` — validacion previa de sample en `register_play`
  - `src/workers/cancion_image_enricher_worker.rs` — nuevo worker
  - `src/workers/mod.rs` — registro del worker
  - `src/main.rs` — spawn del worker en `BackgroundWorkerHandles`
- **Gotchas:**
  - SQLX_OFFLINE=true exige `sqlx prepare` para macros `query!`/`query_as!`. Usado `sqlx::query`/`query_as` untyped para evitar regenerar el cache offline en este bloque pequeno.
  - Clippy `needless_raw_string_hashes` exige `r"..."` en vez de `r#"..."#` cuando no hay `"` literal interno.
  - El usuario tenia la sospecha de "tag source" — en realidad no existia: era confusion con `lado_fuente`. Auditoria de DB lo confirmo (0 ocurrencias de `source`).
  - 224/354 samples sin imagen — auditoria mostro que las canciones origen tampoco tienen imagen (no era bug del publisher). El nuevo worker resuelve la causa raiz aguas arriba.
- **Sentinel:** No aplica.
- **GLORY-RS:** El worker iTunes es agnostico de Kamples, podria moverse a `glory-rs/src/services/` como utilidad de enriquecimiento de metadata musical. Pendiente para cuando otro proyecto lo necesite — por ahora es especifico al schema `canciones`/`artistas_musicales` de Kamples.
