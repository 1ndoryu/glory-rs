## 086A-1 — Fix pricing: descuento porcentual + cargo Stripe en checkout

- **Que:** Corrección de dos bugs de pricing en nakomi.studio: (1) descuento por periodo cambiado de resta fija ($10/$20) a porcentaje real (10%/20%) sobre subtotal; (2) agregado cargo por procesamiento de pago (Stripe: 6.1% + $3 MXN) como línea separada en el resumen del checkout, sin afectar precios base ni Stripe Checkout. Cambios full-stack: Rust backend + React frontend + git push + deploy via coolify-manager-rs.
- **Archivos:**
  - `src/handlers/hosting/checkout.rs` — `hosting_billing_discount_cents` a porcentaje (10%/20%); nueva `hosting_stripe_fee_cents`; `hosting_checkout_config_details` acepta `monthly_price_cents`; `subscribe_self` suma fee al amount
  - `src/handlers/vps.rs` — Usa `vps_stripe_fee_cents` compartida desde `services`
  - `src/handlers/orders.rs` — Fix `naive_utc()` → `chrono::Utc::now()` (type mismatch DateTime<Utc>)
  - `src/services/vps_stripe.rs` — Nuevas constantes públicas `STRIPE_FEE_RATE_CENTS`, `STRIPE_FEE_FIXED_CENTS`; función `vps_stripe_fee_cents`; `VpsCheckoutParams.processing_fee_cents`; 3er line item en Stripe Checkout
  - `src/services/mod.rs` — Export `vps_stripe_fee_cents`
  - `src/services/ai_tools.rs` — Actualizado callsite de VpsCheckoutParams
  - `frontend/src/hooks/useHostingConfiguradorIsland.ts` — `hostingBillingDiscountCents` a porcentaje; `hostingStripeFeeCents`; labels actualizados
  - `frontend/src/hooks/useVpsConfiguradorIsland.ts` — `stripeFeeCents` local; `processingFeeCents` en retorno
  - `frontend/src/islands/HostingConfiguradorIsland.tsx` — Muestra "Procesamiento de pago" condicional
  - `frontend/src/islands/VpsConfiguradorIsland.tsx` — Muestra "Procesamiento de pago" condicional
- **Gotchas:**
  - `vps_stripe.rs`: `format!()` retorna `String` pero `form.extend()` espera `(&str, String)` — solución con `Box::leak` closure `k(suffix)` para obtener `&'static str`
  - `orders.rs`: `chrono::Utc::now().naive_utc()` retorna `NaiveDateTime` pero campo espera `DateTime<Utc>` — fix directo
  - Primer deploy (17:54) se quedó estancado en build por 10 min y fue terminado; segundo deploy (17:57) completó en 605s (~10 min)
  - `cargo check` 0 errores, `tsc --noEmit` 0 errores previo a commit
- **Deploy:** `deploy --name studio --update --skip-backup` exitoso (build 605s, swap zero-downtime). Sitio nakomi.studio actualizado con descuentos porcentuales y cargo de procesamiento visible.
