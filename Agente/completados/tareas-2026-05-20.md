## 205A-1 — Onboarding hosting y dominios Guillermo
- **Que:** se agrego facturacion pendiente separada del estado tecnico de hosting, UI de pagos pendientes en Mi Hosting, checkout Stripe para items pendientes, email editable de perfil y bootstrap admin idempotente para crear/vincular el caso Guillermo sin fixtures TOML en produccion.
- **Archivos:** frontend API/hooks/panel/sidebar/Tarjeta, handlers y repositorios billing, Stripe billing, perfil usuario, migracion `billing_items`, bootstrap admin Guillermo, documentacion hosting.
- **Gotchas:** no sincronizar TOML en produccion para este cliente; dominios GoDaddy vencen/renuevan el 2027-01-14; los dominios `wandori.us` no se cobran; los hostings reales quedan `active` y no se reprovisionan.
- **Sentinel:** no aplica.
- **GLORY:** no aplica; logica especifica de Nakomi/cliente Guillermo.

## 195A-1 — Correcciones UI billing y hosting panel
- **Que:** 13 fixes de UI/UX descubiertos al testear en el navegador: precios en verde sin bold, thumbnail de dominio con thum.io en `panelCardIcono`, descripcion de items de billing simplificada (sin texto de estado operativo ni "transferencia manual"), fecha `grace_period_ends_at` en lugar de `due_at`, descuento 20% en checkout anual (via aritmetica entera i32 en `prepay_amount_cents`), bypass de test para `prepay_year` checkout sin Stripe key local, acciones hosting con estado a izquierda y botones a derecha, mensaje SFTP legacy correcto, DNS verification header inline (h4 izq, boton der), colision de dominio largo en InfoRow, `próximo cobro estimado` en TabFacturacion para hostings activos.
- **Archivos:** `billing_stripe.rs`, `billing.rs`, `admin_client_bootstrap.rs`, `PendingBillingPanel.tsx`, `PendingBillingPanel.css`, `HostingSubComponents.tsx`, `PanelIsland.css`, `HostingDetalleAccess.tsx`, `HostingDetalleTabs.tsx`, `HostingDetalle.css`, `TabFacturacion.tsx`.
- **Gotchas:** `HostingSubscription` no tiene `stripe_subscription_id` — condicion de proximo cobro cambiada a `sub.status === 'active'`. Cast f64→i32 en clippy resuelto con aritmetica entera. Semicolon doble en `admin_client_bootstrap.rs` por boundary de oldString incompleto.
- **Sentinel:** no aplica.
- **GLORY:** no aplica.

## 205A-2+205A-3 — Perfil permite cambiar email y contraseña con confirmacion
- **Que:** se agrego cambio de contraseña desde configuracion de perfil, cambio de correo sin verificacion por email pero con confirmacion por modal reescribiendo el nuevo correo, notificaciones SMTP informativas para cambio de correo/contraseña y ajuste visual del bloque de perfil sin borde superior en los subtitulos.
- **Archivos:** `src/models/user.rs`, `src/models/mod.rs`, `src/repositories/user.rs`, `src/services/auth.rs`, `src/services/email.rs`, `src/handlers/profile.rs`, `frontend/src/api/profile.ts`, `frontend/src/hooks/usePerfil.ts`, `frontend/src/hooks/useSeccionPerfil.ts`, `frontend/src/components/panel/SeccionPerfil.tsx`, `frontend/src/components/panel/SeccionPerfil.css`.
- **Gotchas:** el cambio de correo mantiene `email_verified` como estaba porque aun no existe flujo de verificacion; el modal de confirmacion tuvo que adaptarse a las reglas de Sentinel (sin titulo en modal y con hook extraido del componente).
- **Sentinel:** corregido `sqlx::query!` en `update_email`; frontend limpio tras extraer `useSeccionPerfil` y reutilizar recetas de `Modal`.
- **GLORY:** no aplica; comportamiento especifico del perfil autenticado en Nakomi Studio.