# Tareas completadas — 2026-03-30

## 303A-1 — Revisión profunda del sistema de marketing/SMS/email/WhatsApp
- **Qué:** Auditoría completa del sistema de marketing con corrección de 6 bugs y varias mejoras. Investigación de las APIs de Meta WhatsApp Cloud y Twilio para validar la implementación.
- **Archivos:**
  - `migrations/20260330000000_meta_phone_number_id.up.sql` — nueva columna `meta_phone_number_id`
  - `src/models/integracion_marketing.rs` — nuevo campo + `meta_templates_configurado()` + `meta_configurado()` actualizado
  - `src/repositories/integracion_marketing.rs` — UPDATE con nuevo campo
  - `src/repositories/campana.rs` — `set_estado` con contadores reales + `actualizar_estado_destinatario`
  - `src/repositories/recordatorio.rs` — query con `prefijo_telefono` del cliente
  - `src/services/meta_whatsapp.rs` — endpoint corregido: Phone-Number-ID en vez de WABA ID, API v23.0
  - `src/services/plantilla_whatsapp.rs` — `enviar_a_meta` implementación real (no stub)
  - `src/services/campana.rs` — `enviar_por_canales` retorna contadores, actualiza destinatarios
  - `src/services/recordatorio.rs` — compone número E.164 con prefijo
  - `frontend/src/componentes/IntegracionesMarketing.tsx` — campo Phone Number ID
  - `frontend/src/hooks/useIntegraciones.ts` — soporte para nuevo campo
  - `frontend/src/api/generated/gestiNRestauranteAPI.schemas.ts` — tipos actualizados
  - `.env.example` — variables SMTP y marketing documentadas
- **Gotchas:**
  - La API de mensajes de Meta usa `Phone-Number-ID` (POST /{Phone-Number-ID}/messages), NO el WABA ID. El WABA ID solo sirve para gestión de templates.
  - Meta API actual es v23.0, teníamos v21.0.
  - Los recordatorios usaban `r.telefono` de reservas que NO tiene prefijo de país. Se necesita JOIN con `clientes.prefijo_telefono`.
  - `enviar_por_canales` era fire-and-forget (no retornaba nada). Los contadores `total_enviados`/`total_fallidos` quedaban siempre en 0.
  - Los destinatarios individuales de campañas quedaban eternamente en estado "pendiente".
- **Sentinel:** La regla de "no usar stubs en producción" podría detectar patrones como `stub_` o `integración pendiente` en código, pero es bajo valor operacional.
