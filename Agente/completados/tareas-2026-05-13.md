# Tareas completadas — 2026-05-13

## 135A-1 — Recuperación permanente de studio unhealthy
- **Que:** se endureció `studio` contra el fallo donde el proceso Rust seguía vivo y aceptaba TCP pero dejaba de responder HTTP. Se agregó `/healthz` fuera de `/api`, watchdog interno de self-heal, procesamiento de imágenes CPU-bound fuera de los workers async, labels explícitos de Traefik para Rust y healthcheck Docker sin `awk`.
- **Archivos:** `src/handlers/health.rs`, `src/handlers/mod.rs`, `src/main.rs`, `src/services/image_processing.rs`, `src/services/vps_stripe.rs`, `.agent/coolify-manager-rs/templates/rust-stack.yaml`, `.agent/coolify-manager-rs/config/settings.json`.
- **Gotchas:** el contenedor `bookworm-slim` no trae `awk`; `curl` conectaba a `127.0.0.1:3000` pero no recibía bytes, así que no era un problema de DNS/Traefik solamente.
- **Sentinel:** No aplica; es patrón runtime/operacional. Queda como lección para futura regla sobre CPU-bound sync en handlers async.
- **GLORY:** No aplica; el cambio es específico de este stack Nakomi/Rust y del template operativo de coolify-manager.
