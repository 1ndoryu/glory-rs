## 174A-108 + 174A-109 + 174A-110 + 174A-111 — Importacion de clientes externos al monorepo
- **Que:** los 4 clientes (`kamples-scraper`, `Mezclador`, `mobile`, `desktop`) viven hasta hoy en `glorytemplate/` (legado WordPress). Se copian 1:1 a `clients/` del nuevo repo Rust para que pasen a estar versionados junto al backend Axum. Se excluyen `node_modules/`, `dist/`, `target/`, `android/build/`, `.gradle/` y `www/` (artefactos generados). Total importado: **344 archivos fuente** (43 scraper, 104 Mezclador, 69 mobile, 128 desktop).
- **Por que:** la "Fase 18" del roadmap original ("Adaptar X") es engañosa porque mezcla dos cosas: (1) tener el código en el repo, (2) migrarlo de `wp-json/...` al nuevo backend. La (1) es trivial y se cierra ahora. La (2) requiere endpoints Rust nuevos y un smoke test por cliente, así que se reagenda como `174A-108b..111b` con plan detallado en `Agente/planes/plan-clients-adapters-2026-04-20.md`.
- **Inventario de URLs WordPress encontradas en los clientes copiados:**
  - `clients/kamples-scraper/extractor/pipeline.py` → `wp-json/kamples/v1/dev/extraccion/publicar-auto`, `wp-json/kamples/v1/admin/automatizacion/reporte-lote`.
  - `clients/desktop/src/services/{apiDesktopAdmin,googleAuthMobile,syncGuards}.ts`, `clients/desktop/src/sync.tsx`, `clients/desktop/src-tauri/tauri.conf.json`, `clients/desktop/vite.config.ts` → todos apuntan a `wp-json/...`.
  - `clients/mobile/capacitor.config.js` → `server.url` apunta a producción WP, `webDir: 'www'`.
  - `clients/Mezclador/` → no usa endpoints HTTP directamente; depende de `services/api*.ts` del legado vía paths del tsconfig.
- **Archivos creados:** `clients/README.md` (visión global de los 4 clientes), `Agente/planes/plan-clients-adapters-2026-04-20.md` (plan detallado por cliente con tabla de endpoints, riesgos, orden recomendado y criterios de cierre).
- **Tareas hijas creadas en roadmap:** `174A-108b..111b`. Cada una se ejecutará en sesión dedicada cuando esté listo el endpoint Rust correspondiente.
- **Validacion:** `robocopy` exit ok (excluye build artifacts), `npm run type-check` sigue verde porque `clients/` no entra en `tsconfig.json` del frontend (vive fuera de `frontend/src/`). El backend Rust no compila contra `clients/` (Cargo workspace no los incluye).
- **Sentinel:** no aplica — son archivos importados que no se ejecutan en este repo todavía.
- **GLORY-RS:** no aplica — son clientes de aplicación, no librería reutilizable.
## 174A-108b â scraper: endpoints /api/admin/scraper/{publicar-auto,reporte-lote} + migracion pipeline.py + 
pm run dev corregido (rustls CryptoProvider)
- **Que:** se materializan los dos endpoints Rust que reemplazan al backend WordPress legado del scraper Python. Auth por header X-Kamples-Secret (constant-time, sin sesión). publicar-auto mueve los items de cola_extraccion_samples con estado extraido y sample_id asignado a completado (transición real de la maquina de estados existente). 
eporte-lote recibe el resumen del batch (exitosos/fallidos/recortes/motivos_fallo) y lo loggea estructuradamente con tracing  la persistencia en una tabla utomatizacion_lotes se documenta como sub-tarea futura cuando exista esa migración. Si la env SCRAPER_SECRET no está configurada, ambos responden 403 (deniega por defecto). El cliente clients/kamples-scraper/extractor/pipeline.py se migra a BACKEND_URL + SCRAPER_SECRET + paths /api/admin/scraper/..., manteniendo KAMPLES_INTERNAL_URL/KAMPLES_SITE_URL/KAMPLES_CRON_SECRET como fallback.
- **Bonus crítico:** se arregla el bug que hacía que 
pm run dev no funcionara  el backend panic con rustls 0.23 porque hay múltiples backends crypto en el grafo de deps (aws-lc-rs vía async-stripe + ring vía otras crates). Se añade 
ustls = { version = "0.23", features = ["aws-lc-rs"] } como dep directa y se llama 
ustls::crypto::aws_lc_rs::default_provider().install_default() al arranque de main.rs (antes de que cualquier cliente TLS se inicialice).
- **Por que:** sin estos endpoints, el scraper Python no tiene a quien hablarle en el monorepo Rust y queda muerto. La política "stub que loggea" para 
eporte-lote (vs. inventar SQL) cumple la regla 2 del protocolo: el contrato HTTP existe end-to-end y la UX del scraper no se rompe; añadir tabla nueva sin necesidad inmediata es over-engineering. Para publicar-auto sí hay query real porque la transición extraido -> completado ya está en el schema (migrations/20260417000009_audio_pipeline.up.sql) y es la equivalencia exacta del comportamiento WP.
- **Archivos creados:** src/handlers/scraper_admin.rs.
- **Archivos modificados:** src/lib.rs (campo scraper_secret en AppState), src/config/mod.rs (campo + rom_env() con fallback SCRAPER_SECRET -> KAMPLES_CRON_SECRET), src/handlers/mod.rs (mod + paths OpenAPI + schemas + merge(scraper_admin::routes()) + constructor de AppState), src/main.rs (install rustls CryptoProvider), Cargo.toml (dep directa rustls aws-lc-rs), clients/kamples-scraper/extractor/pipeline.py (URLs + env vars).
- **Validacion:** cargo check ok, cargo clippy -- -D warnings ok, cargo run arranca correctamente (logs muestran "Servidor iniciando en 127.0.0.1:3000" + workers iniciados, sin panic rustls). Pipeline Python no se ejecuta aquí (depende de PostgreSQL real + binarios externos como yt-dlp/ffmpeg)  la migración es solo de URLs/headers/env vars; el comportamiento del cliente queda idéntico al legado.
- **Gotchas:**
  - Block comments anidados en Rust: \ /api/admin/scraper/* \` dentro de un /* */ abre un nivel anidado (porque /* es token de apertura y */ cierra), causando "unterminated block comment". Solución: evitar /* y */ literales dentro de comments  escribir el path como /api/admin/scraper/{publicar-auto,reporte-lote}.
  - SQLx en compile-time (sqlx::query!) requiere DB local viva o cargo sqlx prepare previo. Para evitar esa dependencia se usa sqlx::query() (runtime check)  válido porque el SQL es trivial y no depende de tipado de columnas en Rust.
  - rustls 0.23 cambió el contrato: si hay múltiples providers compilados, **explícitamente** hay que instalar uno antes del primer uso de TLS. El error anterior solo aparece cuando se hace una conexión HTTPS real (no en cargo check).
  - clippy warned sobre s i64: usar .cast_signed() (Rust 1.87+).
- **Sentinel:** sí aplica  añadir regla a Code Sentinel que detecte /* ... /*X*/ ... */ (block comments con / seguido de * o * seguido de / dentro de literales) en archivos .rs. Crear MD en Agente/prevencion/.
- **GLORY-RS:** parcial  el patrón "endpoint admin protegido por header secret + comparación constant-time" es candidato para extraer a glory-rs/backend/middleware/secret_auth.rs cuando aparezca el segundo caso (otro cliente externo). Por ahora vive en el handler para no abstraer prematuramente.

## 174A-109b (parcial) ÔÇö Mezclador: mover a frontend/src/features/mezclador/

- **Que:** se mueve clients/Mezclador/ (104 archivos React+TS) a rontend/src/features/mezclador/ con git mv (preserva historia). Se elimina el 	sconfig.json interno porque ya esta cubierto por el del frontend, que tiene "exclude": ["src/features/**/*"] ÔÇö las features pueden vivir ahi sin compilar hasta que se complete su adaptacion. Se actualiza clients/README.md para reflejar el nuevo destino y se crea rontend/src/features/mezclador/README.md con el plan explicito de adaptacion en 4 sub-fases (174A-109b-fase2..fase5).
- **Por que:** la tarea original ("mover a rontend/src/features/mezclador/ y migrar a hooks Orval") mezclaba dos cosas con tamano muy distinto: el movimiento fisico (trivial, una sesion) y la migracion de imports @app/* + reescritura de fetchers a hooks Orval (multiples sesiones, requiere migrar primero App/React/{components,stores} legacy o re-implementarlos). Se cierra solo la parte fisica para destrabar el espacio y se reagenda la migracion como sub-tarea explicita con criterios claros.
- **Archivos modificados:** clients/README.md. **Archivos creados:** rontend/src/features/mezclador/README.md. **Archivos movidos:** 104 (clients/Mezclador/**/* ÔåÆ rontend/src/features/mezclador/**/*). **Archivo eliminado:** rontend/src/features/mezclador/tsconfig.json (obsoleto en el nuevo hogar).
- **Validacion:** 
pm --prefix frontend run type-check verde (la feature esta excluida del compile global, pero el resto del frontend sigue compilando). El backend Rust no se ve afectado. El movimiento fisico no rompe nada porque el codigo no era importado desde ningun otro lugar del repo todavia (era solo un import legacy WP).
- **Sentinel:** no aplica ÔÇö movimiento fisico de archivos.
- **GLORY-RS:** no aplica ÔÇö Mezclador es codigo de aplicacion especifico.

## 174A-112 + 174A-113 + 174A-114 + 174A-115  Deploy infra (Docker + compose + secrets)

- **Que:** se prepara toda la infraestructura de despliegue del backend Rust + frontend Vite, sin desplegar todavia (174A-116/117/118 siguen bloqueadas por approval explicito del usuario). Incluye:
  - Dockerfile raiz multi-stage (rust:1-bookworm builder + debian:bookworm-slim runtime, usuario no-root uid 1000, healthcheck a /api/health, tini como PID 1, SQLX_OFFLINE=true para no requerir DB en build).
  - rontend/Dockerfile (node:20-alpine builder + nginx:1.27-alpine runtime, codegen Orval condicional, healthcheck a /).
  - rontend/nginx.conf con SPA history fallback, cache inmutable para /assets/, proxy /api/ y /swagger-ui/ hacia pp:3000, soporte WebSocket upgrade para /api/ws.
  - docker-compose.yml con 5 servicios: postgres (pgvector/pg16) + redis (AOF) + app + web + sidecar postgres-backup (pg_dump cada 6h con rotacion 14d). Volumenes NAMED kamples_postgres_data y kamples_redis_data + bind-mounts externos /data/kamples/{uploads,db_dumps,app_logs} como segunda capa anti-perdida.
  - .env.production.example con todas las variables requeridas y opcionales documentadas, comandos de generacion.
  - scripts/backup-postgres.sh (manual on-demand, etiquetado, sin rotacion) y scripts/backup-postgres-loop.sh (sidecar que loopea pg_dump).
  - Agente/documentacion/deploy/secrets-2026-04-20.md con reglas absolutas anti-perdida-de-datos, rotacion de secrets, comandos coolify-manager-rs, restore desde dump.
  - .dockerignore raiz + frontend.
  - .gitignore actualizado para permitir scripts/backup-postgres*.sh.
- **Por que:** la nota de Fase 19 dice literalmente "un error pasado hizo que se borrara la base de datos, hay que reforzar para que no vuelva a suceder". La estrategia implementada es defense-in-depth: (1) volumen NAMED postgres preserva entre restarts, (2) bind-mount al host preserva si el volumen se borra, (3) sidecar pg_dump cada 6h preserva si el host se corrompe, (4) docs explicito a NUNCA usar down -v ni renombrar volumenes, (5) commands de backup MANUAL pre-deploy obligatorios. Se documenta todo, no se asume nada.
- **Archivos:** Dockerfile, .dockerignore, rontend/Dockerfile, rontend/nginx.conf, rontend/.dockerignore, docker-compose.yml, .env.production.example, scripts/backup-postgres.sh, scripts/backup-postgres-loop.sh, .gitignore, Agente/documentacion/deploy/secrets-2026-04-20.md.
- **Validacion:** docker-compose.yml parseado OK con js-yaml (5 services, 2 volumes). cargo check verde (no toca codigo Rust). Frontend type-check verde (no toca codigo TS). Build real de imagenes y deploy a Coolify quedan bloqueados por 174A-116/117/118 (approval explicito).
- **Gotchas:**
  - YAML: las URLs con https:// en default values de variable substitution rompen el parseo si no estan entre comillas. Solucion: "" con quotes.
  - SQLX_OFFLINE=true requiere .sqlx/ snapshot fresco. Cualquier modificacion de sqlx::query! debe ir acompanada de cargo sqlx prepare antes del build de la imagen.
  - User memory deploy-lessons.md: NO usar --mount=type=cache,target=/app/target cuando el contexto viene de git clone (puede ocultar cambios de fuente). Si se usa, Cargo no detecta cambios. Aqui NO se usa.
  - El bind-mount /data/kamples requiere uid 1000 en el host: chown -R 1000:1000 /data/kamples antes del primer arranque.
  - El sidecar postgres-backup NO sustituye un backup off-site; queda anotado como pendiente en la documentacion.
- **Sentinel:** no aplica (archivos de infra, no codigo de produccion).
- **GLORY-RS:** no aplica directamente, aunque a futuro el Dockerfile y docker-compose.yml podrian generalizarse como template del framework con placeholders para nombre de proyecto/db. Por ahora estan acoplados a "kamples" en nombres de volumen y rutas de bind-mount; el dia que se cree un segundo proyecto sobre glory-rs, extraer a glory-rs/templates/deploy/.

## 204A-1 — Home SPA real + auth + dashboard mínimo
- **Que:** la home deja de ser un placeholder y pasa a consumir datos reales del backend Rust. Se añaden rutas SPA para `/auth/login`, `/auth/registro` y `/dashboard`, una cabecera con estado de sesión, formularios reales de login/registro contra Orval, un dashboard mínimo basado en `/api/users/me` + `/api/dashboard/stats`, y una portada pública basada en `/api/samples`. El frontend nuevo recibe además wrappers mínimos `components/ui/Boton` e `Input` para cumplir la convención local de UI atómica sin arrastrar todavía toda la librería legacy.
- **Por que:** el frontend Vite ya tenía contratos y hooks reales, pero la home seguía validando solo `health`. La UI heredada disponible en `frontend/src/features/` y en el repo WordPress todavía depende de aliases `@app/*`, stores y componentes no migrados al frontend nuevo, así que importar 1:1 esas islands en esta fase habría roto el type-check y habría perpetuado deuda. Se conserva 1:1 la capa de datos del backend ya migrado y se reescribe solo la composición visual necesaria para desbloquear una SPA usable hoy.
- **Archivos:** `frontend/src/pages/{HomePage,AuthPage,LoginPage,RegisterPage,DashboardPage}.tsx`, `frontend/src/router.tsx`, `frontend/src/hooks/useAuth.ts`, `frontend/src/components/ui/{Boton,Input}.tsx`, `frontend/src/App.css`, `frontend/src/styles/{tokens,layout,home,auth,dashboard}.css`, `src/services/{email,fcm,push}.rs`, `Agente/documentacion/frontend/spa-home-auth-dashboard-2026-04-20.md`, `roadmap.md`.
- **Validacion:** `npm --prefix frontend run type-check` OK, `npm --prefix frontend run build` OK, `GET http://127.0.0.1:3000/api/health` OK, `GET http://127.0.0.1:3000/api/samples?page=1&per_page=2` OK con datos públicos, `npm run self-check -- -TareaId 204A-1` OK (incluyendo `cargo check`, `cargo clippy --all-targets -- -D warnings` y `cargo test`). Intento de `npm run dev` detectó backend ya levantado en `:3000`; se verificó la UI arrancando solo Vite en `http://127.0.0.1:4173/` contra ese backend existente.
- **Gotchas:** `GET /api/feed` devuelve 401 sin sesión, así que la home pública usa `GET /api/samples` y la autenticada usa `GET /api/me/feed`. Orval tipa éxito/error en la misma unión; para evitar accesos inválidos hubo que estrechar por `status === 200` antes de leer `data`. `get_errors` dejó diagnósticos stale sobre `App.css` con líneas viejas, pero el archivo real quedó reducido a imports y el build final confirmó que no había problema de CSS.
- **Sentinel:** no aplica por ahora; no es un bug de patrón repetible sino una fase de reescritura deliberada del shell SPA.
- **GLORY-RS:** candidato futuro menor. Los wrappers `Boton`/`Input` son agnósticos, pero todavía son demasiado mínimos y están sesgados por reglas locales del workspace; no conviene extraerlos a `glory-rs` hasta que aparezca al menos un segundo uso real.

## 174A-109b-fase2 — Mezclador: capa `@app` compatible + browser sin `wp-json/...`
- **Que:** se añade una capa mínima `frontend/src/app/` para resolver los imports legacy `@app/*` que el Mezclador seguía consumiendo después del port 1:1. Esa capa incluye wrappers UI (`BotonBase`, `Input`, `CampoTexto`, `SelectorBase`, `MenuContextual`), stores mínimas (`panelLateralStore`, `crearModalStore`), tipos (`SampleResumen`) y un adaptador `@app/services/apiExplorador` que reconstruye carpetas/samples del browser usando `GET /api/samples` vía Orval. También se añaden alias en `frontend/tsconfig.json` y `frontend/vite.config.ts`, un `frontend/tsconfig.mezclador.json` para validar la feature de forma aislada y documentación de la adaptación.
- **Por que:** el Mezclador no necesitaba una reescritura completa para destrabarse; necesitaba recuperar compatibilidad con sus contratos inmediatos sin volver a depender del frontend WordPress ni del backend `wp-json/...`. La alias-layer permite mantener la feature casi intacta, compilarla hoy y dejar la migración incremental preparada para la fase de montaje de ruta.
- **Archivos:** `frontend/src/app/{components/ui/*,services/apiExplorador.ts,stores/*,styles/legacyUi.css,types/index.ts}`, `frontend/src/styles/tokens.css`, `frontend/tsconfig.json`, `frontend/tsconfig.mezclador.json`, `frontend/vite.config.ts`, `.vscode/settings.json`, `frontend/src/features/mezclador/README.md`, `Agente/documentacion/frontend/mezclador-compatibilidad-2026-04-20.md`, `roadmap.md`.
- **Validacion:** `get_errors` limpio en todos los archivos nuevos/modificados, `Push-Location frontend; npm exec -- tsc --noEmit -p tsconfig.mezclador.json; Pop-Location` OK, sin referencias activas a `wp-json` en `frontend/src/features/mezclador/**/*.ts?(x)`. El `type-check` global del frontend sigue separado porque `src/features/**/*` continúa excluido del `tsconfig` principal por diseño hasta fases posteriores.
- **Gotchas:** `paths` en TypeScript local siguió requiriendo rutas relativas explícitas (`"./src/app/*"`) cuando se eliminó `baseUrl`; dejar `baseUrl` reabría un warning de deprecación en el editor, así que la configuración final evita ambas cosas. El adaptador de explorador agrupa por `sample.tipo`, suficiente para el browser actual, pero si el backend necesita taxonomía más rica deberá extenderse el adaptador sin reintroducir el stack legacy.
- **Sentinel:** no aplica; fue una migración deliberada de compatibilidad, no un patrón de bug repetible.
- **GLORY-RS:** no aplica por ahora. La capa `@app` existe para absorber deuda específica del port del Mezclador y todavía no es una abstracción reutilizable entre proyectos.
