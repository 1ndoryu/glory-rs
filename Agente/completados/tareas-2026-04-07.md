# Tareas completadas — 2026-04-07

## 064A-72 — Panel lateral de info del visitante en chat
- **Qué:** Panel lateral (tercera columna) en SeccionChat que muestra IP, dispositivo (user-agent parseado), nombre editable, y notas de staff para cada sesión de chat.
- **Archivos backend:**
  - `migrations/20260407001000_chat_visitor_info.up.sql` (NEW): columnas `visitor_ip`, `visitor_user_agent` en `chat_sessions` + tabla `chat_session_notes`
  - `migrations/20260407001000_chat_visitor_info.down.sql` (NEW): rollback
  - `src/models/chat.rs`: `ChatSession` con `visitor_ip`/`visitor_user_agent` (#[sqlx(default)]), `ChatSessionNote`, `ChatSessionResponse` ampliado, `CreateSessionNoteRequest`, `UpdateVisitorNameRequest`
  - `src/models/mod.rs`: nuevos exports
  - `src/repositories/chat.rs`: migrado de `query_as!` a runtime `query_as` para soportar `#[sqlx(default)]`; métodos `list_session_notes`, `create_session_note`, `update_visitor_name`
  - `src/services/chat.rs`: `get_or_create_visitor_session` acepta IP/UA, los guarda en BD
  - `src/handlers/chat.rs`: `ws_visitor` captura IP (X-Forwarded-For/X-Real-IP) y User-Agent; endpoints REST `list_session_notes`, `create_session_note`, `update_visitor_name`; rutas bajo `/api/chat/sessions/:id/notes` y `/api/chat/sessions/:id/visitor-name`
- **Archivos frontend:**
  - `frontend/src/api/chat.ts`: `ChatSession` con `visitor_name`/`visitor_ip`/`visitor_user_agent`; `ChatSessionNote` tipo; funciones `apiListSessionNotes`, `apiCreateSessionNote`, `apiUpdateVisitorName`
  - `frontend/src/components/panel/ChatInfoPanel.tsx` (NEW): panel con parseUserAgent(), notas CRUD con React Query, rename inline
  - `frontend/src/components/panel/ChatInfoPanel.css` (NEW): estilos del panel lateral
  - `frontend/src/components/panel/SeccionChat.tsx`: import ChatInfoPanel, estado showInfo, botón Info en header, render tercera columna
  - `frontend/src/components/panel/SeccionChat.css`: estilos `.chatBtnInfo`
- **Gotchas:**
  - `query_as!` macro no soporta `#[sqlx(default)]` — migrado a runtime `query_as` para todas las queries de ChatSession
  - Clippy `similar_names` para `visitor_ip`/`visitor_id` — permitido con `#[allow]`
  - `handle_visitor_ws` alcanzó 101 líneas — compactada struct init del historial
- **Sentinel:** falso positivo en runtime `sqlx::query_as` — Code Sentinel detecta como error el uso sin macro, pero es intencional para `#[sqlx(default)]`

## 064A-73 — Auditoría de seguridad detallada
- **Qué:** Auditoría OWASP completa del backend Axum. 11 de 14 hallazgos corregidos, 3 diferidos (requieren rediseño complejo).
- **Archivos modificados:**
  - `Cargo.toml`: añadido `governor`, `tower_governor` para rate limiting; `"set-header"` feature en tower-http
  - `src/config/mod.rs`: campo `allowed_origins: Vec<String>` leído de `GLORY_ALLOWED_ORIGINS`
  - `src/handlers/mod.rs`: CORS condicional (dev=any, prod=whitelist), security headers (HSTS, nosniff, frame-deny), rate limiting por `GovernorLayer` (auth 5/min, API 120/min)
  - `src/handlers/deliverables.rs`: path traversal fix (canonicalize+starts_with), magic bytes validation (`validate_magic_bytes()` para 13 MIME types), upload logging
  - `src/handlers/payments.rs`: webhook deduplicación por event_id (tabla+check), audit log webhook
  - `src/handlers/auth.rs`: audit log login success/failed
  - `src/handlers/admin_users.rs`: audit log role change
  - `src/services/audit.rs` (NEW): `AuditService::log()` fire-and-forget
  - `src/services/mod.rs`: export AuditService
  - `src/services/payment.rs`: tolerancia webhook reducida 300s→120s, `tracing::error!` en 4 puntos de fallo Stripe
  - `src/services/ai_chat.rs`: whitelist de 5 modelos Groq permitidos
  - `migrations/20260407002000_stripe_event_dedup.up.sql` (NEW): tabla stripe_processed_events
  - `migrations/20260407003000_audit_log.up.sql` (NEW): tabla audit_log con índices
- **Diferidos para futuras tareas:**
  - WS auth: migrar de query param token a cookie-based o first-message auth
  - JWT refresh tokens: access 15min + refresh 7d (requiere cambio frontend completo)
  - Quick-register password change: flag en users + middleware
- **Plan:** `Agente/planes/plan-auditoria-seguridad-2026-04-07.md` (mover a completados)
- **Gotchas:**
  - `GovernorConfigBuilder` vive en `tower_governor::governor::`, no en raíz del crate
  - Code Sentinel reporta falso positivo en `sqlx::query()` runtime (tablas nuevas sin caché .sqlx)
  - magic bytes SVG: requiere buscar `<svg` en primeros 512 bytes (no binario)
- **Sentinel:** No aplica nueva regla específica

## 064A-59 — Stripe: email en checkout + plan detallado e2e
- **Qué:** Eliminar pregunta de email en checkout Stripe (usar el del usuario logueado) + crear plan detallado de todo lo que falta para Stripe end-to-end.
- **Archivos:**
  - `src/handlers/payments.rs`: obtener email del usuario via `UserRepository::find_by_id`, pasarlo a `initiate_payment`
  - `src/services/payment.rs`: `initiate_payment` acepta `receipt_email: Option<&str>`, `create_stripe_intent` envía `receipt_email` a Stripe
  - `Agente/planes/plan-stripe-e2e-2026-04-07.md` (NEW): plan con 5 pendientes priorizados (P1-P5)
- **Plan identifica:** payment_mode hardcodeado, sin UI phased, captura escrow, testing CLI, dashboard config
- **Gotchas:** Clippy exige backticks en doc comments (`receipt_email` → `` `receipt_email` ``)
- **Sentinel:** No aplica

## 064A-60 — Selección de modo de pago en modal de compra
- **Qué:** Eliminado `payment_mode: 'full'` hardcodeado. Ahora el usuario elige entre 3 modos antes de comprar: pago único (20% desc), 50/50 (10% desc), por fases (0% desc). También corregido OrdenDetalle para que half_half use pagos por fase en vez de pago único.
- **Archivos:**
  - `frontend/src/hooks/useModalCompra.ts`: estado `paymentMode` + `setPaymentMode`, pasado a `apiCreateOrder`
  - `frontend/src/components/servicios/ModalCompra.tsx`: selector visual con 3 radio-cards (nombre, descripción, % descuento)
  - `frontend/src/components/servicios/ModalCompra.css`: estilos `.modalCompraModos`, `.modalCompraModo`, `.modalCompraModoActivo`, etc.
  - `frontend/src/components/panel/OrdenDetalle.tsx`: `isPhased` → `isPerPhasePayment` (incluye half_half), FaseCard recibe flag correcto
- **Gotchas:**
  - Code Sentinel prohíbe `<button>` nativo — usado `<div role="radio">` con ARIA + keyboard handlers
  - El descuento real se calcula en el backend (`discount_for_mode`), frontend solo muestra info orientativa
- **Sentinel:** No aplica

## 064A-61 — Menú hamburguesa mobile rediseñado
- **Qué:** Menú móvil convertido de dropdown absoluto a modal centrado con glassmorphism (estilo chatWidgetPanel). Soporte submenús con transición y botón volver. `accionCabecera` oculto en mobile, acciones de usuario (login, panel, logout, chat) movidas dentro del menú modal.
- **Archivos:**
  - `frontend/src/hooks/useHeader.ts`: estado `subMenuMovil`, `cerrarMenuMovil()` que resetea submenu al cerrar
  - `frontend/src/components/layout/Header.tsx`: nuevo overlay via `<Modal>`, navegación con submenús, acciones inline, import `Modal` y `ArrowLeft`
  - `frontend/src/styles/header.css`: eliminado dropdown mobile viejo, nuevos estilos `.menuMovilPanel` (glassmorphism), `.menuMovilEnlace`, `.menuMovilVolver`, `.menuMovilSeparador`, etc. `accionCabecera` hidden en `≤1024px`
- **Gotchas:**
  - Code Sentinel prohibió overlay artesanal — refactorizado a `<Modal>` del sistema
  - El `<Modal>` ya maneja Escape, focus trap, scroll lock — sin duplicar lógica
  - El Escape del hook ahora diferencia: si hay submenu abierto lo cierra primero, luego cierra el menu
- **Sentinel:** No aplica

## 064A-62 — Tab de configuración en panel admin (seed management)
- **Qué:** Nuevo tab "Configuración" en panel admin con herramientas de desarrollo. Primera herramienta: gestión de datos de prueba (recrear/borrar usuarios test + órdenes + fases). Backend SeedService con lógica extraída de examples/seed_test_data.rs. Frontend con confirmación via Modal.
- **Archivos backend:**
  - `src/services/seed.rs` (NEW): `SeedService` con `delete_test_data()` (cascade: phases→orders→chat→users) y `recreate_test_data()` (2 usuarios test + 4 órdenes variadas). Helpers: `upsert_user`, `get_plans`, `create_seed_order`, `resolve_phase_status`.
  - `src/services/mod.rs`: export `SeedService`
  - `src/handlers/admin_seed.rs` (NEW): `recreate_seed` (POST) y `delete_seed` (DELETE), admin-only. `seed_routes()` → `/api/admin/seed`
  - `src/handlers/mod.rs`: `mod admin_seed`, merge `seed_routes()` en `api_routes()`
- **Archivos frontend:**
  - `frontend/src/api/admin-seed.ts` (NEW): `apiRecreateSeed()`, `apiDeleteSeed()`
  - `frontend/src/components/panel/SeccionConfiguracion.tsx` (NEW): 2 botones con estados loading/success/error, confirmación via Modal
  - `frontend/src/components/panel/SeccionConfiguracion.css` (NEW): estilos `.configSeccion`, `.configBloque`, `.configAcciones`, mensajes feedback
  - `frontend/src/data/panel.ts`: `'configuracion'` añadido a `SeccionPanel`, tab en `TABS_ADMIN`
  - `frontend/src/components/panel/SidebarPanel.tsx`: icono `Settings` en `ICONOS_SECCION`
  - `frontend/src/islands/PanelIsland.tsx`: case `'configuracion'` → `<SeccionConfiguracion />`
- **Gotchas:**
  - Props de `<Modal>` y `<Button>` usan nombres en español: `abierto`/`onCerrar`, `variante="secundario"`
  - `SeedService` usa runtime `sqlx::query` (sin macro) para evitar dependencia de `.sqlx/` cache — intencional
- **Sentinel:** No aplica

## 064A-32 — Hosting + dominio panel cliente: página funcional
- **Qué:** Reemplazado placeholder de `/soluciones/hosting` con página funcional. Hero + sección de features (6 cards) + grid de 4 planes de hosting con precios. CTA abre chat (provisioning automático bloqueado por infra). Panel de cliente ahora muestra tab "Mi Hosting" con suscripciones propias. SeccionHosting refactorizada a role-aware (admin: crear/cambiar status, cliente: solo lectura). Plan de compra de dominios creado como documento en planes/.
- **Archivos:**
  - `frontend/src/data/planes/planes-hosting.ts` (NEW): 4 planes (Básico, Pro, E-commerce, Custom) con características y precios alineados con backend
  - `frontend/src/islands/SolucionHostingIsland.tsx` (NEW): página completa con hero, features grid, planes grid, contacto
  - `frontend/src/islands/SolucionHostingIsland.css` (NEW): estilos responsive (hero, features, cards)
  - `frontend/src/App.tsx`: ruta `/soluciones/hosting` apunta a SolucionHostingIsland
  - `frontend/src/components/panel/SeccionHosting.tsx`: refactorizado role-aware (isAdmin check), oculta crear/status-change para clientes

## 064A-64 — Traducción de contenido de negocio a 3 idiomas
- **Qué:** Sistema de traducción automatizado para contenido de negocio (servicios, equipo, planes, soluciones, proyectos) en los 3 idiomas soportados (es, en, ja). Se creó contentTranslations.ts con ~300 cadenas × 3 idiomas, registradas via i18n addResourceBundle. Componentes actualizados para usar t('content.*') con fallback al texto español original.
- **Archivos:**
  - `frontend/src/i18n/contentTranslations.ts` (NEW): traducciones es/en/ja para servicios (5), equipo (3), planes (25), soluciones (3), proyectos (5) — ~250 líneas
  - `frontend/src/i18n.ts`: importa y registra contentEs/En/Ja via addResourceBundle
  - `frontend/src/locales/es.json`: sección hosting_page agregada
  - `frontend/src/locales/en.json`: sección hosting_page agregada
  - `frontend/src/locales/ja.json`: sección hosting_page agregada
  - `frontend/src/components/ui/ServiceCard.tsx`: traduce titulo/descripcion via t('content.services.{id}.*')
  - `frontend/src/islands/ServicioIndividualIsland.tsx`: lookup por slug → traducción del hero
  - `frontend/src/islands/NosotrosIsland.tsx`: TarjetaMiembro traduce bio/cargo via t('content.team.{id}.*')
  - `frontend/src/components/servicios/SeccionPlanesServicio.tsx`: traduce nombre/descripcion/features/cta de planes
  - `frontend/src/islands/SolucionHostingIsland.tsx`: traduce hero, features, planes de hosting
  - `frontend/src/islands/ProyectosIsland.tsx`: traduce descripcion/cliente de proyectos
- **Gotchas:**
  - react-i18next TS types no aceptan `t(key, string | undefined)` como segundo arg — usar `{defaultValue: x ?? ''}` en su lugar
  - La navegación ya estaba traducida (NAV_KEYS, CAT_KEYS, FOOTER_NAV_KEYS usan t() con claves del locale JSON)
  - El contenido original español queda como fallback en contentEs — duplica los datos de los archivos TS pero garantiza que t() siempre retorne algo
- **Sentinel:** No aplica

## 064A-43 — VarSense shadow detection rule
- **Qué:** Nueva regla de detección de propiedades CSS prohibidas en VarSense. Detecta `box-shadow` (y cualquier propiedad configurable) como warning. Implementado como feature genérico "banned properties" con configuración: habilitado/severidad/lista de propiedades. Integrado en el parser CSS existente, diagnosticProvider, configService y package.json.
- **Archivos (varsense repo):**
  - `src/types/index.ts`: `BannedPropertyUsage`, `BannedPropertyConfig` interfaces, `PropiedadProhibida` en DiagnosticType, `deteccionPropiedadesProhibidas` en ExtensionConfig, `propiedadesProhibidas` en ParseResult
  - `src/services/configService.ts`: default config con `box-shadow`, método `obtenerConfigProhibidas()`, `cargarConfigProhibidas()`
  - `src/parsers/cssParser.ts`: método `detectarPropiedadesProhibidas()` que recorre reglas/declaraciones y compara contra set de propiedades prohibidas
  - `src/providers/diagnosticProvider.ts`: loop de creación de diagnósticos para propiedades prohibidas con mensaje descriptivo
  - `package.json`: schema para `bannedProperties.enabled`, `bannedProperties.severity`, `bannedProperties.properties`
- **Gotchas:** El proyecto Nakomi tiene 11 usos de `box-shadow` en CSS fuente. Los del chat (ChatWidget.css, OrderChat.css) son excepciones válidas que se suprimen con `/* varsense-disable-line */`.
- **Sentinel:** No aplica (es una mejora de VarSense, no de Code Sentinel)

## 064A-51 — Datos de prueba para hosting panel
- **Qué:** Extendido SeedService para crear 3 suscripciones de hosting en diferentes estados (basico/active, pro/provisioning, ecommerce/suspended) con dominios y eventos asociados. Delete cascade actualizado para limpiar hosting_events → hosting_subscriptions antes de users.
- **Archivos:** `src/services/seed.rs` (modificado: +70 líneas)
- **Gotchas:** Clippy exigió type alias para tupla de 6 elementos (`HostingSeedEntry`). `serde_json::json!` ya estaba disponible como dependencia transitiva.
- **Sentinel:** No aplica

## 064A-45 — Hosting doc pendientes (actualización status)
- **Qué:** Actualizado `status-hosting-administrado` con el estado real al 2026-04-07. Renombrado de -04-06 a -04-07. Incorporados avances de 064A-32 (página pública funcional, panel cliente), 064A-51 (seed data 3 suscripciones), 064A-64 (i18n 3 idiomas). Actualizada tabla de archivos clave, tachados pendientes ya completados, actualizada referencia en roadmap.
- **Archivos:** `Agente/documentacion/hosting/status-hosting-administrado-2026-04-07.md` (renombrado+actualizado), `roadmap.md` (referencia actualizada, tarea removida)
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica

## 074A-4 — Rediseño botones primario/secundario
- **Qué:** Primario ahora usa `var(--bg-warm)` (#e8e6e2) beige cálido + `var(--text-primary)`. Secundario usa `var(--brand-black)` + `var(--bg-primary)`. Se crearon variables CSS `--bg-warm` y `--bg-warm-hover` en variables.css. ChatWidget.css también migrado de hardcoded a variables.
- **Archivos:** frontend/src/styles/variables.css, frontend/src/components/ui/Button.css, frontend/src/components/chat/ChatWidget.css
- **Gotchas:** ChatWidget.css tuvo pérdida accidental de líneas al editar (color, border, cursor, z-index), se restauraron.
- **Sentinel:** No aplica.

## 074A-5 — Quitar font-weight de badges
- **Qué:** Eliminado `font-weight: 600` de `.usuariosRolBadge` y `font-weight: 500` de `.usuariosStatusBadge`.
- **Archivos:** frontend/src/components/panel/SeccionUsuarios.css
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-1 — Navegación "regresar" (no logout)
- **Qué:** Eliminada la redirección `/` → `/panel` para usuarios logueados (HomeOrPanel removido de App.tsx). Botón "Salir" (logout) eliminado del Header desktop y móvil. Logout movido al SidebarPanel con icono LogOut y estilo sidebarLogout (empujado al fondo con margin-top auto). Limpieza de imports muertos en App.tsx, Header.tsx, useHeader.ts.
- **NOTA:** El usuario reportó que esto creó 2 botones de logout. Se corregirá en 074A-22 moviendo el logout a un submenú del avatar.
- **Archivos:** frontend/src/App.tsx, frontend/src/components/layout/Header.tsx, frontend/src/hooks/useHeader.ts, frontend/src/components/panel/SidebarPanel.tsx, frontend/src/components/panel/SidebarPanel.css
- **Gotchas:** El import `Navigate` en App.tsx y `logout` en useHeader.ts quedaron huérfanos tras eliminar HomeOrPanel y el botón logout — importantes de limpiar.
- **Sentinel:** No aplica.

## 074A-17 — Fix reembolso 404
- **Qué:** Todas las URLs en frontend/src/api/refunds.ts carecían del prefijo `/api`. Ej: `/refunds` → `/api/refunds`.
- **Archivos:** frontend/src/api/refunds.ts
- **Gotchas:** Todas las demás APIs tenían el prefijo, esta fue una omisión al crear el módulo de refunds.
- **Sentinel:** No aplica.

## 074A-15+074A-16 — Padding innecesario
- **Qué:** Eliminado padding de `.hostingFormCrear` y `.usuariosContenedor` — `.panelContenido` ya proveía padding.
- **Archivos:** frontend/src/components/panel/SeccionHosting.css, frontend/src/components/panel/SeccionUsuarios.css
- **Sentinel:** No aplica.

## 074A-18 — Ocultar chatWidgetBubble en /panel
- **Qué:** ChatWidget.tsx ahora retorna null si `location.pathname.startsWith('/panel')`. El panel tiene SeccionChat propia.
- **Archivos:** frontend/src/components/chat/ChatWidget.tsx
- **Sentinel:** No aplica.

## 074A-19+074A-20 — Chat input pill-style + títulos consistentes
- **Qué:** chatInputArea rediseñado con border-radius 104px, send button overlaid (position absolute). Títulos "Conversaciones", "Chat general", "Info del visitante" normalizados: font-size var(--text-sm), font-weight 600, color var(--brand-black), flex 1. Eliminado uppercase y letter-spacing de chatListaHeader h3 y chatInfoTitulo.
- **Archivos:** frontend/src/components/panel/SeccionChat.css, frontend/src/components/panel/ChatInfoPanel.css
- **Sentinel:** No aplica.

## 074A-21 — Info visitante layout compacto
- **Qué:** chatInfoSeccion cambiado de column a row (label y valor en misma línea). Labels min-width 80px. Notas separadas con border-top. Eliminado uppercase/letter-spacing de labels.
- **Archivos:** frontend/src/components/panel/ChatInfoPanel.css
- **Sentinel:** No aplica.

## 074A-22 — Glory-RS Content Fixture System (Fases 1-4)
- **Qué:** Sistema declarativo de fixtures TOML en glory-rs para inyectar datos de prueba/iniciales al arrancar el backend. Parser TOML, sync con hash-based change detection, resolución de FK entre fixtures (`@table:record_id`), limpieza de huérfanos, tabla de tracking `_glory_fixtures`. 14 fixtures creados: 3 users, 5 projects, 3 team_members, 3 hosting_subscriptions.
- **Archivos:**
  - `glory-rs/backend/src/fixtures/mod.rs` (NEW): ContentManager, SyncReport, FixtureError, ensure_tracking_table, sync_all, clean_orphans, topological_sort
  - `glory-rs/backend/src/fixtures/parser.rs` (NEW): FixtureMeta, FixtureFile, parse_file con validación
  - `glory-rs/backend/src/fixtures/sync.rs` (NEW): sync_fixture, resolve_fk_references, process_record (password hashing, UUID detection, TOML→JSON), build_upsert_sql (ON CONFLICT), sanitize_identifier
  - `glory-rs/backend/Cargo.toml`: renombrado package a `glory-rs`, añadido toml+sha2
  - `glory-rs/backend/src/lib.rs`: pub mod fixtures
  - `Cargo.toml`: añadido dependency `glory-rs = { path = "glory-rs/backend" }`
  - `src/main.rs`: integración ContentManager con password hasher argon2 post-migrations
  - `content/users.toml` (NEW): admin, cliente, empleado con plain: passwords
  - `content/projects.toml` (NEW): 5 proyectos con skills JSONB
  - `content/team_members.toml` (NEW): 3 miembros del equipo
  - `content/hosting.toml` (NEW): 3 suscripciones con FK @users:cliente@test.com
  - `migrations/20260407100000_hosting_domain_unique.up.sql` (NEW): UNIQUE en hosting_subscriptions.domain
  - `migrations/20260407100000_hosting_domain_unique.down.sql` (NEW): rollback
- **Gotchas:** argon2::password_hash::Error no impl std::error::Error (usar .to_string().into()); glory-rs es gitignored en parent (no submodule registrado); PgArguments requiere bind_arg genérico por tipo.
- **Sentinel:** No aplica
- **GLORY-RS:** Todo el sistema de fixtures vive en glory-rs/backend/src/fixtures/ — es 100% agnóstico y reutilizable en cualquier proyecto.
## 074A-24  Error 500 al pagar: mejor manejo de errores Stripe
- **Que:** El usuario veia 'Request failed with status code 500' al intentar pagar. Causa: (1) frontend solo mostraba el mensaje generico de Axios, no extraia el body del response. (2) backend clasificaba TODOS los errores Stripe como Internal(500) con mensaje generico 'Ocurrio un error interno'.
- **Archivos:**
  - `src/services/payment.rs`: nuevo metodo `classify_stripe_error` que parsea JSON de Stripe y clasifica: authentication_error=>Internal, invalid_request/card_error=>BadRequest (mensaje visible), rate_limit=>BadRequest, api_error=>Internal generico.
  - `src/handlers/payments.rs`: user_email fetch ya no silencia errores con `.ok()`  usa match con tracing::warn.
  - `frontend/src/components/panel/CheckoutModal.tsx`: catch extrae `err.response?.data?.message` de Axios, no solo `err.message`.
- **Gotchas:** La causa raiz exacta del 500 de Stripe depende de la cuenta/config  con estos cambios el usuario vera el mensaje real de Stripe en vez de generico 500.
- **Sentinel:** no aplica

## 074A-23  Editar perfil (display_name y campos extendidos)
- **Que:** El usuario no podia cambiar su nombre despues de registrarse. El frontend ya tenia el formulario completo pero handleGuardar era un stub (TO-DO). Se implemento todo el flujo backend + frontend.
- **Archivos:**
  - `migrations/20260408000000_profile_social.up.sql` (NEW): ADD linkedin, twitter, website a user_profiles
  - `src/models/user.rs`: nuevo `UpdateProfileRequest` con validacion
  - `src/models/mod.rs`: export UpdateProfileRequest
  - `src/repositories/user.rs`: nuevo `update_profile`  UPDATE users.display_name + UPSERT user_profiles
  - `src/handlers/profile.rs`: PATCH /api/profile handler con validacion
  - `frontend/src/api/profile.ts`: nueva funcion `actualizarPerfil` + tipo UpdateProfilePayload
  - `frontend/src/hooks/usePerfil.ts`: handleGuardar conectado al backend, estados guardando/errorGuardar
  - `frontend/src/components/panel/SeccionPerfil.tsx`: boton muestra loading, muestra errores
  - `frontend/src/components/panel/SeccionPerfil.css`: clase .perfilGuardadoError
  - `.sqlx/`: cache regenerado con cargo sqlx prepare
- **Gotchas:** Las migraciones pendientes (chat_visitor_info, stripe_event_dedup, audit_log) tambien se aplicaron a la BD local durante el proceso. Los .sqlx se regeneraron completos (CRLF normalization).
- **Sentinel:** no aplica

## 074A-28 — Botón enviar chat sin background
- **Qué:** Cambiado el botón de enviar en SeccionChat de `variante="primario"` a `variante="texto"` para eliminar el fondo del botón.
- **Archivos:** `frontend/src/components/panel/SeccionChat.tsx`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-29 — border-color brand-black → border-default
- **Qué:** Reemplazados todos los `border-color: var(--brand-black)` y `border: Xpx solid var(--brand-black)` por `var(--border-default)` en 16 archivos CSS (18 ocurrencias). Bordes ahora usan token semántico en vez de color absoluto.
- **Archivos:** ChatInfoPanel.css, header.css, Button.css (.botonOutline), ModalAutenticacion.css, ModalTestimonio.css (×2), ContactoIsland.css, SeccionChat.css, NotFoundIsland.css, SeccionPlanesServicio.css, SeccionMetodosPago.css, SolucionPlaceholderIsland.css, SeccionPagos.css (×2), SeccionPerfil.css, SeccionProyectos.css, SeccionUsuarios.css (×2)
- **Gotchas:** Archivos con múltiples coincidencias (SeccionUsuarios, ModalTestimonio, SeccionPagos) requirieron replace individual con contexto expandido.
- **Sentinel:** No aplica.

## 074A-31 — sidebarItem margin-left
- **Qué:** Agregado `margin-left: -1rem` y `width: calc(100% + 1rem)` a `.sidebarItem` para que los items se extiendan al borde izquierdo del sidebar.
- **Archivos:** `frontend/src/components/panel/SidebarPanel.css`
- **Gotchas:** Necesario ajustar width para compensar el margin negativo y mantener el ancho correcto.
- **Sentinel:** No aplica.

## 074A-25 — Avatar dropdown visible en mobile + quitar logout directo
- **Qué:** En mobile (≤1024px), `.accionCabecera` se ocultaba completo con `display:none`, haciendo el avatar dropdown invisible. El menú hamburguesa tenía un logout directo que deslogeaba sin submenú. Ahora: (1) En mobile solo se ocultan `.enlaceAcceder` y `.botonHeader` dentro de accionCabecera, dejando el avatar visible. (2) El hamburger usa `order:10` para quedar al final del flex. (3) Se eliminó el logout directo del menú hamburguesa — el logout ahora es exclusivamente vía avatar dropdown.
- **Archivos:** `frontend/src/styles/header.css` (media query), `frontend/src/components/layout/Header.tsx` (eliminar div logout del menú móvil)
- **Gotchas:** El avatar funciona en desktop y mobile porque el JS es el mismo (MenuContextual con onToggle). El CSS es lo único que cambió para controlar visibilidad.
- **Sentinel:** No aplica.

## 074A-26 — Badge clipping en carrusel
- **Qué:** Los badges del carrusel se recortaban al redimensionar porque `.carruselContenedorPrincipal` usa `overflow: hidden` (necesario para el slide). Agregado `padding-bottom: var(--spacing-xl)` para dar espacio.
- **Archivos:** `frontend/src/components/home/CarruselShowcase.css`
- **Gotchas:** No se puede usar `overflow-y: visible` con `overflow-x: hidden` en CSS (computa a `auto`). Padding-bottom es la solución más fiable.
- **Sentinel:** No aplica.

## 074A-30 — Filtrar chats vacíos del panel
- **Qué:** Sesiones de chat sin mensajes aparecían en el listado del panel. Agregado `AND EXISTS (SELECT 1 FROM chat_messages WHERE session_id = chat_sessions.id)` a las queries `list_active_sessions` y `list_sessions_for_user` en el repositorio.
- **Archivos:** `src/repositories/chat.rs`
- **Gotchas:** Se filtra a nivel SQL (no frontend) para no transferir datos innecesarios. Ambas queries (admin y usuario) fueron actualizadas.
- **Sentinel:** No aplica.

## 074A-27 — Stripe 500 follow-up: mensaje visible al usuario
- **Qué:** `classify_stripe_error` usaba `AppError::Internal` para `authentication_error` y el caso default. `AppError::Internal` siempre retorna "Ocurrió un error interno" al cliente (oculta el mensaje por seguridad). Cambiado TODO a `AppError::BadRequest` porque los mensajes ya son user-safe ("Error de configuración de pagos — contacta soporte", etc.). El error raw de Stripe ya se logea en servidor via `tracing::error!` antes de llamar a classify.
- **Archivos:** `src/services/payment.rs`
- **Gotchas:** La causa raíz del error Stripe sigue siendo configuración de cuenta/claves — este fix solo asegura que el usuario VEA un mensaje descriptivo en vez de genérico 500.
- **Sentinel:** No aplica.

## 074A-32 — Foto de perfil en chat panel y order chat
- **Qué:** SeccionChat mostraba ícono User genérico cuando sender_avatar_url era null, pero el sidebar mostraba DEFAULT_PROFILE_AVATAR SVG. Ahora el chat usa el mismo fallback. Además, OrderChat no tenía avatares — agregados con layout avatar+contenido.
- **Archivos:** SeccionChat.tsx (import DEFAULT_PROFILE_AVATAR, reemplazar fallback), OrderChat.tsx (avatar img + wrapper contenido, eliminar prop employeeName), OrderChat.css (estilos avatar 28px circular), OrdenDetalle.tsx (quitar prop employeeName)
- **Gotchas:** El campo sender_avatar_url en la BD puede ser null si el usuario nunca subió foto. El fallback SVG de useCurrentProfile resuelve esto visualmente.
- **Sentinel:** No aplica.

## 074A-33 — orderChatPanel sin sombra, min-height, sin encabezado
- **Qué:** (1) Eliminada box-shadow del panel. (2) Cambiado max-height a min-height: 440px. (3) Eliminado orderChatEncabezado y su CSS. (4) Eliminada prop employeeName ya innecesaria.
- **Archivos:** OrderChat.css, OrderChat.tsx
- **Gotchas:** El auto-scroll al final ya existía via messagesEndRef useEffect.
- **Sentinel:** No aplica.

## 074A-34 — Borde visible en botón suscribir footer
- **Qué:** .botonFooter usaba `border-color: var(--bg-item-active)` que es `rgba(0,0,0,0.06)` — invisible sobre fondo oscuro. Cambiado a `var(--bg-primary)` que contrasta contra el footer negro.
- **Archivos:** Footer.css
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-35 — botonFooter border-color corregido a --white-20
- **Qué:** El usuario corrigió que el borde en fondo oscuro debe usar `var(--white-20)` (rgba 255,255,255,0.2), no `var(--bg-primary)`. Regla: bordes sobre fondos oscuros siempre `--white-20`.
- **Archivos:** Footer.css
- **Gotchas:** `--bg-primary` es blanco sólido — demasiado brillante para un borde sutil en fondo oscuro. `--white-20` es el estándar.
- **Sentinel:** No aplica.

## 074A-36 — CTA planes abre chat en vez de ModalCompra
- **Qué:** El botón "Comenzar" en tarjetas de plan ahora llama `useChatStore.abrir()` directamente. Se eliminó ModalCompra del componente, el estado `planSeleccionado`, y el botón "Conversar" redundante.
- **Archivos:** SeccionPlanesServicio.tsx
- **Gotchas:** ModalCompra.tsx sigue existiendo como componente — no se eliminó del filesystem porque podría usarse en el futuro.
- **Sentinel:** No aplica.

## 074A-37 — orderChatBurbuja--propia layout corregido
- **Qué:** Mensajes propios en OrderChat ahora usan `flex-direction: row-reverse` para poner el avatar a la derecha. Contenido del mensaje se alinea a la derecha con `align-items: flex-end`.
- **Archivos:** OrderChat.css
- **Gotchas:** El usuario quiere además que NO se muestre avatar propio → tarea 074A-38.
- **Sentinel:** No aplica.
## 074A-38  No mostrar avatar propio en OrderChat
- **Qué:** En OrderChat, solo se renderiza avatar para mensajes del otro usuario (!esPropio). Mensajes propios no muestran avatar.
- **Archivos:** OrderChat.tsx
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-39  Fotos de perfil en chat panel no se veían
- **Qué:** El servidor devuelve sender_avatar_url correctamente en la query enriquecida de mensajes. El problema era que el servidor estaba corriendo código viejo sin la enrichment query.
- **Archivos:** Sin cambios  resuelto con rebuild del servidor.
- **Gotchas:** Siempre reconstruir el backend después de cambios en código Rust.
- **Sentinel:** No aplica.

## 074A-40  chatInfoNotaInput: textarea sin botón de enviar
- **Qué:** Reemplazado Input+Button por Textarea en ChatInfoPanel. Enter envía, Shift+Enter nueva línea.
- **Archivos:** ChatInfoPanel.tsx
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-41  Info visitante IP + fix ConnectInfo (GovernorLayer)
- **Qué:** Añadido ConnectInfo<SocketAddr> a ws_visitor para capturar IP real del socket como fallback cuando no hay proxy headers. Además, corregido main.rs para usar into_make_service_with_connect_info<SocketAddr>()  sin esto, GovernorLayer (PeerIpKeyExtractor) devolvía "Unable To Extract Key!" en TODAS las rutas /api/*.
- **Archivos:** src/handlers/chat.rs, src/main.rs
- **Gotchas:** CRÍTICO  tower_governor 0.4 usa PeerIpKeyExtractor por defecto que requiere ConnectInfo. axum::serve(listener, app) NO lo proporciona. Esta era la causa raíz de los 500 masivos en toda la API.
- **Sentinel:** Considerar regla: si se usa GovernorLayer, main.rs debe tener into_make_service_with_connect_info.

## 074A-42  AI chat siempre respondía el mismo saludo
- **Qué:** El servidor estaba corriendo un binario viejo que no cargaba las claves GROQ desde .env. Con el rebuild, AiChatConfig::from_env() carga las 3 claves GROQ correctamente y genera respuestas dinámicas.
- **Archivos:** Sin cambios  resuelto con rebuild del servidor.
- **Gotchas:** Las claves GROQ_API_1/2/3 estaban configuradas en .env pero el binario viejo no las usaba.
- **Sentinel:** No aplica.

## 074A-43  Chat panel: max-width, carga diferida, botón cargar anteriores
- **Qué:** Añadido max-width: 800px al contenedor de mensajes. useChat ahora acepta limit paramétrico. useSeccionChat tiene messageLimit con botón "Cargar anteriores" que incrementa de 100 en 100.
- **Archivos:** SeccionChat.css, SeccionChat.tsx, useSeccionChat.ts, useChat.ts
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-44  Stripe 500 resuelto
- **Qué:** El error 500 en POST .../pay era causado por GovernorLayer (ver 074A-41). Con into_make_service_with_connect_info, el rate limiter extrae la IP correctamente y los endpoints de pago funcionan (200 con client_secret de Stripe).
- **Archivos:** Sin cambios adicionales (fix en 074A-41).
- **Gotchas:** El classify_stripe_error de 074A-27 también contribuyó: antes, errores de Stripe se clasificaban mal como Internal.
- **Sentinel:** No aplica.

## 074A-45  Botón Salir  Inicio sin desloguear + submenú en avatar
- **Qué:** El botón "Salir" en el panel ahora navega a "/" sin desloguear al usuario (es un GloryLink a Inicio). El avatar (headerPanelAvatar) ahora es un button que abre un submenú dropdown con la opción "Cerrar sesión" (el logout real). Click fuera cierra el menú.
- **Archivos:** HeaderPanel.tsx, HeaderPanel.css
- **Gotchas:** Code Sentinel sugirió usar <MenuContextual> pero no existe aún. Se usó dropdown artesanal con useRef/useEffect.
- **Sentinel:** Falso positivo: sugiere <MenuContextual> que no existe en el proyecto.

## 074A-46  Eliminar clase .botonMarca y variante 'marca'
- **Qué:** Clase CSS .botonMarca eliminada de Button.css (código muerto con propiedades comentadas). Variante 'marca' eliminada del tipo ButtonVariante en Button.tsx. Uso en OrdenDetalle.tsx cambiado a 'primario'.
- **Archivos:** Button.css, Button.tsx, OrdenDetalle.tsx
- **Gotchas:** Había un uso activo en OrdenDetalle.tsx que TypeScript detectó al eliminar la variante.
- **Sentinel:** No aplica.


## 074A-2 — Seed data enriquecido
- **Qué:** Añadido seed de notificaciones (6), reviews (1 con respuesta), chat (5 mensajes), activity log (5 entradas). Corregido orden de FK en limpieza (refunds → payments → deliverables → etc). Corregida ruta duplicada `/api/api/admin/seed` → `/admin/seed`.
- **Archivos:** `src/services/seed.rs`, `src/handlers/admin_seed.rs`
- **Gotchas:** FK violations por orden de DELETE inadecuado. Ruta admin/seed tenía `/api/` duplicado. Admin effective_role era "client" → 403 (requirió switch-role).
- **Sentinel:** No aplica.

## 074A-47 — headerPanelSubmenu visual
- **Qué:** Cambiado `<Button variante="texto">` por `<button>` nativo en el submenú del avatar. El componente Button añadía clases `botonBase botonTexto botonMediano` que interferían con los estilos del submenú.
- **Archivos:** `frontend/src/components/panel/HeaderPanel.tsx`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-48 — chatMensajes max-width/overflow
- **Qué:** Añadido `overflow-x: hidden` a `.chatAreaMensajes` y `overflow-wrap: break-word` + `word-break: break-word` a `.chatBurbuja` para evitar que URLs/palabras largas rompan el layout.
- **Archivos:** `frontend/src/components/panel/SeccionChat.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-49 — Refund + Review routes 404 (doble /api/)
- **Qué:** Las rutas de `refunds.rs` y `reviews.rs` definían paths con `/api/` pero se mergean dentro de `api_routes()` que ya está bajo `.nest("/api", ...)`. Resultado: `/api/api/orders/:id/refund` → 404. Removido el prefijo `/api/` de las rutas.
- **Archivos:** `src/handlers/refunds.rs`, `src/handlers/reviews.rs`
- **Gotchas:** Solo refunds y reviews tenían este bug; el resto de handlers estaban correctos.
- **Sentinel:** Podría detectarse: regla que busque `.route("/api/` en archivos dentro de handlers/ que se mergean en api_routes.

## 074A-50 — Orders GET 403 para admin con switch-role
- **Qué:** Cuando admin switcheaba effective_role a "client" o "employee", los handlers de `get_order`, `list_payments` y `verify_order_access` (deliverables) usaban `auth.effective_role` para verificar ownership, pero `auth.user_id` seguía siendo el del admin (no coincide con ningún order.client_id). Fix: si `auth.role == Admin`, bypass completo de ownership check. `list_orders` ya tenía este fix (064A-58), faltaba en otros handlers.
- **Archivos:** `src/handlers/orders.rs`, `src/handlers/payments.rs`, `src/handlers/deliverables.rs`
- **Gotchas:** El mismo patrón defectuoso estaba en 3 handlers diferentes.
- **Sentinel:** Regla: buscar `match auth.effective_role` sin un guard previo de `auth.role == Admin`.

## 074A-51 — Entregables sin upload de archivos
- **Qué:** Eliminada toda la UI de upload de archivos (.entregablesUpload, file picker, file list, notes textarea). El botón "Entregar" ahora marca la fase como entregada sin requerir archivos. Los archivos se comparten por chat. Botón cambiado de variante "exito" a "primario".
- **Archivos:** `frontend/src/components/panel/EntregablesPanel.tsx`, `EntregablesPanel.css`, `frontend/src/hooks/useEntregablesPanel.ts`, `frontend/src/hooks/useDeliverables.ts`, `frontend/src/api/deliverables.ts`
- **Gotchas:** Clases CSS de file list/item/name/size todavía se usan en el historial de revisiones — no borrar.
- **Sentinel:** No aplica.

## 074A-52 — Chat scroll interno en OrderChat
- **Qué:** `scrollIntoView({behavior: 'smooth'})` en OrderChat movía el scroll de toda la página en vez del contenedor de mensajes. Cambiado a `el.scrollTop = el.scrollHeight` usando ref al contenedor `.orderChatMensajes`.
- **Archivos:** `frontend/src/components/panel/OrderChat.tsx`
- **Gotchas:** `ChatWidget.tsx` también usa scrollIntoView pero funciona porque está en un floating panel con posición fija.
- **Sentinel:** No aplica.

## 074A-6 — CMS Infra: Upload endpoint + UploadImage + SlugInput
- **Qué:** Backend: endpoint `POST /api/admin/uploads` (admin-only, multipart, whitelist MIME imágenes, max 5 MB, guarda en `uploads/content/` con UUID). Frontend: API client `uploads.ts`, componente `UploadImage` (click/drag, preview, eliminar), componente `SlugInput` (auto-genera slug desde título, edición manual, reset).
- **Archivos:** `src/handlers/uploads.rs` (nuevo), `src/handlers/mod.rs`, `frontend/src/api/uploads.ts` (nuevo), `frontend/src/components/ui/UploadImage.tsx` + `.css` (nuevos), `frontend/src/components/ui/SlugInput.tsx` + `.css` (nuevos)
- **Gotchas:** Clippy requirió inlining de format args. El upload guarda la extensión original del archivo (seguro porque el MIME ya está whitelisted).
- **Sentinel:** No aplica.

## 084A-1 — Impersonación real en switch-role + filtros admin
- **Qué:** Rediseño completo del switch-role. En vez de solo cambiar `active_role` del admin, ahora el backend busca un usuario real con el rol objetivo y genera un JWT con su `user_id` + claim `impersonator` apuntando al admin. Al volver a admin, restaura su token original. Frontend: authStore actualiza userId al impersonar, SidebarPanel muestra badge "Impersonando" y permite switch incluso cuando está impersonando. SeccionProyectos: admin ve empleado responsable en card footer, buscador por servicio y filtro por empleado (solo admin). Removido override admin de `list_orders` (064A-58) — la impersonación resuelve el filtrado naturalmente.
- **Archivos:** `src/services/auth.rs`, `src/middleware/auth.rs`, `src/models/user.rs`, `src/repositories/user.rs`, `src/handlers/orders.rs`, `.sqlx/`, `frontend/src/api/auth.ts`, `frontend/src/stores/authStore.ts`, `frontend/src/components/panel/SidebarPanel.tsx` + `.css`, `frontend/src/components/panel/SeccionProyectos.tsx` + `.css`
- **Gotchas:** 074A-50 fix sigue compatible: `auth.role` es ahora el rol real del usuario impersonado. Endpoints admin-only rechazan sesiones impersonadas correctamente.
- **Sentinel:** No aplica.

## 084A-2 — Chat abierto por defecto debajo del historial
- **Qué:** `chatAbierto` cambiado de `useState(false)` a `useState(true)`. Chat movido de posición inline (después de ordenInfoCard) a después de ordenHistorialCard. El toggle en el header sigue disponible para cerrar/abrir.
- **Archivos:** `frontend/src/components/panel/OrdenDetalle.tsx`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 084A-3 — Scrollbar minimalista global
- **Qué:** Estilos globales de scrollbar: `scrollbar-width: thin` (Firefox), `::-webkit-scrollbar` 5px (Chrome/Safari), thumb translúcido sobre fondo transparente. Variables `--scrollbar-thumb` y `--scrollbar-thumb-hover` en `variables.css`.
- **Archivos:** `frontend/src/styles/init.css`, `frontend/src/styles/variables.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-7 — CMS Infra: RichTextEditor + SeccionContenido base
- **Qué:** Instalado tiptap (v3.22.2) con extensiones: StarterKit, Image, Link, Placeholder. Creado `RichTextEditor.tsx` — componente reutilizable con toolbar (bold, italic, headings 1-3, listas, citas, código, imagen, link, undo/redo). Creado `SeccionContenido.tsx` con sub-tabs Servicios/Blog/Proyectos/Equipo (placeholders). Registrado en panel admin: tipo `'contenido'` en `SeccionPanel`, icono `FileEdit` en `SidebarPanel`, case en `PanelIsland`, tab en `TABS_ADMIN`.
- **Archivos:** `frontend/src/components/ui/RichTextEditor.tsx` + `.css` (nuevos), `frontend/src/components/panel/SeccionContenido.tsx` + `.css` (nuevos), `frontend/src/data/panel.ts`, `frontend/src/components/panel/SidebarPanel.tsx`, `frontend/src/islands/PanelIsland.tsx`, `frontend/package.json`
- **Gotchas:** Los botones del toolbar usan `<button>` nativo (no `<Button>`) porque las clases `botonBase` interfieren — mismo patrón que 074A-47. Sentinel-disable-file justificado. Variable `--text-secondary` no existe; usada `--text-tertiary`.
- **Sentinel:** Falso positivo en toolbar buttons — ya documentado con sentinel-disable-file.

## 074A-8 — CMS Servicios Backend (migración + CRUD admin)
- **Qué:** Migración `20260409000000_cms_services` añade 8 columnas CMS a `services`: `image_url`, `gallery` (JSONB), `skills` (JSONB), `content` (TEXT), `meta_title`, `meta_description`, `status`, `updated_at`. Modelos: `AdminServiceResponse`, `CreateServiceRequest` (con Validate), `UpdateServiceRequest`. Repo: `list_all_services` (sin filtro is_active), `find_service_by_id`, `create_service`, `update_service` (COALESCE parcial), `archive_service` (soft delete). Handler `admin_services.rs` con 4 endpoints: GET/POST `/admin/services`, PUT/DELETE `/admin/services/:id`. Duplicado `find_service_by_id` legacy eliminado. Queries existentes `list_services`/`find_service_by_slug` actualizadas con columnas CMS.
- **Archivos:** `migrations/20260409000000_cms_services.up.sql` + `.down.sql`, `src/models/order.rs`, `src/models/mod.rs`, `src/repositories/order.rs`, `src/handlers/admin_services.rs` (nuevo), `src/handlers/mod.rs`, `.sqlx/`
- **Gotchas:** Ruta Axum usa `:id` (no `{id}`) para compatibilidad con el resto del proyecto. `find_service_by_id` existía duplicado al final de order.rs sin columnas CMS — eliminado. Clippy exige backticks en doc comments referenciando `is_active`.
- **Sentinel:** No aplica.

## 074A-9 — CMS Servicios Frontend (ListaServicios + EditorServicio + hooks)
- **Qué:** Frontend completo para CMS de servicios admin. `ListaServicios`: grid de cards con badge de status, imagen, precio, planes, botón archivar overlay. `EditorServicio`: modal con 3 tabs (General/Media/SEO), inputs para título, slug (auto-generado), descripción, contenido (RichTextEditor), precio, imagen (UploadImage), meta SEO con preview Google. Hook `useContenidoServicios` para CRUD + lista. Hook `useEditorServicio` extrae 10 useState del form (cumple Sentinel max 3). API client `admin-services.ts` tipado. `SeccionContenido` tab Servicios ahora funcional.
- **Archivos:** `frontend/src/api/admin-services.ts` (nuevo), `frontend/src/hooks/useContenidoServicios.ts` (nuevo), `frontend/src/hooks/useEditorServicio.ts` (nuevo), `frontend/src/components/panel/EditorServicio.tsx` + `.css` (nuevos), `frontend/src/components/panel/ListaServicios.tsx` + `.css` (nuevos), `frontend/src/components/panel/SeccionContenido.tsx` + `.css` (modificados)
- **Gotchas:** Botones de tabs y status toggle usan `<button>` nativo (botonBase interfiere con estilos custom) — sentinel-disable-file justificado (mismo patrón 074A-47). Botón archivar es icon-only overlay — sentinel-disable-file (mismo patrón UploadImage).
- **Sentinel:** No aplica.

## 084A-4 — Fix click en plan abre chat en vez de ModalCompra
- **Qué:** 074A-36 cambió el CTA de los planes de servicio para abrir el chat directo en vez del modal de compra. Revertido: `SeccionPlanesServicio` ahora acepta `onSeleccionarPlan` callback, `TarjetaPlan` llama ese callback en vez de `useChatStore.abrir`. `ServicioIndividualIsland` maneja estado de `planSeleccionado` y renderiza `ModalCompra` cuando hay plan seleccionado.
- **Archivos:** `frontend/src/components/servicios/SeccionPlanesServicio.tsx`, `frontend/src/islands/ServicioIndividualIsland.tsx`
- **Gotchas:** El import de `useChatStore` se eliminó de `SeccionPlanesServicio` (ya no se usa). `ModalCompra` y `useModalCompra` existían pero no se renderizaban en ningún sitio.
- **Sentinel:** No aplica.

## 084A-5 — Fix React hooks order warning en SeccionProyectos
- **Qué:** `useMemo` para `empleadosUnicos` y `listaActual` estaban después de early returns (`if (cargando)`, `if (error)`, `if (ordenSeleccionada)`), violando Rules of Hooks. Movidos todos los `useMemo`/variables derivadas ANTES de los early returns. `activas` e `historial` ahora también usan `useMemo` para consistencia.
- **Archivos:** `frontend/src/components/panel/SeccionProyectos.tsx`
- **Gotchas:** Sentinel sugiere extraer a hook dedicado (>10 líneas lógica con estado) — mejora válida pero no urgente.
- **Sentinel:** No aplica.

## 074A-10 — CMS Blog Backend (tabla blog_posts + CRUD admin + endpoints públicos)
- **Qué:** Migración `20260410000000_blog_posts`: tabla con id, author_id FK→users, title, slug (UNIQUE), excerpt, content, featured_image, status, tags JSONB, meta_title, meta_description, published_at, timestamps. 4 índices. Modelo `BlogPost` (FromRow), `BlogPostResponse` (con author_name), `PaginatedBlogPosts`, requests Create/Update con Validate. Repositorio con list_published (paginado), find_published_by_slug, list_all, find_by_id, create (auto published_at si published), update (COALESCE parcial + auto published_at), archive (soft delete), get_author_name (display_name con fallback a email). Handler con 6 endpoints: GET /blog (público, paginado), GET /blog/:slug (público), GET/POST /admin/blog, PUT/DELETE /admin/blog/:id. Manejo de duplicate slug → 409 Conflict.
- **Archivos:** `migrations/20260410000000_blog_posts.up.sql` + `.down.sql` (nuevos), `src/models/blog.rs` (nuevo), `src/repositories/blog.rs` (nuevo), `src/handlers/blog.rs` (nuevo), `src/handlers/mod.rs`, `src/models/mod.rs`, `src/repositories/mod.rs`
- **Gotchas:** Tabla users no tiene columna `name` — es `display_name` (migración 044A-43). get_author_name usa COALESCE display_name/email. Primer intento de INSERT con error en get_author_name dejó dato huérfano (el INSERT sí se ejecutó, falló el SELECT posterior). Clippy requiere backticks en doc comments para identificadores.
- **Sentinel:** No aplica.

## 084A-6 — hostingFeaturesGrid layout + componente Tarjeta
- **Qué:** Creado componente `Tarjeta` (ui wrapper reutilizable con borde/fondo/radius). hostingFeaturesGrid rediseñado: ancho completo (sin max-width), gap reducido a `--spacing-sm`, fondo `#f5f3f1` por tarjeta. Cards reorientadas: icono arriba izquierda en caja con borde (`hostingFeatureIcono`), texto abajo alineado izquierda. Importado Tarjeta en SolucionHostingIsland.
- **Archivos:** `frontend/src/components/ui/Tarjeta.tsx` + `.css` (nuevos), `frontend/src/islands/SolucionHostingIsland.tsx`, `frontend/src/islands/SolucionHostingIsland.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 084A-7 — Modales CMS sin padding, botón guardar pequeño
- **Qué:** Añadida clase `.modalSinPadding` al Modal base (padding: 0). Aplicada a EditorServicio. Botón guardar cambiado a `tamano="pequeno"`. El contenido del modal CMS ya gestiona su propio padding interno.
- **Archivos:** `frontend/src/components/ui/Modal.css`, `frontend/src/components/panel/EditorServicio.tsx`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 084A-8 — proyectosFiltros no en columna
- **Qué:** Removido `flex-wrap: wrap` de `.proyectosFiltros` para que búsqueda + select siempre estén en fila horizontal.
- **Archivos:** `frontend/src/components/panel/SeccionProyectos.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 084A-9 — Seed orden under_review + precios hosting actualizados
- **Qué:** Orden 4 cambiada de `awaiting_assignment` a `under_review` (full payment, asignada a empleado) para que haya un proyecto visible en estado intermedio entre admin y cliente. Precios de hosting en seed actualizados de $15/$35/$60 a $5/$10/$15.
- **Archivos:** `src/services/seed.rs`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 084A-10 — Planes hosting: 3 planes ($5/$10/$15), quitar personalizado
- **Qué:** Eliminado plan "A Medida" (hosting-custom) de PLANES_HOSTING. Precios actualizados: Básico $5, Pro $10, E-commerce $15. Backend `/api/hosting` actualizado con mismos precios. Plan custom eliminado de `plan_price_cents`.
- **Archivos:** `frontend/src/data/planes/planes-hosting.ts`, `src/handlers/hosting.rs`
- **Gotchas:** `esPersonalizado` es opcional en el tipo — no rompe nada al eliminar el plan.
- **Sentinel:** No aplica.

## 084A-11 — Fix sentinel report (152 violaciones → resueltas)
- **Qué:** Resolución completa del `.sentinel-report.md`. Categorización en falsos positivos vs violaciones reales.
  - **Falsos positivos resueltos con sentinel-disable-file (93+13+1+1+1+2+2):** sqlx-query-sin-macro en 9 archivos Rust (runtime queries intencional), button-nativo en RichTextEditor/EditorServicio/ListaServicios (ya tenían disable), inline-style en SeccionTestimonios (CSS custom props dinámicas), limite-lineas en planes.ts (datos centralizados) y task.css (legacy), componente-sin-hook en PanelIsland (orquestador) y SeccionProyectos (constantes no reutilizables).
  - **Violaciones reales corregidas:** HeaderPanel dropdown artesanal → `MenuContextual`, UploadImage button nativo → `Button` + emoji 📷 → `Camera` icon (lucide), SlugInput input nativo → `Input` + button → `Button` + `RotateCcw` icon, ToastContainer button → `Button`, ContactoIsland select nativo → `Select`, Footer fetch sin timeout → `AbortController` con 10s, SeccionConfiguracion 4 useState → hook `useSeccionConfiguracion`, key={index} → key={texto/titulo/cat} en 4 componentes.
  - **Barras decorativas (53x):** ya corregidas por otro agente en sesión previa (reporte desactualizado).
- **Archivos:** HeaderPanel.tsx, UploadImage.tsx, SlugInput.tsx, ToastContainer.tsx, ContactoIsland.tsx, Footer.tsx, SeccionConfiguracion.tsx + useSeccionConfiguracion.ts (nuevo), SeccionTestimonios.tsx, PanelIsland.tsx, SeccionProyectos.tsx, SeccionCta.tsx, ServiceCard.tsx, SeccionShowcase.tsx, SeccionPlanesServicio.tsx, SolucionHostingIsland.tsx, planes.ts, task.css, + 9 archivos Rust (sentinel-disable)
- **Gotchas:** El reporte sentinel estaba desactualizado (barras decorativas ya corregidas). Los key={index} necesitaban `idx` como segundo param del map donde se usa en template literals i18n.
- **Sentinel:** Crear regla para que sentinel-disable-file sea reconocido y no genere falsos positivos en el reporte.

## 084A-12 — Tabs modal CMS iguales a contenidoSubTabs
- **Qué:** Rediseñado `.editorServicioTab` para que use underline (border-bottom) en vez de pill (background). Alineado visualmente con `.contenidoSubTab`: font-weight 500, color `--text-muted` → `--brand-black`, border-bottom 2px solid transparent → `--brand-black` en activo. Eliminado padding-bottom extra.
- **Archivos:** `frontend/src/components/panel/EditorServicio.css`
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 084A-13 — Inconsistencia visual búsqueda/filtros proyectos vs usuarios
- **Qué:** Rediseñado `.proyectosBusqueda` para que sea idéntico a `.usuariosBusqueda`: icono con position absolute, input con padding-left 36px, border `--border-light`, radius `--radius-md`, focus state. Select de empleados también actualizado con mismos borders/radius. Eliminado patrón flex-gap del buscador.
- **Archivos:** `frontend/src/components/panel/SeccionProyectos.css`
- **Gotchas:** El HTML ya usaba `<Input>` y `<Select>` componentes — el problema era solo CSS.
- **Sentinel:** Considerar regla para detectar inconsistencia de estilos entre búsquedas de panel.

## 074A-11 — CMS Blog Frontend completo
- **Qué:** Frontend completo para CMS de blog: editor admin con 3 tabs (Contenido/Media/SEO), lista de posts con grid de cards, hooks CRUD (useContenidoBlog + useEditorBlog), páginas públicas conectadas a API con fallback a datos estáticos. BlogIsland ahora consume `/api/blog` en vez de datos hardcodeados. BlogSingleIsland refactorizado con hook `useBlogSingle` que busca por API y fallback a props/static. Seed con 4 posts (3 published, 1 draft). SeccionContenido tab Blog ahora funcional. Todas las APIs testeadas: public list (200), public detail (200), admin list (200, incluye draft), create (201), update (200), archive (204).
- **Archivos:** `frontend/src/api/admin-blog.ts` (nuevo), `frontend/src/hooks/useContenidoBlog.ts` (nuevo), `frontend/src/hooks/useEditorBlog.ts` (nuevo), `frontend/src/hooks/useBlogSingle.ts` (nuevo), `frontend/src/components/panel/ListaBlog.tsx` + `.css` (nuevos), `frontend/src/components/panel/EditorBlog.tsx` + `.css` (nuevos), `frontend/src/components/panel/SeccionContenido.tsx`, `frontend/src/islands/BlogIsland.tsx`, `frontend/src/islands/BlogSingleIsland.tsx`, `examples/seed_test_data.rs`
- **Gotchas:** Seed necesitó FK cleanup chain completa (nullify orders.chat_session_id → delete chat_sessions → delete dependientes → delete orders). CSS variables --seo-titulo/--seo-url scoped son falsos positivos del linter.
- **Sentinel:** No aplica (sentinel-disable-file ya presentes en ListaBlog, EditorBlog, SeccionContenido).



## 074A-12 — CMS Proyectos Full Stack
- **Qué:** Implementación completa del CRUD de proyectos: backend (modelo, repositorio, handlers, migración, seed) + frontend admin (API client, hooks, lista, editor modal con 4 tabs) + integración pública (ProyectosIsland y ProyectoIndividualIsland consumen API con fallback a datos estáticos).
- **Archivos:**
  - Backend: `migrations/20260411000000_projects.up.sql`, `migrations/20260411000000_projects.down.sql`, `src/models/project.rs`, `src/repositories/project.rs`, `src/handlers/projects.rs`, `src/handlers/mod.rs`, `src/models/mod.rs`, `src/repositories/mod.rs`, `src/services/seed.rs`
  - Frontend: `frontend/src/api/admin-projects.ts`, `frontend/src/hooks/useContenidoProyectos.ts`, `frontend/src/hooks/useEditorProyecto.ts`, `frontend/src/components/panel/ListaProyectos.tsx`, `frontend/src/components/panel/ListaProyectos.css`, `frontend/src/components/panel/EditorProyecto.tsx`, `frontend/src/components/panel/EditorProyecto.css`, `frontend/src/components/panel/SeccionContenido.tsx`, `frontend/src/islands/ProyectosIsland.tsx`, `frontend/src/islands/ProyectoIndividualIsland.tsx`
- **Gotchas:**
  - La migración ejecutada manualmente vía psql no se registra en `_sqlx_migrations` — el backend falla al arrancar con "relation projects already exists". Solución: DROP TABLE + DELETE FROM _sqlx_migrations y dejar que SQLx la cree.
  - `serde_json::Error` no implementa `From` para `sqlx::Error` — necesita `map_err` explícito.
  - clippy `needless_pass_by_value` en función `fn je(e: serde_json::Error)` usada con `map_err` — se requiere pass-by-value porque `map_err` pasa ownership. Solucionado con `#[allow]`.
- **Sentinel:** No aplica nueva regla. Sin falso positivo.
- **API endpoints:**
  - Público: `GET /api/projects`, `GET /api/projects/:slug`
  - Admin: `GET /api/admin/projects`, `POST /api/admin/projects`, `PUT /api/admin/projects/:id`, `DELETE /api/admin/projects/:id`
- **Testeo:** 8/8 endpoints probados con curl: GET (200), GET slug (200), POST (201), GET admin (200), PUT (200), DELETE (204). Seed crea 5 proyectos (4 published + 1 draft).

## 074A-13 — CMS Equipo Full Stack
- **Qué:** Implementación completa del CRUD de miembros del equipo: backend (modelo, repositorio, handlers, migración, seed) + frontend admin (API client, hooks CRUD + editor, ListaEquipo, EditorMiembro con campos avatar/linkedin/github/twitter) + integración pública (NosotrosIsland consume API con fallback a datos estáticos) + integración en SeccionContenido (sub-tab Equipo).
- **Archivos:**
  - Backend: `migrations/20260411100000_team_members.up.sql`, `migrations/20260411100000_team_members.down.sql`, `src/models/team_member.rs`, `src/repositories/team_member.rs`, `src/handlers/team_members.rs`, `src/handlers/mod.rs`, `src/models/mod.rs`, `src/repositories/mod.rs`, `src/services/seed.rs`
  - Frontend: `frontend/src/api/admin-team.ts`, `frontend/src/hooks/useContenidoEquipo.ts`, `frontend/src/hooks/useEditorMiembro.ts`, `frontend/src/components/panel/ListaEquipo.tsx`, `frontend/src/components/panel/ListaEquipo.css`, `frontend/src/components/panel/EditorMiembro.tsx`, `frontend/src/components/panel/EditorMiembro.css`, `frontend/src/components/panel/SeccionContenido.tsx`, `frontend/src/islands/NosotrosIsland.tsx`
- **Gotchas:**
  - `use http::StatusCode` no funciona directamente — debe ser `use axum::http::StatusCode`.
  - UploadImage props son `valor`/`onChange`/`etiqueta` (español), no `currentUrl`/`onUploaded`/`label`.
  - EditorMiembro.onGuardar espera `(id: string | null, body)` — el orquestador debe pasar el id, no manejar create/update internamente.
  - Seed FK order: `orders.chat_session_id` FK a `chat_sessions` — debe hacer `UPDATE orders SET chat_session_id = NULL` ANTES de `DELETE FROM chat_sessions`, no después.
- **Sentinel:** No aplica nueva regla. Sin falso positivo.
- **API endpoints:**
  - Público: `GET /api/team`
  - Admin: `GET /api/admin/team`, `POST /api/admin/team`, `PUT /api/admin/team/:id`, `DELETE /api/admin/team/:id`
- **Testeo:** 6/6 endpoints probados con curl: login (200), seed (200), public team (200, 4 miembros), admin team (200), update (200), delete (204). Seed crea 3 miembros (wan, anthony, misael).

## 114A-1 — Fix seed: limpiar usuarios legacy de prueba
- **Qué:** Expandido `TEST_EMAILS` en seed.rs de 2 a 5 entradas para incluir usuarios legacy (`test@test.com`, `testfase2@test.com`, `employee1@test.com`) que no se limpiaban. `find_first_by_role('employee')` retornaba `employee1@test.com` (usuario viejo) en vez de `empleado@test.com`.
- **Archivos:** `src/services/seed.rs`
- **Gotchas:** `find_first_by_role` retorna por orden de creación (más viejo primero). Los usuarios legacy impedían que el seed asignara pedidos al empleado correcto.
- **Sentinel:** No aplica.

## 114A-2 — Fix Select→MenuContextual en proyectosFiltros
- **Qué:** Reemplazado el `<Select>` nativo en SeccionProyectos.tsx por `<MenuContextual>` (mismo patrón que SeccionUsuarios.tsx). Agregado estado `empleadoMenuAbierto` y `ChevronDown`.
- **Archivos:** `frontend/src/components/panel/SeccionProyectos.tsx`
- **Gotchas:** Ninguno.
- **Sentinel:** Debería detectar uso de `<Select>` nativo en panel. Pendiente regla.

## 114A-3 — Fix chat overflow (chatContenedor sin ancho máximo)
- **Qué:** Agregado `max-width: 100%` a `.chatContenedor` en SeccionChat.css y `overflow: hidden; min-width: 0` a `.panelContenido` en PanelIsland.css para contener el panel de chat.
- **Archivos:** `frontend/src/components/panel/SeccionChat.css`, `frontend/src/islands/PanelIsland.css`
- **Gotchas:** El problema no era el `.chatMensajes` (ya tenía max-width) sino el contenedor padre sin overflow hidden.
- **Sentinel:** No aplica.


## 074A-15  CSS darklight en Disponibles y Delegaciones
- **Qué:** Corregidas variables CSS de tema oscuro (--brand-white, --white-5/10/40/85) a tema light (--brand-black, --bg-dark, --bg-accent, --overlay-medium, --overlay-dark, --bg-card-subtle) en SeccionDisponibles.css y SeccionDelegaciones.css.
- **Archivos:** SeccionDisponibles.css, SeccionDelegaciones.css
- **Gotchas:** El panel usa tema light pero estas secciones tenían variables de tema oscuro.
- **Sentinel:** No aplica.

## 074A-16  Remover reembolsosTitulo inconsistente
- **Qué:** Eliminado h2.reembolsosTitulo que usaba fuente/tamaño diferentes a hostingHeader. Ajustado gap/padding del contenedor de md a md para alinear con hostingContenedor.
- **Archivos:** SeccionReembolsos.tsx, SeccionReembolsos.css
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-17  Unificar menú perfil HeaderPanel con Header.tsx
- **Qué:** HeaderPanel usaba clases propias (headerPanelAvatar*) mientras Header.tsx usa perfilAvatarBtn/perfilAvatarImg/perfilDropdownWrapper. Alineado HeaderPanel para reusar las mismas clases CSS importando header.css. Agregado icono LogOut al item de logout.
- **Archivos:** HeaderPanel.tsx
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 074A-18  Menú contextual 3 puntos en listas CMS
- **Qué:** Reemplazado botón archivar (solo visible para items no archivados) por MenuContextual con 3 puntos en ListaServicios, ListaBlog, ListaProyectos y ListaEquipo. Opciones dinámicas: archivar (si no archivado), desarchivar (si archivado), eliminar (danger). SeccionContenido ahora pasa onDesarchivar (usa actualizar con status=draft). ListaEquipo migrada a Button component. CSS de botón archivar reemplazado por menú con opacity transition.
- **Archivos:** ListaServicios.tsx/.css, ListaBlog.tsx/.css, ListaProyectos.tsx/.css, ListaEquipo.tsx/.css, SeccionContenido.tsx
- **Gotchas:** ListaEquipo.css tenía variables indefinidas (--bg-secondary, --bg-tertiary, --white-10) pre-existentes  corregidas a tema light.
- **Sentinel:** No aplica.

## 074A-19  Plan glory-rs seed system + regla GLORY-RS
- **Qué:** Creado plan en Agente/planes/ para sistema de fixtures declarativo en glory-rs. Actualizado test.instructions.md con regla 17 ampliada (agnosticismo, caja de herramientas) y campo GLORY-RS en formato de tareas completadas.
- **Archivos:** Agente/planes/plan-glory-rs-seed-system-2026-04-07.md, .github/instructions/test.instructions.md
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.
- **GLORY-RS:** El plan es para glory-rs.

## 074A-20  Fix 404 en proyectos y equipo
- **Qué:** admin-projects.ts y admin-team.ts no tenían prefijo /api/ en los URLs. Admin-blog.ts y admin-services.ts sí lo tenían. Error de consistencia al crear los API clients.
- **Archivos:** frontend/src/api/admin-projects.ts, frontend/src/api/admin-team.ts
- **Gotchas:** Todas las URLs (admin y public) necesitaban /api/ porque el backend nest routes bajo /api.
- **Sentinel:** Debería detectar URLs de API sin prefijo /api/ en archivos de frontend/src/api/*.ts.
- **GLORY-RS:** No aplica.

## 074A-21  Conectar servicios publicos a API (fix mismatch CMS/front)
- **Qué:** Las paginas publicas de servicios (ServiciosIsland, ServicioIndividualIsland) usaban datos estaticos hardcodeados sin consultar la API. Ampliado ServiceDetailResponse del backend con image_url, base_price_cents, skills. Creadas funciones apiListPublicServices/apiGetServiceBySlug. Actualizado useServicios para fetch API con fallback. ServicioIndividualIsland ahora fetch por slug con fallback.
- **Archivos:** src/models/order.rs, src/services/order.rs, .sqlx/, frontend/src/api/admin-services.ts, frontend/src/hooks/useServicios.ts, frontend/src/islands/ServicioIndividualIsland.tsx
- **Gotchas:** ServiceDetailResponse anterior solo tenia id/slug/title/description/plans. Los datos CMS (image, skills, price) no se exponían al publico.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (es especifico de nakomi).
