## 174A-91 — Sync changelog endpoint
- **Que:** se portó el contrato legacy `GET /me/sync/delta` a Axum como `GET /api/sync/changelog`. Acepta `cursor=` (canonico) y `since=` (alias por compat con la spec del roadmap), `limite` con clamp `[1,500]` (default 100). Auth obligatoria por bearer JWT — el `usuarioId` se toma del token (`CurrentUser.user_id`), no de query (mejora de seguridad sobre el legado). Detecta purga del changelog (cursor < `MIN(id)`) y conexión inicial → marca `full_sync_required=true` para que el cliente rehidrate desde colecciones. Reusa la tabla `sync_changelog` ya creada en la migración `20260417000011_indexes_planificador_changelog.up.sql`.
- **Archivos:** `src/models/sync.rs`, `src/models/mod.rs`, `src/repositories/sync_changelog.rs`, `src/repositories/mod.rs`, `src/handlers/sync.rs`, `src/handlers/mod.rs`, `.sqlx/*`, `openapi.json`, `frontend/src/api/generated.ts`, `Agente/documentacion/backend/sync-changelog-2026-04-19.md`, `roadmap.md`.
- **Gotchas:** el extractor `CurrentUser` expone `user_id`, no `id` — se confundió la primera vez y `cargo check` lo reportó. Para la paginación se hace `LIMIT limite+1` y se trunca en memoria, evitando una segunda query para `hay_mas`. Los 7 tipos de evento (`sample_added`, `sample_removed`, `sample_updated`, `collection_created`, `collection_renamed`, `collection_deleted`, `collection_merged`) se replicaron 1:1 con la `CHECK` de la migración.
- **Validacion:** `cargo sqlx prepare --workspace`, `cargo clippy --all-targets -- -D warnings` (clean), `cargo test --lib` (181 passed), `cargo run -- --emit-openapi openapi.json`, `npm run codegen`, `npm --prefix frontend run type-check` (clean).
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; el contrato y los tipos de evento son específicos del dominio Kamples (samples + colecciones).

## 174A-90 — Dashboard creador endpoints
- **Que:** se portaron los 4 endpoints legacy del dashboard de creador a Axum: `GET /api/dashboard/stats`, `GET /api/dashboard/top-samples`, `GET /api/dashboard/transacciones` y `GET /api/dashboard/ingresos`. Cada handler valida el `usuarioId` (404 si no existe), agrega métricas reales contra `transacciones`, `descargas`, `reproducciones`, `follows` y `samples`, y se exponen en OpenAPI bajo el tag `payments`. Se reaprovecha el cliente Stripe ya conectado (174A-83) y se genera el cliente TS (Orval) para el frontend.
- **Archivos:** `src/models/dashboard/mod.rs`, `src/models/mod.rs`, `src/repositories/creator_dashboard.rs`, `src/repositories/mod.rs`, `src/handlers/dashboard.rs`, `src/handlers/mod.rs`, `.sqlx/*`, `openapi.json`, `frontend/src/api/generated.ts`, `Agente/documentacion/backend/dashboard-creador-2026-04-19.md`, `roadmap.md`.
- **Gotchas:** `src/models/` ya estaba al límite del Sentinel (10 archivos), así que `dashboard` se creó como subdirectorio (`models/dashboard/mod.rs`) en vez de añadir excepción al settings. La query de ingresos usaba `ORDER BY fecha` (alias) y rompía `cargo sqlx prepare`; se reescribió como `ORDER BY DATE(t.created_at) ASC`. El payout legacy era stub frontend sin backend, así que queda explícitamente fuera del alcance. Estado de transacción se acepta tanto `completada` (legacy ES) como `completed` (current EN). Además, `cargo clippy -- -D warnings` reportó 24 errores preexistentes en `src/repositories/admin_panel/*`, `src/services/{search,email}.rs`, `src/handlers/{samples,comments,reports,dashboard}.rs`, `src/repositories/music/support.rs` y `src/main.rs` (raw string hashes innecesarios, format args inline, `;` faltantes, similar/struct names, `too_many_lines/arguments`, `cast_precision_loss`, `assigning_clones`, `single_match_else`); todos corregidos en este mismo commit aplicando refactors o `#[allow]` con justificación breve.
- **Validacion:** `cargo sqlx prepare --workspace`, `cargo check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --lib` (181 passed), `cargo run -- --emit-openapi openapi.json`, `npm run codegen`, `npm --prefix frontend run type-check`, `npm run self-check -- -TareaId 174A-90`.
- **Sentinel:** falso positivo evitado. Como `src/models/` cumple con subdirectorio en vez de excepción, no hace falta nueva regla. Para los `#[allow]` clippy: `too_many_lines/too_many_arguments` se permiten puntualmente en pipelines secuenciales (`upload`, `parse_upload_multipart`, `create_comment`, `fetch_extraction_queue`, `main`), `struct_field_names` en `RelationLinkContextRecord` por semántica fuente/destino, y `cast_precision_loss` en `calculate_score` con justificación inline (posición acotada).
- **GLORY-RS:** no aplica; el dashboard de creador, los tipos de transacción legacy (`compra_sample` → `venta`) y el payout son específicos del dominio Kamples.

## 174A-89 — Panel admin endpoints
- **Que:** se portó la superficie legacy del panel admin con rutas en español para `GET /api/admin/resumen`, `GET /api/admin/actividad`, `GET /api/admin/usuarios`, `PUT /api/admin/usuarios/{id}`, acciones `suspender`, `desuspender`, `eliminar`, `cancelar-eliminacion`, y listados `GET /api/admin/scrapers` + `GET /api/admin/cola-extraccion`. También se añadieron DTOs/OpenAPI para estos contratos, repositorio dedicado `AdminPanelRepository` y regeneración de `openapi.json` + `frontend/src/api/generated.ts`.
- **Archivos:** `src/models/admin.rs`, `src/models/mod.rs`, `src/repositories/admin_panel.rs`, `src/repositories/admin_panel/*`, `src/repositories/mod.rs`, `src/handlers/admin.rs`, `src/handlers/mod.rs`, `openapi.json`, `frontend/src/api/generated.ts`, `roadmap.md`.
- **Gotchas:** el repo bloquea `sqlx::query` y `sqlx::query_as` sin macro, pero estos endpoints necesitan filtros dinámicos; se resolvió usando `QueryBuilder` y partiendo el repositorio en submódulos para no romper los límites internos de tamaño. `desuspender` y `cancelar-eliminacion` no reutilizan `activate()` del flujo previo porque el schema actual separa `estado` de `baneado_hasta`, y una “reactivación” agresiva limpiaba más estado del debido. En scrapers hubo que traducir estados internos (`pendiente`, `procesado`, `skip`) al contrato legacy esperado por el panel (`pending`, `scraped`, `skipped`).
- **Validacion:** `cargo check`, `cargo run -- --emit-openapi openapi.json`, `npm --prefix frontend run codegen`, `npm --prefix frontend run type-check`.
- **Sentinel:** `src/handlers/mod.rs` mantiene `sentinel-disable-file limite-lineas` porque el agregador central de rutas/schemas OpenAPI del repo ya opera como registro único y seguir fragmentándolo no aporta separación real.
- **GLORY-RS:** no aplica; el dashboard admin, los KPIs, la semántica de suspensión/eliminación y los contratos de scrapers/cola están acoplados al dominio Kamples.

## 174A-88 — Catálogo canciones/artistas + relaciones
- **Que:** se portó el núcleo del catálogo musical legacy con lectura pública de canciones, artistas, relaciones y cadena recursiva de sampleo, más CRUD admin para artistas/canciones/relaciones y endpoints autenticados para vincular, desvincular y verificar samples dentro de `relaciones_sample`. También se modeló el dominio completo en OpenAPI/Orval y se dejó el contrato frontend regenerado para los nuevos endpoints `/api/canciones*`, `/api/artistas*`, `/api/relaciones/*` y `/api/sample-discovery/*`.
- **Archivos:** `src/models/music.rs`, `src/models/mod.rs`, `src/repositories/music/*`, `src/repositories/mod.rs`, `src/handlers/music/*`, `src/handlers/mod.rs`, `.sqlx/*`, `openapi.json`, `frontend/src/api/generated.ts`.
- **Gotchas:** el primer corte superó los límites internos de tamaño, así que hubo que partir handlers y repositorios en submódulos antes de validar. `cargo sqlx prepare` obligó a corregir aliases SQL mal escapados, casts `smallint`, un `ORDER BY COUNT(*)` y nulabilidad explícita en el CTE recursivo para que la caché offline quedara verde. En OpenAPI, utoipa no bastó con registrar los paths: hubo que añadir explícitamente los schemas musicales a `components` y evitar referencias fully-qualified tipo `crate::models::SongDetailResponse`, porque Orval fallaba con `Ref not found` al resolver esos nombres.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; el catálogo musical, las relaciones de sampleo y el vínculo con `samples` están acoplados al dominio Kamples.

## 174A-85 — Reportes (legales, contenido, errores)
- **Que:** se portó la superficie principal de reportes legacy con `POST /api/reportar`, `POST /api/reportar-usuario/{userId}`, `POST /api/reportar-error`, `POST /api/reportar-legal`, `GET /api/admin/reportes/legales`, `POST /api/comentarios/{id}/reportar` y `POST /api/publicaciones/{id}/reportar`. También se añadió la migración para aceptar reclamaciones legales públicas sin `reportador_id`, persistiendo `ip_origen`, el auto-hide legacy de samples/publicaciones con umbral `3`, y la auto-suspensión de usuarios tras `4` reportes en `2h` por `48h`.
- **Archivos:** `migrations/20260419000033_reports_public_legal.up.sql`, `src/models/report.rs`, `src/models/mod.rs`, `src/repositories/report.rs`, `src/repositories/mod.rs`, `src/repositories/sample_catalog.rs`, `src/repositories/sample_catalog/query.rs`, `src/repositories/sample_catalog/similar.rs`, `src/repositories/post.rs`, `src/services/sample_catalog/mod.rs`, `src/handlers/reports.rs`, `src/handlers/reports/support.rs`, `src/handlers/sample_catalog.rs`, `src/handlers/feed.rs`, `src/handlers/mod.rs`, `.sqlx/*`, `openapi.json`, `frontend/src/api/generated.ts`.
- **Gotchas:** el repo fuerza controladores de máximo 500 líneas y desaconseja `sqlx::query_as` dinámico en estas rutas, así que el handler se partió en `support.rs` y la caché offline se regeneró con `cargo sqlx prepare` tras aplicar la migración local. La validación de email terminó usando el trait `validator::ValidateEmail` ya presente en el proyecto, evitando introducir una dependencia nueva solo para este flujo.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; los tipos de reporte, umbrales de auto-hide, suspensión automática y contrato legal/admin están acoplados al dominio Kamples.

## 174A-84 — Códigos gratis CRUD + uso
- **Que:** se portó el flujo legacy de códigos gratis con tablas dedicadas (`codigos_descarga_gratis`, `codigos_gratis_usos`), endpoints `POST /api/codigos-gratis/generar`, `GET /api/codigos-gratis/verificar`, `POST /api/codigos-gratis/reclamar` y `POST /api/codigos-gratis/invalidar`, más integración en `POST /api/samples/:id/descargar` y `POST /api/colecciones/:id/descargar-zip` para aceptar `codigoGratis` opcional y saltar restricciones de plan/crédito/precio cuando el usuario ya reclamó un código válido. También se replicó la compensación legacy de 50 `creditos_bonus` al reclamar un código expirado, de forma idempotente.
- **Archivos:** `migrations/20260419000032_free_codes.up.sql`, `src/repositories/free_code.rs`, `src/repositories/download.rs`, `src/repositories/mod.rs`, `src/handlers/free_codes.rs`, `src/handlers/downloads.rs`, `src/handlers/colecciones/zip.rs`, `src/handlers/mod.rs`, `src/models/payment.rs`, `src/models/mod.rs`, `.sqlx/*`, `openapi.json`, `frontend/src/api/generated.ts`.
- **Gotchas:** el esquema OpenAPI necesitó registrar explícitamente `crate::domain::KamplesPlanId` en `components` porque Orval fallaba con `Ref not found: #/components/schemas/KamplesPlanId`. Además el target temporal de Cargo (`C:/tmp/glory-target`) perdió parte del árbol `.fingerprint`, así que hubo que recrearlo antes de reemitir `openapi.json`. El `cargo clippy -- -D warnings` sigue rojo por warnings preexistentes en `src/handlers/comments.rs`, `src/handlers/samples.rs` y `src/services/email.rs`; no son regresiones de esta tarea.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; el flujo de códigos gratis, expiración compensada y bypass de descarga están acoplados al dominio Kamples.

## 174A-83 — Connect onboarding + revenue share
- **Que:** se portaron las rutas legacy de Stripe Connect (`POST /api/connect/onboarding`, `GET /api/connect/estado`, `POST /api/connect/dashboard`, `GET /api/connect/balance`) reutilizando el runtime `async-stripe`, incluyendo promoción automática de `usuario` a `creador`, resolución semántica del estado (`no_configurado`, `pendiente`, `activo`, `restringido`, `error`) y consulta del balance de la cuenta conectada usando `Stripe-Account`. Además se completó la segunda mitad del alcance del roadmap registrando revenue share por descarga de suscripción tanto en descarga individual como en ZIP de colección.
- **Archivos:** `src/handlers/connect.rs`, `src/handlers/mod.rs`, `src/handlers/downloads.rs`, `src/handlers/colecciones/zip.rs`, `src/models/payment.rs`, `src/models/mod.rs`, `src/repositories/billing.rs`, `src/repositories/mod.rs`, `src/services/stripe_service.rs`, `src/services/mod.rs`, `src/domain/payments.rs`, `src/domain/mod.rs`, `Cargo.toml`, `.sqlx/*`.
- **Gotchas:** el balance de cuentas Connect no estaba expuesto por el wrapper previo; se resolvió usando `RetrieveForMyAccountBalance` con `customize().account_id(...)` y activando el feature `balance` en `async-stripe-core`. El revenue share legacy por descarga mensual no trabaja con importes exactos en centavos, así que se modeló con redondeo entero equivalente a la fracción mensual `precio_mensual / 200` antes de repartir creador/plataforma.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; el contrato `/connect/*`, la semántica del dashboard de creador y el revenue share por descarga están acoplados a Kamples.

## 174A-82 — Webhook con HMAC + idempotencia
- **Que:** se añadió `POST /api/pagos/webhook` con verificación de firma Stripe usando el runtime ya integrado, caché idempotente por `event.id` vía `IdempotencyStore`, sincronización de suscripciones (`checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`) y registro idempotente de compras individuales de samples en `transacciones`. También se conectó el flujo de descargas para reconocer compras completadas y permitir descarga sin bloquear al usuario free.
- **Archivos:** `src/handlers/payments.rs`, `src/handlers/payments/webhook.rs`, `src/handlers/mod.rs`, `src/handlers/downloads.rs`, `src/repositories/billing.rs`, `src/repositories/mod.rs`, `src/models/payment.rs`, `src/models/mod.rs`, `src/services/stripe_service.rs`, `.sqlx/*`.
- **Gotchas:** el handler del webhook se movió a submódulo porque el controlador original superó el límite de 500 líneas; además hubo que evitar `NUMERIC` tipado directo en SQLx construyendo decimales desde centavos como texto para no introducir `bigdecimal` en el workspace.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; los eventos, metadata y persistencia están acoplados al esquema Kamples.

## 174A-81 — Checkout suscripción + sample + portal
- **Que:** se añadieron `POST /api/pagos/checkout`, `POST /api/pagos/checkout-sample` y `POST /api/pagos/portal`, reutilizando `StripeService` para abrir Checkout/Customer Portal y validando reglas de negocio legacy: solo planes `pro/premium`, sample premium con precio, no compra propia y no recompra del mismo sample. Se añadieron DTOs de request/response y soporte de repositorio para lookup de sample y verificación de compra previa.
- **Archivos:** `src/handlers/payments.rs`, `src/models/payment.rs`, `src/models/mod.rs`, `src/repositories/billing.rs`, `src/repositories/mod.rs`, `src/handlers/mod.rs`, `src/services/stripe_service.rs`, `.sqlx/*`.
- **Gotchas:** el checkout anual sigue explicitamente bloqueado porque todavía no existe price id separado ni contrato confirmado para periodo anual; además se corrigió la composición de `success_url` para no generar una query string inválida cuando la URL ya llevaba parámetros (`?checkout=...`).
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; el shape y validaciones del checkout son específicas de Kamples.

## 174A-80 — GET /pagos/planes
- **Que:** se añadió el endpoint público `GET /api/pagos/planes`, cableado en router y OpenAPI, usando el dominio de pagos como fuente única para precios, límites, trials y revenue share. La respuesta expone catálogo ordenado (`free`, `pro`, `premium`), precios en centavos y decimal string, pricing anual legacy (10 meses), ahorro anual y visibilidad del estado Stripe del entorno.
- **Archivos:** `src/handlers/payments.rs`, `src/models/payment.rs`, `src/models/mod.rs`, `src/handlers/mod.rs`.
- **Gotchas:** el catálogo público no depende de que Stripe esté habilitado; si faltan credenciales, responde igual con `stripeHabilitado=false`, para no bloquear el frontend ni mezclar disponibilidad del checkout con definición de planes.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; el shape del endpoint y el catálogo están acoplados a Kamples.


## 174A-79 — Wrapper Stripe + planes Kamples
- **Que:** se integró `async-stripe` como runtime opcional de backend, con configuración agrupada en `AppConfig`, catálogo de planes Kamples (`free`, `pro`, `premium`), cálculo centralizado de revenue share, repositorio de billing para `usuarios_ext`/`suscripciones` y un wrapper específico para customer, checkout de suscripción, checkout de sample, billing portal, Connect y verificación de webhook. También se regeneró la caché `.sqlx` y se ajustaron fixtures de tests que construyen `AppConfig` manualmente.
- **Archivos:** `Cargo.toml`, `Cargo.lock`, `src/config/mod.rs`, `src/domain/payments.rs`, `src/domain/mod.rs`, `src/repositories/billing.rs`, `src/repositories/mod.rs`, `src/services/stripe_service.rs`, `src/services/mod.rs`, `src/lib.rs`, `src/handlers/mod.rs`, `src/main.rs`, `src/services/{email,fcm,push}.rs`, `.sqlx/*`, documentación backend relacionada.
- **Gotchas:** `async-stripe` con rustls exige seleccionar explícitamente un crypto provider (`rustls-aws-lc-rs`) y cualquier alta de queries nuevas obliga a ejecutar `cargo sqlx prepare --workspace` antes de cerrar la tarea. Este corte no publica endpoints todavía: deja la base lista para `174A-80` a `174A-84`.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; el catálogo de planes, metadata, revenue share y compatibilidad de env vars (`GLORY_STRIPE_*` / `STRIPE_*`) son específicos de Kamples.

## 174A-78 — Pipeline notify(user, event) integrado
- **Que:** se implementó `NotificationFanoutService` como orquestador central para follows, likes de sample/publicación, comentarios, replies, likes a comentario y mensajes directos; persiste notificaciones sociales, emite websocket legacy (`notificacion` / `mensaje_nuevo`) y dispara Web Push + FCM en modo best-effort. También se añadieron lookups mínimos de metadata para sample/publicación y se regeneró la caché `.sqlx`.
- **Archivos:** `src/services/notification_fanout.rs`, `src/repositories/notification_target.rs`, `src/handlers/social.rs`, `src/handlers/likes.rs`, `src/handlers/comments.rs`, `src/handlers/comments/notifications.rs`, `src/handlers/messages.rs`, `.sqlx/*`, documentación backend relacionada.
- **Gotchas:** el smoke test local confirmó `follow` y mensajería directa, pero `POST /api/publicaciones` sigue rompiendo por un decode error preexistente (`unexpected null`), así que la validación manual de likes/comentarios sobre publicaciones quedó bloqueada por ese bug ajeno al fanout.
- **Sentinel:** no aplica.
- **GLORY-RS:** no aplica; los tipos, eventos y mensajes del fanout son específicos de Kamples.