# Tareas completadas — 2026-04-13

## 134A-1 — Frontend completo Data VIII: Trabajadores + Reseñas + Inactividad + Configuración
- **Que:** Implementación frontend completa de las funcionalidades Data VIII que faltaban:
  - Página Trabajadores (`/trabajadores`): CRUD completo con permisos por sección
  - Página Reseñas (`/resenas`): Lista admin con ratings, solicitar reseña a clientes
  - Página Inactividad (`/inactividad`): CRUD de reglas de inactividad automática
  - Configuración: 3 campos nuevos (google_review_url, telefono_restaurante, url_reservas)
  - Sidebar y rutas actualizados con nuevas secciones
- **Archivos:**
  - `frontend/src/hooks/useTrabajadores.ts` (nuevo)
  - `frontend/src/componentes/ListaTrabajadores.tsx` (nuevo)
  - `frontend/src/hooks/useResenas.ts` (nuevo)
  - `frontend/src/componentes/ListaResenas.tsx` (nuevo)
  - `frontend/src/hooks/useInactividad.ts` (nuevo)
  - `frontend/src/componentes/Inactividad.tsx` (nuevo)
  - `frontend/src/hooks/useConfiguracion.ts` (modificado)
  - `frontend/src/componentes/Configuracion.tsx` (modificado)
  - `frontend/src/App.tsx` (rutas nuevas)
  - `frontend/src/components/app-sidebar.tsx` (nav items nuevos)
  - `frontend/src/api/generated.ts` (barrel re-exports nuevos)
- **Gotchas:**
  - Orval no generaba hooks de inactividad por conflicto de operationIds con trabajadores (ambos tenían `listar`, `crear`, etc.). Se resolvió con operation_id explícito en backend.
  - ConfiguracionRestaurante type no incluye los 3 campos nuevos en el schema generado (aún faltan en utoipa). Workaround con `as unknown as Record<string, string>` cast.
- **Sentinel:** No aplica

## 134A-2 — Paredes en PlanoSala
- **Que:** Integración de paredes (muros/columnas) en el constructor visual del plano de sala:
  - Renderizado de paredes como rectángulos coloreados con rotación
  - Panel de configuración lateral (PanelConfigPared) con: dimensiones, posición, color picker, rotación
  - Botón "Pared" en toolbar para crear nuevas paredes
  - Selección visual de paredes con highlight
  - CRUD completo via API generada (crearPared, actualizarPared, eliminarPared)
- **Archivos:**
  - `frontend/src/componentes/plano-sala/usePlanoSala.ts` (modificado - imports + handlers + estado)
  - `frontend/src/componentes/PlanoSala.tsx` (modificado - renderizado + UI)
  - `frontend/src/componentes/plano-sala/PanelConfigPared.tsx` (nuevo)
- **Gotchas:** Ninguno significativo. La API de paredes ya estaba registrada en OpenAPI (mod.rs).
- **Sentinel:** No aplica

## 134A-3 — Fix backend Inactividad operationIds
- **Que:** Agregados operation_id explícitos a los 4 handlers de inactividad en backend para evitar colisión con trabajadores en Orval.
- **Archivos:**
  - `src/handlers/inactividad.rs` (listar_inactividad, crear_inactividad, actualizar_inactividad, eliminar_inactividad)
  - `src/handlers/mod.rs` (paredes ya registradas previamente)
- **Gotchas:** Sin operation_id explícito, utoipa generaba IDs genéricos que colisionaban entre tags.
- **Sentinel:** Posible regla: detectar handlers utoipa sin operation_id explícito cuando hay múltiples CRUDs similares.

## 134A-4 — Fix npm run dev (@tailwindcss/vite + concurrently)
- **Que:** Resueltos errores de `npm run dev`: `concurrently` no encontrado (npm install raíz), `@tailwindcss/vite` no encontrado (npm install frontend).
- **Archivos:** package-lock.json (raíz + frontend)
- **Gotchas:** node_modules se limpia entre sesiones cuando OneDrive sincroniza.
- **Sentinel:** No aplica
