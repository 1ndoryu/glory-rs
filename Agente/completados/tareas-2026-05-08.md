# Tareas completadas — 8 mayo 2026

## 085A-1 — Centralizar launcher y autorreparar dependencias frontend al cambiar de rama
- **Que:** `glory-rs/scripts/dev.mjs` ahora compara una huella de `frontend/package.json` + `frontend/package-lock.json` contra un marker local en `node_modules` y ejecuta `npm install --no-audit --no-fund` antes de Vite si la rama dejó dependencias desfasadas. `npm run dev` apunta al launcher compartido y `scripts/dev.mjs` queda solo como wrapper de compatibilidad.
- **Archivos:** `glory-rs/scripts/dev.mjs`, `glory-rs/scripts/clean-cargo-target.ps1`, `glory-rs/scripts/watch-cargo-target.ps1`, `scripts/dev.mjs`, `.gitignore`, `package.json`, `frontend/src/components/seo/schemas.ts`, `Agente/documentacion/tooling/dependencias-frontend-por-rama-2026-05-08.md`, `Agente/lecciones/lecciones-aprendidas.md`
- **Gotchas:** Vite reoptimiza cuando cambia el lockfile, pero no instala paquetes ausentes. La huella se recalcula tras instalar porque npm puede ajustar el lockfile. Los scripts operativos `push-services-cms.ps1` y `seed-chat.sql` se sacaron del tracking. Self-check detectó un `slug` sin uso en `personSchema`; ahora se usa como fragmento estable del schema `Person`.
- **Sentinel:** No aplica; es estado local de package manager, no patrón estático fiable.
- **GLORY:** Movido a `glory-rs/scripts/` por ser tooling agnóstico de desarrollo.

## 085A-4 — Detectar especificaciones de diseño CSS locales en Sentinel
- **Que:** Se registro el plan Sentinel + VarSense editor-agnostico como plan activo y se agrego en Sentinel la regla core `css-especificacion-diseno-local`, capaz de detectar triggers, opciones y dropdowns que recrean recetas visuales del sistema aunque no incluyan `boton`/`button` en el selector.
- **Archivos:** `Agente/planes/plan-sentinel-varsense-editor-agnostico-2026-05-08.md`, `Agente/lecciones/lecciones-aprendidas.md`, `.agent/code-sentinel/src/analyzers/static/staticCssRules.ts`, `.agent/code-sentinel/src/analyzers/staticAnalyzer.ts`, `.agent/code-sentinel/src/config/ruleRegistry.ts`, `.agent/code-sentinel/src/test/suite/cssDesignSpec.test.ts`, `.agent/code-sentinel/fixtures/equivalence/language-selector-design-spec/LanguageSelector.css`, `.agent/code-sentinel/fixtures/equivalence/language-selector-design-spec/expected-findings.json`
- **Gotchas:** El reporte era ciego porque `css-adhoc-button-style` solo miraba `boton/button`. La nueva regla cruza rol interactivo local con 2+ propiedades visuales y se valido con fixture core vs CLI. No se reinstalo la extension por estar el refactor a mitad de plan.
- **Sentinel:** Nueva regla `css-especificacion-diseno-local`; smoke CLI sobre `LanguageSelector.css` detecto `.selectorIdiomaTrigger`, `.selectorIdiomaLista` y `.selectorIdiomaOpcion`.
- **GLORY:** No aplica; cambio en herramienta del agente.
