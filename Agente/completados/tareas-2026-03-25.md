# Tareas completadas — 2026-03-25

## 253A-10: Resolver 52 violaciones de Glory Sentinel (html-nativo, usestate-excesivo, limite-lineas)

**Qué se hizo:**
- Creados 4 componentes UI atómicos: Input, Select, Textarea, Boton en `componentes/ui/`
- Barrel export en `componentes/ui/index.ts`
- Creados 4 hooks para extraer useState excesivo: useFormularioVenta (10→2), useFormularioGasto (10→2), useFormularioReserva (8→2), useLoginForm (4→3)
- Refactorizados 9 componentes para usar componentes UI: FormularioVenta, FormularioGasto, FormularioReserva, Login, ListaVentas, ListaGastos, ListaReservas, Inicio, BarraLateral
- Mejorada función `debeExcluirse` en Sentinel para soportar exclusiones de archivos individuales (no solo carpetas)
- Añadida exclusión `**/api/generated.ts` para código auto-generado por Orval
- Añadido `.sentinel-report.md` a `.gitignore`

**Archivos tocados:**
- Creados: `componentes/ui/{Input,Select,Textarea,Boton,index}.tsx`, `hooks/{useFormularioVenta,useFormularioGasto,useFormularioReserva,useLoginForm}.ts`
- Modificados: FormularioVenta.tsx, FormularioGasto.tsx, FormularioReserva.tsx, Login.tsx, ListaVentas.tsx, ListaGastos.tsx, ListaReservas.tsx, Inicio.tsx, BarraLateral.tsx, .gitignore
- Sentinel: `ruleLoader.ts`

**Validaciones:**
- `npm run type-check` ✓ (0 errores)
- `npx tsc --noEmit` en Sentinel ✓ (0 errores)
- `git status` limpio

**Test/evidencia:**
- 51 de 52 violaciones resueltas en el código
- 1 restante (generated.ts limite-lineas) resuelta por exclusión en Sentinel

**Prevención (Glory Sentinel):**
- Mejorada `debeExcluirse()` para soportar patrones de archivo (`**/api/generated.ts`) además de carpetas
- Exclusión default añadida para `**/api/generated.ts` (código auto-generado Orval)
- Las reglas `html-nativo-en-vez-de-componente` y `usestate-excesivo` seguirán detectando futuros usos nativos

**Lecciones:**
- Los comentarios JS con `**/` dentro confunden a TypeScript (interpreta como cierre de comentario regex). Evitar rutas glob en comentarios
- Los hooks de formulario consolidan N useState en 1 objeto de estado + 1 setter genérico (patrón escalable)
- Los componentes UI atómicos son wrappers mínimos — el punto es centralizar futuras mejoras sin tocar consumidores

---

## 253A-1: Crear template glory-rs (Rust + React + OpenAPI + Codegen + Clippy paranoia)

**Qué se hizo:**
- Creado proyecto completo desde cero con Axum, SQLx, utoipa, serde, tracing, thiserror, jsonwebtoken, argon2, validator, tower-http, dotenvy
- Backend con arquitectura en capas: handlers/ → services/ → repositories/ → models/ → errors/
- Ejemplo funcional: CRUD de notas con autenticación JWT (register, login, CRUD)
- Middleware de autenticación JWT como Axum extractor (AuthUser)
- Migraciones SQLx reversibles (up/down) para users y notes
- OpenAPI generado desde código con utoipa, Swagger UI integrado
- Clippy nivel paranoia: `clippy::all = deny`, `clippy::pedantic = warn`
- Frontend React + TypeScript + Vite con React Query, Zustand, Orval para codegen
- Proxy configurado en Vite para requests API al backend
- CSS con variables y nombres en español
- .gitignore, .env.example, README.md completo con instrucciones

**Decisiones:**
- Orval reemplaza a openapi-typescript-codegen (más moderno, genera React Query hooks)
- No se incluyó `config` crate — dotenvy + env vars es el estándar 12-factor para containers
- Se usa `sqlx::query_as` (no macro) para que el template compile sin DB conectada
- `#[async_trait]` necesario para `FromRequestParts` en axum 0.7 (axum-core 0.4.5)

**Validaciones:**
- `cargo check` ✓
- `cargo clippy` ✓ (0 warnings)
- `cargo fmt --check` ✓
- `cargo test` ✓ (0 tests, compilación exitosa)

**Nota:** Test funcional omitido — psql no en PATH y DB glory_db no creada aún. El usuario debe crear la DB y configurar .env para probar los endpoints.

---

## 253A-2: Corregir errores del frontend

**Qué se hizo:**
- Instaladas dependencias con `npm install` en `frontend/`
- Actualizado Orval de v6 a v8 para corregir 16 vulnerabilidades (10 críticas)
- `npm run type-check` pasa limpio

## 253A-3: Comando unificado para detectar errores

**Qué se hizo:**
- Creado `package.json` en la raíz con scripts unificados
- `npm run check` ejecuta: cargo check → cargo clippy → npm type-check
- Scripts adicionales: `check:back`, `check:front`, `dev:back`, `dev:front`, `codegen`

## 253A-4: Reorganizar roadmap

**Qué se hizo:**
- Roadmap limpiado: secciones claras (Completado, Stack, Pendientes, Notas)
- Eliminado contenido duplicado y notas obsoletas

---

## 253A-5: Backend restaurante — modelos, repos, services, handlers

**Qué se hizo:**
- Migraciones SQL: categorias_gasto, ventas, gastos, reservas + ALTER users ADD nombre + DROP notes
- Modelos: Venta (con enums Turno/CanalVenta/MetodoPago), Gasto (con TipoDocumento, CategoriaGasto), Reserva (con EstadoReserva, ReservasConteo), ResumenEconomico (dashboard)
- Repositorios con structs de datos (NuevaVenta, NuevoGasto, NuevaReserva, ActualizarReservaData)
- Servicios: VentaService, GastoService, ReservaService, DashboardService
- Handlers con utoipa: CRUD ventas, gastos, reservas + dashboard
- Eliminados archivos de Notes

**Validaciones:**
- `cargo check` ✓, `cargo clippy` ✓ (0 errors/warnings), `cargo fmt` ✓, `cargo test` ✓

---

## 253A-6: Migrar query_as a query_as! + reglas 15-16 + cache .sqlx/

**Qué se hizo:**
- Convertidos todos los `query_as` y `query` a `query_as!` y `query!` en todos los repositorios
- Generada cache offline `.sqlx/` con `cargo sqlx prepare`
- Añadidas reglas 15 (detección en compile-time) y 16 (todo testeable) a test.instructions.md
- Creada base de datos `glory_db`, migraciones aplicadas

**Validaciones:**
- `cargo check` ✓, `cargo clippy` ✓, `cargo test` ✓

---

## 253A-7: Frontend restaurante completo

**Qué se hizo:**
- OpenAPI schema fixes: MetodoPago a common.rs, ErrorResponse short names
- Orval v8 mutator signature corregido (url, config) en axios-instance.ts
- Codegen: 70KB generated.ts con React Query hooks
- Componentes: Login, BarraLateral, Layout, Inicio, FormularioVenta, FormularioGasto, FormularioReserva, ListaVentas, ListaGastos, ListaReservas
- CSS: global.css, Login.css, BarraLateral.css, Layout.css, Inicio.css, Formularios.css
- authStore.ts (Zustand), App.tsx con react-router-dom routes
- Enums corregidos: CanalVenta y TipoDocumento alineados al backend

**Validaciones:**
- `npm run type-check` ✓
- End-to-end: Register ✓, Login ✓, Venta ✓, Gasto ✓, Dashboard ✓, Reserva ✓, Conteo ✓

---

## 253A-8: Soporte Rust en coolify-manager-rs

**Qué se hizo:**
- StackTemplate::Rust en domain/mod.rs con test display
- SiteCapabilities para Rust: postgres DB, /app/data persistente, supports_theme_git: false
- rust_vars() en template_engine.rs (DOMAIN, DB_PASSWORD, JWT_SECRET)
- Match arm en new_site.rs para crear stacks Rust (genera JWT_SECRET de 48 chars)
- Match arm en migration_manager.rs para migrar stacks Rust
- Health check en health_manager.rs: verifica binario /app/app (no wp-load.php)
- rust-stack.yaml: compose con rust-app (build multi-stage) + postgres:16-alpine
- Dockerfile.rust: multi-stage build (rust:1.87-slim → debian:bookworm-slim, usuario no-root)

**Validaciones:**
- `cargo check` ✓ (2 warnings pre-existentes, no relacionados)
- `cargo test` ✓ (61 tests pasados, 0 fallidos)
- Commit: f1e4e0b, pushed to origin/main

---

## 253A-7: Frontend restaurante — UI completa

**Qué se hizo:**
- Login/Registro con Zustand auth store y JWT
- BarraLateral (sidebar) con NavLink activos: Inicio, Ventas, Gastos, Reservas
- Layout con redirección a login si no autenticado
- Inicio (dashboard): resumen económico mensual + conteo reservas + accesos rápidos
- FormularioVenta: fecha, comensales, turno, canal, método pago, IVA auto-calculado
- FormularioGasto: fecha, proveedor, categoría dinámica, tipo documento, recurrente
- FormularioReserva: fecha, hora, cliente, personas, teléfono, notas, estado
- ListaVentas, ListaGastos, ListaReservas: tablas paginadas con eliminación
- App.tsx reescrito con react-router-dom (rutas protegidas)
- main.tsx con BrowserRouter
- Orval codegen generó 70KB de hooks React Query
- axios-instance.ts adaptado para firma Orval v8 (url, config)
- 5 archivos CSS: global, Login, BarraLateral, Layout, Inicio, Formularios
- Corregidos enums CanalVenta (comedor/barra/terraza/delivery/just_eat/eventos) y TipoDocumento (factura/albaran/ticket)
- Movido MetodoPago a models/common.rs para evitar bug de $ref en OpenAPI
- Todos los handlers usan `body = ErrorResponse` (nombre corto) para refs válidos

**Validaciones:**
- `npx tsc --noEmit` ✓ (0 errores)
- Orval codegen ✓

---

## 253A-9: Protocolo v5.0 — Glory Sentinel, git limpio, emojis prohibidos

**Qué se hizo:**
- .gitignore: añadido `node_modules/` raíz, `/logs/`, `/frontend/openapi-debug.json` — 2596 archivos fantasma resueltos
- Protocolo (test.instructions.md): añadidas reglas 12.1 (git limpio), 17 (Glory Sentinel como prevención), 18 (emojis prohibidos)
- Paso 5 actualizado: borrar completamente tareas del roadmap (prohibido ~~tachado~~)
- Paso 7 actualizado: prioridad de prevención: 1) Sentinel, 2) Clippy, 3) Tests, 4) CI
- Formato de tareas completadas ampliado con sección "Prevención (Glory Sentinel)"
- Roadmap limpiado: eliminadas todas las tareas ~~tachadas~~ que ya estaban en completados/
- BarraLateral.tsx: 5 emojis (🍽️🏠💰📊📋) reemplazados por 4 SVG inline components
- BarraLateral.css: .iconoLateral ajustado para SVG (width/height en rem, flex-shrink)
- ListaVentas.tsx y ListaGastos.tsx: 5 inline styles `style={{ textTransform }}` → className="textoCapitalizado"
- Formularios.css: añadida clase `.textoCapitalizado { text-transform: capitalize }`

**Archivos tocados:**
- .gitignore
- .github/instructions/test.instructions.md
- roadmap.md
- frontend/src/componentes/BarraLateral.tsx
- frontend/src/componentes/ListaVentas.tsx
- frontend/src/componentes/ListaGastos.tsx
- frontend/src/estilos/BarraLateral.css
- frontend/src/estilos/Formularios.css

**Validaciones:**
- `npm run type-check` ✓ (0 errores)
- `git status` limpio (solo archivos de la tarea)
- Verificación: 0 emojis Unicode en .tsx, 0 inline styles en .tsx

**Test/evidencia:**
- grep `style={{` en .tsx = 0 resultados
- grep emoji Unicode en .tsx = 0 resultados
- `git status --short` muestra solo los 8 archivos esperados

**Prevención (Glory Sentinel):**
- Regla creada: `emoji-en-codigo` en defaultRules.ts — detecta caracteres emoji Unicode en archivos .tsx/.jsx/.ts/.js/.css/.html
- Regla creada: `inline-style-prohibido` en defaultRules.ts — detecta `style={{` en archivos .tsx/.jsx
- Ambas registradas en ruleRegistry.ts con severidad warning
- La regla 12.1 del protocolo previene archivos untracked en futuros commits
- La regla 17 obliga a consultar Sentinel por cada bug fix y crear reglas para patrones recurrentes

**Lecciones:**
- El .gitignore raíz debe cubrir TODOS los posibles directorios generados, no solo los de subdirectorios
- `node_modules/` a nivel raíz se crea al instalar paquetes con npm desde la raíz del proyecto
- Las tareas completadas nunca deben quedar en el roadmap — ni tachadas, ni con check — solo en completados/
- Glory Sentinel es la primera línea de defensa para patrones prohibidos detectables por regex
