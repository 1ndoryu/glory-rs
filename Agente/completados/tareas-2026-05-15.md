# Tareas completadas — 2026-05-15

## 15A-SENT-1 — Fix todas las violaciones del sentinel report
- **Que:** (a) Bugs en code-sentinel: `tieneSentinelDisable` no escaneaba comentarios multi-línea; `verificarLimiteLineas` usaba `includes()` literal en lugar de parser de tokens; `staticCssRules` no usaba el helper sino comparación directa. (b) Workspace: `codeSentinel.exclude` y `directoryExceptions` movidos a `.code-workspace` (ignorados en `.vscode/settings.json` en multi-root). (c) `usestate-excesivo` en `ModalCrearUsuario` y `HostingCard` → extraídos a `useModalCrearUsuario.ts` y `useHostingCard.ts`. (d) `HostingCreateForm.tsx`: disable comment movido al lugar exacto antes de `document.addEventListener`.
- **Archivos:** `code-sentinel/src/utils/analisisHelpers.ts`, `code-sentinel/src/analyzers/static/staticCodeRules.ts`, `code-sentinel/src/analyzers/static/staticCssRules.ts`, `glory-rust-template.code-workspace`, `frontend/src/hooks/useModalCrearUsuario.ts` (nuevo), `frontend/src/hooks/useHostingCard.ts` (nuevo), `frontend/src/components/panel/ModalCrearUsuario.tsx`, `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/components/panel/HostingCreateForm.tsx`
- **Commits:** `8b5fde5` (glory-rust-nakomi), `ae4561b` (code-sentinel main)
- **Gotchas:** `import type React from 'react'` era innecesario tras mover callbacks al hook — eliminar el import completo. `FormEvent` importado en ModalCrearUsuario tampoco era necesario tras extraer el form handler. `useCallback` duplicaba la lógica ya movida al hook.
- **Sentinel:** Nuevas reglas en code-sentinel — falsos positivos corregidos en `tieneSentinelDisable` y `verificarLimiteLineas`.
- **GLORY-RS:** No aplica

## 15A-1 — Fix Contabo auth, Running:Unknown display, Form→Modal, UserSelector
- **Que:** Corregido auth Contabo (dotenvy interpolando $[), ocultado sub-estado "Unknown" en formatStatus, migrado form inline a Modal, reemplazado input libre de cliente por UserSelector con búsqueda en vivo.
- **Archivos:** `.env`, `src/models/hosting.rs`, `frontend/src/components/panel/Vps2DeploymentsPanel.tsx`, `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/components/panel/VpsPanel.css`
- **Gotchas:** dotenvy interpola `$` en valores no entrecomillados; contraseñas con `$` deben ir entre comillas simples.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica — lógica específica de panel admin Nakomi

## 15A-2 — Despliegues multi-VPS (VPS Principal + VPS2) en panel unificado
- **Que:** El panel de despliegues ahora muestra servicios de ambas VPS. Se añadió `CoolifyConfig::from_env_with_prefix()` para soportar `COOLIFY_VPS1_*`. Se añadió `coolify_config_vps1` a `AppState`. El handler consulta ambos configs y une resultados. Cada tarjeta muestra un badge `server_label` ("VPS Principal" / "VPS2"). Si VPS1 no está configurada (optional), se omite silenciosamente.
- **Archivos:** `src/services/coolify.rs`, `src/models/hosting.rs`, `src/lib.rs`, `src/handlers/mod.rs`, `src/handlers/hosting.rs`, `frontend/src/api/hosting.ts`, `frontend/src/components/panel/Vps2DeploymentsPanel.tsx`, `frontend/src/components/panel/VpsPanel.css`
- **Gotchas:** `CoolifyServiceSummary` no estaba re-exportado en `services/mod.rs`; se importó desde `crate::services::coolify::CoolifyServiceSummary` directamente en el handler.
- **Sentinel:** No aplica
- **GLORY-RS:** `from_env_with_prefix()` es potencialmente reutilizable pero requiere que el patrón multi-config se estabilice antes de moverlo.

## 155A-3 — Corregir errores de compilación Rust por SQLx offline
- **Que:** `cargo check` fallaba por cinco consultas `sqlx::query_as!` en `src/repositories/service.rs` sin cache `.sqlx` disponible con `SQLX_OFFLINE=true`. Se convirtieron esas consultas de catálogo/admin de servicios a `sqlx::query_as::<_, ServiceRecord>()` con `FromRow`, manteniendo el SQL y los bindings.
- **Archivos:** `src/repositories/service.rs`, `roadmap.md`
- **Gotchas:** La BD local no permite regenerar cache con `cargo sqlx prepare` porque tiene checksums de migraciones previas modificadas; esta corrección evita depender de cache para esas consultas sin cambiar comportamiento.
- **Sentinel:** No aplica; es un caso conocido de cache SQLx offline roto por historial local.
- **GLORY:** No aplica — repositorio específico de Nakomi.

## 155A-4+155A-5+155A-6+155A-7+155A-8+155A-9+155A-10 — Ajustes UX de navegación, servicios, soluciones y panel hosting
- **Que:** Se cambió el launcher de apps a Catask con SVG negro y trigger `apps.svg`; el CTA “Comenzar proyecto” abre el chat; `/soluciones` y `/soluciones/` quedan en 404 mientras hosting/VPS siguen accesibles; `/servicios` incluye menú contextual de acceso directo a servicios y grid desktop de 2 columnas; Hosting/VPS tienen imagen estática optimizada con geometría igual a `galeriaHeroContenedor` de home y botón admin de 3 puntos para cambiar imagen localmente; el panel hosting sincroniza `hostingId` limpiando/escribiendo URL antes del estado para evitar el bucle abrir-cerrar y arreglar Volver.
- **Archivos:** `frontend/public/assets/icons/catask.svg`, `frontend/public/assets/icons/apps.svg`, `frontend/src/components/layout/AppLauncher.tsx`, `frontend/src/components/ui/ContextMenu.css`, `frontend/src/components/home/SeccionHero.tsx`, `frontend/src/data/navegacion.ts`, `frontend/src/App.tsx`, `frontend/src/hooks/useServicios.ts`, `frontend/src/islands/ServiciosIsland.tsx`, `frontend/src/islands/ServiciosIsland.css`, `frontend/src/components/servicios/GridServicios.css`, `frontend/src/components/soluciones/SolucionHeroImagen.tsx`, `frontend/src/components/soluciones/SolucionHeroImagen.css`, `frontend/src/islands/SolucionHostingIsland.tsx`, `frontend/src/islands/SolucionVpsIsland.tsx`, `frontend/src/hooks/useSeccionHosting.ts`, `frontend/src/components/panel/SeccionHosting.tsx`, `roadmap.md`
- **Gotchas:** En local `127.0.0.1` abre el portal VPS por regla existente; para validar la home principal se usa `localhost`. El build estaba bloqueado por un junction `frontend/public/legacy-assets/colors` hacia una carpeta inexistente del tema WordPress; se creó la carpeta destino vacía para que Vite pudiera copiar `public/`. La edición de imagen admin por ahora persiste en `localStorage` como placeholder hasta conectar CMS/backend.
- **Sentinel:** No aplica; cambios cubiertos por type-check/build y validación visual.
- **GLORY:** No aplica — comportamiento específico de Nakomi.

## 155A-DEPLOY-1 — Corregir status HTTP de rutas SPA públicas tras deploy
- **Que:** En la validación post-deploy, `/servicios`, `/soluciones/hosting` y `/soluciones/vps` servían el HTML correcto del SPA pero con status HTTP 404 porque `ServeDir.not_found_service(ServeFile(index.html))` preservaba el 404 del fallback. Se agregaron rutas shell explícitas en Axum para rutas React válidas, sirviendo `index.html` con status 200 y `Cache-Control: no-cache`, dejando `/soluciones` y `/soluciones/` como 404 reales.
- **Archivos:** `src/handlers/mod.rs`
- **Gotchas:** Validar solo visualmente en navegador puede ocultar este bug porque React hidrata igual; hay que verificar códigos HTTP directos con `curl` después del deploy.
- **Sentinel:** No aplica.
- **GLORY:** Potencialmente reusable como patrón de serving SPA, pero por ahora queda específico del template Nakomi.

## 155A-12 — Agente con venta y consultas VPS
- **Que:** El agente ahora lista planes VPS, lista VPS del cliente y crea checkout VPS con tools backend; el prompt guía confirmación de tier, checkout mensual y estado `pending_approval`.
- **Archivos:** `src/services/ai_tools.rs`, `src/services/ai_prompts.rs`, `src/handlers/vps.rs`, `frontend/src/islands/SolucionVpsIsland.tsx`
- **Gotchas:** El agente solo vende/consulta; provisioning y operaciones privilegiadas siguen detrás de flujos admin.
- **Sentinel:** No aplica.
- **GLORY:** No aplica — flujo específico de Nakomi/Contabo/VPS.

## 155A-13 — Hosting WordPress y hosting normal separados
- **Que:** Se agregaron planes `normal-*` con precio 30% mayor, catálogo filtrable por tipo, copy público/panel genérico de hosting y provisioning normal con Nginx + SFTP sin WordPress/MariaDB.
- **Archivos:** `migrations/20260515010000_hosting_normal_plans.up.sql`, `migrations/20260515010000_hosting_normal_plans.down.sql`, `src/handlers/hosting.rs`, `src/services/coolify.rs`, `src/services/hosting_stripe.rs`, `frontend/src/islands/SolucionHostingIsland.tsx`, `frontend/src/hooks/useHostingCatalog.ts`, `frontend/src/api/hosting.ts`, `frontend/src/components/panel/SeccionHosting.tsx`, `frontend/src/components/panel/HostingDetalleAccess.tsx`, `frontend/src/components/panel/HostingDetalleTabs.tsx`, `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/components/panel/HostingPlanSelector.tsx`, `frontend/src/data/panel.ts`
- **Gotchas:** Los slugs legacy `basico`, `pro`, `ecommerce` quedan reservados para WordPress; hosting normal usa `normal-basico`, `normal-pro`, `normal-ecommerce`.
- **Sentinel:** No aplica.
- **GLORY:** No aplica — pricing/provisioning específico de Nakomi.

## 155A-14 — Menús contextuales de Soluciones y Servicios
- **Que:** `Soluciones` dejó de tener página propia y ahora abre menú contextual hacia Hosting WordPress, Hosting y VPS; `Servicios` mantiene menú contextual con nombres de servicios.
- **Archivos:** `frontend/src/data/navegacion.ts`, `frontend/src/components/layout/Header.tsx`, `frontend/src/types/navegacion.ts`, `frontend/src/styles/header.css`, `frontend/src/App.tsx`, `src/handlers/mod.rs`, `Agente/documentacion/publico/navegacion-soluciones-2026-05-15.md`
- **Gotchas:** `/soluciones` debe seguir mostrando 404 real; solo las tres subrutas son válidas.
- **Sentinel:** No aplica.
- **GLORY:** No aplica — navegación pública Nakomi.

## 155A-15 — Nakomi no renderiza el portal VPS por hostname
- **Que:** Se eliminó la detección de `vps.nakomi.studio`/`127.0.0.1` dentro de la SPA principal y la ruta local `/portal-vps`; la home de Nakomi siempre renderiza Nakomi.
- **Archivos:** `frontend/src/App.tsx`, `Agente/documentacion/vps/arquitectura-vps-nakomi-studio-2026-05-10.md`, `Agente/documentacion/publico/navegacion-soluciones-2026-05-15.md`
- **Gotchas:** El portal VPS pertenece al despliegue separado de `coolify-manager-rs`; enlazarlo es correcto, renderizarlo desde este bundle no.
- **Sentinel:** No aplica.
- **GLORY:** No aplica — boundary de despliegue Nakomi/VPS.

## 155A-16 — Sin flash de proyectos legacy antes del CMS
- **Que:** El detalle `/proyectos/:slug` ya no recibe `PROYECTOS_DATA` desde el router ni lo usa como estado inicial; relacionados tampoco caen a data estática mientras carga la API.
- **Archivos:** `frontend/src/App.tsx`, `frontend/src/islands/ProyectoIndividualIsland.tsx`, `frontend/src/components/proyectos/SeccionProyectosRelacionados.tsx`, `Agente/documentacion/publico/proyectos-cms-publicos-2026-04-09.md`
- **Gotchas:** En preview sin backend, el detalle ahora muestra 404 tras fallar la API en vez de pintar un proyecto viejo; en producción debe hidratar desde CMS.
- **Sentinel:** No aplica.
- **GLORY:** No aplica — comportamiento CMS público Nakomi.

## 155A-17 — Manager logs/git-status compatible con studio Rust
- **Que:** `coolify-manager-rs logs --name studio` ya no busca contenedor WordPress por defecto en stacks Rust; resuelve el target por defecto a `app`. `git-status --name studio` ya no intenta entrar al contenedor WordPress y responde que no aplica para templates Rust.
- **Archivos:** `.agent/coolify-manager-rs/src/commands/view_logs.rs`, `.agent/coolify-manager-rs/src/commands/git_status.rs`, `roadmap.md`
- **Gotchas:** El binario release debe recompilarse con `cargo build --release --target-dir target`; antes de rebuild, el exe viejo seguía buscando `wordpress`.
- **Sentinel:** No aplica.
- **GLORY:** Aplica al manager operacional, no al framework Glory.

## 155A-18 — PageSpeed mobile: reducir imagen hero y JS inicial
- **Que:** El reporte PageSpeed mobile de `https://nakomi.studio/` marcaba rendimiento 59, FCP 6.6s, LCP 10.2s, payload total 7.7 MiB y ahorro de imágenes ~5.9 MiB. Se cambió el hero para renderizar solo la imagen activa del carrusel con `srcset` responsive, `quality=72` y `fetchPriority=high`, en vez de montar todas las imágenes superpuestas. También se difirieron `ChatWidget` y `AdminEditorProvider` hasta idle para que sus chunks no entren en la carga crítica.
- **Archivos:** `frontend/src/components/home/GaleriaHero.tsx`, `frontend/src/App.tsx`, `roadmap.md`
- **Gotchas:** `React.lazy` por sí solo no evita coste inicial si el componente lazy se monta en el primer render; hay que diferir el montaje de widgets no críticos.
- **Sentinel:** No aplica.
- **GLORY:** No aplica — optimización específica de la home Nakomi.

## 155A-19+155A-20 — Navegación real de servicios y copy de hosting administrado
- **Que:** El submenú de `Servicios` del header dejó de depender del catálogo estático y ahora usa el catálogo público real para mantener nombres y slugs sincronizados con `/servicios`. Además se eliminó el menú contextual redundante dentro de la propia página de servicios y se reescribió el copy del hosting sin WordPress para presentarlo como hosting administrado para sitios a medida, landings y frontends tanto en UI como en la API pública/Stripe.
- **Archivos:** `frontend/src/components/layout/Header.tsx`, `frontend/src/islands/ServiciosIsland.tsx`, `frontend/src/islands/SolucionHostingIsland.tsx`, `frontend/src/api/hosting.ts`, `src/handlers/hosting.rs`, `src/services/hosting_stripe.rs`, `Agente/documentacion/publico/navegacion-soluciones-2026-05-15.md`, `Agente/documentacion/hosting/checkout-publico-hosting-2026-04-10.md`, `Agente/lecciones/lecciones-aprendidas.md`, `roadmap.md`
- **Gotchas:** El preview local de Vite sin backend no puede comprobar el dropdown dinámico de `Servicios`, pero sí permitió verificar que `/servicios` ya no muestra menú embebido y que `/soluciones/hosting` renderiza el nuevo copy y los nuevos textos de planes.
- **Sentinel:** No aplica.
- **GLORY:** No aplica — navegación y posicionamiento comercial específicos de Nakomi.
