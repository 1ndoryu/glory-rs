## 054A-14+054A-16+054A-17 — Commit pendiente + bugs CSS/etiqueta + todos los errores Code Sentinel + VarSense

- **Que:**
  1. **054A-14 Commit masivo pendiente:** Commiteados todos los archivos que el agente anterior dejó sin commitear (40+ archivos: ChatWidget, SeccionChat, NotificationBell, EmployeesSection, ServicesCatalog, ContextMenu, hooks, APIs, BarraFiltros, BlogIsland, etc.)
  2. **054A-16 Bugs rápidos:**
     - `.employeesStats div` → `var(--bg-accent)` (antes `var(--bg-secondary)` causaba fondo negro)
     - `font-family: var(--font-serif)` eliminado de `.ordenCardFooter` en SeccionProyectos.css
     - `PAYMENT_MODE_LABELS['full']` = `'Pago único'` (before = `'Pago completo'` confundiendo con pago parcial)
  3. **054A-17 Code Sentinel — todos los errores:** Reemplazados todos los `<button>` nativos por `<Button>` componente en 13 archivos TSX. LanguageSelector reescrito para usar `<MenuContextual>` (eliminó artisanal dropdown con useRef+useEffect). ChatWidget: botones, emoji `👋` eliminado, inline `style={{color}}` → clases CSS `.chatWidgetSender--*`. SeccionTestimonios: `style={{touchAction}}` → CSS, transform/transition → CSS custom properties. Rust: `deliverables.rs` + `seed_admin.rs` → `query_scalar!`/`query!` macros tipadas. Cache `.sqlx` regenerado. TypeScript limpio (0 errores).
  4. **VarSense rebuild:** Fuente compilada (`npm run compile`) → `.vsix` empaquetado → copiado manualmente a `~/.vscode/extensions/1ndoryu.varsense-2.0.0/dist/extension.js` (la fuente tenía 40 días de mejoras sin compilar).
- **Archivos modificados principales:** frontend/src/components/chat/ChatWidget.{tsx,css}, frontend/src/components/home/SeccionTestimonios.{tsx,css}, frontend/src/components/layout/Header.tsx, frontend/src/components/panel/{SeccionReembolsos,EntregablesPanel,CheckoutModal,ReviewPanel,SeccionDelegaciones,SeccionDashboard,SeccionDisponibles,SeccionHosting,SeccionPagos,SeccionUsuarios,SeccionProyectos,EmployeesSection}.{tsx,css}, frontend/src/components/servicios/BarraFiltros.tsx, frontend/src/components/ui/LanguageSelector.tsx, frontend/src/islands/BlogIsland.tsx, src/handlers/deliverables.rs, examples/seed_admin.rs, .sqlx/
- **Gotchas:** `code --install-extension --force` no actualiza los archivos del directorio instalado; requiere copia manual. ReviwPanel.tsx: multi-replace generó un `>` sobrante que causaba error TS — corregido. ChatWidget.tsx: `const color = SENDER_COLORS[...]` quedó como variable sin uso (TS6133) — eliminada.
- **Sentinel:** Falsos positivos documentados: Button.tsx (el wrapper usa `<button>` nativo), ToastContainer.tsx, `<input type="file">` en EntregablesPanel (file upload), ModalAutenticacion overlay (diferido pendiente refactor useAutenticacion).

## 054A-3+054A-8+054A-9+054A-10 — ChatWidget flotante + throttle typing + cerrar sesión + seed chat

- **Que:** 
  1. **054A-3 ChatWidget:** Componente flotante `ChatWidget.tsx` + `ChatWidget.css` que aparece en todas las páginas públicas (no en /panel). Burbuja inferior derecha, se expande a panel de chat. Formulario inicial para nombre del visitante (opcional), conexión WebSocket anónima via `buildVisitorWsUrl()`, envío/recepción de mensajes, indicador de typing con animación de dots. Hook `useChatWidget.ts` gestiona WS, persistencia de visitor_id/session_id en localStorage, auto-reconexión.
  2. **054A-8 Throttle typing:** Implementado en useChatWidget.ts lado visitante (200ms throttle). Los keystrokes se limitan a 1 mensaje typing cada 200ms para no saturar el WS.
  3. **054A-9 Cerrar sesión:** Nuevo endpoint REST `POST /api/chat/sessions/:session_id/close` (solo admin/employee). Frontend: botón XCircle en header del chat activo en SeccionChat, llama `apiCloseSession()`, muestra toast de éxito/error. CSS para botón con hover rojo.
  4. **054A-10 Seed chat:** Script SQL `scripts/seed-chat.sql` que inserta sesión de visitante "María García" con 5 mensajes de prueba (2 del visitante, 2 de IA, 1 preguntando por staff).
  5. **Typing indicator en panel staff:** Integrado `useChatWs` en SeccionChat para mostrar indicador de typing en tiempo real con animación de dots.
- **Archivos:** frontend/src/components/chat/ChatWidget.tsx (nuevo), frontend/src/components/chat/ChatWidget.css (nuevo), frontend/src/hooks/useChatWidget.ts (nuevo), frontend/src/App.tsx, frontend/src/api/chat.ts, frontend/src/components/panel/SeccionChat.tsx, frontend/src/components/panel/SeccionChat.css, src/handlers/chat.rs, scripts/seed-chat.sql (nuevo)
- **Gotchas:** Code Sentinel reporta falsos positivos por `<button>` nativo en ChatWidget (botones de UI interna del widget, no acciones de formulario — misma excepción que Button.tsx/ToastContainer.tsx). Style inline necesario para colores dinámicos de sender_type (patrón establecido en SeccionChat).
- **Sentinel:** Falsos positivos conocidos en ChatWidget.tsx (buttons nativos del widget, style inline dinámico)

## 044A-43 — Cambiar foto de perfil (avatar upload)

- **Que:** Implementado desde cero: migración DB (avatar_url, display_name en users), endpoint REST (GET /api/profile, POST /api/profile/avatar multipart), servido estático de uploads/, frontend conectado con input file oculto activado por botón "Cambiar foto". Las imágenes se guardan en uploads/avatars/{user_id}.{ext} con validación MIME (jpeg/png/webp/gif) y límite 2MB.
- **Archivos:** migrations/20260406000000_user_profile.up.sql, migrations/20260406000000_user_profile.down.sql, src/models/user.rs, src/repositories/user.rs, src/handlers/profile.rs (nuevo), src/handlers/mod.rs, frontend/src/api/profile.ts (nuevo), frontend/src/hooks/usePerfil.ts, frontend/src/components/panel/SeccionPerfil.tsx, frontend/src/components/panel/SeccionPerfil.css
- **Gotchas:** `image/*` dentro de comentarios Rust `/* */` causa nested block comment error. utoipa no soporta `request_body(content_type = "multipart/form-data")` — omitir como en deliverables. `crate::lib::AppState` no funciona — usar `crate::AppState`.
- **Sentinel:** No aplica

## 044A-40 — Flujo de compra de servicios + componentes UI base + Modal base

- **Que:** 
  1. Creados componentes UI base: `Input`, `Textarea`, `Modal`, variante `texto` en `Button`.
  2. Modal de compra (`ModalCompra`): muestra resumen del plan → si no logueado pide email/contraseña inline → crea orden → inicia pago Stripe → redirige a panel. Hook `useModalCompra` con toda la lógica.
  3. Eliminados todos los planes "A medida" (7 en total): cada servicio ahora tiene solo Básico + Avanzado.
  4. Botón "Conversar" debajo de cada CTA de plan (redirige a /contacto/ temporalmente, TO-DO: abrir chat con contexto).
  5. Refactorizado `ModalTestimonio` para usar `<Modal>` base + componentes `<Input>`/`<Textarea>`.
  6. Eliminados botones X de todos los modales (se cierran con overlay click o Escape).
  7. Quitado `font-family` de `.sidebarRol`.
- **Archivos:** frontend/src/components/ui/{Modal,Input,Textarea,Button}.{tsx,css}, frontend/src/hooks/useModalCompra.ts, frontend/src/components/servicios/{ModalCompra,SeccionPlanesServicio}.{tsx,css}, frontend/src/data/planes/planes.ts, frontend/src/islands/ServicioIndividualIsland.tsx, frontend/src/components/home/{ModalTestimonio}.{tsx,css}, frontend/src/components/panel/SidebarPanel.css
- **Gotchas:** `CreateOrderRequest.client_notes` espera `string | undefined`, no `null`. Button.tsx usa `<button>` nativo internamente — falso positivo de Sentinel esperado. Los errores del IDE en useModalCompra.ts son cache desfasado vs TSC real.
- **Sentinel:** Falso positivo en Button.tsx (el componente wrapper necesita el nativo).

## 044A-44 — Migración de todos los repositorios a query_as!/query!/query_scalar! (compilación verificada)

- **Que:** Migrados los 8 archivos de repositorios restantes de `sqlx::query_as::<_, T>(...)` (runtime) a `sqlx::query_as!(T, ...)` (macro, verificación en compilación). También migrados `sqlx::query()` a `sqlx::query!()`, `sqlx::query_scalar::<_, T>()` a `sqlx::query_scalar!()`, y `sqlx::query_as::<_, (T,)>()` a `sqlx::query_scalar!()`. Para modelos con enums PostgreSQL (OrderStatus, PaymentMode, PhaseStatus, DelegationStatus, RefundStatus, UserRole) se utilizan type overrides: `column as "column: EnumType"` en SELECTs y `value as EnumType` en bindings. Las queries con `RETURNING *` se reemplazaron por listas explícitas de columnas cuando el modelo contiene enums. Cache SQLx offline regenerado con `cargo sqlx prepare`.
- **Archivos:** src/repositories/user.rs, src/repositories/payment.rs, src/repositories/order.rs, src/repositories/note.rs, src/repositories/chat.rs, src/repositories/delegation.rs, src/repositories/deliverable.rs, src/repositories/refund.rs, .sqlx/ (105 archivos de cache)
- **Gotchas:** No se pueden usar constantes string en macros proc (queries deben ser literales). `RETURNING *` no funciona con `query_as!` cuando hay enums — hay que listar columnas. Para `COUNT(*)` usar `as "count!: i64"` type override. Para `COALESCE` en JOINs usar `"field!: Type"` para indicar NOT NULL.
- **Sentinel:** No aplica

## 054A-1 — Panel admin de gestión de usuarios

- **Que:** Backend: 3 endpoints REST (GET /api/admin/users con paginación+búsqueda+filtros, PATCH /api/admin/users/:id/role, PATCH /api/admin/users/:id/status). Queries en user.rs con COUNT(*) OVER() para total sin query extra, filtros opcionales (role, status, search ILIKE). Modelos: AdminUserItem, PaginatedUsers, ChangeRoleRequest, ChangeStatusRequest con validación. Frontend: SeccionUsuarios con tabla de usuarios, búsqueda por email/nombre, filtros por rol y status, paginación, menú de acciones (cambiar rol, banear/reactivar), modal de confirmación usando <Modal> base. Hook useAdminUsers con React Query. Tab "Usuarios" añadida al panel admin con ícono UserCog.
- **Archivos:** src/handlers/admin_users.rs (nuevo), src/handlers/mod.rs, src/models/user.rs, src/models/mod.rs, src/repositories/user.rs, src/repositories/mod.rs, frontend/src/api/admin-users.ts (nuevo), frontend/src/hooks/useAdminUsers.ts (nuevo), frontend/src/components/panel/SeccionUsuarios.tsx (nuevo), frontend/src/components/panel/SeccionUsuarios.css (nuevo), frontend/src/components/panel/SidebarPanel.tsx, frontend/src/data/panel.ts, frontend/src/islands/PanelIsland.tsx, .sqlx/ (3 queries nuevas)
- **Gotchas:** `Option<&str>` no se puede castear a `Option<String>` en binds de query_as! — convertir a owned con `.map(str::to_string)`. Code Sentinel reporta falsos positivos por `<select>` y `<button>` nativos cuando no existen componentes Select/Boton en el sistema UI.
- **Sentinel:** Falsos positivos en SeccionUsuarios.tsx: no existe componente Select ni Boton (los existentes son Input, Textarea, Button, Modal)

## 054A-5 — Sistema de toast notifications

- **Que:** Store Zustand `toastStore` con tipos (success/error/info/warning), auto-dismiss configurable, helpers globales (`toast.success()`, `toast.error()`, etc.). Componente `ToastContainer` con íconos lucide-react, animación slide-in, posición fija top-right, z-index 9999. Montado en App.tsx como primer hijo de QueryClientProvider. Responsive en mobile.
- **Archivos:** frontend/src/stores/toastStore.ts (nuevo), frontend/src/components/ui/ToastContainer.tsx (nuevo), frontend/src/components/ui/ToastContainer.css (nuevo), frontend/src/App.tsx
- **Gotchas:** El botón de cerrar usa `<button>` nativo intencionalmente (componente UI base, misma excepción que Button.tsx). Sentinel reporta falso positivo esperado.
- **Sentinel:** Falso positivo en ToastContainer.tsx: botón X nativo dentro de componente UI base

## 054A-6 — Rediseño OrdenCard

- **Que:** Rediseñada OrdenCard: eliminada numeración (#order_number), nombre del plan, barra de progreso y texto de fases. Agregada imagen del servicio resuelta por slug. Backend: añadido `service_slug` a `OrderResponse` + modificado `get_order_display_info` para devolver 3-tupla (title, slug, plan_name). Actualizados todos los callsites (5 en order.rs, 5 en assignment.rs, 1 en ai_chat.rs). Frontend: mapa `SERVICE_IMAGE` de slug→imagen estática. Card ahora es horizontal (imagen + body) en desktop, stack vertical en mobile.
- **Archivos:** src/models/order.rs, src/repositories/order.rs, src/services/order.rs, src/services/assignment.rs, src/services/ai_chat.rs, frontend/src/api/orders.ts, frontend/src/components/panel/SeccionProyectos.tsx, frontend/src/components/panel/SeccionProyectos.css, .sqlx/ (1 query actualizada)
- **Gotchas:** DelegationResponse spots usaban `(title, _)` que necesitó `(title, _, _)`. El `<button>` nativo en OrdenCard es intencional — es una card interactiva con imagen, no un botón simple de acción.
- **Sentinel:** Falso positivo: `<button>` en OrdenCard es card wrapper, no botón de acción.

## 054A-7 — Reemplazar input/textarea nativos por componentes UI

- **Que:** Reemplazados todos los `<input>` y `<textarea>` nativos por `<Input>` y `<Textarea>` de `components/ui/` en 11 archivos. Solo quedan 3 `<input type="file">` nativos (ModalTestimonio, EntregablesPanel, SeccionPerfil) que son file uploaders ocultos — no aplica reemplazo. Total: 24 `<input>` → `<Input>` y 7 `<textarea>` → `<Textarea>`.
- **Archivos:** ModalAutenticacion.tsx (7 inputs), ContactoIsland.tsx (3 inputs + 1 textarea), SeccionPerfil.tsx (6 inputs + 1 textarea), SeccionMetodosPago.tsx (5 inputs), BarraFiltros.tsx (1 input), SeccionReembolsos.tsx (1 textarea), SeccionPagos.tsx (1 textarea), EntregablesPanel.tsx (1 textarea), SeccionChat.tsx (1 textarea), ReviewPanel.tsx (2 textareas), Footer.tsx (1 input)
- **Gotchas:** `<input type="file">` no se reemplaza — `<Input>` es wrapper de text/email/password/url/tel, no file uploads. PowerShell Select-String es case-insensitive por default, necesita `-CaseSensitive` para distinguir `<input` vs `<Input>`.
- **Sentinel:** No aplica

## 054A-4 — Handler de typing preview en WebSocket

- **Que:** Agregado `case 'typing'` al switch de `useChatWs()` que faltaba. El backend ya enviaba `Typing { session_id, sender, content }` pero el frontend ignoraba el mensaje. Ahora: mantiene `typingMap` (Record<string, TypingInfo>) con auto-limpieza a los 3s vía setTimeout. Al desconectar, limpia todos los timers. Exporta `typingMap` y tipo `TypingInfo` para uso en componentes.
- **Archivos:** frontend/src/hooks/useChat.ts
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica

## 054A-2 — Servicio de hosting completo (backend + frontend + panel admin)

- **Que:** Implementación completa del servicio de hosting para que clientes puedan comprar y gestionar suscripciones. Backend: migración con tablas `hosting_subscriptions` (plan, dominio, estado, Stripe, Coolify) y `hosting_events` (log de cambios). Modelo con validaciones. Repositorio con 6 métodos (list_all, find_by_id, create con struct params, update_status, add_event, list_events). 5 endpoints REST (GET/POST subscriptions, GET subscription/:id, PATCH status, GET events) con control de acceso (admin ve todo, cliente ve lo suyo). Planes: basico ($15, 5GB), pro ($35, 20GB), ecommerce ($60, 50GB), custom (cotización, 100GB). Frontend: API client con 5 funciones + constantes de labels/colores por plan y estado. SeccionHosting con tabla de suscripciones, modal de creación, menú dropdown de cambio de estado, modal de historial de eventos. Tab "Hosting" integrada en panel admin con ícono Server en sidebar.
- **Archivos:** migrations/20260407000000_hosting.up.sql (nuevo), migrations/20260407000000_hosting.down.sql (nuevo), src/models/hosting.rs (nuevo), src/models/mod.rs, src/repositories/hosting.rs (nuevo), src/repositories/mod.rs, src/handlers/hosting.rs (nuevo), src/handlers/mod.rs, frontend/src/api/hosting.ts (nuevo), frontend/src/components/panel/SeccionHosting.tsx (nuevo), frontend/src/components/panel/SeccionHosting.css (nuevo), frontend/src/components/panel/SidebarPanel.tsx, frontend/src/data/panel.ts, frontend/src/islands/PanelIsland.tsx, .sqlx/ (6 queries nuevas)
- **Gotchas:** `clippy::too_many_arguments` en HostingRepository::create — resuelto con struct `CreateHostingParams<'a>`. `clippy::match_same_arms` en plan_storage_mb — resuelto colapsando "basico" en wildcard `_`. `clippy::unreadable_literal` — 102400 → 102_400. Modal de Nakomi usa props `abierto` + `onCerrar` (no `onClose`).
- **Sentinel:** No aplica

## 054A-11+054A-12+054A-13+054A-14 — Correcciones de panel, rutas reales y falso positivo de Sentinel

- **Que:** 
  1. Corregidos los clientes frontend de chat, notificaciones y dashboard para usar rutas `/api/...`; los WebSocket de chat/notificaciones ahora derivan el host desde la misma base URL del API.
  2. Eliminados los subrayados accidentales del `Button` variante `texto`; arreglado el nav del panel; `NotificationBell` migrado a `MenuContextual`; `SeccionChat` refactorizada a `useSeccionChat`; eliminado el botón `Nuevo`; `OrdenDetalle` ahora usa topbar, menú de 3 puntos, modal de cancelación y solo muestra fases en pagos `phased`.
  3. Backend local reiniciado contra el código actual: `/api/profile` y `/api/admin/dashboard` dejaron de devolver 404 y ahora responden 401 sin token, que es el comportamiento correcto.
  4. `src/bin/seed.rs` ahora crea una conversación de prueba idempotente; se ejecutó `cargo run --bin seed` y quedó sembrada la sesión `d302ab89-ff95-4078-97ed-9b81f4fba806`.
  5. Las tabs `Empleados` y `Servicios` del panel dejaron de renderizar `PlaceholderSeccion`: ahora muestran `EmployeesSection` y `ServicesCatalogSection` con datos reales del equipo y del catálogo.
  6. Glory Sentinel ya no marca `<select>` como falso positivo cuando el workspace no tiene componente `Select`, y excluye `Button.tsx` del autodiagnóstico de HTML nativo.
- **Archivos:** frontend/src/api/{axios-instance,chat,dashboard,notifications}.ts, frontend/src/components/panel/{HeaderPanel,NotificationBell,OrdenDetalle,SeccionChat,SeccionProyectos,SidebarPanel,EmployeesSection,ServicesCatalogSection}.{tsx,css}, frontend/src/components/ui/{Button,ContextMenu}.{css,tsx}, frontend/src/core/router.tsx, frontend/src/hooks/{useAutenticacion,useCurrentProfile,useOrdenes,usePerfil,useSeccionChat}.ts, frontend/src/islands/PanelIsland.tsx, src/handlers/profile.rs, src/bin/seed.rs, .agent/code-sentinel/src/analyzers/react/reactComponentRules.ts
- **Gotchas:** el backend en `:3000` estaba ejecutando un binario viejo aunque el path seguía apuntando a `target/debug/glory-backend.exe`; por eso `profile` y `dashboard` fallaban aunque el source ya tenía las rutas. `psql` no está instalado en esta máquina, así que el seed de chat tuvo que moverse a un binario Rust real.
- **Sentinel:** Corregido falso positivo de `<select>` sin componente equivalente; las advertencias restantes del reporte corresponden a pantallas no tocadas en este bloque.
