## 215A-1 — Billing Guillermo: restaurante Pro pendiente + guillechatbots pagado

- **Que:** Modificado `admin_client_bootstrap.rs` para agregar campo `initially_paid` al struct `GuillermoBillingItem`. `guillechatbots.es` marcado como `initially_paid: true` (pagado, sin deuda visible). Nuevo billing item `d1000001-0001-4000-8000-000000000005` para `restaurante.wandori.us`, Plan Pro, $4.13/mes, `initially_paid: false` (pendiente). SQL de upsert actualizado para usar `$12 = initial_status` en INSERT y ON CONFLICT (idempotente).
- **Datos aplicados en producción:** UPDATE directo via psql en `postgres-do8k4w8swccwwogoc0os0ck0` / `rust_db`. Resultado verificado: 5 items, guillechatbots → paid, restaurante → pending $4.13.
- **Archivos:** `src/handlers/admin_client_bootstrap.rs`
- **Rama:** glory-rust-nakomi, commit `537b915`
- **Gotchas:** DB es `rust_db` (no `rust_app`). Worktree usado para no cambiar rama current (`glory-rs-rest`). Redeploy lanzado para que bootstrap futuro también funcione.
- **Sentinel:** No aplica (cambio de datos/bootstrap)
- **GLORY:** No aplica (lógica específica de cliente Guillermo)

## 215A-2..215A-13 — Overhaul hosting checkout, backups, métricas, VPS2 y tooling

- **Que:** Se reemplazaron los modales de compra de hosting por configuradores públicos reutilizables para Hosting WordPress y Hosting normal, con plan primero, dominio opcional, wp-admin/idioma para WordPress, credenciales SFTP opcionales, cuenta y checkout 1/6/12 meses con descuentos. Se normalizaron beneficios (`Tráfico ilimitado`, `Free temporary domain`, SSL, Free CDN en WordPress), nombres genéricos (`Avanzado` en vez de E-commerce/WooCommerce), copy compacto de descripciones y `WP-CLI + SSH` en todos los planes WordPress.
- **Infra:** Se diagnosticó y rescató el Gateway Timeout del hosting `wordpress-hosting-de17015b...`: Traefik elegía una red interna porque faltaba `traefik.docker.network`. Se conectó `coolify-proxy` a las redes del stack y el generador de compose ahora fija la red del servicio Coolify tras crear el servicio.
- **Backups/storage:** El compose nuevo crea sidecar de backup para todos los planes que lo anuncian: básico semanal, Pro/Avanzado diario + semanal, WordPress con archivos+MariaDB y hosting normal con `site-data`. El cálculo de almacenamiento ahora resuelve nombres reales `wordpress-{uuid}`, `mariadb-{uuid}`, `site-{uuid}` con fallback legacy y el enforcement usa el mismo selector.
- **UX:** Nuevo `PlanFeatureTooltip` explica características no obvias de planes de hosting/VPS en hover/foco; los valores obvios de almacenamiento/RAM no reciben tooltip.
- **Análisis:** VPS2 real medido: 4 vCPU, 7.9 GB RAM, 290 GB disco; el supuesto viejo de 6 vCPU/16 GB ya no aplica. Documentos nuevos: `Agente/documentacion/hosting/vps2-capacidad-rentabilidad-2026-05-21.md`, `Agente/documentacion/hosting/buzones-correo-clientes-2026-05-21.md`, `Agente/documentacion/tooling/cargo-target-cleanup-2026-05-21.md`.
- **Tooling:** Se agregó `scripts/clean-cargo-target.ps1` y `npm run clean:cargo`. Si el `cargo run` activo usa directamente `C:\tmp\glory-target`, la limpieza se pospone por seguridad.
- **Archivos:** `roadmap.md`, `src/services/coolify.rs`, `src/services/docker_stats.rs`, `src/services/storage_enforcement.rs`, `src/handlers/hosting.rs`, `src/services/hosting_stripe.rs`, `src/models/hosting.rs`, `src/services/ai_tools.rs`, `frontend/src/hooks/useHostingConfiguradorIsland.ts`, `frontend/src/islands/HostingConfiguradorIsland.tsx`, `frontend/src/islands/HostingConfiguradorIsland.css`, `frontend/src/islands/SolucionHostingIsland.tsx`, `frontend/src/islands/SolucionVpsIsland.tsx`, `frontend/src/components/servicios/PlanFeatureTooltip.tsx`, `frontend/src/components/servicios/SeccionPlanesServicio.tsx`, `frontend/src/components/servicios/SeccionPlanesServicio.css`, `frontend/src/api/hosting.ts`, `frontend/src/data/planes/planes-hosting.ts`, `frontend/src/i18n/contentTranslations.ts`, `frontend/src/islands/SolucionesIsland.tsx`, `package.json`, `scripts/clean-cargo-target.ps1`.
- **Validación:** `cargo test services::coolify::tests -- --nocapture` (42 ok), `cargo test services::docker_stats::tests -- --nocapture` (12 ok), `cargo test services::hosting_stripe::tests -- --nocapture` (2 ok), `cargo check` ok, `frontend npx tsc --noEmit` ok. CSS diagnostics solo mantienen dos avisos preexistentes de `2px` hardcodeado en `SeccionPlanesServicio.css`.
- **Gotchas:** Los hostings existentes no reciben el sidecar `backup-data` hasta refresh/reprovisioning del compose. Los backups locales pueden consumir mucho disco si un plan avanzado usa toda su cuota; antes de escalar conviene implementar backup remoto o cuota de backups. `npm run clean:cargo` no borra el target global mientras un `cargo run` activo lo está usando.
- **Sentinel:** No aplica.
- **GLORY:** Parcial tooling (`glory-rs/scripts/clean-cargo-target.ps1`) aplica al framework; el resto es producto Nakomi.
