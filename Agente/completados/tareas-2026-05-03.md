## 035A-1+035A-6 — Roadmap limpio y guardarrailes de modales
- **Que:** se limpio `roadmap.md` para dejar solo pendientes reales, se agrego la regla `modal-semantica-no-canonica` a Glory Sentinel, se migro el copy neutral de modales a `.modalTexto`, se fijo `--border-default` en `#dcdcdc` y se reforzo el protocolo para evitar clases visuales ad hoc en modales/paneles.
- **Archivos:** `roadmap.md`, `frontend/src/components/ui/Modal.css`, `frontend/src/components/servicios/ModalCompra.tsx`, `frontend/src/components/servicios/ModalCompra.css`, `frontend/src/components/panel/OrderDetailModals.tsx`, `frontend/src/components/panel/OrdenDetalle.css`, `frontend/src/components/panel/SeccionUsuarios.tsx`, `frontend/src/components/panel/UsuariosAcciones.css`, `frontend/src/styles/variables.css`, `.github/instructions/conducta.instructions.md`, `.agent/code-sentinel/src/analyzers/static/staticCssRules.ts`, `.agent/code-sentinel/src/analyzers/staticAnalyzer.ts`, `.agent/code-sentinel/src/config/ruleRegistry.ts`, `.agent/code-sentinel/src/test/suite/modalSemanticCss.test.ts`
- **Gotchas:** en reglas CSS por bloque, el `sentinel-disable-next-line` debe evaluarse sobre la linea del selector y no sobre el inicio del bloque cuando hay comentarios previos.
- **Sentinel:** nueva regla `modal-semantica-no-canonica`.
- **GLORY:** No aplica.

## 035A-7 — Generalizar el refuerzo de semantica compartida
- **Que:** se corrigio el protocolo para que el refuerzo no quede limitado a modales; ahora aplica a cualquier componente compartido del sistema y el documento operativo deja los modales solo como primer caso ya cubierto.
- **Archivos:** `.github/instructions/conducta.instructions.md`, `Agente/documentacion/tooling/guardarrailes-modal-semantica-2026-05-03.md`
- **Gotchas:** si el protocolo se formula sobre un unico componente, la IA lo interpreta como excepcion local en vez de principio transversal.
- **Sentinel:** no cambia la regla ya implementada; se amplia el alcance del principio para futuros detectores.
- **GLORY:** No aplica.

## 035A-8+035A-9 — Checkout inicial sin brief previo y sin 403 por rol empleado
- **Que:** se simplifico `ModalCompra` para quitar `modalCompraResumen` y el textarea de discovery previo en servicios normales, y se agrego un guard en `useModalCompra` para bloquear el intento de compra desde sesiones `employee` antes de que el frontend dispare `POST /api/orders` y reciba `403`.
- **Archivos:** `frontend/src/components/servicios/ModalCompra.tsx`, `frontend/src/components/servicios/ModalCompra.css`, `frontend/src/hooks/useModalCompra.ts`, `roadmap.md`
- **Gotchas:** al validar el flujo real en local, la creacion de orden ya no fallo con `403`; el siguiente bloqueo paso a ser un `404` al iniciar el checkout, asi que se dejo como pendiente separado en el roadmap.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 035A-10 — Fases de orden alineadas con el CMS
- **Que:** se corrigio la creacion de ordenes para que `payment_mode = phased` use siempre los titulos y descripciones definidos en `service_plan_phases` del CMS, y se bloqueo tanto la compra como el guardado admin de planes sin fases configuradas.
- **Archivos:** `src/services/order.rs`, `src/handlers/admin_services.rs`, `Agente/documentacion/orders/fases-cms-phased-2026-05-03.md`, `Agente/lecciones/lecciones-aprendidas.md`, `roadmap.md`
- **Gotchas:** el problema no estaba en que faltara UI de empleado; la raiz era que el backend sobreescribia las plantillas del CMS con fases genericas `por definir`, lo que hacia parecer que habia que redefinirlas manualmente.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 035A-11 — Slug canonico de compra publica restaurado
- **Que:** se corrigio `apiCreateOrder()` para que `diseno-web` deje de remapearse al slug obsoleto `diseno-de-sitios-web`; el checkout publico vuelve a crear la orden y avanzar al modal de Stripe en local.
- **Archivos:** `frontend/src/api/orders.ts`, `Agente/documentacion/orders/normalizacion-slugs-compra-2026-04-09.md`, `Agente/lecciones/lecciones-aprendidas.md`, `roadmap.md`
- **Gotchas:** el `404` parecia del paso de pago, pero el `catch` interno de `apiInitiatePayment()` ya redirigia al panel; el error real seguia viniendo de `POST /api/orders` por un alias legacy invertido en `service_slug`.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 035A-12 — Chat intermediario de órdenes vuelve a responder
- **Que:** se corrigio el chat intermediario de órdenes para que `ai_intermediary` pueda persistirse en `chat_messages`, se dejó logging explícito si guardar la respuesta IA falla y se acotó la espera del proveedor a 10s con acuse visible de respaldo.
- **Archivos:** `src/handlers/chat/rest_messages.rs`, `src/models/chat.rs`, `migrations/20260503000100_expand_chat_sender_type.up.sql`, `migrations/20260503000100_expand_chat_sender_type.down.sql`, `Agente/documentacion/chat/auditoria-chatbot-2026-04-16.md`, `Agente/lecciones/lecciones-aprendidas.md`, `roadmap.md`
- **Gotchas:** la IA sí generaba texto, pero `chat_messages.sender_type` seguía en `VARCHAR(10)` y `ai_intermediary` nunca cabía; como el resultado de `send_message()` se ignoraba, el síntoma se veía como "la IA no responde" cuando en realidad el insert estaba fallando.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 035A-13 — Deeplinks internos del panel y notificaciones que abren el proyecto correcto
- **Que:** se hizo URL-driven el estado profundo del panel para órdenes, hosting y chat; el panel ahora rehidrata detalles desde query params, las notificaciones del header ya navegan a su destino real y los mensajes de órdenes nuevos generan un link directo al proyecto en vez del chat legacy.
- **Archivos:** `frontend/src/utils/panelUrlState.ts`, `frontend/src/islands/PanelIsland.tsx`, `frontend/src/hooks/useSeccionProyectos.ts`, `frontend/src/components/panel/SeccionProyectos.tsx`, `frontend/src/hooks/useSeccionHosting.ts`, `frontend/src/hooks/useSeccionChat.ts`, `frontend/src/components/panel/NotificationBell.tsx`, `frontend/src/App.tsx`, `src/handlers/chat/rest_messages.rs`, `Agente/documentacion/panel/deeplinks-panel-y-notificaciones-2026-05-03.md`, `Agente/lecciones/lecciones-aprendidas.md`, `roadmap.md`
- **Gotchas:** si el hook dueño del detalle sincroniza a la URL demasiado pronto, borra `?order=` o `?hostingId=` en el primer render antes de hidratar la selección; primero hay que leer `location.search` y solo después persistir el estado efectivo.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 035A-15 — Descripción de proyecto: solo staff puede editar
- **Que:** `PUT /api/orders/:id/project-description` restringido a admin o empleado asignado; el cliente queda en lectura. `OrdenDetalle.tsx` oculta el campo editable para clientes.
- **Archivos:** `src/handlers/orders.rs`, `src/services/order.rs`, `frontend/src/components/panel/OrdenDetalle.tsx`
- **Gotchas:** El guard original permitía al cliente (`require_role(&[Client, Admin])`). El service hacía `client_id != user_id`, lo que nunca bloqueaba al cliente dueño. Se reemplazó con match explícito de `UserRole`.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.

## 035A-18 — Historial de pagos: lista compacta + modal factura
- **Que:** SeccionPagos rediseñada como lista de cards con imagen del servicio (84×84 px), título, badge, precio y fecha. Los detalles de cada factura se abren en un modal tipo invoice. Uso de `<Button variante="texto">` para alinearse visualmente con `OrdenCard`. Clippy fixes en `rest_messages.rs` (`map_or_else`) y `order.rs` (wildcard match → `UserRole::Client`).
- **Archivos:** `frontend/src/components/panel/SeccionPagos.tsx`, `frontend/src/components/panel/SeccionPagos.css`, `src/handlers/chat/rest_messages.rs`, `src/services/order.rs`
- **Gotchas:** `<img>` requiere `display: block` para evitar espacio inferior (baseline gap). El tamaño final fue 84×84 px (30% menos que 120×120 px inicial) por petición explícita del usuario.
- **Sentinel:** no aplica.
- **GLORY:** No aplica.