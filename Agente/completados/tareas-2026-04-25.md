# Tareas completadas â€” 2026-04-25

## 254A-1 â€” Restaurar 3 procesos legacy en admin (scraping, extraccion, seed)

- **Que:** El tab de Procesos en admin mostraba "No se encontraron procesos configurados" porque el backend Rust no exponÃ­a las rutas `/api/admin/procesos*` que el cliente legacy (`apiProcesos.ts`) consume vÃ­a `wpJsonStub`. Se creÃ³ el servicio `AdminProcessService` con los 3 procesos hardcodeados (scraping, extraccion, seed) â€” equivalente al comportamiento PHP que siempre devolvÃ­a los 3 aunque no hubiera locks. Se agregaron handlers, rutas y modelos OpenAPI para `list_processes`, `process_state`, `start_process`, `stop_process`, `process_cookies` (GET/POST).
- **Archivos:**
  - `src/services/admin_processes.rs` (nuevo, ~489 lÃ­neas) â€” servicio con enum `ProcessKind`, lock files, log tail, cookie info para yt-dlp.
  - `src/services/mod.rs` â€” registro del mÃ³dulo.
  - `src/models/admin.rs` â€” structs `AdminProcessState`, `AdminProcessesResponse`, `AdminProcessStartRequest`, `AdminProcessCookiesRequest`, `AdminProcessCookieInfo`.
  - `src/models/mod.rs` â€” re-exports.
  - `src/handlers/admin.rs` â€” 6 handlers nuevos con `#[utoipa::path]` para codegen.
  - `src/handlers/mod.rs` â€” rutas + registro en `ApiDoc`.
  - `openapi.json` â€” regenerado.
- **Validacion:** `cargo clippy -- -D warnings` limpio Â· `cargo build --bin glory-backend` OK Â· `npx tsc --noEmit` (frontend) limpio Â· `GET /api/admin/procesos` sin token responde **401** (ruta montada correctamente, requiere `require_admin`). El frontend legacy ya consume estos endpoints sin cambios â€” su `apiProcesos.ts` hace fetch a `/wp-json/kamples/v1/admin/procesos*` y `wpJsonStub.ts` los reescribe a `/api/admin/procesos*`.
- **Gotchas:**
  - El binario `glory-backend.exe` quedÃ³ bloqueado en el target principal mientras un proceso del usuario lo tenÃ­a abierto; resuelto cerrando ese proceso y compilando con `SQLX_OFFLINE=true`.
  - Para emitir `openapi.json` no es necesario `cargo run` â€” basta `cargo build` + ejecutar el binario directamente con `--emit-openapi`.
  - `apiProcesos.ts` NO usa Orval, asÃ­ que no se necesitÃ³ codegen frontend para esta tarea (solo verificar tsc).
- **Sentinel:** No aplica â€” ningÃºn patrÃ³n nuevo a detectar.
- **GLORY-RS:** No aplica â€” `AdminProcessService` es especÃ­fico de Kamples (procesos `scraping`/`extraccion`/`seed` con paths/log files propios del proyecto). No es agnÃ³stico.

## 254A-2 â€” Procesos admin funcionales y errores explicativos

- **Que:** Los 3 procesos del tab admin ahora arrancan con validaciones reales y reportan la causa cuando fallan. `scraping` y `extraccion` usan el Python del venv local del scraper si existe, validan dependencias antes de lanzar, escriben logs, detectan muerte inmediata y marcan el lock como `error` con el motivo inferido del log. `seed` deja de ser placeholder y ejecuta un servicio Rust que crea usuarios seed, asigna contribuidor a relaciones pendientes y reasigna samples del usuario sistema.
- **Archivos:**
  - `src/services/admin_processes.rs` â€” preflight Python, env DB desde `DATABASE_URL`, deteccion de fallos por log, seed async con `PgPool`.
  - `src/services/admin_seed.rs` y `src/repositories/admin_seed.rs` â€” port Rust del seed legacy con queries SQLx cacheadas.
  - `clients/kamples-scraper/scripts/cron_runner.py` â€” propaga exit code real para que scraping no falle silenciosamente.
  - `src/handlers/admin.rs`, `src/services/mod.rs`, `src/repositories/mod.rs` â€” wiring del proceso seed real.
  - `src/handlers/seo.rs` y `src/repositories/seo.rs` â€” queries SEO movidas a repositorio para mantener reglas locales durante clippy.
  - `.sqlx/` â€” cache offline de las queries nuevas.
- **Validacion:** `SQLX_OFFLINE=true cargo check --target-dir C:\tmp\glory-target-procesos` OK; `SQLX_OFFLINE=true cargo clippy --target-dir C:\tmp\glory-target-procesos -- -D warnings` OK; `python -m py_compile scripts/cron_runner.py` OK; imports venv (`scrapy`, `psycopg2`, `dotenv`, `yt_dlp`, `librosa`, `soundfile`, `numpy`) OK. Smoke HTTP local: `GET /api/admin/procesos` devolvio los 3; `POST /scraping/start` inicio PID real y `stop` lo detuvo; `POST /extraccion/start` con `limit=1` termino sin pendientes; `POST /seed/start` completo y devolvio resultado.
- **Gotchas:** `cron_runner.py` antes logueaba codigo de fallo pero salia con 0, asi que el backend podia interpretar un fallo como exito. Tambien se evito guardar el PID del backend en el lock de `seed` para que el boton detener no pueda matar el servidor.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica â€” sigue siendo logica especifica Kamples/scraper.

## 254A-3 â€” Restaurar endpoints legacy faltantes de Rust SPA

- **Que:** Se restaurÃ³ paridad legacy para los endpoints que la SPA heredada consumÃ­a vÃ­a `wpJsonStub`: cola IA admin, colecciones relevantes/por slug con `incluirSubcolecciones=1`, secciones de canciones, creaciÃ³n de publicaciones y subida de avatar/portada. La correcciÃ³n se hizo comparando llamadas frontend con controladores/repositorios PHP y portando el contrato al backend Rust, no cambiando la UI para esconder errores.
- **Archivos:**
  - `src/handlers/admin_ia_queue.rs` y `src/repositories/admin_ia_queue.rs` â€” endpoints `/api/admin/cola-ia*`, estadÃ­sticas, reintentos, estado de keys Groq y procesamiento manual de jobs de audio.
  - `src/handlers/colecciones.rs`, `src/handlers/colecciones/legacy.rs`, `src/repositories/coleccion/legacy.rs` â€” `/colecciones/relevantes/:sample_id`, slug real/fallback por sufijo, boolean legacy `1/0`, `contiene_el_sample`.
  - `src/handlers/music/public.rs`, `src/handlers/music/mod.rs` â€” `/canciones/secciones` antes de `/canciones/:slug`, con secciones top/recientes/gÃ©nero/artistas.
  - `src/handlers/posts.rs` â€” acepta `samplesAdjuntos` camelCase ademÃ¡s de `samples_adjuntos`.
  - `src/handlers/users.rs` â€” `POST /users/me/avatar` y `/users/me/portada` multipart, validaciÃ³n de tipo/tamaÃ±o, storage y persistencia en perfil.
  - `frontend/src/bootstrap/wpJsonStub.ts` â€” adaptaciÃ³n de publicaciones camelCase y wrapper de avatar/portada sin corromper `data`.
  - `migrations/20260425000001_colecciones_slug_compat.up.sql` y `.sqlx/` â€” compat de `colecciones.slug` para bases antiguas + cache SQLx.
- **Validacion:** `cargo sqlx migrate run` aplicado localmente; `cargo sqlx prepare` OK; `SQLX_OFFLINE=true cargo check` OK; `SQLX_OFFLINE=true cargo clippy --target-dir C:\tmp\glory-target-254A3 -- -D warnings` OK; `frontend: npx tsc --noEmit` OK; `get_errors` limpio en archivos editados. Smoke HTTP contra `:3000` pegÃ³ a una instancia vieja y reprodujo los errores originales, por lo que no cuenta como validaciÃ³n de runtime del binario nuevo. `cargo run` en el target principal fallÃ³ por binario Windows bloqueado (`glory-backend.exe`, acceso denegado); se evitÃ³ dejar procesos vivos.
- **Gotchas:** La BD local no tenÃ­a `colecciones.slug` aunque la migraciÃ³n base Rust lo declara; se agregÃ³ migraciÃ³n idempotente para bases creadas antes de ese campo. `/canciones/secciones` debe registrarse antes de `/canciones/:slug`. `wpJsonStub` adaptaba cualquier `/users/me/*` como si fuera un usuario plano, rompiendo wrappers de avatar/portada.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica â€” son contratos legacy especÃ­ficos de Kamples/PHP.


## 254A-4 — Diagnóstico scraping "se sigue deteniendo"

- **Que:** Investigación del reporte del usuario. Logs muestran que el spider corre y termina con código 0 ("completada OK") en ~5 segundos. La línea clave es "Spider no encontró ningún entry en las páginas de listas". El backend Rust funciona correctamente: lanza el proceso, capta su salida, libera el lockfile.
- **Causa raíz:** Externa al backend — el spider de Scrapy no encuentra entries (cambios en la página objetivo, falta de `SOUNDCLOUD_OAUTH_TOKEN`, o credenciales DataImpulse). Requiere revisión por separado del paquete Python `clients/kamples-scraper/`.
- **Archivos:** ninguno modificado (solo diagnóstico).
- **Gotchas:** Un primer run con la Python equivocada (sin scrapy en PATH) falló con `ModuleNotFoundError: No module named scrapy`. Posterior se arregló usando el venv del scraper.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (es problema de Scrapy, no de framework).

## 254A-5 — Implementar 7 endpoints admin faltantes (paridad legacy)

- **Que:** El admin SPA mostraba 404 en `/api/admin/contribuciones`, `/api/admin/duplicados`, `/api/admin/duplicados/contar`, `/api/admin/moderacion`, `/api/admin/moderacion/historial`, `/api/admin/automatizacion/estado`, `/api/admin/automatizacion/historial`. Se implementaron los 7 endpoints en Rust replicando los contratos legacy de PHP.
- **Archivos:**
  - `src/handlers/admin_automatizacion.rs` (NUEVO) — `/admin/automatizacion/{estado,historial}` con DTOs `LoteResumen`, `EstadoTipo`, `EstadoAutomatizacion`. Calcula `fallos_consecutivos` desde `lotes_procesamiento`. Defaults para `activo/limite_por_lote/intervalo_segundos` (no hay tabla de settings aún).
  - `src/handlers/admin_contribuciones.rs` (NUEVO) — `/admin/contribuciones` con JOIN a `usuarios_ext` + `canciones` (destino y fuente).
  - `src/handlers/admin_duplicados.rs` (NUEVO) — `/admin/duplicados` (filtros `estado`/`tipo`, paginación) + `/admin/duplicados/contar`. JOIN doble a `samples` + `usuarios_ext`.
  - `src/handlers/admin_moderacion.rs` (NUEVO) — `/admin/moderacion` (publicaciones + artículos + reportes pendientes con count) y `/admin/moderacion/historial?dias=` con `make_interval(days => $1)`.
  - `src/handlers/mod.rs` — registrados los 4 módulos y `.merge(...)`.
- **Gotchas:**
  - clippy `needless_raw_string_hashes`: las queries no contienen `"` así que `r"..."` (sin `#`) es lo correcto. Reemplazo masivo en los 4 archivos.
  - Las tablas `publicaciones` y `articulos` no tienen `moderado_por`/`moderacion_revisado_at`; el "historial" se aproxima filtrando por `moderacion_estado <> 'pendiente'` + ventana temporal sobre `created_at`.
  - El frontend espera `{ ok, ... }` o `{ data: ... }` según endpoint (legacy). Cada handler respeta la forma exacta legacy.
- **Validación:** `cargo check` OK, `cargo clippy -- -D warnings` OK, `npx tsc --noEmit` OK (frontend ya tenía los servicios).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — son endpoints específicos de Kamples/admin.


## 254A-6 — Fix /api/publicaciones 500 (LEFT JOIN nullability + soporte autor=username)

- **Que:** Diagnosticado y arreglado el 500 en GET/POST /api/publicaciones. Causa raiz real: en PostRepository::list y PostRepository::get el SELECT proyecta ou.nombre_visible y ou.avatar_url desde un LEFT JOIN usuarios_ext ou; sqlx no infiere nullability automatica para columnas LEFT JOIN, por lo que las trataba como String no-null y al recibir NULL fallaba con 'unexpected null; try decoding as Option' en columna 30/31. Adicionalmente apiSocial.listarPublicacionesUsuario envia ?autor=<username> (no ?author_id=<int>), parametro que el handler ignoraba silenciosamente.
- **Archivos:** src/repositories/post.rs (anadidos markers ? a original_author_display_name/original_author_avatar_url en ambos query_as!), src/handlers/posts.rs (anadido utor: Option<String> a PostListQuery + resolucion username->id via UserRepository::find_by_username, devuelve lista vacia si username no existe), .sqlx/* (cache regenerado con cargo sqlx prepare).
- **Validacion:** cargo check OK, cargo clippy -- -D warnings OK, npx tsc --noEmit (frontend) OK. Smoke test en local: GET ?filtro=todos&page=1 -> 200, GET ?autor=admin&page=1 -> 200 (antes ambos 500). POST usa el mismo SELECT en PostRepository::get, por lo que tambien queda arreglado.
- **Gotchas:** sqlx en modo OFFLINE necesita .sqlx/*.json regenerado tras tocar cualquier query; los markers ? fuerzan Option<T> independientemente de la inferencia de nullability del planner. Ediciones de raw strings r#"..."# requieren cuidado: las primeras pasadas escaparon comillas con backslash y rompieron el SQL, fix manual posterior.
- **Sentinel:** Posible regla preventiva: detectar columnas LEFT JOIN proyectadas en sqlx::query_as! sin marker ? cuando el destino es Option<T>. Pendiente evaluar viabilidad.
- **GLORY-RS:** No aplica — fix local del proyecto.

## 254A-10 — Fix /api/users/me/bloqueados 404

- **Que:** El legacy bloqueosStore llama a apiGet('/me/bloqueados'). wpJsonStub mapeaba genericamente /me/* -> /users/me/*, lo que producia /api/users/me/bloqueados (404). El backend Rust expone /api/me/bloqueados (con shape full users) Y /api/users/me/blocked (solo IDs) — semanticamente distintos. Solucion: anadir lista PATH_MAP_KEEP en wpJsonStub para excepciones donde el path /me/* tiene su propia ruta en Rust con shape compatible. /me/bloqueados queda sin transformar y golpea social::my_blocks (BlockedListResponse correcto).
- **Archivos:** frontend/src/bootstrap/wpJsonStub.ts (anadido PATH_MAP_KEEP set + chequeo antes de la transformacion generica).
- **Validacion:** npx tsc --noEmit OK. La ruta /api/me/bloqueados ya esta registrada en el router social (handlers/social.rs:207).
- **Gotchas:** No agregar alias /users/me/bloqueados en Rust porque el handler list_blocked retorna Vec<i32>, no BlockedListResponse — habria roto el shape esperado por el legacy store. Si en el futuro hay mas paths /me/* con misma colision, agregarlos a PATH_MAP_KEEP.
- **Sentinel:** Posible regla preventiva: detectar paths legacy en wpJsonStub que mapean a rutas Rust con shape distinto. Complejo de detectar staticamente — diferir.
- **GLORY-RS:** No aplica — fix local del proyecto (compatibilidad legacy de Kamples).

## 254A-9 — Verificar publicacion de blog

- **Que:** Smoke test end-to-end del flujo de publicacion de blog en backend Rust. Confirmado: GET /api/articulos retorna 200 con paginacion (vacio en BD limpia), GET /api/articulos/categorias retorna 200, POST /api/auth/login con admin/admin retorna 200 con JWT, POST /api/articulos con multipart/form-data (titulo, contenido, extracto, categoria) retorna 201 Created con el articulo persistido (id=1, slug autogenerado, moderacion_estado=aprobado por ser admin). Todos los campos que envia el legacy apiArticulos.crearArticulo (titulo, contenido, extracto, categoria, portada, embeds, descarga_publica) estan soportados por parse_create_article_multipart.
- **Archivos:** Ninguno modificado — solo verificacion. La task no especificaba sintoma concreto del fallo.
- **Validacion:** Smoke test directo via curl (Invoke-WebRequest) contra backend local en :3000.
- **Gotchas:** Si el usuario reporta que la UI del blog no publica, el problema esta en el frontend (componente del editor o llamada a apiArticulos.crearArticulo), no en el backend. Pedir reproduccion concreta (URL, payload exacto, mensaje de error en consola).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

## 254A-7 — Auditoria sync desktop (Tauri)

- **Que:** Auditoria estatica del cliente desktop en clients/desktop/src/services/sync*.ts. Hallazgo critico: el desktop hace fetch a ${origenServidor}/wp-json/kamples/v1/*, pero el backend Rust NO expone esas rutas (usa /api/*). El desktop solo funciona contra WordPress legacy en kamples.com. Ademas, varios endpoints especificos de sync (delta, colecciones de sync, coleccionados) no existen en Rust.
- **Archivos:** Agente/documentacion/sync/auditoria-sync-desktop-2026-04-25.md (nuevo, detalla cada endpoint con su estado), oadmap.md (cierra 254A-7, agrega 254A-7a/b/c/d como tareas independientes derivadas de la auditoria).
- **Validacion:** Inspeccion estatica con grep_search comparando rutas del cliente desktop contra rutas registradas en src/handlers/*.rs. No se ejecuto el desktop (Tauri local no puede levantarse sin entorno completo y la tarea era auditar, no probar end-to-end).
- **Gotchas:** El frontend web ya tiene wpJsonStub.ts que reescribe /wp-json/kamples/v1/* ? /api/*. El desktop NO tiene equivalente — cualquier migracion debe replicar esa logica o sustituir todos los fetch.
- **Sentinel:** No aplica.
- **GLORY-RS:** Si se generaliza el adapter wpJsonStub (web + desktop) podria moverse a glory-rs/frontend/. Pendiente de evaluar cuando se haga 254A-7d.

## 254A-8 — Auditoria menu contextual de samples

- **Que:** Inventario completo de los ~21 items del menu contextual (construirItemsMenuSample.ts) cruzando cada accion con sus endpoints en backend Rust. Resultado: 15 funcionan, 3 estan rotos (editar, verificar — PUT/PATCH + id/slug mismatch; corregir-ia y extender-recorte — endpoints inexistentes), 1 a verificar (reportar).
- **Archivos:** Agente/documentacion/ui/auditoria-menu-contextual-2026-04-25.md (nuevo, tabla detallada item por item con estado y endpoint), oadmap.md (cierra 254A-8, agrega 254A-8a/b/c/d como tareas independientes).
- **Validacion:** grep_search comparativo entre rontend/src/legacy/services/api*.ts y src/handlers/**/*.rs para mapear cada accion del menu a su ruta backend y verificar metodo + path.
- **Gotchas:** El bug mas serio es que ctualizarSample envia PUT /samples/{id} (numerico) mientras Rust expone PATCH /samples/{slug} (string). Esto rompe acciones admin como verificar, y la accion editar para todos los usuarios. La fix recomendada es mover el frontend a PATCH+slug (no al reves).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica.

## 254A-8a — Migrar actualizarSample de PUT/{id} a PATCH/{slug}
- **Que:** Migrado helper frontend `actualizarSample(slug, datos)` para usar `apiPatch('/samples/{slug}', datos)` (antes `apiPut('/samples/{id}', datos)`). Actualizados 3 callsites (construirItemsMenuSample, useEditar, BadgesMetadata) para pasar `sample.slug` en vez de `sample.id`. Se descubrió y corrigió un bug pre-existente en `SampleRepository::update_sample_metadata`: el QueryBuilder con `Separated::push_bind` insertaba un separador antes de cada bind, generando SQL inválido tipo `tipo = , $1` y devolviendo 500. Cambiado a `push_bind_unseparated`/`push_unseparated`.
- **Archivos:** frontend/src/legacy/services/apiSamples.ts, frontend/src/legacy/utils/construirItemsMenuSample.ts, frontend/src/legacy/hooks/useEditar.ts, frontend/src/legacy/components/ui/BadgesMetadata.tsx, src/repositories/sample_catalog.rs.
- **Validacion:** `npx tsc --noEmit` (exit 0). `cargo build --bin glory-backend` + `cargo clippy -- -D warnings` OK. Smoke test: login admin/admin ? `PATCH /api/samples/blue-zenith-ambient-loop-71bpm-gm-billvenb` con `{tipo:'loop'}` ? 200 OK con sample actualizado.
- **Gotchas:** `Separated::push` y `Separated::push_bind` ambos insertan el separador por adelante; cuando se usa el patrón `push('col = ')` + `push_bind(value)`, hay que llamar `push_bind_unseparated` para no obtener `col = , $1`. La validación 422 inicial era porque `UpdateSampleRequest` no contiene `verificado` (campo solo-admin); el frontend lo enviaba pero Rust lo ignora silenciosamente y la 422 venía cuando todo el body quedaba vacío en el lado serde. Pendiente: ampliar `UpdateSampleRequest` con `verificado`/`estado` para que el menú admin de verificar funcione end-to-end (queda como tarea derivada implícita).
- **Sentinel:** Considerar regla que detecte `Separated::push` seguido de `Separated::push_bind` en un mismo bloque (suele ser bug de QueryBuilder).
- **GLORY-RS:** No aplica (helper específico del cliente Kamples; el bug de QueryBuilder es del repositorio de samples).

## 254A-7d — Adapter de path desktop: /wp-json/kamples/v1 ? /api
- **Que:** Creado `clients/desktop/src/services/wpJsonRustAdapter.ts`: interceptor de fetch espejo del `frontend/src/bootstrap/wpJsonStub.ts` pero absolute-URL aware (preserva el host original al reescribir el path). Replica `mapPath` con `PATH_MAP` + `PATH_MAP_KEEP` (incluyendo `/me/sync/delta`, `/me/sync/colecciones`, `/me/coleccionados` y `/me/coleccionados/{id}/carpeta` que mantienen su nombre porque la implementacion Rust pendiente —ver 254A-7a/7b/7c— los expondra bajo `/api/me/...`). Adapta body para login/register (`email` ? `identifier`, `nombreVisible` ? `nombre_visible`) y respuesta para auth (`user` ? `usuario`). El adapter se instala desde `apiDesktopAdapter.configurarApiDesktop()` SOLO cuando `import.meta.env.VITE_KAMPLES_BACKEND === 'rust'`; en modo `wp` (default) no toca el fetch.
- **Archivos:** clients/desktop/src/services/wpJsonRustAdapter.ts (nuevo, 200 LOC), clients/desktop/src/services/apiDesktopAdapter.ts (cableado condicional).
- **Validacion:** `npx tsc --noEmit` aislado sobre el archivo nuevo (exit 0). El tsconfig de `frontend/` no incluye `clients/desktop/` y este workspace no tiene `tsc` instalado bajo `clients/desktop/`, por lo que la validacion completa del cliente desktop ocurre en el repo glorytemplate (donde tambien vive este codigo via path mapping). Sentinel reporta warning preexistente `services/ contiene 37 archivos` que no es bloqueante para este commit.
- **Gotchas:** El interceptor del adapter desktop ya existente envuelve fetch con `inyectarAuthHeader`. El nuevo adapter Rust se instala ANTES de ese para que la cadena de wrappers sea: app ? desktop auth ? rust adapter ? real fetch (orden importante porque el desktop auth wrapper espera URLs `/wp-json/` para inyectar Bearer; con el rust adapter delante, solo el resultado `/api/...` llega a fetch real, lo que NO recibe el header de desktop auth). Esto es un deuda menor: cuando se active `rust` como backend default, hay que migrar el desktop a usar `Authorization` en URLs `/api/` directamente; mientras tanto, en el flujo actual el rust adapter recibe el header ya seteado en `init.headers` por el wrapper desktop, lo preserva al llamar a `fetchOriginal(reescrita.rustUrl, newInit)` y todo funciona. Endpoints todavia no existentes en Rust (sync/delta, coleccionados...) responderan 404 hasta que aterricen 254A-7a/b/c — la UI ya tolera 404 como `{ ok: false }`.
- **Sentinel:** No aplica (solo warning preexistente de tamano de directorio).
- **GLORY-RS:** No aplica (logica especifica de la integracion desktop con un backend que migra de WP a Rust).

## 254A-8d — Auditar flujo reportar (sample/usuario/comentario/publicacion/error_plataforma)
- **Que:** Auditoria del endpoint que invoca `reportarStore.abrir` desde el menu contextual de samples. La pista era `frontend/src/legacy/utils/construirItemsMenuSample.ts:175` que llama `useReportarStore.getState().abrir('sample', s.id, s.titulo)`. El hook `useReportar.ts:70` envia `apiPost('/reportar', { tipo, targetId, razon, detalles })`. Verificado en `src/handlers/reports.rs`: la ruta `POST /api/reportar` esta implementada (`report_generic`) y soporta los 5 tipos (`usuario`, `publicacion`, `comentario`, `sample`, `error_plataforma`). El payload Rust `CreateGenericReportRequest` acepta tanto `targetId` (camelCase del frontend) como `target_id` via `serde(rename = "targetId", alias = "target_id")`. Tambien existen las rutas alternativas `/reportar-usuario/{id}`, `/reportar-error`, `/reportar-legal`, `/comentarios/{id}/reportar`, `/publicaciones/{id}/reportar`.
- **Archivos:** ninguno modificado (auditoria pura). Conclusion: NO falta `POST /api/reportes` — el endpoint vigente es `POST /api/reportar` y el frontend ya lo usa correctamente.
- **Validacion:** Smoke test con admin token + sampleId=7 (`blue-zenith-ambient-loop-71bpm-gm-billvenb`):
  `Invoke-RestMethod POST /api/reportar { tipo='sample', targetId=7, razon='test', detalles='smoke' }` ? 200 `{ ok: true, message: 'Reporte enviado' }`.
- **Gotchas:** El nombre de la ruta es singular `/reportar` (no `/reportes`) — el roadmap original mencionaba `/api/reportes` por confusion con el endpoint admin `/api/admin/reportes/legales`. La distinccion: `POST /api/reportar` crea reportes (cualquier usuario); `GET /api/admin/reportes/legales` lista pendientes (solo admin).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica (logica especifica de Kamples).
