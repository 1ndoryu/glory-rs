# Tareas completadas — 2026-04-25

## 254A-1 — Restaurar 3 procesos legacy en admin (scraping, extraccion, seed)

- **Que:** El tab de Procesos en admin mostraba "No se encontraron procesos configurados" porque el backend Rust no exponía las rutas `/api/admin/procesos*` que el cliente legacy (`apiProcesos.ts`) consume vía `wpJsonStub`. Se creó el servicio `AdminProcessService` con los 3 procesos hardcodeados (scraping, extraccion, seed) — equivalente al comportamiento PHP que siempre devolvía los 3 aunque no hubiera locks. Se agregaron handlers, rutas y modelos OpenAPI para `list_processes`, `process_state`, `start_process`, `stop_process`, `process_cookies` (GET/POST).
- **Archivos:**
  - `src/services/admin_processes.rs` (nuevo, ~489 líneas) — servicio con enum `ProcessKind`, lock files, log tail, cookie info para yt-dlp.
  - `src/services/mod.rs` — registro del módulo.
  - `src/models/admin.rs` — structs `AdminProcessState`, `AdminProcessesResponse`, `AdminProcessStartRequest`, `AdminProcessCookiesRequest`, `AdminProcessCookieInfo`.
  - `src/models/mod.rs` — re-exports.
  - `src/handlers/admin.rs` — 6 handlers nuevos con `#[utoipa::path]` para codegen.
  - `src/handlers/mod.rs` — rutas + registro en `ApiDoc`.
  - `openapi.json` — regenerado.
- **Validacion:** `cargo clippy -- -D warnings` limpio · `cargo build --bin glory-backend` OK · `npx tsc --noEmit` (frontend) limpio · `GET /api/admin/procesos` sin token responde **401** (ruta montada correctamente, requiere `require_admin`). El frontend legacy ya consume estos endpoints sin cambios — su `apiProcesos.ts` hace fetch a `/wp-json/kamples/v1/admin/procesos*` y `wpJsonStub.ts` los reescribe a `/api/admin/procesos*`.
- **Gotchas:**
  - El binario `glory-backend.exe` quedó bloqueado en el target principal mientras un proceso del usuario lo tenía abierto; resuelto cerrando ese proceso y compilando con `SQLX_OFFLINE=true`.
  - Para emitir `openapi.json` no es necesario `cargo run` — basta `cargo build` + ejecutar el binario directamente con `--emit-openapi`.
  - `apiProcesos.ts` NO usa Orval, así que no se necesitó codegen frontend para esta tarea (solo verificar tsc).
- **Sentinel:** No aplica — ningún patrón nuevo a detectar.
- **GLORY-RS:** No aplica — `AdminProcessService` es específico de Kamples (procesos `scraping`/`extraccion`/`seed` con paths/log files propios del proyecto). No es agnóstico.

## 254A-2 — Procesos admin funcionales y errores explicativos

- **Que:** Los 3 procesos del tab admin ahora arrancan con validaciones reales y reportan la causa cuando fallan. `scraping` y `extraccion` usan el Python del venv local del scraper si existe, validan dependencias antes de lanzar, escriben logs, detectan muerte inmediata y marcan el lock como `error` con el motivo inferido del log. `seed` deja de ser placeholder y ejecuta un servicio Rust que crea usuarios seed, asigna contribuidor a relaciones pendientes y reasigna samples del usuario sistema.
- **Archivos:**
  - `src/services/admin_processes.rs` — preflight Python, env DB desde `DATABASE_URL`, deteccion de fallos por log, seed async con `PgPool`.
  - `src/services/admin_seed.rs` y `src/repositories/admin_seed.rs` — port Rust del seed legacy con queries SQLx cacheadas.
  - `clients/kamples-scraper/scripts/cron_runner.py` — propaga exit code real para que scraping no falle silenciosamente.
  - `src/handlers/admin.rs`, `src/services/mod.rs`, `src/repositories/mod.rs` — wiring del proceso seed real.
  - `src/handlers/seo.rs` y `src/repositories/seo.rs` — queries SEO movidas a repositorio para mantener reglas locales durante clippy.
  - `.sqlx/` — cache offline de las queries nuevas.
- **Validacion:** `SQLX_OFFLINE=true cargo check --target-dir C:\tmp\glory-target-procesos` OK; `SQLX_OFFLINE=true cargo clippy --target-dir C:\tmp\glory-target-procesos -- -D warnings` OK; `python -m py_compile scripts/cron_runner.py` OK; imports venv (`scrapy`, `psycopg2`, `dotenv`, `yt_dlp`, `librosa`, `soundfile`, `numpy`) OK. Smoke HTTP local: `GET /api/admin/procesos` devolvio los 3; `POST /scraping/start` inicio PID real y `stop` lo detuvo; `POST /extraccion/start` con `limit=1` termino sin pendientes; `POST /seed/start` completo y devolvio resultado.
- **Gotchas:** `cron_runner.py` antes logueaba codigo de fallo pero salia con 0, asi que el backend podia interpretar un fallo como exito. Tambien se evito guardar el PID del backend en el lock de `seed` para que el boton detener no pueda matar el servidor.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — sigue siendo logica especifica Kamples/scraper.

## 254A-3 — Restaurar endpoints legacy faltantes de Rust SPA

- **Que:** Se restauró paridad legacy para los endpoints que la SPA heredada consumía vía `wpJsonStub`: cola IA admin, colecciones relevantes/por slug con `incluirSubcolecciones=1`, secciones de canciones, creación de publicaciones y subida de avatar/portada. La corrección se hizo comparando llamadas frontend con controladores/repositorios PHP y portando el contrato al backend Rust, no cambiando la UI para esconder errores.
- **Archivos:**
  - `src/handlers/admin_ia_queue.rs` y `src/repositories/admin_ia_queue.rs` — endpoints `/api/admin/cola-ia*`, estadísticas, reintentos, estado de keys Groq y procesamiento manual de jobs de audio.
  - `src/handlers/colecciones.rs`, `src/handlers/colecciones/legacy.rs`, `src/repositories/coleccion/legacy.rs` — `/colecciones/relevantes/:sample_id`, slug real/fallback por sufijo, boolean legacy `1/0`, `contiene_el_sample`.
  - `src/handlers/music/public.rs`, `src/handlers/music/mod.rs` — `/canciones/secciones` antes de `/canciones/:slug`, con secciones top/recientes/género/artistas.
  - `src/handlers/posts.rs` — acepta `samplesAdjuntos` camelCase además de `samples_adjuntos`.
  - `src/handlers/users.rs` — `POST /users/me/avatar` y `/users/me/portada` multipart, validación de tipo/tamaño, storage y persistencia en perfil.
  - `frontend/src/bootstrap/wpJsonStub.ts` — adaptación de publicaciones camelCase y wrapper de avatar/portada sin corromper `data`.
  - `migrations/20260425000001_colecciones_slug_compat.up.sql` y `.sqlx/` — compat de `colecciones.slug` para bases antiguas + cache SQLx.
- **Validacion:** `cargo sqlx migrate run` aplicado localmente; `cargo sqlx prepare` OK; `SQLX_OFFLINE=true cargo check` OK; `SQLX_OFFLINE=true cargo clippy --target-dir C:\tmp\glory-target-254A3 -- -D warnings` OK; `frontend: npx tsc --noEmit` OK; `get_errors` limpio en archivos editados. Smoke HTTP contra `:3000` pegó a una instancia vieja y reprodujo los errores originales, por lo que no cuenta como validación de runtime del binario nuevo. `cargo run` en el target principal falló por binario Windows bloqueado (`glory-backend.exe`, acceso denegado); se evitó dejar procesos vivos.
- **Gotchas:** La BD local no tenía `colecciones.slug` aunque la migración base Rust lo declara; se agregó migración idempotente para bases creadas antes de ese campo. `/canciones/secciones` debe registrarse antes de `/canciones/:slug`. `wpJsonStub` adaptaba cualquier `/users/me/*` como si fuera un usuario plano, rompiendo wrappers de avatar/portada.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica — son contratos legacy específicos de Kamples/PHP.
