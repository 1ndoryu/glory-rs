# Tareas completadas — 2026-04-18

## 174A-56 — Endpoints feed personalizado (`/feed`, `/me/feed`)

- **Que:** Handler `src/handlers/feed.rs` que expone `GET /api/feed` y `GET /api/me/feed` (alias para compatibilidad con cliente legado). Ambos requieren auth (`CurrentUser`), aceptan `?limit=&offset=` con clamp 1..=100 / >=0 y delegan en `RecommenderService::feed` (que ya hace cache fresh→stale→cómputo). Respuesta `FeedResponse { items: Vec<RankedSample>, limit, offset }`. `RankedSample` recibió `derive(ToSchema)` para que aparezca en el OpenAPI generado. `GET /api/samples/random` ya existía en `sample_catalog::random_sample` (no duplicado). Codegen Orval regeneró `getFeed`, `getMeFeed`, `GetFeedParams`, `getFeedResponse200/401`. Type-check frontend OK.
- **Archivos:** `src/handlers/feed.rs` (creado), `src/handlers/mod.rs` (mod + paths + schemas + merge), `src/algorithm/recommender.rs` (+`derive(ToSchema)` en `RankedSample`), `openapi.json` (regenerado), `frontend/src/api/generated.ts` (regenerado).
- **Gotchas:**
  - `DEFAULT_LIMIT as usize` cast falla clippy `cast_possible_truncation`. Reemplazado por constante `DEFAULT_LIMIT_USIZE: usize = 20` separada del `i64`.
  - `cargo run -- --emit-openapi openapi.json` debe ejecutarse ANTES de `npm run codegen`. El alias `npm run codegen` solo invoca Orval; no rebuilda el schema Rust.
  - Pendiente sistémico (no de esta tarea): `frontend/src/api/generated.ts` es monolítico; el protocolo exige Orval `tags-split`. Crear tarea aparte.
  - 110/110 tests verde, clippy verde, type-check verde.
- **GLORY-RS:** No aplica. Los endpoints son específicos del dominio Kamples (feed personalizado de samples). El patrón "handler delgado → service" ya está bien establecido en glory-rs.
- **Sentinel:** No aplica.

## 174A-55 — algorithm/algo_planner.rs (PlanificadorAlgoritmo portado)

- **Que:** Worker orquestador del algoritmo. Reemplazo Rust del `PlanificadorAlgoritmo.php` + WP-cron del legado. Expone tres APIs: (1) `register_interaction(pool, redis, user_id, kind)` para el path caliente — upsert atómico en `algoritmo_estado` + increment + evaluar umbrales fast/precise; (2) `process_temporal(pool, redis)` — recálculo batch para usuarios cuyo `ultimo_rapido` superó el intervalo; (3) `spawn_periodic_loop(pool, redis, token)` — `tokio::spawn` con `tokio::select!` sobre 3 `interval`s: refresh MV (5min), recalculate_active tag_affinity (10min), process_temporal (5min). El loop se cancela vía `CancellationToken` para shutdown limpio. `execute_fast` invalida cache + reset contadores; `execute_precise` además dispara `TagAffinityService::schedule_recalc` async + `version_perfil + 1`. Hook documentado para enchufar `GeneradorEmbeddings` cuando se porte. `AlgoPlannerConfig::legacy_defaults` con triggers fast `{likes:3, repro:8, completas:5, descargas:2, follows:2, comentarios:5}` y precise = 2× fast.
- **Archivos:** `src/algorithm/algo_planner.rs` (creado, ~310 LoC), `src/algorithm/mod.rs` (+`pub mod algo_planner`), `Cargo.toml` (tokio-util feature `rt`), `.sqlx/` (1 nuevo), `Agente/documentacion/backend/algorithm-algo-planner-2026-04-18.md`.
- **Gotchas:**
  - Clippy `type_complexity` exigió alias `CounterRow = (i32; ×12)` para la tupla del RETURNING.
  - Clippy `match_unit` exige `() = token.cancelled()` y `() = sleep(...)` en `tokio::select!` (no `_ =`).
  - `RedisPool` se importa de `deadpool_redis::Pool as RedisPool` (no existe `crate::cache::redis_pool`).
  - PowerShell wrappea stderr de cargo como `RemoteException` y devuelve exit=1 erróneo aunque clippy/test pasen — verificar con `cmd /c "... > log 2>&1"` y `$LASTEXITCODE` para evitar falsos negativos.
  - 110/110 tests verde (105 anteriores + 5 nuevos: defaults, columnas únicas, fast/precise threshold, cancel loop).
- **GLORY-RS:** No aplica. La lógica de triggers, columnas `cnt_*_preciso` y la definición de "rápido vs preciso" son específicas de Kamples. El patrón genérico (worker tokio + `CancellationToken` + `interval_at` + `select!`) ya está disponible en glory-rs vía `tokio` y `tokio-util` directamente.
- **Sentinel:** No aplica.

## 174A-54 — algorithm/tag_affinity.rs (TagAffinityService portado)

- **Que:** Reemplazo Rust del `TagAffinityService.php` legado. Mantiene la decisión arquitectónica del legado: el cómputo pesado (UNNEST de `tags_enriquecidos` + 7 JOINs) corre 1× en BD vía un único INSERT-FROM-WITH, y los lectores hacen JOIN indexado de O(1). API: `has_recent_scores`, `fetch_for_user` (HashMap<String, TagWeights>), `recalculate_for_user`, `recalculate_active`, `schedule_recalc` (con InflightLocks `Arc<Mutex<HashSet<i32>>>` para evitar duplicados in-process). Las 7 CTEs `utag_likes/repro/tiempo/descargas/completadas/dislikes/ctx` mantenidas verbatim del legado para garantizar paridad funcional. `recalculate_for_user` usa transacción explícita: DELETE + INSERT-ON-CONFLICT-DO-UPDATE.
- **Archivos:** `src/algorithm/tag_affinity.rs` (creado, ~290 LoC), `src/algorithm/mod.rs` (+`pub mod tag_affinity`), `.sqlx/` (3 archivos JSON nuevos), `Agente/documentacion/backend/algorithm-tag-affinity-2026-04-18.md`.
- **Gotchas:**
  - `sqlx::query!` con `SELECT 1 AS "_!"` falla por type inference (E0282). Reemplazado por `sqlx::query_scalar::<_, i32>` con `.bind()`.
  - Clippy `map_with_zero_sized_value_type` exige HashSet (no HashMap<_,()>) para inflight locks.
  - Clippy `too_many_lines` en `recalculate_for_user` (104 líneas por la CTE gigante) → `#[allow(clippy::too_many_lines)]` justificado.
  - Clippy `passed_by_value_not_consumed` exige `&InflightLocks` en `schedule_recalc` (clone interno via Arc::clone).
  - 105/105 tests verde (103 anteriores + 2 nuevos).
- **GLORY-RS:** No aplica. Schema `user_tag_scores` y los 7 CTEs son específicos del dominio Kamples (tags_enriquecidos, likes/reproducciones/descargas con columnas y enums propios).
- **Sentinel:** No aplica.

## 174A-53 — algorithm/precompute.rs (PrecomputadorFeed portado)

- **Que:** Reemplazo Rust del `PrecomputadorFeed.php` legado (18 CTEs SQL inline). Decisión arquitectónica: NO portar los CTEs (contradice 174A-52 de scoring en Rust); en su lugar, MATERIALIZED VIEW `mv_trending_samples` con 7 métricas pre-agregadas (`likes_24h`, `reproducciones_24h`, `descargas_7d`, `follows_creador_7d`, `tiempo_escucha_24h`, `completadas_24h`, `dislikes_7d`) + servicio Rust `PrecomputeService::{refresh, fetch_trends}`. Refresh CONCURRENTLY no bloquea SELECTs (UNIQUE INDEX `idx_mv_trending_samples_pk`). Cae a refresh no-CONCURRENTLY si la MV nunca fue populada. `recommender::fetch_enriched` ahora hace `LEFT JOIN mv_trending_samples` para alimentar tendencias reales (antes hardcoded a 0.0). Refresh automático será orquestado por 174A-55 (worker `algo_planner`).
- **Archivos:** `migrations/20260418000020_mv_trending_samples.up.sql` (creada y aplicada en BD local), `src/algorithm/precompute.rs` (creado, ~120 LoC), `src/algorithm/mod.rs` (+`pub mod precompute`), `src/algorithm/recommender.rs` (modificada `fetch_enriched`: LEFT JOIN con MV en ambas ramas — con candidatos y scan completo; eliminó LEFT JOIN LATERAL que tenía likes_24h pero NULLs en repro/descargas/follows), `.sqlx/` (regenerado), `Agente/documentacion/backend/algorithm-precompute-2026-04-18.md`.
- **Gotchas:**
  - `REFRESH MATERIALIZED VIEW CONCURRENTLY` exige UNIQUE INDEX o falla con error críptico.
  - La primera vez que se hace REFRESH después del CREATE no se puede usar CONCURRENTLY ("must be populated"). Workaround implementado: detectar mensaje y caer a refresh normal.
  - `i32 → f64` clippy exige `From::from` (infallible cast). Aplicado en `fetch_trends`.
  - LEFT JOIN con la MV permite samples sin entrada (recién creados); `COALESCE(.., 0)` garantiza 0 como fallback.
  - 103/103 tests verde (102 anteriores + 1 nuevo de defaults de TrendStats).
- **GLORY-RS:** No aplica. La MV depende del schema Kamples (likes/reproducciones/descargas/follows con columnas y enums específicos). El patrón "MV refrescada por worker" sí es agnóstico pero no se extrajo aún (no hay segundo proyecto que lo necesite todavía).
- **Sentinel:** No aplica.

## 174A-52 — algorithm/recommender.rs (MotorRecomendacion portado)

- **Que:** Reemplazo Rust del `MotorRecomendacion.php` legado (1013 LoC PHP → ~1100 LoC Rust limpio). Las 5 fases del plan en un solo módulo cohesivo: (1) API `feed` + cache stale-while-revalidate dual Redis-o-DashMap, (2) bulk-fetch de 3 páginas en una pasada (`PAGINAS_BULK=3`), (3) warm async con `tokio::spawn` + lock SETNX (`warm_lock_ttl=90`), (4) `similar_to_sample` (pgvector con fallback Jaccard+tipo), (5) `invalidate_user_feed` y `invalidate_global`. Decisión arquitectónica clave: scoring en Rust usando `algorithm::signals` (no SQL inline gigante como el legado). `RecommenderConfig::legacy_defaults()` con paridad exacta de `algoritmoPesos.php` (TTL 300/900/7200, umbral 5000, max creador/género/tipo 3/4/5). Cold start cae a `fallback_recientes` (top trending por engagement). Diversidad post-scoring penaliza creador/género/tipo repetidos. Filtro bidireccional de bloqueos vía `collect_blocked` (list_blocked + list_blockers). Hooks documentados para conectar `PrecomputadorFeed` (174A-53) y `TagAffinityService` (174A-54).
- **Archivos:** `src/algorithm/recommender.rs` (creado, ~1100 LoC con queries SQL + lógica), `src/algorithm/recommender/tests.rs` (8 tests), `src/algorithm/mod.rs` (+`pub mod recommender`), `.sqlx/` (5 archivos JSON nuevos generados por `cargo sqlx prepare`), `Agente/documentacion/backend/algorithm-recommender-2026-04-18.md`, `Agente/planes/completados/plan-recommender-2026-04-18.md` (movido).
- **Gotchas:**
  - Las queries `sqlx::query!` necesitan `cargo sqlx prepare` con BD local viva. Sin eso, falla con "no cached data".
  - `seed.tipo` y `seed.tags` venían como `Option` por defecto: usar `AS "tipo!"` y `AS "tags!"` para forzar NOT NULL en el tipo derivado.
  - Clippy disparó muchos `cast_precision_loss`/`cast_possible_wrap` inherentes al dominio numérico → `#![allow]` a nivel de módulo justificado en comentario.
  - `RecommenderConfig` es 352B (>256B) → clippy `large_types_passed_by_value` se permite por diseño (config se construye una vez).
  - `signals::AlgorithmSignalConfig` es `Copy`, así que `compute_and_cache` lo recibe por ref pero `spawn_warm` lo mueve por valor a `tokio::spawn` sin clone.
  - El primer tag se usa como proxy de "género dominante" para diversidad (el legado leía `metadata->'genero'` JSONB; paridad funcional, no literal).
  - Resultados: 102/102 tests verde (94 anteriores + 8 nuevos).
- **GLORY-RS:** No aplica. Toda la lógica es específica del dominio Kamples (samples musicales, scoring por BPM/key/escala/tipo, queries contra schema kamples). Lo agnóstico ya está en `algorithm::signals` (174A-49) y patrón de cache dual Redis-o-DashMap (que sí podría extraerse a glory-rs como helper genérico en una iteración futura, pero no en esta tarea).
- **Sentinel:** No aplica.


## 174A-30 — Base FFmpeg del pipeline de audio

- **Que:** Crear `src/audio/ffmpeg.rs` como base híbrida del pipeline de audio. El módulo detecta `ffmpeg`/`ffprobe`, inspecciona duración/formato/sample-rate/canales/tamaño, genera `waveform_peaks` y expone conversiones `convert_to_mp3()` y `convert_to_flac()`. Cuando FFmpeg no está disponible, metadata y waveform caen a Symphonia; las conversiones fallan de forma explícita.
- **Archivos:** `src/audio/ffmpeg.rs`, `src/audio/ffmpeg/detection.rs`, `src/audio/ffmpeg/tests.rs`, `src/audio/mod.rs`, `Agente/documentacion/backend/audio-ffmpeg-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El repo impone límite de líneas por archivo, así que la detección de binarios y los tests se movieron a submódulos. `clippy -D warnings` también obligó a evitar casts flotantes amplios para calcular duración y a usar conversión segura de índices/buckets.
- **Sentinel:** No aplica.
- **GLORY-RS:** La capa de detección/conversión/waveform es agnóstica y candidata a `glory-rs`, pero se deja local hasta cerrar `174A-34` para no congelar prematuramente una interfaz compartida.

## 174A-31 — Detector BPM con Symphonia + onset + autocorrelación

- **Que:** Crear `src/audio/bpm.rs` con detección de tempo determinista sobre muestras mono. El módulo remuestrea a `8 kHz`, calcula energía RMS en ventanas de `50ms`, construye una función de onset por diferenciación positiva y estima el BPM con autocorrelación en el rango `60..200`. Expone API para muestras en memoria y para archivos reales vía Symphonia.
- **Archivos:** `src/audio/bpm.rs`, `src/audio/bpm/tests.rs`, `src/audio/mod.rs`, `Agente/documentacion/backend/audio-bpm-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** Para pasar `clippy --all-targets -D warnings` hubo que evitar casts flotantes innecesarios en fixtures y trabajar con duraciones enteras. El remuestreo quedó en nearest-neighbor porque el objetivo actual es tempo grueso, no análisis espectral fino.
- **Sentinel:** No aplica.
- **GLORY-RS:** El detector BPM y su API son agnósticos y muy movibles a `glory-rs`, pero conviene esperar a estabilizar `174A-34` y `174A-35` para extraerlo junto al resto del pipeline compartido.

## 174A-32 — Detector de tonalidad con cromagramas FFT

- **Que:** Crear `src/audio/key.rs` con detección de `music_key` y `scale` usando FFT sobre audio mono remuestreado a `22.05 kHz`, acumulación de cromagrama por pitch class y comparación contra perfiles mayor/menor rotados. El módulo expone API para muestras en memoria y para archivos reales con Symphonia.
- **Archivos:** `src/audio/key.rs`, `src/audio/key/tests.rs`, `src/audio/mod.rs`, `Agente/documentacion/backend/audio-key-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El primer mapeo cromático tenía un sesgo en la frontera `C5/B4`; se corrigió comparando cada pitch class contra varias octavas. También hubo que bajar el peso de armónicos altos y usar fixtures sinusoidales/escalas en lugar de tríadas cuadradas para validar la tónica real sin ambigüedad artificial.
- **Sentinel:** No aplica.
- **GLORY-RS:** Igual que BPM, el detector de key es agnóstico y candidato claro a `glory-rs`, pero se mantiene local hasta cerrar la interfaz del pipeline de audio completo.

## 174A-33 — Embeddings 128d deterministas para samples y perfiles

- **Que:** Crear `src/audio/embeddings.rs` con el vector `128d` legacy-compat para similitud y perfilado. El módulo codifica BPM, tonalidad, escala, tipo, duración, premium y tags hasheados con `CRC32`, además de helpers de ida y vuelta con `pgvector::Vector` y agregado ponderado con normalización L2 para perfiles de usuario.
- **Archivos:** `src/audio/embeddings.rs`, `src/audio/embeddings/tests.rs`, `src/audio/mod.rs`, `Cargo.toml`, `Agente/documentacion/backend/audio-embeddings-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** `serde` no derivó bien sobre arrays de `128`, así que el embedding se dejó como tipo puro sin serialización automática. También hubo que respetar el comportamiento legacy de `sample_type`: solo distingue `loop` y `one_shot`, aunque el vector reserve cinco slots.
- **Sentinel:** No aplica.
- **GLORY-RS:** El generador de embeddings y el perfil ponderado son suficientemente agnósticos para moverse a `glory-rs`, pero conviene hacerlo cuando `174A-34` fije el contrato del pipeline y las necesidades reales de persistencia.

## 174A-34 — Orquestador técnico `audio_pipeline.rs`

- **Que:** Crear `src/services/audio_pipeline.rs` como servicio técnico reutilizable para el pipeline de audio. El servicio descarga el original desde `FileStorage`, trabaja en un directorio temporal, corre inspección `ffmpeg/symphonia`, detección BPM, detección de tonalidad, genera `waveform` JSON, convierte a MP3 optimizado, construye el embedding `128d` y persiste resultados por etapas hasta activar el sample.
- **Archivos:** `src/services/audio_pipeline.rs`, `src/services/audio_pipeline/tests.rs`, `src/services/mod.rs`, `src/repositories/sample.rs`, `src/repositories/mod.rs`, `.sqlx/query-108725a2454cc291259b2c8dd57609acb890e3d3e6f4393eb64f2a54b8780d88.json`, `.sqlx/query-4409b35859c832d56ce7243b01473f477ba1e3482829d7a3f8a96f45459c8dc6.json`, `.sqlx/query-7fddbdb6614a807a885c107763f28cf1f86c06e403b8e0053d467f51c685b077.json`, `.sqlx/query-bbcbf80539f846ac22f6e9ebeb6ed5a62f7229628a815355ec2f5b66b04be20d.json`, `.sqlx/query-e47101be3abbd50568acb1914c73d9b41f60e565ea2c1980c24ac37097d897c7.json`, `Agente/documentacion/backend/audio-pipeline-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El plan original hablaba de `pending_processing`, pero el estado real del repo usa `samples.estado = 'procesando'` más `cola_procesamiento_ia.operacion = 'analisis_audio'`; el servicio quedó desacoplado de la cola para que `174A-35` resuelva el consumidor real. También hubo que regenerar `.sqlx/` porque todas las queries nuevas se escribieron con macros `query!` / `query_as!`.
- **Sentinel:** No aplica.
- **GLORY-RS:** El servicio es bastante agnóstico y reusable, pero todavía depende del schema concreto de `samples` y de la convención local de derivados (`ruta_waveform`, `ruta_optimizada`), así que aún no conviene moverlo fuera del repo.

## 174A-35 — Worker async para la cola real de análisis de audio

- **Que:** Crear el consumidor background del pipeline técnico usando la tabla real `cola_procesamiento_ia` en lugar del nombre abstracto `pending_processing`. El worker reclama jobs con `FOR UPDATE SKIP LOCKED`, ejecuta `AudioPipelineService`, marca `completado` al éxito y aplica backoff exponencial (`15/30/60/120 min`) cuando hay errores reintentables.
- **Archivos:** `src/repositories/processing_queue.rs`, `src/repositories/mod.rs`, `src/workers/audio_pipeline_worker.rs`, `src/workers/mod.rs`, `src/main.rs`, `.sqlx/query-ac8eaaca695b1b75ff3d59e70f7e655e88ab07510657dd7f5c9e0dc96bc97dbc.json`, `.sqlx/query-bb91037c898d550fca34d3449af2d34b5fceee273dfdb15d6bd6da0c532e1691.json`, `.sqlx/query-cfd095508489697b6598cc0375476db563ac5470c23ec151ff3e8229746cb436.json`, `Agente/documentacion/backend/audio-pipeline-worker-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** La cola compartida ya tenía semántica legacy propia (`pendiente`, `procesando`, `error_reintento`, `error_final`), así que el worker debía alinearse con ella y no inventar un segundo sistema. También hizo falta arrancar los consumidores desde `main.rs` sin bloquear el servidor y sin romper el soporte actual de `LocalFs`/S3.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de worker es portable, pero el repositorio sigue atado a `cola_procesamiento_ia` y a las convenciones de esta app. Conviene extraerlo solo cuando exista un contrato de cola compartido entre proyectos.

## 174A-36 — Fixtures de audio para pruebas compuestas del pipeline

- **Que:** Ampliar `src/services/audio_pipeline/tests.rs` con WAVs sintéticos que combinan pulsos rítmicos y contenido tonal en un mismo archivo para probar de forma conjunta `inspect_audio_file`, BPM, key/scale, metadata del pipeline y embedding.
- **Archivos:** `src/services/audio_pipeline/tests.rs`, `Agente/documentacion/backend/audio-pipeline-fixtures-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El primer intento mezclando clicks y notas dejó onsets ambiguos y el detector BPM no convergía bien; el fixture se rediseñó como una nota por beat con silencio entre eventos para que el ritmo y la tonalidad salgan del mismo WAV sin ruido artificial. También hubo que evitar comparaciones exactas de `f32` para respetar `clippy -D warnings`.
- **Sentinel:** No aplica.
- **GLORY-RS:** Este patrón de fixtures sintéticos es reusable fuera del repo, especialmente para validar futuros extractores DSP sin cargar binarios pesados en control de versiones.

## 174A-37 — Cliente Groq reusable para la fase IA

- **Que:** Crear `src/audio/ia/groq.rs` como cliente HTTP reusable para Groq con rotación round-robin de `GROQ_API_1..3`, retry configurable por modelo y fallback chain de modelos LLM antes de llegar al service IA de negocio.
- **Archivos:** `src/audio/ia/mod.rs`, `src/audio/ia/groq.rs`, `src/audio/ia/groq/tests.rs`, `src/audio/mod.rs`, `Agente/documentacion/backend/audio-groq-client-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** En PHP la key se fijaba por proceso corto y se rotaba al final del cron; en Rust el proceso es persistente, así que la rotación por intento era más defendible para repartir cuota en caliente. El cliente también devuelve contenido bruto y fallos tipados, porque `JsonRepairer` y el parsing semántico todavía pertenecen a las tareas siguientes.
- **Sentinel:** No aplica.
- **GLORY-RS:** La lógica HTTP y de retry es bastante reusable, pero los nombres de env vars y la cadena de modelos siguen siendo decisiones del producto Kamples; mejor extraerlo después de estabilizar Groq + OpenAI + JsonRepairer.

## 174A-38 — Cliente OpenAI como fallback final

- **Que:** Crear `src/audio/ia/openai.rs` como wrapper del fallback final OpenAI, con `OPENAI_API_KEY`, modelo por defecto `gpt-4o-mini` y soporte explícito para reintentar sin `response_format=json_object` cuando OpenAI devuelve `json_validate_failed`.
- **Archivos:** `src/audio/ia/openai.rs`, `src/audio/ia/openai/tests.rs`, `src/audio/ia/mod.rs`, `Agente/documentacion/backend/audio-openai-client-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** Intentar abstraer Groq y OpenAI en este punto era tentador, pero prematuro: ambos proveedores todavía difieren en política de errores y el valor real ahora era capturar el caso concreto de OpenAI donde falla el modo JSON estricto y hay que recuperar una respuesta bruta para `JsonRepairer`.
- **Sentinel:** No aplica.
- **GLORY-RS:** Este wrapper es reusable como patrón de cliente HTTP tipado, pero el retry sin `response_format` responde a un comportamiento específico de OpenAI en este pipeline.

## 174A-39 — JsonRepairer local y tolerante

- **Que:** Crear `src/audio/ia/json_repairer.rs` para rescatar metadata creativa desde respuestas LLM imperfectas usando parseo directo, fenced blocks, extracción de objeto balanceado, limpieza de control chars y `json5` como parser tolerante.
- **Archivos:** `Cargo.toml`, `Cargo.lock`, `src/audio/ia/json_repairer.rs`, `src/audio/ia/json_repairer/tests.rs`, `src/audio/ia/mod.rs`, `Agente/documentacion/backend/audio-json-repairer-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El peligro aquí era sobre-ingenierizar una “reparación mágica” remota antes de tiempo. La tarea pedía resolver el caso local de respuestas mal formadas; usar `json5` más un extractor balanceado cubre mucho más que el regex greedy del legacy y evita dependencia circular entre proveedores para reparar JSON roto.
- **Sentinel:** No aplica.
- **GLORY-RS:** El módulo es altamente reusable para cualquier flujo LLM que devuelva JSON aproximado, aunque la normalización final de campos sigue siendo específica del dominio samples/audio.

## 174A-40 — Prompts IA centralizados

- **Que:** Crear `src/audio/ia/prompts.rs` para centralizar el system prompt compartido, el contrato JSON esperado y los builders de prompt para análisis, transcripción y corrección de metadata.
- **Archivos:** `src/audio/ia/prompts.rs`, `src/audio/ia/prompts/tests.rs`, `src/audio/ia/mod.rs`, `src/audio/ia/groq.rs`, `src/audio/ia/openai.rs`, `src/audio/ia/json_repairer.rs`, `src/audio/ia/json_repairer/tests.rs`, `Agente/documentacion/backend/audio-prompts-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** Al extraer prompts apareció un drift real del contrato: el parser Rust ya no estaba contemplando `carpeta_primaria` y `carpeta_secundaria`, aunque el prompt legacy y la BD sí. Se corrigió en esta misma tarea para no contaminar `ia_service.rs` con inconsistencias heredadas.
- **Sentinel:** No aplica.
- **GLORY-RS:** La estructura de builders y constantes es reusable para otros dominios IA, pero el texto concreto del prompt y el shape JSON siguen siendo propios del dominio Kamples audio.

## 174A-41 — Servicio IA para metadata creativa

- **Que:** Crear `src/services/ia_service.rs` como capa de negocio que compone prompts, Groq, OpenAI fallback y `JsonRepairer` para devolver `AudioCreativeMetadata` ya normalizada, con señales de retry/backoff listas para el worker de cola.
- **Archivos:** `src/services/ia_service.rs`, `src/services/ia_service/tests.rs`, `src/services/mod.rs`, `Agente/documentacion/backend/audio-ia-service-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El riesgo aquí era mezclar lógica de proveedor con lógica de orquestación y volver a duplicar fallback en el worker. El servicio quedó puro y sin dependencia de BD/cola para que `174A-42` solo consuma `ia_queue` y tome decisiones de scheduling.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de orquestación es reusable, pero el contrato concreto de prompt, shape JSON y política de fallback sigue siendo específico del dominio samples/audio en Kamples.

## 174A-42 — Worker y cola `ia_queue`

- **Que:** Crear una cola separada `ia_queue` y su worker background para procesar metadata creativa con backoff exponencial y respeto por `retry_after`, sin reutilizar la cola técnica `cola_procesamiento_ia` que en Rust ya quedó dedicada al pipeline DSP.
- **Archivos:** `migrations/20260418000010_ia_queue.up.sql`, `src/repositories/ia_queue.rs`, `src/repositories/mod.rs`, `src/repositories/sample.rs`, `src/services/ia_queue.rs`, `src/services/ia_queue/tests.rs`, `src/services/audio_pipeline.rs`, `src/services/mod.rs`, `src/workers/ia_queue_worker.rs`, `src/workers/mod.rs`, `src/main.rs`, `.sqlx/query-0322d15289521d9d6c0dc9efb885f7f971b98e1d13a1f1ef4395a7f47f0bd971.json`, `.sqlx/query-180e0e52a66a54759ddcb15a1e56cd6e8fa3d6732b1abd617cb0695403ed19b1.json`, `.sqlx/query-528261fcca8aead9b2feffe3a7f3df50ed42327e4c45b6650ddaf7f2d0c4271d.json`, `.sqlx/query-59fb1e41226a173aacd0023ee2e6479ba4d68bdae6a79c0ed3608c9d198f4855.json`, `.sqlx/query-75a248dde57cb6b9c4a79dcf8fd0e4f3b371d052bf3f6b0c1d5c7e0c47f42601.json`, `.sqlx/query-a2e237dacf206d5a5b202bbb44eb0e010f8e2858b5f516f54613b207acb8c8e3.json`, `.sqlx/query-f3cee0d7c20efc59bd895f2679dcd39ce99c2ce053f2c6d60ed1c41c034beeb0.json`, `.sqlx/query-bbcbf80539f846ac22f6e9ebeb6ed5a62f7229628a815355ec2f5b66b04be20d.json` (eliminado), `Agente/documentacion/backend/audio-ia-queue-worker-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El plan hablaba de `ia_queue`, pero el repo no la tenía y la cola legacy ya estaba reasignada al pipeline técnico. La corrección raíz fue separar ambas colas y hacer atómica la activación del sample con el enqueue IA, para evitar samples activos que jamás reciban metadata creativa.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de cola/worker es reusable, pero la transacción de activación, el shape de metadata y la decisión de preservar carpetas manuales siguen siendo específicas del dominio samples/audio en Kamples.

## 174A-43 — Servicio de moderación en 4 capas

- **Que:** Crear `src/services/moderation/` como motor reusable de moderación con cuatro capas explícitas: pre-filtro local barato, categorización IA con Groq + fallback OpenAI, decisión final con umbrales de confianza y payload listo para panel admin. El contrato devuelve veredicto, evidencia, fallos de proveedor y `recommended_sample_state()` para integrar luego con `samples.estado` sin reescribir reglas.
- **Archivos:** `src/audio/ia/groq.rs`, `src/services/mod.rs`, `src/services/moderation/mod.rs`, `src/services/moderation/types.rs`, `src/services/moderation/local_rules.rs`, `src/services/moderation/tests.rs`, `Agente/documentacion/backend/moderation-service-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** La carpeta raíz `src/services/` ya estaba al límite de archivos y Code Sentinel bloqueó agregar otro módulo plano; por eso el servicio se movió a `src/services/moderation/` en lugar de silenciar la regla. También se evitó conectar el servicio a una `moderation_queue` inexistente en Rust para no reintroducir el drift entre el schema legacy y el runtime nuevo.
- **Sentinel:** No aplica.
- **GLORY-RS:** El motor de decisión y el patrón de degradación segura son bastante agnósticos, pero los prompts, categorías y fallback actual siguen reflejando políticas concretas de Kamples; conviene extraerlo recién cuando exista un segundo proyecto con reglas comparables.

## 174A-44 — GET /samples con paginación y filtros

- **Que:** Implementar el catálogo público `GET /samples` con paginación y filtros combinables sobre BPM, key, type, tags, premium y creator. La solución quedó separada en handler, servicio y repositorio para no seguir cargando `handlers/samples.rs` y `repositories/sample.rs`, y devuelve `data[]` más `pagination` en un contrato ya documentado en OpenAPI.
- **Archivos:** `src/handlers/mod.rs`, `src/handlers/sample_catalog.rs`, `src/models/mod.rs`, `src/models/sample.rs`, `src/repositories/mod.rs`, `src/repositories/sample_catalog.rs`, `src/services/mod.rs`, `src/services/sample_catalog/mod.rs`, `src/services/sample_catalog/tests.rs`, `Agente/documentacion/backend/samples-listing-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** `cargo fmt` tocó muchos archivos fuera del scope por normalización del worktree, así que el diff se contuvo manualmente a los módulos reales de la tarea. También se eligió `QueryBuilder` en vez de una explosión de `sqlx::query!` para mantener filtros opcionales seguros sin duplicar SQL por combinación.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de paginación es reusable, pero este catálogo todavía depende del schema concreto de `samples`, `usuarios_ext` y de reglas de visibilidad comunitaria (`estado activo`, `mostrar_en_comunidad`), así que por ahora no conviene moverlo a `glory-rs`.

## 174A-45 — GET /samples/{slug} y GET /samples/random

- **Que:** Completar las lecturas públicas de samples con detalle por `slug`/`id_corto` y sample aleatorio del catálogo. La implementación también corrigió la normalización de assets del catálogo para devolver URLs públicas consumibles en `preview`, `waveform`, `imagen`, y exponer `ruta_original`/`ruta_optimizada` solo al creador autenticado.
- **Archivos:** `src/handlers/mod.rs`, `src/handlers/sample_catalog.rs`, `src/models/mod.rs`, `src/models/sample.rs`, `src/repositories/mod.rs`, `src/repositories/sample_catalog.rs`, `src/services/sample_catalog/mod.rs`, `src/services/sample_catalog/tests.rs`, `.sqlx/query-33dc1859c59a876b39a67291658a7c8fbda894e28093b28d8d0126663d584f7b.json`, `.sqlx/query-6937b153a91c9fb61e7d2479a4f037e4e1b51ce67050339c4a237ddb5a32444b.json`, `Agente/documentacion/backend/samples-detail-random-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El roadmap ya advertía que había queries sin macro, y justamente detalle/random quedaron bloqueados por `SQLX_OFFLINE=true` hasta migrarlos a `query_as!` y regenerar `.sqlx/`. Además hubo que proteger explícitamente el orden de rutas para que `random` no cayera en `:slug`.
- **Sentinel:** No aplica.
- **GLORY-RS:** La normalización de URLs y el lookup dual son patrones reutilizables, pero el endpoint sigue atado al schema local de `samples`, a `PUBLIC_BASE_URL` y a la política concreta de visibilidad pública vs. owner assets de Kamples.

## 174A-46 — PATCH /samples/{slug} y DELETE /samples/{slug}

- **Que:** Completar las mutaciones owner-only de samples con edición parcial por `slug`/`id_corto` y soft-delete a papelera. El patch quedó con normalización explícita de título, descripción, tags, `type`, flags de visibilidad/licencia y limpieza automática de `precio` cuando un sample deja de ser premium.
- **Archivos:** `src/handlers/mod.rs`, `src/handlers/sample_catalog.rs`, `src/models/mod.rs`, `src/models/sample.rs`, `src/repositories/mod.rs`, `src/repositories/sample_catalog.rs`, `src/services/sample_catalog/mod.rs`, `src/services/sample_catalog/tests.rs`, `.sqlx/query-a7e7a1e955d29b0c7289ed089e14c8d158fe42c3a2673a30de27b0ec75c8378f.json`, `.sqlx/query-d1a814592f1392a0c694d8e2c39c09ca334a9bc4c7cf03379ae8d86c4ab5c4b8.json`, `Agente/documentacion/backend/samples-owner-mutations-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El update necesitaba SQL dinámico porque los campos son opcionales; la solución fue usar `QueryBuilder` solo para el `PATCH` y mantener `query_as!` / `query!` en el lookup owner-only y el soft-delete para seguir respetando el objetivo de `SQLX_OFFLINE=true`. Clippy también obligó a eliminar un `drop(separated)` artificial en favor de un scope explícito.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón de mutación owner-only es reusable, pero sigue acoplado al schema concreto de `samples`, al estado `eliminado` y a la política local de papelera, así que todavía conviene dejarlo en este repo.

## 174A-47 — Búsqueda fuzzy trigram en `GET /samples`

- **Que:** Extender el catálogo público con búsqueda textual fuzzy sobre el mismo `GET /samples`, combinable con los filtros ya existentes. El ranking quedó cercano al legado: `FTS(titulo+descripcion)` + boost por tags + `FTS(titulo)` + `word_similarity()` + bonus por prefijo exacto del título.
- **Archivos:** `src/handlers/sample_catalog.rs`, `src/models/sample.rs`, `src/repositories/mod.rs`, `src/repositories/sample_catalog.rs`, `src/services/sample_catalog/mod.rs`, `src/services/sample_catalog/tests.rs`, `Agente/documentacion/backend/samples-fuzzy-search-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** No hizo falta tocar migraciones porque el repo ya tenía `pg_trgm`, índice trigram de título e índices FTS. La complejidad real estuvo en no romper el listado paginado existente y mantener toda la búsqueda como un bloque opcional dentro del `QueryBuilder` para no multiplicar variantes de SQL.
- **Sentinel:** No aplica.
- **GLORY-RS:** La composición `texto + filtros + ranking` es reusable como patrón, pero por ahora sigue acoplada a `samples`, `tags_enriquecidos` y a las reglas concretas de visibilidad pública de Kamples.

## 174A-48 — Similitud pgvector en `GET /samples/{id}/similar`

- **Que:** Añadir el endpoint público de samples similares por `id`, priorizando búsqueda ANN con `pgvector` y manteniendo fallback contextual cuando el sample base no tiene embedding o la búsqueda vectorial no devuelve filas. La respuesta reutiliza `SampleSummary` en un envelope ligero `data[]`.
- **Archivos:** `src/handlers/mod.rs`, `src/handlers/sample_catalog.rs`, `src/models/mod.rs`, `src/models/sample.rs`, `src/repositories/sample_catalog.rs`, `src/repositories/sample_catalog/query.rs`, `src/repositories/sample_catalog/similar.rs`, `src/services/sample_catalog/mod.rs`, `src/services/sample_catalog/tests.rs`, `Agente/documentacion/backend/samples-similarity-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El repo rechaza `sqlx::query_as()` runtime y además impone límite de líneas por archivo. Para cerrar la tarea hubo que resolver la similitud con `QueryBuilder` y partir `sample_catalog.rs` en submódulos internos (`query.rs`, `similar.rs`) en vez de seguir creciendo el archivo principal.
- **Sentinel:** No aplica.
- **GLORY-RS:** El patrón `ANN + fallback contextual` es reusable, pero hoy sigue acoplado a `samples`, a los pesos heredados del algoritmo Kamples y a la política concreta de visibilidad pública del catálogo.

## 174A-49 — `algorithm/signals.rs` con las 6 señales

- **Que:** Crear el núcleo puro del algoritmo de descubrimiento en `src/algorithm/signals.rs`, con pesos, inputs y breakdown ponderado para las 6 señales: similitud de contenido, comportamiento, contexto, tendencias, grafo social y novedad.
- **Archivos:** `src/algorithm/mod.rs`, `src/algorithm/signals.rs`, `src/algorithm/signals/tests.rs`, `Agente/documentacion/backend/algorithm-signals-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El comentario legacy del módulo y parte del plan seguían hablando de pesos placeholder `0.25/0.25/0.15/0.15/0.10/0.10`, pero el legado vivo ya estaba recalibrado (`0.28/0.27/0.15/0.12/0.10/0.0`). La tarea se cerró contra `algoritmoPesos.php` y `ConstructorSenales.php`, no contra el comentario desfasado.
- **Sentinel:** No aplica.
- **GLORY-RS:** Este módulo es buen candidato a moverse a `glory-rs` cuando existan `profile`, `candidates` y `recommender`, porque ya quedó puro y sin dependencia de infraestructura.
## 174A-50  PerfilUsuario portado a Rust con cache 30min

- **Que:** Crear `src/algorithm/profile.rs` con `UserProfile` y `ProfileService::build/invalidate` replicando el contrato del legado vivo (`PerfilUsuario.php` + `UsuariosExtRepository::perfilCompletoParaAlgoritmo`). Una CTE única extrae interacciones + bpm + key + escala + tipo en un roundtrip; query separada para top 5 creadores favoritos con pesos legacy (encanta=2, like=1, reproducción=0.5, descarga=1.5); géneros declarados desde `usuarios_ext.generos_favoritos`. Cache dual Redis-or-memory con TTL 1800s y clave compatible `kamples_perfil_usr_{id}`.
- **Archivos:** `src/algorithm/mod.rs`, `src/algorithm/profile.rs`, `src/algorithm/profile/tests.rs`, `.sqlx/query-2ceea36701be3af36c0f3ed9cc4a5996c20c7f9469a9bb969a63784a657a066d.json`, `.sqlx/query-afc05e51f1de7da379bc2d80aa68bbfa8c9989eb83fea39008e93e1fa153a16e.json`, `.sqlx/query-dc5ef582675bc605534022c8d3cb7dcc85704afb9fd369f95aba850b0ff2c56b.json`, `Agente/documentacion/backend/algorithm-profile-2026-04-18.md`, `Agente/completados/tareas-2026-04-18.md`, `roadmap.md`
- **Gotchas:** El plan colapsaba en uno solo `PerfilUsuario` y `GeneradorEmbeddings::perfilUsuario`, pero en el legado son dos cosas distintas: el primero son señales escalares (este módulo) y el segundo es un vector 128d; este último se portará en el recomendador/precomputador. También el plan sugería TTL 30min y el legado ya usa exactamente 1800s, así que coinciden y se documentó. Para que `query!` validara las CTE hubo que correr `cargo sqlx prepare` contra la BD local y commitear las 3 nuevas entradas en `.sqlx/`.
- **Sentinel:** No aplica.
- **GLORY-RS:** Específico de Kamples (tablas, columnas, pesos del dominio musical). No procede mover a `glory-rs`.

## 174A-51  SelectorCandidatos portado (algorithm/candidates.rs)

- **Que:** Porte de SelectorCandidatos.php a Rust. 6 fuentes de candidatos (trending, embedding ANN, seguidos, top-tags, populares, no reproducidos), filtro bidireccional de bloqueos, cache 1h del conteo de samples activos (Redis o memoria). Diseño retorna Vec<i32> en lugar de fragmento SQL: más seguro, compile-time check vía sqlx::query!. Añadido ModerationRepository::list_blockers para el filtro bidireccional.
- **Archivos:** src/algorithm/mod.rs, src/algorithm/candidates.rs, src/algorithm/candidates/tests.rs, src/repositories/moderation.rs, .sqlx/query-*.json (5 nuevos), Agente/documentacion/backend/algorithm-candidates-2026-04-18.md, 
oadmap.md.
- **Gotchas:** sqlx macros no aceptan INTERVAL parametrizado; resuelto con match exhaustivo sobre la whitelist [7,14,30,60,90] + literales validados estáticamente. El vector del perfil (embedding 128d) sigue pendiente para 174A-52: aquí se acepta como Option<&Vector> y se omite la fuente embedding si es None.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica  lógica 100% específica de Kamples (samples/follows/reproducciones con semántica bpm/key/escala).

## 174A-57 - Metricas algo_timing + endpoint admin
- **Que:** Port directo de AlgoTimingLogger.php (solo userId target). Singleton in-process con DashMap (en vuelo) + RwLock<VecDeque> (cap 100). Hooks integrados en RecommenderService::feed: start, mark fresh/stale hit/miss, mark compute, save. Endpoint admin GET /api/admin/algo-timing?limit=N (1..=100, default 50, requiere admin).
- **Archivos:** src/services/algo_timing.rs (nuevo), src/services/mod.rs, src/algorithm/recommender.rs, src/handlers/admin.rs, src/handlers/mod.rs, openapi.json, frontend/src/api/generated.ts, Agente/documentacion/backend/services-algo-timing-2026-04-18.md.
- **Gotchas:** indexmap NO esta en Cargo.toml -> reemplazado por Vec<TimingStage{name,ms}> que mantiene orden de insercion sin dep extra. EXPLAIN ANALYZE NO portado (hook futuro documentado).
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica - el target_user_id default 1 es especifico de Kamples QA. Si en el futuro se generaliza a 'profiling per-user opt-in', considerar mover a glory-rs como middleware de tracing.

## 174A-58 - POST /samples/{id}/play
- **Que:** Endpoint nuevo POST /api/samples/{id}/play con debounce 3s, incremento de samples.total_reproducciones, y trigger de AlgoPlanner::register_interaction(Reproduccion / Completa). AppState recibe Arc<AlgoPlanner> inicializado con legacy_defaults.
- **Archivos:** src/repositories/play.rs (nuevo), src/repositories/mod.rs, src/handlers/plays.rs (nuevo), src/handlers/mod.rs, src/algorithm/mod.rs (re-exports), src/lib.rs (AppState), .sqlx/, openapi.json, frontend/src/api/generated.ts.
- **Gotchas:** RateLimiter 60/min NO portado (no existe el modulo aun) -> dejado TODO en el handler. clippy excessive_bools: separar triggered_fast/precise en sub-struct PlayTriggered. clippy cast_precision_loss: anotacion #[allow] en cast i64->f64 para make_interval.
- **Sentinel:** No aplica.
- **GLORY-RS:** No aplica - el endpoint y la tabla reproducciones son especificos de Kamples. Si en el futuro se generaliza tracking de eventos (events table), considerar mover a glory-rs.
