# Tareas completadas — 2026-04-06

## 064A-56 — Stripe VITE_STRIPE_PUBLISHABLE_KEY: crear frontend/.env
- **Qué:** Vite necesita variables con prefijo `VITE_` para exponerlas al frontend. El .env raíz tiene `GLORY_STRIPE_PUBLISHABLE_KEY` pero el frontend buscaba `VITE_STRIPE_PUBLISHABLE_KEY` que no existía. Creado `frontend/.env` con el valor correcto. El archivo no se commitea (.gitignore lo excluye).
- **Archivos:** frontend/.env (nuevo, local), roadmap.md
- **Gotchas:** PS5 no soporta `-Encoding utf8NoBOM`, usar `-Encoding utf8`. El patrón `.env` del .gitignore raíz cubre recursivamente todos los .env del repo.
- **Sentinel:** No aplica.

## 064A-55 — CheckoutModal: instalar Stripe types + migrar a <Modal> base
- **Qué:** Instalados @stripe/stripe-js y @stripe/react-stripe-js (estaban en package.json pero no en node_modules). Migrado CheckoutModal de overlay artesanal a <Modal> del sistema (focus trap, Escape, scroll lock). Eliminado CSS overlay redundante. El Modal base ya provee padding 30px, border-radius, background y border.
- **Archivos:** CheckoutModal.tsx, CheckoutModal.css, frontend/package-lock.json
- **Gotchas:** El componente se monta/desmonta condicionalmente desde OrdenDetalle (checkout && <CheckoutModal>), por lo que usa `abierto={true}` (siempre visible cuando montado).
- **Sentinel:** No aplica.

## 064A-54 — UI fixes: panel padding, checkout modal, footer btn, typing indicator
- **Qué:** panelContenedor: padding top/bottom → 0 (usuario pidió quitar). panelContenido: padding var(--spacing-xl)→var(--spacing-lg) para igualar panelSidebar. CheckoutModal: padding var(--spacing-xl)→var(--spacing-lg) (~30px), eliminado botón X (cerrar con clic en overlay). Footer botonFooter: border-color transparent→var(--bg-item-active). Chat: visitante ya no ve su propio "escribiendo" (filtro sender !== 'visitor'/'client').
- **Archivos:** PanelIsland.css, CheckoutModal.css, CheckoutModal.tsx, Footer.css, useChatWidget.ts
- **Gotchas:** CheckoutModal tiene @stripe imports preexistentes sin paquete local. Code Sentinel sugiere usar <Modal> compartido — pendiente refactor.
- **Sentinel:** No aplica.

## 064A-53 — UI quick fixes batch: send btn, showcase, panel, selects, sidebar, avatar
- **Qué:** Chat send button sin fondo (background: none). showcaseCategoria: position: static en mobile (≤1024px) para evitar sticky feo. panelContenedor: removido padding-bottom excesivo (var(--spacing-3xl) → 0). MenuContextual: removido box-shadow prohibido del panel dropdown. usuariosFiltroBtn: border-color var(--bg-item-active), padding explícito. sidebarUsuario: display: none (redundante con avatar en HeaderPanel). headerPanelAvatar: 40px→38px (2px más pequeño). Default avatar: SVG inline con fondo taupe (#c4b5a5) y silueta clara (#f5f3f1), más contraste y aspecto moderno.
- **Archivos:** ChatWidget.css, SeccionShowcase.css, PanelIsland.css, ContextMenu.css, SeccionUsuarios.css, SidebarPanel.css, HeaderPanel.css, useCurrentProfile.ts
- **Gotchas:** El MenuContextual tenía box-shadow con var(--border-light) que violaba la regla "ningún menú tiene que usar sombras". El botonOutline base usa --brand-black para border, pero los filtros necesitan --bg-item-active.
- **Sentinel:** No aplica.

## 064A-52 — Chat widget UI batch: 10 fixes
- **Qué:** Eliminada lógica de pedir nombre (ChatWidgetNameForm) — el chat abre directamente y el agente IA pide el nombre. Input restyled: background unset, border-radius 104px, 100% ancho con send button overlaid (position: absolute). Removido border-top de chatWidgetInputArea. Más padding en chatWidgetBubble (6px→8px izq, 16px→20px der). chatWidgetMessages: padding aumentado a var(--spacing-md), padding-top a +40px, removido min-height para evitar espacio vacío bajo el input. chatWidgetMsgBubbleOther ahora rgba(253,252,252,0.4) (60% transparente). Mensajes de AI muestran avatar JPG al lado del bubble en vez de icono Bot + label. Minimize button: min-width y padding explícitos para que el icono sea visible sobre el fondo translúcido. CSS muerto del name form eliminado. Seed re-ejecutado: datos limpios sin duplicados.
- **Archivos:** frontend/src/components/chat/ChatWidget.tsx, frontend/src/components/chat/ChatWidget.css, roadmap.md
- **Gotchas:** El Button base `.botonPequeno` sobreescribía padding del minimize btn — se necesita `padding: 0; min-width: 32px` explícito. Los estilos del name form (.chatWidgetNameForm, .chatWidgetFormAvatar, .chatWidgetWelcome, etc.) quedaban como CSS muerto tras eliminar el componente.
- **Sentinel:** No aplica.

## 064A-21 — Fix seed: LIMIT 10 impedía crear 6 órdenes (solo 3)
- **Qué:** La query de planes en el seed tenía `LIMIT 10` pero hay 21 planes no-custom (7 servicios × 3: basico, medio, avanzado). Solo se obtenían planes de los primeros 3 servicios (diseno-web, desarrollo-apps, agentes-ia), por lo que las órdenes 4 (branding), 5 (seo) y 6 (ecommerce) se saltaban silenciosamente. Fix: remover LIMIT. Además se agregaron `else` branches con warnings para debugging futuro. Clippy fixes: merged match arms en `determine_phase_status`, allow too_many_lines/too_many_arguments en funciones del seed.
- **Archivos:** examples/seed_test_data.rs
- **Gotchas:** La query original asumía solo 2 planes por servicio (basico, avanzado) — la migración tiene 3 (basico, medio, avanzado). El falso positivo de "órdenes con pago único pendientes" era porque solo se veían 3 órdenes y el usuario no las reconocía como correctas.
- **Sentinel:** No aplica.

## 064A-30 — Detalles de pedido: info card + historial de fases
- **Qué:** Backend: agregados `assigned_employee_name` y `started_at` a `OrderResponse`. Nuevo método `get_employee_display_name()` en OrderRepository (query_scalar sin macro para evitar cache offline). Actualizados los 5 puntos de construcción de OrderResponse (order.rs ×3, assignment.rs ×2). Frontend: TypeScript OrderResponse actualizado. OrdenDetalle.tsx rediseñado completamente: nuevo cuadro de info (freelancer, servicio, número orden, fecha inicio, precio, modo pago, badge de status + menú contextual). Menú 3 puntos expandido con opción "Reportar problema" (disponible para clientes con empleado asignado). FaseCard extraída a archivo propio (SRP + límite 300 líneas). useOrdenDetalle hook creado (4 useState extraídos del componente). Cuadro de historial muestra todas las fases (no solo en modo phased). CSS nuevo: ordenInfoCard con grid responsive + ordenHistorialCard con bordes var(--bg-item-active). Fix clippy: doc_markdown en ai_chat.rs, needless raw string hashes en repository.
- **Archivos:** src/models/order.rs, src/repositories/order.rs, src/services/order.rs, src/services/assignment.rs, src/services/ai_chat.rs, frontend/src/api/orders.ts, frontend/src/components/panel/OrdenDetalle.tsx, frontend/src/components/panel/FaseCard.tsx (nuevo), frontend/src/hooks/useOrdenDetalle.ts (nuevo), frontend/src/components/panel/SeccionProyectos.css
- **Gotchas:** sqlx::query_scalar! macro requiere cache offline — usar query_scalar sin macro para queries nuevas en modo SQLX_OFFLINE. Clippy doc_markdown exige backticks en nombres de env vars en doc comments.
- **Sentinel:** No aplica.

## 064A-29 — Chat persistence + Groq AI con rotación de keys
- **Qué:** Backend: migrado de Gemini API a Groq API (OpenAI-compatible). AiChatConfig ahora carga GROQ_API_1/2/3 con rotación round-robin via AtomicUsize. Modelo por defecto: llama-3.3-70b-versatile. Formato de request/response cambiado a OpenAI-compatible (system/user/assistant roles). Frontend: useChatWidget ahora deduplica mensajes por ID. Backend WS handler envia historial de mensajes al reconectar visitante. ChatWidget auto-detecta sesión previa en localStorage y salta formulario de nombre.
- **Archivos:** src/services/ai_chat.rs, src/handlers/chat.rs, frontend/src/hooks/useChatWidget.ts, frontend/src/components/chat/ChatWidget.tsx
- **Gotchas:** REST endpoint get_messages requiere JWT auth → visitantes no pueden usarlo. Solución: enviar historial via WebSocket en el handler de visitor.
- **Sentinel:** No aplica.

## 064A-28 — Chat widget redesign completo
- **Qué:** Redesign completo del ChatWidget segun especificaciones del usuario. Eliminado ChatWidgetHeader (sin header ni boton cerrar). Burbuja flotante cambiada de circulo con MessageCircle a pill con avatar JPG + texto "Chat". Panel: fondo semi-transparente rgba(245,242,239,0.8) con backdrop-filter blur, border var(--bg-item-active), min-height 440px, sombra especifica del usuario. Boton minimizar redondo flotante bg #e8e6e2. Animacion suave open/close via CSS transitions (no @keyframes). Botones secundarios bg #e8e6e2/#e9e7e4 sin borde. Formulario de nombre con avatar grande. Burbujas de mensaje: propias #e9e7e4, ajenas var(--bg-primary). Input border var(--bg-item-active). Send button circular bg #e9e7e4. Fix adicional: ArrowLeftRight→ArrowRightLeft en SidebarPanel.tsx (nombre incorrecto del icono Lucide).
- **Archivos:** ChatWidget.tsx, ChatWidget.css, SidebarPanel.tsx
- **Gotchas:** Code Sentinel exige usar componente Button en vez de <button> nativo — se uso Button con variante="texto" y CSS overrides completos para mantener diseño custom.
- **Sentinel:** No aplica.

## 064A-46+47+53 — Modal compra precio solo en botón, imagen servicio grande, favicon punto negro
- **Qué:** Modal compra: eliminado bloque .modalCompraPrecio del texto, precio permanece solo en botón, gap aumentado entre botón y label. Servicio individual: imagen del grid ahora aparece en hero con aspect-ratio 16/10 (proporción laptop), full width. Favicon: SVG cambiado de "N." en cuadrado negro a círculo negro sobre fondo blanco (coincide con Logo.tsx).
- **Archivos:** ModalCompra.tsx, ModalCompra.css, App.tsx, ServicioIndividualIsland.tsx, SeccionHeroServicio.tsx/css, favicon.svg
- **Sentinel:** No aplica.

## 064A-48+52+54+55+56 — CSS fixes batch 2: chat border, toast, badges fase, button gap, footer, sidebar
- **Qué:** Chat panel border → var(--bg-item-active). Toast movido abajo-derecha con fondo var(--bg-accent) uniforme, sin sombra. Phase badges todos en escala de grises. Button base con gap para separar icono de texto. Subscribe button footer sin borde. Sidebar height calc(100vh - 80px) con sticky.
- **Archivos:** SeccionChat.css, ToastContainer.css, SeccionProyectos.css, Button.css, Footer.css, SidebarPanel.css
- **Sentinel:** No aplica.

## 064A-33+34+37+38+39+40+41+42 — CSS fixes batch 1 + seed data + borrar páginas panel
- **Qué:** Batch de correcciones UI: bordes claros (--border-default) en Input/Select/Badge, padding modal ~30px, sombras eliminadas de modal, fondos oscuros del chat panel reemplazados por claros, botón 3 puntos sin borde en usuarios, badges escala de grises, botón 3 puntos tamaño correcto en órdenes, display_name en seeds, seed idempotente, eliminadas páginas Empleados y Servicios del panel.
- **Archivos:** Input.css, Select.css, Modal.css, Badge.css, SeccionChat.css, SeccionUsuarios.css, SeccionProyectos.css, OrdenDetalle.tsx, SidebarPanel.tsx, PanelIsland.tsx, panel.ts, seed_admin.rs, seed_test_data.rs. Borrados: EmployeesSection.tsx/css, ServicesCatalogSection.tsx/css.
- **Gotchas:** --border-color (#4b5563) era raíz de bordes oscuros en toda la UI; reemplazado por --border-default. SidebarPanel necesitaba actualizar ICONOS_SECCION al quitar secciones.
- **Sentinel:** No aplica (CSS puro + seeds + eliminación).

## 064A-1 — Eliminar estilos ad-hoc de botones, usar Button component con variantes semánticas
- **Qué:** Expandido el componente Button de 4 a 13 variantes (`exito`, `exitoSuave`, `peligro`, `peligroSuave`, `advertencia`, `advertenciaSuave`, `info`, `infoSuave`, `marca`). Refactorizados todos los archivos TSX que usaban `variante="texto"` con clases CSS ad-hoc para botones. Eliminados ~200 líneas de CSS redundante en 10 archivos. Eliminado CSS muerto en SeccionContacto, SeccionClientes. Creada regla `css-adhoc-button-style` en Code Sentinel con soporte `excluirArchivos`.
- **Archivos:** Button.tsx, Button.css, OrdenDetalle.tsx, SeccionProyectos.css, ChatWidget.tsx/css, CheckoutModal.tsx/css, EntregablesPanel.tsx, SeccionChat.css, SeccionHosting.tsx/css, SidebarPanel.css, BarraFiltros.css, SeccionTestimonios.tsx/css, LanguageSelector.css, SeccionContacto.css, SeccionClientes.css + Code Sentinel (defaultRules.ts, staticAnalyzer.ts, types/index.ts)
- **Gotchas:** 
  - `variante="texto"` era el "blank slate" usado como base para overrides — la raíz de todos los estilos ad-hoc
  - Algunos CSS como `.filtroBoton` (pill/chip), `.chatSesionItem` (list item) necesitan estilos contextuales legítimos (layout, border-radius, padding) — solo se eliminaron los redundantes (cursor:pointer, background:none, border:none, etc.)
  - 37 errores TypeScript pre-existentes (módulos npm no instalados) — no relacionados con esta tarea
- **Sentinel:** Regla `css-adhoc-button-style` implementada. Detecta `cursor:pointer` en archivos .css excepto Button.css y variables.css. Nuevo campo `excluirArchivos` en `ReglaEstatica` + filtro en `staticAnalyzer.ts`.

## 064A-2 — Overhaul de variables CSS: renombrar --bg-secondary y --text-secondary
- **Qué:** Renombrado `--bg-secondary` (#141413) → `--bg-dark` y `--text-secondary` (#141413) → `--text-dark` en 36 archivos. Cero cambio visual — solo nombres claros. El usuario reportó confusión recurrente con `--bg-secondary` (era negro pero sonaba como fondo claro alternativo).
- **Archivos:** variables.css + 35 archivos CSS de componentes/islands/styles
- **Gotchas:** La mayoría de usos de `--text-secondary` eran sobre fondos claros (correcto), pero en SeccionClientes el fondo es `--bg-dark` y el texto también era `--text-dark` — potencial bug de texto invisible (a verificar visualmente).
- **Sentinel:** No aplica (rename puro, no requiere nueva regla).

## 064A-19 — Order badges todos gris oscuro, sin colores de status
- **Qué:** Eliminados los 8 estilos individuales de status (color-warning, color-info, etc.) y reemplazados por un único estilo base en `.ordenCardBadge`/`.ordenDetalleBadge`: `background-color: #161616; color: #ffffff; padding: 6px 10px;`. Las clases `ordenBadge--*` siguen en TSX como atributos semánticos sin CSS.
- **Archivos:** SeccionProyectos.css
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 064A-6 — Scroll to top en cada cambio de ruta
- **Qué:** Creado componente `ScrollToTop` que escucha `useLocation().pathname` y ejecuta `window.scrollTo(0,0)`. Montado en `App.tsx` dentro de BrowserRouter.
- **Archivos:** ScrollToTop.tsx (nuevo), App.tsx
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 064A-9 — Quitar sección clientes y testimonios de "nosotros"
- **Qué:** Removidos `<SeccionClientes />` y `<SeccionTestimonios />` de NosotrosIsland. Eliminados imports muertos.
- **Archivos:** NosotrosIsland.tsx
- **Sentinel:** No aplica.

## 064A-12 — Quitar "hablemos" del blog
- **Qué:** Removido `<SeccionContacto />` de BlogIsland. Eliminado import muerto.
- **Archivos:** BlogIsland.tsx
- **Sentinel:** No aplica.

## 064A-25 — faseCard con padding, borde y radius
- **Qué:** Añadidos `padding: var(--spacing-md)`, `border: 1px solid var(--border-default)`, `border-radius: var(--radius-md)` a `.faseCard`. Estado activo resaltado con `border-color: var(--brand-black)`. Gap de `.fasesTimeline` cambiado de 0 a `var(--spacing-sm)`.
- **Archivos:** SeccionProyectos.css
- **Sentinel:** No aplica.

## 064A-26 — ordenDetalleAcciones consistencia de botones
- **Qué:** Ya resuelto implícitamente por 064A-1. Todos los botones de acciones usan `tamano="pequeno"` consistentemente.
- **Archivos:** Ninguno adicional.
- **Sentinel:** No aplica.

## 064A-14 — Login redirige al panel
- **Qué:** Añadido `useNavigate` al hook `useAutenticacion`. Después de login/registro exitoso, se llama `navigate('/panel')` para redirigir al panel. Antes solo se cerraba el modal (`onCerrar()`) dejando al usuario en la misma página.
- **Archivos:** useAutenticacion.ts
- **Gotchas:** `HomeOrPanel` en App.tsx ya redirigía a /panel si estás logueado en `/`, pero no cubría login desde otras páginas (servicios, blog, etc.).
- **Sentinel:** No aplica.

## 064A-3 — Auto-crear cuenta desde email de compra
- **Qué:** Nuevo endpoint `POST /api/auth/quick-register` que acepta solo email, genera password aleatorio (32 bytes hex) y crea la cuenta. Si el email ya existe retorna 409. Frontend simplificado: ModalCompra solo pide email. Si 409 → muestra campo password para login de usuario existente. El usuario puede cambiar su password después desde el panel (pendiente implementar).
- **Archivos:** auth.rs (handler + service), user.rs (QuickRegisterRequest), mod.rs (exports + OpenAPI), auth.ts (apiQuickRegister), useModalCompra.ts (flujo simplificado), ModalCompra.tsx (password condicional)
- **Gotchas:** Requiere `hex` crate (ya estaba en Cargo.toml). El random password usa `OsRng.fill_bytes` de argon2 (CSPRNG seguro).
- **Sentinel:** No aplica. Pendiente: feature "cambiar contraseña" en SeccionPerfil para usuarios quick-register.

## 064A-27 — Eliminar Dashboard del panel
- **Qué:** Removido SeccionDashboard del panel admin. Eliminado de panel.ts (type + tab config), PanelIsland.tsx (import + case), SidebarPanel.tsx (icono LayoutDashboard). El componente SeccionDashboard.tsx/css sigue en disco pero no se usa.
- **Archivos:** panel.ts, PanelIsland.tsx, SidebarPanel.tsx
- **Sentinel:** No aplica.

## 064A-18 — Fix overflow del menú contextual en tabla de usuarios
- **Qué:** El MenuContextual panel se clippeaba por `overflow-x: auto` del wrapper de tabla. Fix: removido overflow del wrapper + z-index subido de 3 a 100 + max-height 320px con overflow-y:auto + text-align:left explícito en items del menú.
- **Archivos:** ContextMenu.css, SeccionUsuarios.css
- **Gotchas:** `overflow-x: auto` + `position: absolute` no funcionan juntos — el overflow crea stacking context que clipea children.
- **Sentinel:** No aplica.

## 064A-17 — Selects personalizados en panel de usuarios
- **Qué:** Reemplazados los `<Select>` nativos por `<MenuContextual>` con triggerContent customizado (label + ChevronDown) para los filtros de rol y status. Eliminado import de Select. Añadido estilo `.usuariosFiltroBtn`.
- **Archivos:** SeccionUsuarios.tsx, SeccionUsuarios.css
- **Gotchas:** El usuario pidió explícitamente que los selects usen el componente de menú. MenuContextual ya soporta `triggerContent`, `triggerVariante` y `triggerTamano`.
- **Sentinel:** Pendiente crear regla para detectar `<select>` nativo en componentes UI (el usuario dice "siempre sucede").

## 064A-16 — Fix imagen de perfil no carga (Vite proxy /uploads)
- **Qué:** En desarrollo el frontend corre en Vite (5173) y las imágenes de avatar se guardan en el backend (3000) bajo `/uploads/avatars/`. El `<img src="/uploads/...">` no llegaba al backend porque Vite no tenía proxy para esa ruta. Añadido `/uploads` al proxy de vite.config.ts.
- **Archivos:** vite.config.ts
- **Gotchas:** En producción no hay problema (mismo origen). Solo afecta dev mode.
- **Sentinel:** No aplica.

## 054A-22 — Eliminar --font-serif de ModalCompra
- **Qué:** Removido `font-family: var(--font-serif)` de `.modalCompraTitulo` y `.modalCompraPrecioCifra`. No hay razón para usar serif en estos elementos.
- **Archivos:** ModalCompra.css
- **Sentinel:** No aplica.

## 054A-23 — ModalCompra resumen: gap y columna en vez de márgenes
- **Qué:** Cambiado `.modalCompraResumen` de márgenes individuales a `display: flex; flex-direction: column; gap: var(--spacing-md)`. Eliminados `margin-top` y `margin-bottom` de los hijos.
- **Archivos:** ModalCompra.css
- **Sentinel:** No aplica.

## 064A-10 — SeccionContacto padding bottom
- **Qué:** Aumentado el padding inferior de `.seccionContacto` de `var(--spacing-xl)` a `var(--spacing-2xl)` para dar más espacio visual. Aplica en nosotros y soluciones.
- **Archivos:** SeccionContacto.css
- **Sentinel:** No aplica.

## 064A-20 — ordenDetalleTopbar botón con icono SVG
- **Qué:** Reemplazado el carácter textual `←` por el icono `<ArrowLeft>` de lucide-react en el botón "Volver" del detalle de orden.
- **Archivos:** OrdenDetalle.tsx
- **Sentinel:** No aplica.

## 064A-22 — Eliminar ordenDetalleNotas
- **Qué:** Removido el bloque condicional de `order.client_notes` del detalle de orden. El usuario no le veía sentido. Eliminado el CSS asociado (`.ordenDetalleNotas`).
- **Archivos:** OrdenDetalle.tsx, SeccionProyectos.css
- **Sentinel:** No aplica.

## 064A-23 — Eliminar numeración automática de fases
- **Qué:** Removido `<span className="faseNumero">Fase {phase.phase_number}</span>` del FaseCard. Los títulos de fase ya son descriptivos por sí solos. Eliminado CSS de `.faseCardTitulo` y `.faseNumero`.
- **Archivos:** OrdenDetalle.tsx, SeccionProyectos.css
- **Sentinel:** No aplica.

## 064A-24 — MetaRow del detalle de orden con badges uniformes
- **Qué:** Convertidos `plan_name`, `payment_mode` y `final_price` de `<span className="ordenDetalleMeta">` (texto gris muted) a `<span className="ordenDetalleBadge">` (badge oscuro). Todos los items del meta row ahora usan el mismo estilo visual. Eliminado CSS muerto de `.ordenDetalleMeta`. Corregidos colores hardcoded a variables CSS (`var(--bg-dark)` y `var(--brand-white)`).
- **Archivos:** OrdenDetalle.tsx, SeccionProyectos.css
- **Sentinel:** No aplica.

## 064A-15 + 064A-21 — Seed datos de prueba realistas + fix inconsistencia pago único
- **Qué:** Creado `examples/seed_test_data.rs` que genera: 2 usuarios (cliente@test.com/cliente, empleado@test.com/empleado), 6 órdenes con estados coherentes usando 3 modos de pago distintos y fases derivadas de los templates de plan. Regla implementada: órdenes `full` NUNCA tienen status `pending_payment` — el pago único se hace al crear la orden, por lo que arrancan en `in_progress`, `under_review` o `completed`. Solo `phased` puede tener `pending_payment`.
- **Archivos:** examples/seed_test_data.rs (nuevo)
- **Gotchas:** La tabla orders NO tiene `service_title` ni `plan_name` — son JOINed en el service layer. El seed usa `sqlx::query_as` (runtime, no macro) para no depender de `.sqlx/` offline.
- **Sentinel:** No aplica. Considerar regla para detectar INSERT de orders con mode=full + status=pending_payment en migrations.

## 054A-21 — Plan Medio para todos los servicios (3 tiers)
- **Qué:** Añadido plan "Medio" entre Basico y Avanzado para los 7 servicios (Web, Apps, IA, Branding, E-commerce, SEO, Marketing). El Medio es ahora el plan destacado. Precios intermedios coherentes. Creada migración SQL con INSERT ON CONFLICT + templates de fases genéricas (3 por plan medio).
- **Archivos:** frontend/src/data/planes/planes.ts, migrations/20260406000000_add_medium_plans.up.sql (nuevo), migrations/20260406000000_add_medium_plans.down.sql (nuevo)
- **Gotchas:** `planesIA.ts` y `planesExtras.ts` son archivos muertos (no importados por nadie). Solo `planes.ts` es la fuente de verdad via `index.ts`. Sentinel reporta falso positivo de "300 líneas" en planes.ts — es un archivo de datos, no un componente.
- **Sentinel:** Falso positivo: la regla de límite de líneas no debería aplicar a archivos de datos estáticos puros (sin JSX/lógica).

## 054A-19 — Corregir Code Sentinel falsos positivos + fix code real
- **Qué:** Corregidos 3 falsos positivos en Code Sentinel y 1 issue real:
  - **Real:** `seed_admin.rs` convertido de `sqlx::query!` (macro, requiere offline cache) a `sqlx::query` (runtime) — los seeds no son producción.
  - **FP1:** Límite 300 líneas flaggeaba `planes.ts` (archivo de datos puros). Fix: `lineCounter.ts` ahora retorna `null` para archivos `.ts/.js` bajo `/data/`.
  - **FP2:** Reglas sqlx flaggeaban `examples/seed_test_data.rs`. Fix: `staticAnalyzer.ts` ahora excluye todo el directorio `/examples/` del análisis.
  - **FP3:** `inline-style-prohibido` en `SeccionTestimonios.tsx` — ya estaba corregido con check de CSS custom properties (`--var` keys).
  - **Nota:** Diagnósticos de "hardcoded #161616" en `SeccionProyectos.css` son de VarSense (cache stale), no de Code Sentinel. El archivo ya usa `var(--bg-dark)`.
- **Archivos:** examples/seed_admin.rs, code-sentinel/src/analyzers/staticAnalyzer.ts, code-sentinel/src/utils/lineCounter.ts
- **Gotchas:** Code Sentinel es solo extensión VS Code (sin CLI). El reporte `.sentinel-report.md` estaba limpio (0 violaciones del 04-05), los problemas se vieron via `get_errors` que agrega diagnostics de todas las extensiones.
- **Sentinel:** 2 reglas corregidas directamente (limite-lineas para /data/, excluir /examples/). VSIX reempaquetado e instalado.

## 064A-5 + 054A-20 — Contacto → Chat + fondo claro del panel
- **Qué:** Eliminada la página /contacto. Todos los CTAs que navigaban a /contacto ahora abren el ChatWidget via `useChatStore` (Zustand). Panel de chat cambiado de fondo negro (`--bg-dark`) a fondo claro (`--bg-primary`). Textos invertidos a `--text-dark`/`--text-muted`.
- **Archivos:** stores/chatStore.ts (nuevo), ChatWidget.tsx/css, SeccionContacto.tsx, Header.tsx, ServicioIndividualIsland.tsx, ProyectoIndividualIsland.tsx, SeccionPlanesServicio.tsx, HeaderPanel.tsx, PanelIsland.tsx, App.tsx
- **Gotchas:**
  - ChatWidget usaba `useState(false)` — migrado a `useChatStore` global para poder abrir desde cualquier componente.
  - HeaderPanel (panel) no usa el ChatWidget flotante (se oculta en /panel). Implementado custom event `panel-cambiar-tab` que PanelIsland escucha para cambiar al tab "mensajes".
  - SeccionCta ya soportaba `onBotonPrimarioClick`/`onBotonSecundarioClick` callbacks — no requirió cambios.
  - SolucionPlaceholderIsland tenía `#contacto` anchor que scrollea a SeccionContacto (que ya abre chat), no requirió cambio.
  - ContactoIsland.tsx sigue en disco pero ya no tiene ruta — puede eliminarse más adelante.
- **Sentinel:** No aplica.

## 064A-4 — Fix globo chat y botón envío
- **Qué:** El botón burbuja del chat (`.chatWidgetBubble`) no se veía redondo porque `botonMediano` le inyectaba padding que deformaba las dimensiones fijas 56×56. El botón de envío (`.chatWidgetSendBtn`) igual sufría de padding desproporcionado y se veía estirado. Se añadió `padding: 0` y `min-width` explícitos a ambos selectores CSS, y se cambió `tamano="pequeno"` en los componentes TSX para reducir el padding heredado de botonBase.
- **Archivos:** ChatWidget.tsx, ChatWidget.css
- **Gotchas:** `botonBase` tiene `width: fit-content` y `botonMediano` tenía padding `var(--spacing-md) var(--spacing-lg)` que se sumaba a las dimensiones fijas del globo. La solución es sobreescribir padding en los selectores específicos.
- **Sentinel:** No aplica — es un override CSS legítimo por diseño.

## 064A-8 — Detalles técnicos y enlaces en proyectoHero
- **Qué:** Agregada sección de detalles técnicos en el hero de cada proyecto individual. Muestra tags de tecnologías usadas (React, Node.js, PostgreSQL, etc.) y enlaces con iconos (GitHub, web, npm, demo) usando lucide-react. Extendida la interfaz `Proyecto` con campos `tecnologias?: string[]` y `enlaces?: EnlaceProyecto[]`. Datos añadidos a los 5 proyectos en showcase.ts.
- **Archivos:** types/contenido.ts (nueva interfaz EnlaceProyecto + campos en Proyecto), data/showcase.ts (datos técnicos por proyecto), ProyectoIndividualIsland.tsx (rendering con iconos), ProyectoIndividualIsland.css (estilos tags + enlaces)
- **Gotchas:** Los iconos de lucide-react se mapean con un Record por tipo de enlace. Los campos son opcionales para no romper proyectos sin datos técnicos.
- **Sentinel:** No aplica.

## 064A-13 — Tarjetas artículo blog igualadas al diseño del home
- **Qué:** Las tarjetas de artículos en BlogIsland usaban un grid `auto-fill minmax(280px)` que creaba columnas inconsistentes y la tarjeta destacada tenía `aspect-ratio: 16/9` que se veía mal (demasiado ancha y baja). Cambiado el grid a `repeat(3, 1fr)` con gap xl para igualar SeccionBlog del home. La tarjeta destacada ahora ocupa la fila completa (`grid-column: 1 / -1`) con `aspect-ratio: 21/9` panorámico. En mobile vuelve a 3/4. Agregado breakpoint tablet (769-900px) con 2 columnas.
- **Archivos:** BlogIsland.css
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 064A-7 — Imágenes de galería de proyectos más grandes
- **Qué:** Aumentado el tamaño de los items del carrusel de galería. Mobile: 300→320px, Tablet: 450→560px, Desktop: 450→680px. Mayor impacto visual sin romper el scroll infinito.
- **Archivos:** SeccionGaleriaServicio.css
- **Gotchas:** Ninguno.
- **Sentinel:** No aplica.

## 064A-11 — Status hosting administrado (documentación)
- **Qué:** Creado documento detallado con el estado actual del servicio de hosting administrado: backend 100% (5 endpoints, BD, planes), panel admin 100%, página pública 0% (placeholder). Listadas las 10 cosas que faltan para lanzar, priorizadas en alta/media/baja. Archivos clave mapeados.
- **Archivos:** Agente/documentacion/hosting/status-hosting-administrado-2026-04-06.md (nuevo)
- **Gotchas:** Los planes de hosting NO están en planes.ts como producto independiente; solo "hosting incluido" en planes de web. La integración con Coolify Manager RS está planificada pero bloqueada por infra externa.
- **Sentinel:** No aplica.
