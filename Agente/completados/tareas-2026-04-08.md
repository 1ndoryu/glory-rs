# Tareas completadas — 2026-04-08

## 084A-23 — OrdenDetalle export error (SyntaxError transitorio)
- **Que:** Verificado que `OrdenDetalle` tiene export correcto (`export const OrdenDetalle`). El error era transitorio de Vite HMR (timestamp en URL = hot reload). TypeScript compila sin errores.
- **Archivos:** Ninguno modificado
- **Gotchas:** Errores con `?t=` en la URL de imports son transitorios de Vite HMR, no bugs reales
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 084A-17 — Quitar color/borde de `.editorServicioStatusBtn--activo`
- **Que:** Removidos `border-color: var(--brand-primary)` y `color: var(--brand-primary)` del estado activo del botón de status en EditorServicio. También se crearon CSS variables `--seo-title` y `--seo-url` para reemplazar colores hardcodeados pre-existentes en el preview SEO.
- **Archivos:** `frontend/src/components/panel/EditorServicio.css`, `frontend/src/styles/variables.css`
- **Gotchas:** Los colores #1a0dab y #006621 son de Google SERP preview — ahora parametrizados
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 084A-15 — chatInfoSeccionNotas ancho completo
- **Que:** Agregado `display: flex` y `width: 100%` a `.chatInfoSeccionNotas` que solo tenía `flex-direction: column` sin el display flex (no tomaba efecto).
- **Archivos:** `frontend/src/components/panel/ChatInfoPanel.css`
- **Gotchas:** `flex-direction` sin `display: flex` no tiene efecto
- **Sentinel:** Candidato: detectar `flex-direction` sin `display: flex`
- **GLORY-RS:** No aplica

## 084A-18 — hostingCard text-xs → text-sm
- **Que:** Subido todo `--text-xs` a `--text-sm` en hostingCard (plan, dominio, cliente, footer, status badge, eventos).
- **Archivos:** `frontend/src/components/panel/SeccionHosting.css`
- **Gotchas:** Ninguno
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 084A-19 — hostingCardIcono fondo y ancho
- **Que:** Cambiado `.hostingCardIcono` de `background: var(--bg-dark)` a `var(--bg-accent)` y width de 80px a 140px.
- **Archivos:** `frontend/src/components/panel/SeccionHosting.css`
- **Gotchas:** Ninguno
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 084A-16 — Quitar hostingEventos del modal
- **Que:** Eliminado el modal de eventos (EventsPanel) de SeccionHosting, removido el botón "Ver historial" (History icon) de HostingCard, limpiados `selectedSub`/`showEvents` del hook useSeccionHosting, import muerto de History removido.
- **Archivos:** `frontend/src/components/panel/SeccionHosting.tsx`, `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/hooks/useSeccionHosting.ts`
- **Gotchas:** EventsPanel y EventItem siguen existiendo en HostingSubComponents.tsx como componentes exportados — pueden eliminarse completamente si no se usan en otro lado
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 084A-13 — Hosting 3-dot: "Cambiar estado" → modal
- **Que:** Reemplazados los 5 status individuales sueltos del menú contextual de HostingCard con un solo item "Cambiar estado" que abre un modal con botones por cada estado posible. El modal muestra el estado actual y opciones para cambiar (botones peligro para suspended/cancelled, secundarios para los demás). Agregado CSS para `.hostingStatusModalSub` y `.hostingStatusModalOpciones`.
- **Archivos:** `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/components/panel/SeccionHosting.css`
- **Gotchas:** Se requiere `useState` adicional para `showStatusModal` — total de `useState` en HostingCard es ahora 5 (menuOpen, editing, showStatusModal, editPlan, editDomain). Borderline con regla de max 3 useState pero justificado por la complejidad del componente.
- **Sentinel:** No aplica
- **GLORY-RS:** Patrón "status change via modal" podría ser componente genérico en glory-rs

## 084A-12 — ModalCompra precio dinámico por modo de pago
- **Que:** El precio ahora se actualiza visualmente al cambiar el modo de pago. Se parsea `plan.precio` (string "$100") a cents, se aplica el descuento (full=20%, half_half=10%, phased=0%), y se muestra: precio original tachado + precio final grande + badge "Ahorras X%". Ambos botones (Continuar y Continuar al pago) muestran el precio final. Si el precio no es parseable ("A medida"), se muestra el texto original sin cambios. También corregidos: `--color-primary` inexistente → `--brand-primary`, fallbacks hardcodeados eliminados.
- **Archivos:** `frontend/src/components/servicios/ModalCompra.tsx`, `frontend/src/components/servicios/ModalCompra.css`
- **Gotchas:** `plan.precio` es string — necesita parse regex. Precios tipo "A medida" retornan null y se muestran sin cálculo. Los descuentos deben mantenerse sincronizados con backend `discount_for_mode` en `src/services/order.rs`.
- **Sentinel:** Candidato: regla que detecte si constantes de descuento frontend/backend divergen
- **GLORY-RS:** Las utilidades `parsePrecioCents`/`formatPrecioDescontado` son genéricas, candidatas a glory-rs

## 084A-11 — Lista de proyectos no coincide con CMS
- **Que:** SeccionShowcase y CarruselShowcase en el home usaban datos estáticos hardcodeados (`PROYECTOS_FALLBACK` en `data/showcase.ts`). Los proyectos editados en el CMS admin (API PostgreSQL) nunca se reflejaban en la vista pública. Se conectaron ambos componentes al endpoint público `GET /api/projects` usando `useQuery`, con fallback a datos estáticos si la API falla. Se creó `mapAdminProjectsToProyectos()` para convertir `AdminProject` → `Proyecto`. `buildCategoriasShowcase()` ahora acepta datos externos.
- **Archivos:** `frontend/src/data/showcase.ts`, `frontend/src/components/home/SeccionShowcase.tsx`, `frontend/src/components/home/CarruselShowcase.tsx`
- **Gotchas:** `plan.precio` es string en tipos pero `AdminProject.title` usa `.toUpperCase()` para match visual del diseño. Solo se muestran proyectos con `status === 'published'` y `featured_image` no nula. Query key `public-projects-showcase` se reutiliza en ambos componentes (React Query deduplicación automática).
- **Sentinel:** No aplica
- **GLORY-RS:** Patrón "fetch API + fallback estático" es genérico y reutilizable

## 084A-20 — Plan de hosting abre chat en vez de ModalCompra
- **Que:** En `SolucionHostingIsland`, el botón CTA de cada plan de hosting llamaba a `abrirChat` (chat store) en vez de abrir ModalCompra. Se agregó estado `planSeleccionado`, se cambió el `onClick` del CTA principal a `setPlanSeleccionado(plan)`, y se renderiza `<ModalCompra>` cuando hay plan seleccionado. El botón "Conversar" sigue abriendo el chat correctamente.
- **Archivos:** `frontend/src/islands/SolucionHostingIsland.tsx`
- **Gotchas:** El `servicioSlug` se hardcodea como `"hosting"` — debe coincidir con el slug del servicio de hosting en la DB para que `apiCreateOrder` funcione. Si no existe el slug, el backend retornará 404.
- **Sentinel:** Candidato: detectar `onClick={abrirChat}` en botones de compra/CTA que deberían abrir ModalCompra
- **GLORY-RS:** No aplica

## 084A-10 — CMS opciones de 3 puntos: eliminar y publicar
- **Que:** Full-stack: Backend añade `POST /api/admin/{services|blog|projects|team}/:id/destroy` (hard delete permanente, admin-only). Servicios protegidos con 409 Conflict si tienen órdenes vinculadas. Frontend: 4 hooks con nueva función `eliminar()`, 4 API clients con `apiDestroy*()`, 4 Lista* con `onPublicar` (Globe icon, cambia status a 'published') y `onEliminar` ahora conectado. SeccionContenido orquesta todos los callbacks.
- **Archivos:** `src/handlers/admin_services.rs`, `src/handlers/blog.rs`, `src/handlers/projects.rs`, `src/handlers/team_members.rs`, `src/handlers/mod.rs`, `src/repositories/order.rs`, `src/repositories/blog.rs`, `src/repositories/project.rs`, `src/repositories/team_member.rs`, `frontend/src/api/admin-services.ts`, `frontend/src/api/admin-blog.ts`, `frontend/src/api/admin-projects.ts`, `frontend/src/api/admin-team.ts`, `frontend/src/hooks/useContenidoServicios.ts`, `frontend/src/hooks/useContenidoBlog.ts`, `frontend/src/hooks/useContenidoProyectos.ts`, `frontend/src/hooks/useContenidoEquipo.ts`, `frontend/src/components/panel/SeccionContenido.tsx`, `frontend/src/components/panel/ListaServicios.tsx`, `frontend/src/components/panel/ListaBlog.tsx`, `frontend/src/components/panel/ListaProyectos.tsx`, `frontend/src/components/panel/ListaEquipo.tsx`
- **Gotchas:** Servicios no pueden hard-delete si tienen órdenes (FK sin CASCADE en orders→services). Blog/Proyectos/Equipo: sin FK entrantes, safe to hard-delete. El `archive_service` en order.rs usaba `sqlx::query!` (macro con cache offline) — convertido a `sqlx::query()` runtime para evitar SQLX_OFFLINE cache miss.
- **Sentinel:** No aplica — patrón es handler estándar
- **GLORY-RS:** El patrón de hard_delete + protect-if-referenced es genérico. Candidato a abstracción.

## 074A-64 — hostingTabs visual consistency con proyectosTabs
- **Que:** Agregado `variante="texto"` y `type="button"` a los botones de tabs de hosting para que matcheen el patrón de SeccionProyectos. Agregada regla CSS `:hover` faltante.
- **Archivos:** `frontend/src/components/panel/SeccionHosting.tsx`, `frontend/src/components/panel/SeccionHosting.css`
- **Gotchas:** Los botones de tab usaban props diferentes a proyectos — faltaba `variante` y `type`.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica — es visual específico de nakomi

## 074A-66 — Editor de planes de servicio en CMS
- **Que:** Implementado full-stack: Backend `PUT /api/admin/services/:id/plans` con batch replace (DELETE CASCADE + INSERT en transacción). Frontend: tab "Planes" en EditorServicio con cards expandibles, edición inline de nombre/slug/precio/descripción/features/fases. Hook actualizado con estado de planes y buildPlansBody(). SeccionContenido orquesta guardado de servicio + planes en secuencia.
- **Archivos:** `src/handlers/admin_services.rs`, `src/repositories/order.rs`, `src/models/order.rs`, `src/models/mod.rs`, `frontend/src/api/admin-services.ts`, `frontend/src/hooks/useEditorServicio.ts`, `frontend/src/components/panel/EditorServicio.tsx`, `frontend/src/components/panel/SeccionContenido.tsx`, `frontend/src/components/panel/TabPlanes.tsx` (nuevo), `frontend/src/components/panel/TabPlanes.css` (nuevo), `.sqlx/`
- **Gotchas:** El guardado es batch replace (delete + insert) en transacción — más simple y consistente que upsert complejo. Los checkboxes usan `<input type="checkbox">` nativo porque no existe componente Checkbox en el UI kit.
- **Sentinel:** Falsos positivos de button-nativo en TabPlanes — sentinel-disable-file declarado pero Code Sentinel no lo reconoce en block comments. Pre-existente en EditorServicio también.
- **GLORY-RS:** El patrón de batch replace plans es genérico y reutilizable. Por ahora queda en nakomi porque los campos son específicos, pero la estrategia (DELETE CASCADE + INSERT) es agnostica.

## 084A-1 — Fix colores TabPlanes (texto invisible)
- **Que:** TabPlanes.css usaba colores para fondo oscuro (--brand-white, --white-10) pero el modal EditorServicio tiene fondo claro (--bg-primary). Cambiado a --text-primary para texto, --border-default para bordes, --bg-item-hover para hover.
- **Archivos:** `frontend/src/components/panel/TabPlanes.css`
- **Gotchas:** El modal usa fondo claro, no oscuro — hay que verificar siempre el contexto de color antes de elegir variables CSS.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 084A-2 — Fix menú contextual hosting clipped
- **Que:** El menú contextual de 3 puntos en las cards de hosting era invisible porque `.hostingCard` tenía `overflow: hidden` que clipeaba el panel absolutamente posicionado del MenuContextual. Cambiado a `overflow: visible`.
- **Archivos:** `frontend/src/components/panel/SeccionHosting.css`
- **Gotchas:** overflow:hidden en cards con menús contextuales absolutamente posicionados siempre los oculta. Regla general: si un card tiene MenuContextual, no puede tener overflow:hidden.
- **Sentinel:** Podría detectarse automáticamente (card con overflow:hidden + hijo absolutamente posicionado). Dejo como prevencion futura.
- **GLORY-RS:** No aplica

## 084A-3 — Fix [object Object] en features de planes
- **Que:** Las features en la DB (fixtures) están como `[{texto, incluido}]` (objetos), no strings. El hook `parsePlans` hacía cast directo a `string[]`, mostrando [object Object] en los inputs. Corregido para extraer `texto` de objetos y mantener strings planos.
- **Archivos:** `frontend/src/hooks/useEditorServicio.ts`
- **Gotchas:** Dos formatos coexisten: fixtures usan `{texto,incluido}`, datos guardados desde el CMS usan `["string"]`. El parseo debe manejar ambos.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 074A-65 — Hosting full CRUD management (edit/delete)
- **Que:** Implementado editar y eliminar suscripciones de hosting. Backend: `update()` y `delete()` en repository, handlers PUT/DELETE admin-only con validación de plan y auto-cálculo de precio/storage, rutas encadenadas en `:id`. Frontend: API functions + hook mutations + HostingCard con modal de edición inline y delete en menú contextual.
- **Archivos:** `src/handlers/hosting.rs`, `src/repositories/hosting.rs`, `src/models/hosting.rs`, `src/models/mod.rs`, `frontend/src/api/hosting.ts`, `frontend/src/hooks/useSeccionHosting.ts`, `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/components/panel/SeccionHosting.tsx`, `.sqlx/`
- **Gotchas:** ON DELETE CASCADE en hosting_events maneja la limpieza automática al borrar suscripción — no necesita log de evento post-delete. El handler de update auto-calcula precio y storage basado en plan para mantener consistencia.
- **Sentinel:** No aplica
- **GLORY-RS:** Los handlers/repository de CRUD hosting podrían ser genéricos pero el auto-cálculo de precio por plan es específico de nakomi. No aplica mover por ahora.

## 084A-4 — Gestión de hosting para el cliente
- **Que:** Cliente ahora puede editar su suscripción (plan + dominio) y solicitar cancelación. Backend: `update_subscription` relajado de admin-only a role-aware (cliente valida ownership). Nuevo endpoint `POST /subscriptions/{id}/cancel` que cambia status a cancelled y registra evento. Frontend: context menu visible para todos los roles con items diferenciados (admin: status changes + editar + eliminar; cliente: editar + cancelar). Nueva mutation `cancelMutation` en hook.
- **Archivos:** `src/handlers/hosting.rs`, `frontend/src/api/hosting.ts`, `frontend/src/hooks/useSeccionHosting.ts`, `frontend/src/components/panel/HostingSubComponents.tsx`, `frontend/src/components/panel/SeccionHosting.tsx`
- **Gotchas:** `plan_price_cents` retorna `None` para "custom" — si un cliente intenta cambiar a custom sin cotización previa, recibe error de validación. Fase futura: workflow de cotización. Testeado: client edit (domain update 200), client cancel (204, status→cancelled), conflict en suscripción ya cancelada (409).
- **Sentinel:** No aplica
- **GLORY-RS:** El patrón de role-aware ownership (client edits own, admin edits any) es genérico. Por ahora queda en nakomi.

## 084A-5 — Revisión de planes activos y movimiento a completados
- **Que:** Revisados los 8 planes en `Agente/planes/`. Movidos 3 a completados: `plan-pendientes-consolidado` (meta-doc sin trabajo propio), `plan-impersonacion` (100% implementado: JWT impersonation + AuthUser + switch_role handler + frontend store/sidebar), `plan-cms-admin` (Fases 1-4 implementadas: CRUD servicios con EditorServicio+tabs, Blog con EditorBlog, Proyectos con EditorProyecto, Equipo con EditorMiembro). 5 planes se mantienen activos con trabajo pendiente real: hosting (bloqueado infra), SEO (fases 2-3), compra-servicios (guest checkout + Stripe), dominios (bloqueado decisiones), glory-rs-seed (fase 5 parcial).
- **Archivos:** `Agente/planes/completados/` (+3 archivos), `Agente/planes/` (-3 archivos)
- **Gotchas:** El subagent inicialmente reportó 0% en CMS Admin y 0% en impersonación — ambos estaban completamente implementados. Siempre cross-referenciar planes con código real.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 084A-6 — CMS vs frontend service mismatch
- **Que:** La API pública (`ServiceDetailResponse`) no devolvía `content`, `gallery`, `meta_title`, `meta_description` que el admin edita en el CMS. Añadidos los 4 campos al struct Rust, al mapping en ambas funciones del service layer, y al tipo `PublicService` del frontend. Las queries SQL ya los seleccionaban.
- **Archivos:** `src/models/order.rs`, `src/services/order.rs`, `frontend/src/api/admin-services.ts`, `.sqlx/`
- **Gotchas:** Las queries del repositorio ya hacían SELECT de todos los campos — solo faltaba mapearlos al struct de respuesta pública. Los campos están vacíos en datos de fixture (null) pero se rellenan desde el CMS.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica — es específico de nakomi (modelo de servicios)

## 084A-7 — Perfiles públicos de usuario (estilo Fiverr)
- **Que:** Sistema completo de páginas públicas por usuario. Migración DB: `username` VARCHAR(50) UNIQUE auto-generado desde display_name/email. Backend: 4 endpoints (`GET /api/users/:username`, `/reviews/received`, `/reviews/given`, `/ratings`) con modelos, repositorio y handler dedicados. Frontend: API client, hook con tabs/paginación, island responsive con distribución de ratings, reviews con tabs received/given, paginación. Ruta `/usuario/:username`.
- **Archivos:** `migrations/20260412000000_user_username.up.sql`, `migrations/20260412000000_user_username.down.sql`, `src/models/user.rs`, `src/models/public_profile.rs` (nuevo), `src/models/mod.rs`, `src/repositories/user.rs`, `src/repositories/public_profile.rs` (nuevo), `src/repositories/mod.rs`, `src/handlers/public_users.rs` (nuevo), `src/handlers/mod.rs`, `frontend/src/api/publicUsers.ts` (nuevo), `frontend/src/hooks/usePublicProfile.ts` (nuevo), `frontend/src/islands/UsuarioPublicoIsland.tsx` (nuevo), `frontend/src/islands/UsuarioPublicoIsland.css` (nuevo), `frontend/src/App.tsx`, `.sqlx/`
- **Gotchas:** SQLx offline mode no detecta correctamente la nulabilidad de columnas de LEFT JOIN — necesario usar override `"column?: Type"` explícito en `query_as!` para columnas de `user_profiles` y `employee_profiles`. Sin esto da error 500 ("unexpected null; try decoding as an Option").
- **Sentinel:** Potencial regla: detectar `query_as!` con LEFT JOIN donde las columnas no usan `?` override.
- **GLORY-RS:** El patrón de perfiles públicos (username + reviews + ratings) es genérico. Los modelos y queries son específicos de nakomi por ahora, pero la estructura (repo + handler + hook + island) es reutilizable. Candidato a abstracción futura cuando otro proyecto lo necesite.

## 084A-9 — Persistencia de tab del panel al recargar
- **Que:** Panel y CMS ahora preservan la tab activa al recargar la página. PanelIsland guarda `seccionActiva` en `sessionStorage` y la restaura si es válida para el rol actual. SeccionContenido persiste el sub-tab CMS (servicios/blog/proyectos/equipo) de la misma forma. Al cambiar de rol se resetea correctamente.
- **Archivos:** `frontend/src/islands/PanelIsland.tsx`, `frontend/src/components/panel/SeccionContenido.tsx`
- **Gotchas:** Hay que validar el tab almacenado contra las tabs del rol actual — si un admin se logueó como admin y luego cambia a impersonar un cliente, el tab guardado podría no existir en las tabs del cliente. La validación `tabsRol.some(t => t.id === stored)` lo maneja.
- **Sentinel:** No aplica
- **GLORY-RS:** El patrón de persistencia via sessionStorage es genérico. Por ahora queda en nakomi.
- **Que:** Revisión profunda de consistencia visual en todos los modales del sistema. Añadidas clases estándar `.modalTitulo` y `.modalAcciones` en Modal.css. Migrado EditorMiembro de overlay custom a componente base `<Modal>` (ganando focus trap, Escape listener, scroll lock). Estandarizados títulos en 8 modales (SeccionPagos, OrdenDetalle, SeccionUsuarios, EntregablesPanel, CheckoutModal, ModalTestimonio, ModalCompra). Migrados botones de acción de SeccionUsuarios a `<Button>` estándar. Limpiadas 8 clases CSS legacy (custom títulos/acciones) reemplazadas por las centralizadas.
- **Archivos:** `frontend/src/components/ui/Modal.css`, `frontend/src/components/panel/EditorMiembro.tsx`, `frontend/src/components/panel/EditorMiembro.css`, `frontend/src/components/panel/SeccionPagos.tsx`, `frontend/src/components/panel/SeccionPagos.css`, `frontend/src/components/panel/OrdenDetalle.tsx`, `frontend/src/components/panel/SeccionProyectos.css`, `frontend/src/components/panel/SeccionUsuarios.tsx`, `frontend/src/components/panel/SeccionUsuarios.css`, `frontend/src/components/panel/EntregablesPanel.tsx`, `frontend/src/components/panel/EntregablesPanel.css`, `frontend/src/components/panel/CheckoutModal.tsx`, `frontend/src/components/panel/CheckoutModal.css`, `frontend/src/components/home/ModalTestimonio.tsx`, `frontend/src/components/home/ModalTestimonio.css`, `frontend/src/components/servicios/ModalCompra.tsx`, `frontend/src/components/servicios/ModalCompra.css`
- **Gotchas:** EditorMiembro usaba variables CSS deprecadas (`--bg-secondary`, `--bg-tertiary`, `--white-10`) — corregidas a variables vigentes (`--bg-primary`, `--border-default`, `--brand-primary`). Los CMS editors (EditorServicio, EditorProyecto, EditorBlog) usan `.modalSinPadding` con tabs propios — no aplica la estandarización de título/acciones.
- **Sentinel:** Sí — regla `modal-inconsistency` en `Agente/prevencion/prevencion-modal-inconsistency-2026-04-08.md`
- **GLORY-RS:** Las clases `.modalTitulo`/`.modalAcciones` son genéricas y aplican a cualquier proyecto con el sistema de modales. Candidatas a mover al submodulo glory-rs.

## 074A-14 — SEO Fase 2: Performance (Core Web Vitals)
- **Que:** Bundle splitting con manualChunks en Vite (6 chunks: react-core 149KB, editor 372KB, query 41KB, i18n 62KB, stripe 13KB, main 404KB). Lazy loading añadido a 13 imágenes que no lo tenían + loading="eager" explícito para hero de blog (LCP). Preconnect y dns-prefetch para Stripe en index.html. Fonts ya usan font-display:swap (WOFF2).
- **Archivos:** `frontend/vite.config.ts`, `frontend/index.html`, `frontend/src/islands/UsuarioPublicoIsland.tsx`, `frontend/src/islands/BlogSingleIsland.tsx`, `frontend/src/components/chat/ChatWidget.tsx`, `frontend/src/components/home/SeccionTestimonios.tsx`, `frontend/src/components/home/ModalTestimonio.tsx`, `frontend/src/components/panel/SeccionUsuarios.tsx`, `frontend/src/components/panel/ListaServicios.tsx`, `frontend/src/components/panel/ListaProyectos.tsx`, `frontend/src/components/panel/ListaBlog.tsx`, `frontend/src/components/ui/UploadImage.tsx`
- **Gotchas:** `@tiptap/pm` no tiene export entry principal (subpath-only) — no puede incluirse en manualChunks directamente. Se queda en el chunk por defecto.
- **Sentinel:** No aplica
- **GLORY-RS:** El patrón de manualChunks se puede reutilizar directamente en cualquier proyecto con stack similar (React + Tiptap + Stripe + i18n). Candidato a documentar en glory-rs como plantilla de vite.config.
