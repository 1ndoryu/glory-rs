# Tareas completadas — 2026-04-09

## 094A-1 — Bug fix: Reserva completada duplica ventas
- **Qué:** Implementé check `exists_by_reserva_id()` antes de crear venta automática + lógica de "descompletar" en `reserva.update()` que elimina ventas auto-generadas (importe_base=0) al sacar una reserva de estado "completada".
- **Archivos:** `src/repositories/venta.rs`, `src/services/reserva.rs`
- **Gotchas:** Usé queries dinámicas (`sqlx::query_scalar()`) en vez de macros `query!()` para evitar problemas con SQLX_OFFLINE. Solo se eliminan ventas con importe_base=0 para no borrar ventas manuales.
- **Sentinel:** No aplica.

## 094A-2 — Bug fix: Búsqueda ventas no encuentra por nombre completo
- **Qué:** Añadí `CONCAT(c.nombre, ' ', c.apellidos) ILIKE $6` como condición OR adicional en las queries SELECT y COUNT de listar ventas.
- **Archivos:** `src/repositories/venta.rs`
- **Gotchas:** Ya existían 5 condiciones ILIKE. Agregué la 6ta con OR para nombre completo concatenado. Se reutiliza el mismo parámetro `$` del search_term.
- **Sentinel:** No aplica.

## 094A-7 — Paredes en plano de sala (backend)
- **Qué:** Tabla `paredes_sala`, modelo, repositorio CRUD y handlers/rutas. Incluye actualización de posiciones masivas.
- **Archivos:** `migrations/20260409000000_paredes_sala.up.sql`, `.down.sql`, `src/models/plano_sala.rs`, `src/repositories/plano_sala.rs`, `src/services/plano_sala.rs`, `src/handlers/plano_sala.rs`, `src/models/mod.rs`
- **Gotchas:** Queries dinámicas para evitar SQLX_OFFLINE. `plano_completo` ahora carga paredes por zona. `#[allow(clippy::too_many_arguments)]` necesario en `crear_pared` y `actualizar_pared`.
- **Sentinel:** No aplica.

## 094A-3 — Roles y permisos de trabajadores (backend)
- **Qué:** Sistema completo de trabajadores: tabla `trabajadores` + `permisos_trabajador`, modelo, repositorio, handlers CRUD, login separado (`/api/auth/login-trabajador`), JWT con `tid` (trabajador_id), middleware `AuthUser` extendido, variante `AppError::Forbidden`.
- **Archivos:** `migrations/20260409100000_trabajadores.up.sql`, `.down.sql`, `src/models/trabajador.rs`, `src/models/mod.rs`, `src/repositories/trabajador.rs`, `src/repositories/mod.rs`, `src/handlers/trabajadores.rs`, `src/handlers/mod.rs`, `src/services/auth.rs`, `src/middleware/auth.rs`, `src/errors/mod.rs`
- **Gotchas:** `AuthUser` ahora tiene `trabajador_id: Option<Uuid>`. No rompe handlers existentes porque usan `auth.user_id` sin destructuring. El JWT del trabajador tiene `sub=user_id` del propietario + `tid=trabajador_id`, permitiendo que endpoints existentes funcionen transparentemente.
- **Sentinel:** No aplica.

## 094A-4 — Sistema de reseñas con review gating
- **Qué:** Tabla `resenas` con token único, modelo, repositorio, handlers. Flujo: propietario solicita reseña → genera link público con token → cliente puntúa → si ≥4★ y hay URL Google configurada → redirige a Google. Campo `google_review_url` añadido a `configuracion_restaurante`.
- **Archivos:** `migrations/20260409200000_resenas.up.sql`, `.down.sql`, `migrations/20260409200001_google_review_url.up.sql`, `.down.sql`, `src/models/resena.rs`, `src/models/configuracion.rs`, `src/repositories/resena.rs`, `src/repositories/configuracion.rs`, `src/handlers/resenas.rs`, `src/handlers/mod.rs`
- **Gotchas:** `rand 0.8` usa `thread_rng()` no `rng()`. Repositorio `configuracion.rs` convertido de `query_as!` a queries dinámicas para soportar nuevo campo sin romper SQLX_OFFLINE.
- **Sentinel:** No aplica.

## 094A-5 — Automatización mensajes a clientes inactivos
- **Qué:** Campo `ultima_visita` en `clientes` (backfill desde reservas completadas), tablas `reglas_inactividad` + `envios_inactividad`, modelo, repositorio con query JOIN reglas×clientes con NOT EXISTS envíos, handlers CRUD, función `procesar_inactividad()` integrada en scheduler. Servicio reserva actualiza `ultima_visita` al completar.
- **Archivos:** `migrations/20260409300000_inactividad_clientes.up.sql`, `.down.sql`, `src/models/inactividad.rs`, `src/models/cliente.rs`, `src/repositories/inactividad.rs`, `src/repositories/cliente.rs`, `src/handlers/inactividad.rs`, `src/handlers/mod.rs`, `src/services/recordatorio.rs`, `src/services/reserva.rs`
- **Gotchas:** Repositorio `cliente.rs` convertido completamente de macros a queries dinámicas. `procesar_inactividad` usa cache de integraciones por user_id. Intervalo PostgreSQL seguro porque `dias_inactividad` es int column, no input.
- **Sentinel:** No aplica.
