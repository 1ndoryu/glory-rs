## 094A-1 — Plan maestro hosting v2
- **Que:** Creado plan detallado en `Agente/planes/plan-hosting-v2-2026-04-09.md` con 9 fases para transformar el hosting en un servicio profesional. Cubre: detalle individual, self-service, dominios, SSH, upgrade, soporte, stats reales, seguridad, tests.
- **Archivos:** `Agente/planes/plan-hosting-v2-2026-04-09.md`
- **Gotchas:** N/A
- **Sentinel:** No aplica (documentación)
- **GLORY-RS:** No aplica (documentación)

## 094A-2 — Página de detalle individual por hosting (HostingDetalle)
- **Que:** Creado HostingDetalle con 6 tabs (General, Recursos, Dominio & SSL, Acceso SSH, Facturación, Eventos). Click en HostingCard→detalle. Back button para volver a lista. CopyButton e InfoRow como utilities reutilizables. Componente dividido en HostingDetalle.tsx (principal) y HostingDetalleTabs.tsx (contenido tabs) para cumplir límite 300 líneas.
- **Archivos:**
  - `frontend/src/components/panel/HostingDetalle.tsx` (nuevo, ~140 líneas)
  - `frontend/src/components/panel/HostingDetalleTabs.tsx` (nuevo, ~195 líneas)
  - `frontend/src/components/panel/HostingDetalle.css` (nuevo)
  - `frontend/src/hooks/useHostingDetalle.ts` (nuevo)
  - `frontend/src/hooks/useSeccionHosting.ts` (modificado: +selectedHostingId)
  - `frontend/src/components/panel/SeccionHosting.tsx` (modificado: render detalle)
  - `frontend/src/components/panel/HostingSubComponents.tsx` (modificado: +onSelect, stopPropagation)
  - `frontend/src/components/panel/SeccionHosting.css` (modificado: cursor pointer en card)
- **Gotchas:** Code Sentinel detectó 3 issues: archivo >300 líneas (dividido), .then() sin .catch() (corregido), <button> nativo (cambiado a Button). Resueltos todos.
- **Sentinel:** No aplica — issues ya corregidos
- **GLORY-RS:** CopyButton e InfoRow son utilidades genéricas reutilizables. Candidatos para glory-rs si se reutilizan en otros proyectos.

## 094A-3 — Self-service: cliente contrata hosting desde el panel
- **Que:** Endpoint combinado `POST /api/hosting/subscribe` que crea suscripción + Stripe Checkout Session en un solo paso. Cliente solo elige plan y dominio opcional; nombre/email se toman del perfil autenticado. Frontend: selector de planes con 3 cards (Básico $5, Pro $10, E-commerce $15), paso de dominio opcional, redirige a Stripe Checkout. Mutations extraídas a `useHostingMutations.ts` para cumplir límite 120 líneas en hooks.
- **Archivos:**
  - `src/handlers/hosting.rs` (modificado: +subscribe_self handler, +ruta)
  - `src/models/hosting.rs` (modificado: +SelfSubscribeRequest, +SelfSubscribeResponse)
  - `src/models/mod.rs` (modificado: +exports)
  - `frontend/src/api/hosting.ts` (modificado: +apiSelfSubscribe, +HOSTING_PLANS, +tipos)
  - `frontend/src/components/panel/HostingPlanSelector.tsx` (nuevo, 113 líneas)
  - `frontend/src/components/panel/HostingPlanSelector.css` (nuevo)
  - `frontend/src/hooks/useHostingMutations.ts` (nuevo, 100 líneas — mutations extraídas)
  - `frontend/src/hooks/useSeccionHosting.ts` (refactorizado: 75 líneas, usa useHostingMutations)
  - `frontend/src/components/panel/SeccionHosting.tsx` (modificado: botón "Contratar hosting" para clientes, modal con plan selector)
- **Gotchas:** useSeccionHosting ya excedía 120 líneas antes de esta tarea. Extracción de mutations corrigió la violación pre-existente.
- **Sentinel:** No aplica
- **GLORY-RS:** HostingPlanSelector y useHostingMutations son específicos de este proyecto (Stripe pricing). No se mueven a glory-rs.

## 094A-6 — Upgrade/downgrade de plan desde Facturación
- **Que:** Botón "Cambiar plan" en la tab Facturación del detalle de hosting. Muestra grid de planes alternativos con badge Upgrade/Downgrade, features comparativas, y botón para cambiar. Usa updateMutation existente pasado como callback desde SeccionHosting → HostingDetalle → TabFacturacion. CSS responsive con grid auto-fit.
- **Archivos:**
  - `frontend/src/components/panel/HostingDetalleTabs.tsx` (modificado: TabFacturacion expandida con plan change UI, +useState, +HOSTING_PLANS import)
  - `frontend/src/components/panel/HostingDetalle.tsx` (modificado: +onPlanChange/planChangeLoading props, pasados a TabFacturacion)
  - `frontend/src/components/panel/SeccionHosting.tsx` (modificado: pasa onPlanChange y planChangeLoading a HostingDetalle)
  - `frontend/src/components/panel/HostingDetalle.css` (modificado: +estilos plan change grid, badges, opciones)
- **Gotchas:** HostingDetalleTabs.tsx ahora tiene 297 líneas (límite 300). Si se agrega más contenido, habrá que extraer TabFacturacion a su propio archivo.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica (UI específica del proyecto)

## 094A-7 — Contactar soporte desde hosting detalle
- **Que:** Botón "Soporte" en el header meta del detalle de hosting. Al hacer click, despacha `panel-cambiar-tab` con detail `'mensajes'` para navegar directamente a la sección de chat del panel.
- **Archivos:**
  - `frontend/src/components/panel/HostingDetalle.tsx` (modificado: +MessageSquare import, +botón Soporte)
  - `frontend/src/components/panel/HostingDetalle.css` (modificado: +.hostingDetalleSoporte estilos)
- **Gotchas:** PanelIsland usa `'mensajes'` como ID de la sección de chat, no `'chat'`. Verificado en PanelIsland.tsx línea 104.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica

## 094A-8 — Stats reales: uptime desde eventos, endpoint dedicado
- **Que:** Eliminados stats simulados (hash-based). Nuevo endpoint `GET /api/hosting/subscriptions/:id/stats` que calcula uptime real desde el historial de `status_change` en `hosting_events`. Storage/bandwidth muestran límites del plan; uso real marcado como "Monitoreo pendiente" hasta que se configure agente (Coolify/cAdvisor). Frontend HostingStats.tsx ahora usa React Query para consumir datos reales con auto-refresh cada 2min.
- **Archivos:**
  - `src/handlers/hosting.rs` (modificado: +`get_hosting_stats` handler, +`calculate_uptime`, +`plan_bandwidth_gb`, +ruta /stats)
  - `src/models/hosting.rs` (modificado: +`HostingStatsResponse`)
  - `src/models/mod.rs` (modificado: +export `HostingStatsResponse`)
  - `frontend/src/api/hosting.ts` (modificado: +`HostingStatsData`, +`apiGetHostingStats`)
  - `frontend/src/components/panel/HostingStats.tsx` (reescrito: React Query, datos reales, estado "pendiente")
  - `frontend/src/components/panel/HostingStats.css` (actualizado: +estados loading/pendiente/meta)
- **Gotchas:** `calculate_uptime` necesita `#[allow(clippy::cast_precision_loss)]` para conversión i64→f64 de segundos. Suscripciones migradas sin evento status_change explícito se asumen activas desde creación.
- **Sentinel:** VarSense reporta variables CSS no definidas localmente (definidas en global) e inline style dinámico para width — falsos positivos.
- **GLORY-RS:** `calculate_uptime` es potencialmente reutilizable. Evaluar después de estabilización.

## 094A-4+094A-5 — Dominio con DNS records + SSH con PuTTY
- **Que:** TabDominio mejorada: instrucciones de registros A (@ y www) apuntando al IP del VPS, nameservers como alternativa, nota de propagación DNS. TabAcceso mejorada: instrucciones PuTTY para Windows, info SSH+SFTP más detallada con/sin coolify_site_name. TabFacturacion extraída a archivo propio (105 líneas) para dar espacio.
- **Archivos:**
  - `frontend/src/components/panel/HostingDetalleTabs.tsx` (refactorizado: 221 líneas, -TabFacturacion, +DNS records, +PuTTY)
  - `frontend/src/components/panel/TabFacturacion.tsx` (nuevo: 105 líneas, extraído de HostingDetalleTabs)
  - `frontend/src/components/panel/HostingDetalle.tsx` (modificado: import TabFacturacion desde nuevo archivo)
  - `frontend/src/components/panel/HostingDetalle.css` (modificado: +estilos DNS records grid)
  - `frontend/src/hooks/useHostingDetalle.ts` (modificado: +serverIp en DomainInfo, derivado del VPS)
- **Gotchas:** serverIp está hardcodeado a 66.94.100.241 (único VPS actual). Cuando haya multi-VPS, debe venir del backend.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica (UI específica del proyecto)

## 094A-9 — Auditoría de seguridad del módulo hosting
- **Que:** Auditoría profunda de seguridad: handlers, repositorios, servicios Stripe/Contabo, middleware, errores. Se implementaron 6 fixes: validación estricta de campos JSON en webhooks Stripe (no más `unwrap_or_default`), verificación de status "pending/provisioning" antes de activar suscripción, idempotency key en Stripe Checkout, regex RFC 1035/1123 para dominios, event logging con error handling (no más `let _ =`), validación de formato Price IDs al startup. Se descartaron 4 falsos positivos (la firma Stripe se verifica antes del handler de hosting, rate limiting existe como capa general).
- **Archivos:**
  - `src/services/hosting_stripe.rs` (modificado: validación estricta JSON, idempotency key, log errores)
  - `src/handlers/hosting.rs` (modificado: event logging con error handling)
  - `src/models/hosting.rs` (modificado: regex dominio, imports regex/LazyLock)
  - `Cargo.toml` (modificado: +regex = "1")
  - `Agente/documentacion/hosting/auditoria-seguridad-hosting-2026-04-09.md` (nuevo)
- **Gotchas:** CRITICAL-1 y CRITICAL-3 del audit eran falsos positivos — la firma Stripe SÍ se verifica en payments.rs antes del handler de hosting. HIGH-1 también falso positivo — GovernorLayer aplica a todas las api_routes.
- **Sentinel:** No aplica
- **GLORY-RS:** La validación de dominio con regex podría moverse a glory-rs como utilidad común. Evaluar después de estabilización.

## 094A-10 — Tests unitarios del módulo hosting
- **Que:** Creados 21 tests unitarios para la lógica de negocio del hosting sin dependencia de BD. Cubren: funciones de plan (precios, storage, bandwidth), `calculate_uptime()` (5 escenarios: sin eventos+activo, sin eventos+pending, active→suspended, active→suspended→active, ignora non-status events), validación de dominio (regex RFC 1035/1123: 6 válidos, 12 inválidos), integración con `validator` (dominio válido/inválido/none/plan vacío), config Stripe (env vars válidas, formato inválido, vars faltantes, `price_for_plan` mapping).
- **Archivos:**
  - `src/handlers/hosting.rs` (modificado: +módulo #[cfg(test)] con 11 tests)
  - `src/models/hosting.rs` (modificado: +módulo #[cfg(test)] con 6 tests)
  - `src/services/hosting_stripe.rs` (modificado: +módulo #[cfg(test)] con 4 tests)
- **Gotchas:** Tests de env vars en hosting_stripe podrían conflictuar entre sí si se ejecutan en paralelo con --test-threads=1. En la práctica no hubo conflicto porque Rust test runner maneja env vars por proceso.
- **Sentinel:** No aplica
- **GLORY-RS:** No aplica (tests específicos del proyecto)

## 094A-11 — UI polish hosting
- **Que:** 6 fixes de UI para el módulo de hosting:
  1. Eventos legibles: `color: var(--brand-white)` en `.hostingEventoDetalles` (texto negro sobre fondo negro)
  2. Card sin stats: Reemplazado `<HostingStats>` en HostingCard con info simple de almacenamiento/dominio (eliminado "Cargando estadísticas")
  3. Secciones detalle como card: `.hostingDetalleSection` con background white, border, padding, radius
  4. Back button consistente: `.hostingDetalleBack` ahora replica `.ordenDetalleVolver`
  5. Plan selector compacto: Modal más ancho (680px), cards con padding/fuentes reducidas, variables CSS correctas
  6. Fix `--bg-card` inexistente: Reemplazada por `--bg-card-subtle` en HostingDetalle.css y VpsPanel.css; `--text-secondary` → `--text-tertiary` en SeccionHosting.css
- **Archivos:** SeccionHosting.css, HostingSubComponents.tsx, HostingDetalle.css, HostingPlanSelector.css, SeccionHosting.tsx, VpsPanel.css
- **Gotchas:** VarSense reporta falsos positivos (resuelve variables y se queja del valor resuelto como "hardcodeado"). `--bg-card` no existía como variable CSS a pesar de usarse en múltiples archivos.
- **Sentinel:** Falso positivo de VarSense — considerar regla de prevención para detectar variables CSS usadas pero no definidas.
- **GLORY-RS:** No aplica (estilos específicos del proyecto)

## 084A-39 — Deploy al servidor (Coolify)
- **Que:** Primer deploy exitoso de Nakomi Studio (Rust/Axum + React SPA) a producción en nakomi.studio. Resueltos 5 bloqueantes de deploy en secuencia:
  1. **npm ci lockfile mismatch**: `@floating-ui/dom` ghost reference — cambiado `npm ci` → `npm install --no-audit --no-fund` en Dockerfile (compatibilidad npm 10 vs 11).
  2. **SQLX_OFFLINE missing**: Agregado `ENV SQLX_OFFLINE=true` al Dockerfile backend stage.
  3. **glory-rs submodule ausente**: El directorio glory-rs/ está en .gitignore (no se sube al repo). Agregado `RUN git clone glory-rs-framework.git glory-rs` al Dockerfile frontend stage.
  4. **VersionMismatch migraciones**: BD tenía `_sqlx_migrations` de intento previo con checksums diferentes. Reset completo: `DROP SCHEMA public CASCADE; CREATE SCHEMA public`.
  5. **Fixtures content/ no copiados**: Agregado `COPY --from=frontend /build/content /app/content` al Dockerfile runtime stage.
  6. **Env vars faltantes**: Agregadas 9 variables (GROQ_API_1-3, GROQ_API, GOOGLE_GEMINI_API, GLORY_STRIPE_*, GLORY_ALLOWED_ORIGINS) via Coolify API.
- **Archivos:**
  - `config/templates/Dockerfile.rust` (coolify-manager-rs: 6 cambios)
  - `frontend/package-lock.json` (regenerado)
  - Coolify API: 9 env vars añadidas al stack do8k4w8swccwwogoc0os0ck0
- **Gotchas:** Docker `--no-cache` ya se usaba pero diferencias npm 10 vs 11 causaban el error. El postgres volume persiste entre intentos de deploy — siempre resetear migraciones si el schema cambió. `logs --name studio` del coolify-manager conecta al contenedor WordPress (otro stack), no al Rust app — usar SSH + `docker compose logs app` directo.
- **Sentinel:** No aplica
- **GLORY-RS:** El Dockerfile.rust es template genérico para cualquier glory-rs project — ya vive en coolify-manager-rs/config/templates/
