/* [256A-1c] Stub para ErrorBoundaryMezclador — DAW no disponible en desktop Tauri */
import React from 'react';

interface Props {
    children?: React.ReactNode;
}

export function ErrorBoundaryMezclador({ children }: Props) {
    return <>{children}</>;
}
