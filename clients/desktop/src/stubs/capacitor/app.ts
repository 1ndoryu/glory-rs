/* [256A-1c] Stub para @capacitor/app — no disponible en desktop Tauri */
export const App = {
    addListener: async (_event: string, _callback: (data: unknown) => void) => ({ remove: async () => {} }),
    getInfo: async () => ({ version: '0.0.0', build: 0 }),
};
