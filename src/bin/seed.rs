/* [044A-9] Binario seed placeholder — ejecuta migraciones y datos iniciales.
 * Requerido por el Dockerfile de Coolify (rust-stack.yaml).
 * TO-DO: Agregar datos semilla reales cuando se necesiten. */
fn main() {
    println!("seed: no hay datos semilla configurados.");
}
